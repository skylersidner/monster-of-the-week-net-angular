## Learnings

### 2026-07-26 — Tailwind v4 Architectural Phasing

- Angular's emulated encapsulation shields component styles from Tailwind preflight — import full Tailwind immediately, coexistence is safe
- Phasing order: infrastructure → global → layout shell → shared components → simple detail pages → list pages → medium pages → wizard last
- Phase 2 (shell layout) is highest risk — should run in a feature branch
- Phase 7 (mystery wizard) is highest functional risk — test in a feature branch
- `custom-select.component.scss`: `@apply` is correct choice over Tailwind `group` modifier (avoids template restructuring for an already-programmatic widget)
- Tailwind v4 uses `oklch()` for colors in DevTools; `@theme` override pins hex for clarity
- Component style budget should only be tightened after Phase 7 is fully complete

### Phase 8 Planning — Minions UI Flow (2026-07-25)

- **No flat list API endpoint**: `GET /api/minions` does not exist. Only `GET /api/monsters/{monsterId}/minions` is available. Any top-level Minions list view requires adding a new API endpoint — this is the primary architectural fork (Option A vs B).
- **MinionService update gap**: The Angular `MinionService` is missing 5 update methods (`updateAttack`, `removeAttackWeaponTag`, `updatePower`, `updateArmor`, `updateWeakness`) despite the corresponding PUT endpoints existing on `MinionsController`. These must be added regardless of navigation option chosen.
- **Two navigation options**: Option A (top-level `/minions` flat list, new API endpoint) vs Option B (minions embedded in monster detail, no new API endpoint). Option B is architecturally honest; Option A has better UX if cross-monster browsing is needed. Deferred to Skyler to decide.
- **Custom moves ambiguity**: `MinionDetailResponse` includes a `customMoves` field but `MinionsController` has no create/edit endpoints for minion custom moves. Plan defers these to a future phase and renders the field read-only.
- **Pattern to follow**: `features/monsters/` — signal-based state, `forkJoin` for detail load, reactive forms, 5 sub-resource panels (attacks with weapon tags, powers, armors, weaknesses, custom moves). Minions omit mysteries section and add `harmCapacity` as a form field.
- **Key file paths**: `src/web/monster-of-the-week-web/src/app/core/minion.ts` (service), `src/web/monster-of-the-week-web/src/app/app.routes.ts` (route registration), new feature at `src/web/.../features/minions/`.

### Global Search Planning (2026-08-01)

- **Dataset is small/flat/single-tenant**: 5 domains, no auth/campaign scoping, realistically tens-to-hundreds of rows each. This ruled out `pg_trgm`/`tsvector`/persisted index tables for Phase 1 — plain `ILIKE` chains per token beat any indexed alternative on cost/benefit at this scale. Revisit only if row counts or query volume grow materially.
- **Word-boundary prefix match without a token column**: `ILIKE 'token%' OR ILIKE '% token%' OR ILIKE '%-token%'` per query token (AND across tokens) gives "prefix of any word, split on space/dash" semantics fully inside EF Core's LINQ→SQL translation — no raw SQL/regex needed. Reusable pattern for any future "match at word boundaries" requirement in this codebase.
- **Extensibility without a schema**: `ISearchProvider`-per-domain + code-level weight tags (not a persisted `SearchableField` table) is the right call whenever "make X configurable later" doesn't yet have a real scale/ops reason to warrant persisted state — a compile-time registry is strictly simpler and driftless. Only reach for the persisted-table version alongside a real full-text-search migration.
- **Icon reuse pattern**: `page-layout.html`'s nav icons are inline `@switch` SVGs with no shared component — any new UI needing the same 5 domain icons (search dropdown, results page, likely future features) should extract to a `DomainIconComponent` rather than re-copy path data a 3rd/4th time.
- **Route precedent confirmed**: all 5 domain detail routes are flat/top-level (`/mysteries/:id`, `/monsters/:monsterId`, `/minions/:minionId`, `/locations/:locationId`, `/bystanders/:bystanderId`) — useful lookup table for any future cross-domain linking feature.
- **Docs**: `docs/search/README.md`, `architecture.md`, `phases.md`, `open-questions.md`.

### Global Search — Matching Design Revision (2026-08-01, same day as initial plan)

- Owner feedback pattern worth remembering: when a stakeholder floats "maybe X only applies to titles, not sure about long text" as their own tentative idea, the right architect move is confirming it with concrete reasoning (noise + snippet-extraction cost, not just "sounds right") rather than either rubber-stamping or overriding — then extend it into a clean rule if one falls out naturally (here: substring-tier scope = weight-tier scope, once sub-resource names were promoted to Secondary and prose fields correspondingly demoted to Tertiary).
- When a response contract will predictably need a new field in a later phase (here: `snippet` for "what actually matched"), add it now as always-null/optional rather than deferring — cost of adding early is ~zero, cost of adding after the frontend ships against the narrower shape is a real component rework.
- `ILIKE '%token%'` (unanchored) vs `ILIKE 'token%'` (prefix): prefix can use a btree index later (`text_pattern_ops`); unanchored substring never can, under any collation — pg_trgm is the only indexed path for substring/fuzzy matching in Postgres. Worth remembering any time "search" or "contains" comes up again in this codebase.

### Global Search — Phase 4 Spec (2026-08-01, after Phases 1-3 shipped)

- **Always read the shipped code before speccing the next phase of a doc, not just the doc.** Phase 1 deviated from `architecture.md` in a load-bearing way (in-memory matching instead of `EF.Functions.ILike`, for SQLite test portability — Bowser's call, logged in `.squad/decisions/inbox/bowser-search-phase1-backend.md`). Missing this would have specced Phase 4 against a backend strategy that no longer existed.
- **"Revisit later" isn't a decision — a numeric trigger is.** Phase 1's implementation notes flagged the in-memory approach as "revisit later." Phase 4 was "later." Resolved it with concrete numbers (~5,000 rows/request, ~150ms p95) rather than deferring again.
- Snippet/highlight design for a search feature: keep markup out of the API contract (plain text + structured offsets, not embedded `<mark>`/delimiters) — frontend builds highlight segments from offsets. Keeps sanitizer concerns and contract concerns separate; matches this codebase's existing pattern of API returning data only.
- When an entity can match on many fields (post-extensibility), explicitly re-confirm any "pick one, dedupe" behavior that was previously a no-op (only one field existed before) — it's exactly the kind of thing that silently becomes load-bearing once the input space grows, and won't get flagged by existing tests written against the old narrower behavior.
- When a new requirement's own wording already scopes something precisely (here: "full-results-page row" ruling out the dropdown), state it as a confirmation with the quote as evidence rather than reflexively listing it as an open question — reserve open-questions.md for calls that are genuinely mine to make or genuinely ambiguous.

### Theming Architecture Planning (2026-08-01)

- **Tailwind v4 `@theme` custom properties are real CSS variables at `:root`, referenced via `var()` in generated utilities** — not inlined at build time. This is the key mechanism that makes semantic-token dark mode work as pure CSS cascade (`.dark { --color-surface: ...; }` overrides the same property every `bg-surface` utility already points to) with zero `dark:` variant sprinkling needed for color. Already implicitly used in the Tailwind migration for `--color-indigo-700`/breakpoints, but worth stating explicitly as the theming mechanism's foundation.
- Recommended semantic token layer over a mechanical `dark:`-on-every-literal-class pass, explicitly because a 3rd theme (mentioned as a real possibility) is a template-risk-free palette-only addition under tokens, vs. unbounded class-list growth under the sprinkle approach. Accepted trade-off: a re-pointing pass across every already-migrated template, phased identically to the original Tailwind migration (shell first/highest risk, wizard last/highest functional risk).
- CSR-only confirmed (no `provideClientHydration`, no `server` builder target) — FOUC prevention still needs an inline synchronous `<head>` script (not just an Angular `APP_INITIALIZER`/`provideAppInitializer`), since the browser has essentially blank HTML until the JS bundle loads and bootstraps in a CSR app; Angular-side initialization only reconciles, never originates, first paint.
- `pages/*` (dashboard, data-admin) vs `features/*` (mysteries, monsters, minions, bystanders, locations, search) split confirmed as: `pages/` = single cross-cutting app-level views registered via direct `loadComponent` in `app.routes.ts`; `features/` = domain-vertical resources with their own `*.routes.ts` + `loadChildren`. Any future single-page, no-CRUD, app-wide utility view (Settings was the case here) belongs in `pages/`, not `features/`.
- Confirmed remaining color-bearing inline styles outside Tailwind entirely: `page-layout.html`'s "Soon" badge (x2), API-unavailable modal, notification toast `[style.background-color]`. `mystery-create-monster-phase.html`'s `style="width: ..."` and `mystery-create-tracker.html`'s `[style.grid-column]` are structural (not color) and correctly out of scope for any theming pass.
- Type badges (5 domains) and admin table nth-child row striping (`data-admin.scss`/`weapon-tag-admin.scss`, hardcoded hex + `!important`) both need dedicated token namespaces rather than reuse of generic accent/danger/success roles — they're semantically fixed legend colors, not state colors, and easy to silently miss in a dark-mode pass since they don't visually read as "needs a dark variant" the way a page background does.
- Docs: `docs/theming/theming-plan.md`.

### Theming Plan — Skyler's Reconciliation of Rosalina's Six Open Questions (2026-08-02)

- When a downstream doc (Rosalina's `dark-theme-palette.md`) flags catalogue gaps/open questions against my plan, the resolution edits belong *in the plan itself* at the natural section (Decision/Catalogue/Phase/Risk Register), not just appended as a status note — status notes are for pointing at what changed elsewhere, not a substitute for the actual edit.
- Settled: `--color-accent`/`--color-danger` stay single tokens per role even in dark mode — no button-fill split, ever, per Skyler (simplicity for future themes beats per-theme brand-identical buttons). The single-token trick is Rosalina's "lighten base hue + flip the on-color token" construction, not a second token. Worth remembering as the precedent for any future accent-like role that needs to double as both text and button-fill.
- When a badge/token gap is genuinely speculative with no real feature behind it yet (mystery-type badge), the correct move is explicit non-action: no catalogue row, no phase reference — otherwise a "reserved" placeholder value quietly becomes load-bearing by osmosis.
- Table-striping and toast-brightness were framed as "illustrative placeholder pending review" in my own doc — once a designer's override recommendation gets stakeholder sign-off, go back and rewrite the illustrative code block/framing itself as final, don't leave the original placeholder sitting next to a footnote saying it's superseded. Two "correct" answers on the page reads as unresolved even after a decision is made.
- Docs: `docs/theming/theming-plan.md` (Decision B, Token Catalogue, Phases 2/4/5/7, Risk Register).

### Theming Plan — Luigi's Five Pre-Phase-1 Review Points (2026-08-02)

- **Tailwind v4 utility-name generation is a literal string concatenation, not a role-aware parse.** A `--color-X` token's utility name is exactly the family prefix (`bg-`/`text-`/`border-`/etc.) glued in front of everything after `--color-`. Never bake a Tailwind property word (`bg`, `text`, `border`) into the tail of a token name as if it indicated "which utility this is for" — it stutters (`--color-toast-success-bg` → `bg-toast-success-bg`) and any doc prose assuming the stripped form (`bg-toast-success`) is simply wrong and silently unstyled at runtime (Tailwind's on-demand generator gives no build error for an unmatched class name). The `on-*` convention (already used for `--color-on-accent`) is the clean fix for fill/text pairs; a third role like a chip border doesn't fit `on-*` and should avoid the property word entirely (e.g. `-line` not `-border`) rather than stutter too.
- Every `--color-*` token is available under every color-consuming utility prefix regardless of its intended role (`bg-text-primary` is a real, valid, if nonsensical, utility) — this is universal and harmless (no dead-CSS bloat; Tailwind only emits rules for classes actually referenced), distinct from the naming-stutter problem above. Don't conflate the two when explaining scope to a stakeholder.
- `computed()` in Angular must stay pure — it cannot itself own a `matchMedia`/event listener. The valid pattern for "signal driven by a browser event" is `toSignal(fromEvent(target, 'event'))` (subscription lifecycle tied to `DestroyRef` automatically, safe to construct in a field initializer since that runs in the class's injection context) feeding a separate, genuinely pure `computed()`.
- CSR bootstrap verification pattern worth reusing: `bootstrapApplication` only ever touches the DOM subtree under the `app-root` selector match; with no hydration step, there's no mechanism that would diff/strip a pre-bootstrap `<html>` class. Safe general rule for any "set state on `<html>`/`<body>` before Angular boots" design in this app.
- When a stakeholder wants a risk deferred to its own later phase ("if this can be put off... without needing to alter the rest"), the right move is to decouple the underlying service/mechanism (ships in place, on schedule) from the specific guarantee being deferred (its own new phase, own risk/rollback) — not to weaken the mechanism itself.
- Always re-verify a relayed "developer X found Y in file Z" claim by grepping the actual file before acting on it — `page-layout.spec.ts` was flagged as needing test-disabling for color-fragile assertions; on inspection it had none (its `.sidebar-mobile`/`.api-modal` selectors are structural, unrelated to color). The one real hit app-wide was `search-results.spec.ts` (badge literal classes), which also surfaced an undocumented Files-changed gap — the search feature's results page reuses badge tokens but wasn't in any phase's file list.
- Docs: `docs/theming/theming-plan.md` (Decision D/E/I, Token Catalogue, Phases 0/1/4, Risk Register, new Phase 8).

### Theming Plan — `on-*` Naming Rename Applied (2026-08-02)

- Skyler's final call on the naming collision flagged during Luigi's review: extend `on-*` to every `-bg`/`-text`-suffixed token. Applied throughout `theming-plan.md` and `dark-theme-palette.md`: `--color-toast-{success,error}` + `--color-on-toast-{success,error}`; `--color-badge-{domain}` + `--color-on-badge-{domain}` for all six domains; `--color-weapon-chip` + `--color-on-weapon-chip`.
- Weapon-chip border is the one deliberate non-`on-*` case: named `--color-weapon-chip-line`, not `--color-on-weapon-chip-line` — a border isn't a "content on top of a fill" relationship, so forcing `on-*` onto it would misuse the convention. Judgment call, flagged in-doc for Skyler/Rosalina to double-check if they want a different name.
- When a catalogue restructure adds an implied-but-previously-uncatalogued token (toast text was always assumed white but had no dedicated token before this rename), add it explicitly with its actual value rather than leaving the `on-*` pair half-formed — a fill/text pair should always have both halves present in the catalogue.
- When a downstream doc's own token names need to shadow a rename made in the upstream catalogue, split combined "bg/text-in-one-row" table rows into one row per token, matching how `--color-on-accent`/`--color-on-danger` were already given their own rows — keeps a 1:1 mapping between catalogue and downstream value tables (in this case `dark-theme-palette.md`) rather than compressed shorthand.
- Docs: `docs/theming/theming-plan.md` (Token Catalogue, new "Resolved" naming subsection, Phases 1/3/4/5), `docs/theming/dark-theme-palette.md` (Token Value Table, Badges section, Accessibility Notes).

### Theming Plan — Token→Utility Mechanism Correction Formalized (2026-08-02)

- **Tailwind v4 utility generation is pure prefix concatenation with zero name-awareness, and there is a real, mechanically-derivable split it implies for every `@theme` token:** Category A (role name has no utility-family word, genuinely multi-family — leave in `@theme`, let it auto-generate) vs. Category B (role *is* a single family — its name necessarily contains the family word, so auto-generation stutters; fix with a hand-written `@utility` block, don't rename). Worth applying this test *before* writing a new token's Catalogue row, not after discovering a stutter at build time.
- **`@theme` vs `@theme static` is not stylistic.** A bare `@theme` tree-shakes any token no utility currently references out of `:root`, while a `.dark`/theme-override block (plain hand-written CSS) always emits in full — the two disagree, and a token can end up defined in one theme scope only. Any codebase using Tailwind v4's theme-token mechanism for hand-written CSS (not just utility classes) needs `static`, and it needs to be a *hygiene rule enforced going forward*, not a one-time fix — a second bare `@theme` block anywhere silently reintroduces it.
- **`@reference "tailwindcss"` in a component's `@apply`-bearing SCSS only loads Tailwind's stock theme — it cannot see this app's own `@theme`/`@utility` layer.** Must be a relative path to the app's actual `styles.css`. This is a loud build failure (not silent), but it's still a real per-file blocker worth checking proactively before a phase repoints an `@apply` line — I verified by grepping all `.scss` files using `@apply`/`@reference` app-wide (10 files) rather than trusting a prior framing of "which phases are affected"; one phase (4, via `monsters-list.scss`) had been missed by that framing.
- When a downstream implementer's decision file explicitly leaves items "open for Yoshi/Skyler," triage each one individually rather than blanket-deferring: some are genuinely mine to call now (an in-doc naming choice, a twice-deferred instruction conflict with no case for the status quo), some are genuinely someone else's (a values/precedent call belongs with the designer even if the mechanism is mine). Don't let "flag it" become the default move for everything in that section.
- A twice-flagged, low-risk, currently-invisible inconsistency (the API-modal `bg-surface` vs. `surface-raised` conflict — invisible because both tokens were still `white`) is exactly the kind of item that should get resolved on the second flag, not deferred a third time once every other document (palette doc, Catalogue role description, sibling components in the same file) already agrees on the answer.
- Docs: `docs/theming/theming-plan.md` (Overview, Decision B + 3 addenda, Token Catalogue naming subsection + new Conventions subsection, Phases 1-7, Risk Register).

### Theming Plan — `@reference` Directive Independent Verification (2026-08-02)

- **`@reference "<own-stylesheet>"` (not bare `"tailwindcss"`) is Tailwind's own documented mechanism** for component-scoped/isolated CSS compilation contexts needing custom `@theme` tokens — confirmed by reading `/docs/functions-and-directives` directly, which gives Vue/Svelte `<style>`-block examples in exactly this shape. Not a workaround; it's the textbook answer.
- **Angular's own docs (`angular.dev/guide/tailwind`) and Tailwind's Angular framework guide are both bare 5-6-step global-setup walkthroughs** — neither mentions component-scoped styles/`@apply`/`@reference`/theming for *any* framework, so their silence here is scope (getting-started guide), not evidence of an Angular-specific incompatibility. Angular's `ViewEncapsulation.Emulated` component stylesheet compilation is architecturally the same isolated-PostCSS-context shape as a Vue SFC `<style>` block, which Tailwind's docs do cover — reinforces there's nothing Angular-specific blocking this.
- **Node.js subpath imports (`package.json` `imports` field, `#`-prefixed alias) is a real, Tailwind-documented alternative to hand-computed relative `@reference` paths** — resolves per-file/per-depth fragility with one `package.json` entry, no Angular tooling involvement (resolved by Tailwind's PostCSS import resolution, not tsconfig paths). Worth adopting before Phase 2+ propagate more relative-path `@reference` lines.
- Docs: `.squad/decisions/inbox/yoshi-theming-reference-directive-evaluation.md`.

### Theming Plan — `@reference` Subpath-Import Convention Applied; Build Pipeline Corrected (2026-08-02)

- Luigi's empirical verification (real `ng build --configuration production`, negative controls, three-depth proof) confirmed Tailwind's own documented subpath-imports fix (`package.json` `imports: {"#styles.css": "./src/styles.css"}`) works end-to-end in this app's actual build — replaced every hand-computed relative-path `@reference` instruction in `theming-plan.md` with the single alias line. When a fragility complaint targets *how* a documented mechanism is invoked (not the mechanism itself), check whether the same docs page already names the fix before defending the status quo.
- `package.json`'s `imports` field is genuinely load-bearing once any `@apply`-bearing SCSS references it — treat it with the same care as `tsconfig.json` paths or `angular.json` builder config; gave it its own Risk Register row distinct from the general `@reference`-target mistake, since a removed/renamed alias breaks every affected file at once, not just the one being edited.
- Corrected a build-pipeline assumption baked into the Risk Register and Decision B addendum: this app runs `@tailwindcss/postcss` (via `.postcssrc.json`), not `@tailwindcss/vite` — the latter is an unused dependency in `package.json` worth removing eventually (Luigi's finding, folded in as a one-line note, not a new phase). Worth double-checking which Tailwind integration is actually active before writing prose that assumes one.
- Docs: `docs/theming/theming-plan.md` (Overview, Decision B addendum, Conventions subsection, Phases 1-6, Risk Register, Summary).

### Theming Plan — `--color-surface-hover` Added (Luigi's Phase 2 Gap Closed, 2026-08-02)

- Luigi's Phase 2 review found a real recurring gap: no token for a neutral/secondary hover fill (`bg-gray-100`/`hover:bg-gray-200`-shaped literals), stopgapped with `hover:bg-accent-subtle` (semantically wrong — brand-colored, not neutral). Skyler approved `--color-surface-hover`; explicitly declined `--color-accent-muted` (a separate, smaller sub-label shade-loss gap Luigi also flagged) — noted in-doc as deliberate non-action, not tracked as an open gap.
- Confirmed Category A before writing the row: `hover` is a Tailwind *variant* prefix, not a color-consuming utility family, so `surface-hover` has no family word in its name and auto-generates cleanly (`bg-surface-hover`, `hover:bg-surface-hover`) — stays in `@theme static`, no hand-written `@utility` needed. Worth remembering as a concrete instance of the Category A/B test from the token-mechanism correction: check whether the "family word" candidate is actually a variant, not a utility prefix, before assuming a collision.
- Real ambiguity worth flagging for next time: the token's canonical light value (`gray-200`) is anchored to the *first* consumer (confirm-delete modal's Cancel button, which has a distinct base `gray-100`/hover `gray-200` pair). But five later-phase sites (`minion-detail`, `monsters-list`, `monster-detail`, `locations-list`, `bystanders-list`, `search-results`) use a *different* shape — no separate base fill, just `hover:bg-gray-100` directly. Repointing those five onto the same token is a real (if minor) one-step light-mode shade darkening, not a strict no-op repoint the way most token substitutions in this plan are — flagged explicitly in Phases 3/4/5 rather than silently treated as identical to the modal's case.
- Docs: `docs/theming/theming-plan.md` (Overview status update, Token Catalogue + new paragraph, Phases 2/3/4/5).

### Theming Plan — Phase 7 Sweep Scope Widened to Dead CSS (2026-08-02)

- Generalized Luigi's Phase 4 `monsters-list.scss` find (a duplicate, dead `.action-btn:hover:not(:disabled)` rule silently winning cascade order over the token-repointed rule) into an explicit Phase 7 sweep instruction: examine every component `.scss` touched across Phases 1-6 (and any untouched ones) for genuinely dead/unused legacy selectors, not just literal-color classes or `@reference` gaps.
- Worth remembering as a category distinct from "unused token class" checks: dead CSS still compiles and emits — it's simply never matched by any current template — so it's invisible both to "does this class emit" tooling and to a straggler grep that only looks for classes that *are* referenced. The danger is specifically cascade collision (same/higher specificity, later in source order), not just bloat, and it stays invisible until a value actually changes (here: dark mode landing).
- Docs: `docs/theming/theming-plan.md` (Phase 7, item 1).

### Theming Plan — `--color-on-success` Added (Luigi's Phase 6 Gap Closed, 2026-08-03)

- Luigi's Phase 6 review found the one fill role in the Catalogue with no `on-*` partner: `--color-success` (wizard complete-phase bubble). He shipped a flagged placeholder (`text-on-accent`) rather than invent a value — numerically correct in both themes today (light `white`, dark `slate-900`) since Rosalina's `on-accent` value already matches what an `on-success` token would need under her own lighten-and-flip construction, but semantically borrowing a different token's role.
- Skyler's call: formalize it now as its own real token with those same values, not left to diverge later. Confirmed Category A (no utility-family word in `on-success`) before adding — same check as `--color-surface-hover`.
- Placeholder swap (`text-on-accent` → `text-on-success` in `mystery-create.scss`) is Luigi's next edit, not done here — this task was catalogue-only.
- Docs: `docs/theming/theming-plan.md` (Token Catalogue + new paragraph, Phase 6 resolution note), `docs/theming/dark-theme-palette.md` (Token Value Table).

### Bug 2 Review — Mystery Create Wizard Phase Resubmission Duplicates (2026-08-04)

- Root cause was a wiring gap, not a missing mechanism: `MysteryCreateStore` already has a full per-entity ID-upsert pattern (`mysteryId`, `editingMonsterId`, `editingMinionId`, `existingMonster*Ids`, `existingMinion*Ids`, `existingLocationIds`, `existingBystanderIds`) but it's populated *only* by `loadEditData()` (whole-mystery edit-mode route load), never after a fresh create in the create-new-mystery flow. Fix is to generalize: set these signals immediately after every successful save (create or update), not just on edit-load.
- Bug is broader than the doc's repro: `submitPhase0()` guards its branch on `isEditMode() && existingId`, not `existingId` alone — so **phase 0 also duplicates** (creates a second Mystery row) on revisit, not just phase 1's monster. Always re-derive scope from the actual branch conditions in code, not just the one reported repro path.
- `isEditMode` is deliberately whole-mystery/route-scoped (governs `isPhaseAccessible()`'s free-nav short-circuit + the created/updated toast + `loadEditData()`) and should stay that way — it's a different concept from per-entity "does this row exist yet," and the fix must not collapse them into one flag.
- Sub-item collections (monster/minion attacks/powers/weaknesses/armors) stay delete-all-then-recreate-all even after the fix — `MonsterService`/`MinionService` have no per-item `update*` methods (matches the `MinionService` update-gap already logged in Phase 8 minions planning), so true per-item PUT isn't a smaller diff here; only the *when do we (re)populate the tracked IDs* wiring needed to change.
- Locations/bystanders are genuinely many-to-many (`LocationDetailResponse`/`BystanderDetailResponse` carry `mysteryIds: string[]`), which is why their "delete" endpoint is actually `UnlinkFromMystery` (doesn't hard-delete the row) — confirms the existing unlink-then-recreate pattern is deliberate, not a bug, and explains why post-fix revisits will still orphan-but-not-delete old rows in the flat catalog on any edit. Pre-existing behavior (same today in edit mode), not introduced by this fix.
- No mid-wizard persistence exists at all (`init()` reads a route-snapshot `:id` once; no localStorage/sessionStorage draft save) — a reload after phase 0 has already created a Mystery row loses `mysteryId` in memory and would create an unrelated second Mystery, before and after this fix. Real gap, explicitly out of scope for Bug 2, flagged for a product decision.
- Docs: `.squad/decisions/inbox/yoshi-bug2-wizard-phase-resubmission.md`.

### Bug 2 Follow-up — Skyler's Three Decisions Resolved (2026-08-04, same day)

- Locations/bystanders don't have a `create()`/`unlinkFromMystery()`/`getByMystery()` on their Angular services (`LocationService`/`BystanderService`) even though `MonsterService` has the equivalent trio — the mystery-create store bypasses the service layer entirely for these two domains via raw `apiService.post/get/delete`. Worth checking for this "service layer has a gap, store works around it with raw apiService calls" shape any time a domain's wizard-integration code looks inconsistent with its sibling domains.
- Backend already has zero `IsNullOrWhiteSpace`/blank-name enforcement for ANY entity (Monster/Location/Bystander/Minion all rely solely on frontend Angular `Validators.required`) — `required string Name` in the EF entity only blocks null, not empty string. When asked to "make field X required, propagate to the backend," always check whether the gap is actually null-vs-non-null (schema/migration question) or blank-vs-non-blank (application validation question) — they're frequently conflated but need different fixes; here it was purely the latter, no migration needed anywhere.
- Conditionally-required form fields ("required only if a sibling field has content") don't fit a static `Validators.required` array. In a codebase that already leans on `computed()` signals for cross-field derived state (this file's `phaseStepComplete` being the precedent), prefer a derived `computed()` boolean consulted directly by `validateCurrentStep()` and the template's `@if`, over Angular's imperative cross-field FormGroup validator (`group.get('x')?.setErrors(...)`) — keeps the conditional-required logic in the same idiom as everything else in the file instead of introducing a second pattern for one field.
- Docs: `.squad/decisions/inbox/yoshi-bug2-wizard-resubmission-resolved.md`.

### Multi-Minion Support — Architecture Plan (2026-08-04, same day as Bug 2 fix landing)

- **Always check `git log` on the actual file before trusting a task brief's "not yet fixed" claim about a bug.** The brief for this task said Bug 2 was unfixed; `git log` on `mystery-create.store.ts` showed the most recent commit (`e83eebb`, same day) already shipped the exact fix I'd designed earlier that day. Reading the live code confirmed it (guard conditions, `LocationDraft`/`BystanderDraft` diff logic, minion-name conditional-required signals all present). A stale task brief is a real, recurring failure mode worth actively checking for, not just trusting — especially on a fast-moving same-day codebase.
- **When a new "add multiplicity" feature lands right after a same-domain upsert-pattern fix, the new feature should generalize the fix's pattern, not reinvent one.** Multi-minion's `MinionDraft` (`id: string | null` + baseline-ID diff) is a direct extension of the `LocationDraft`/`BystanderDraft` shape Bug 2's fix just established — except minions need nested sub-resource collections per draft (they have children; locations/bystanders don't) and hard `delete()` instead of `unlinkFromMystery()` (1:N-owned, not M:N-shared). Worth checking "did a recent fix in this same file already solve half of my new problem" before designing state management from scratch.
- **Reusing "signal arrays, not FormArrays" for multiplicity-of-a-multiplicity (many minions, each with many sub-items) doesn't require a new pattern** — a single `editingDraftIndex: signal<number | null>` reinterpreting the *existing* single-slot form/signals as "the active draft," with explicit save/edit/remove-from-list actions, adds list-level multiplicity without touching the already-proven sub-item-signal-array mechanism at all. Worth remembering as the general shape for "N of a thing that itself has M sub-things" in this codebase's signals-first style.
- Confirmed by grep across controller/service/repository: `DELETE /api/minions/{id}` (hard delete of the minion itself, not a sub-resource) doesn't exist anywhere in the backend — a real, previously undocumented API gap, not just a frontend wiring gap. Minion sub-resource tables are already `ON DELETE CASCADE` on `minion_id` per the original migration plan, so the fix is a thin 4-file mirror of `MonstersController`'s existing `DELETE api/monsters/{id}`, no new migration.
- When a task explicitly says "check whether X is solved as a side effect before assuming it" (here: does list-of-minions model solve the no-edit-in-place friction), actually trace the mechanism rather than asserting — in this case it genuinely does (the same `editingDraftIndex` load/save cycle needed for multiplicity is in-place edit), but the answer isn't always yes and shouldn't be assumed.
- Docs: `docs/updates/multi-minion-support.md`, `.squad/decisions/inbox/yoshi-multi-minion-architecture.md`.

### Standalone Creation Phase 1 — Monsters (2026-08-04, same day, after multi-minion pivot)

- Skyler pivoted away from the just-completed parked multi-minion plan to a stated prerequisite: standalone (outside-the-wizard) creation, one domain object per phase doc, Monster first. When a task explicitly says "several open questions are yours to leave for the stakeholder, not force-resolve," honor that literally even when the answer looks obvious from the code — e.g. flagging "should monster-detail get swapped to the new shared component in this same phase" as an open question (with a stated lean) rather than just doing it, because the task's own scope/blast-radius framing made it a stakeholder timing call, not a pure architecture call.
- **Monster has no `MysteryId` FK at all — only the M:N `MysteryMonster` bridge table** (`Data/Entities/DomainEntities.cs`). This means "can a monster exist unattached to any mystery" is already a fully-supported schema/DTO state today (`MysteryIds: []` is already safe everywhere it's read), not a hypothetical the backend would need to be taught. Always check whether the schema already tolerates the state a new "unattached creation" UI would produce before assuming it needs real backend risk/migration — here it didn't, the gap was purely "no code path produces that state yet," not "the state is unsupported."
- `GET /api/monsters` (flat, mystery-agnostic list) and `DELETE /api/monsters/{id}` (unconditional hard delete) already exist and are already the only endpoints `monsters-list.ts` uses — that page was already, silently, the one genuinely mystery-agnostic monster view in the app before this phase touched anything.
- The minion precedent ("inline stub-create → navigate to existing detail page for sub-resources") transfers in *shape* (create bare entity, do sub-resources on the already-complete detail page) but its "obvious single parent" reasoning doesn't — Monster:Mystery is M:N, Minion:Monster is 1:N. Don't let a recently-designed sibling pattern's conclusion transfer wholesale just because the shape rhymes; re-derive the parent-scoping question from scratch per domain.
- Shared-component call reversed from the multi-minion doc's decision #8 (which declined sharing) for a principled reason, not inconsistency: decision #8 was rejecting a share across two different *submission models* (wizard batch-at-phase-transition vs. monster-detail immediate-mutation); here, create and edit are both immediate-mutation, so the objection that killed sharing there doesn't apply here. Worth checking *why* a prior "don't share" call was made before assuming it generalizes to a new "should we share" question that looks superficially similar.
- Docs: `docs/updates/standalone-creation-phase1-monsters.md`, `.squad/decisions/inbox/yoshi-standalone-creation-phase1-monsters.md`.

### Standalone Creation Phase 1 — Monsters, Revision After Skyler's 4 Answers (2026-08-05)

- Skyler's answer to "does bare-bones MVP include sub-resource panels" diverged from my recommendation (yes, include them on the create page itself) and broke a load-bearing assumption the original doc was built on (sub-resources deferred to an already-existing monster's detail page). When a stakeholder's answer invalidates a prior architecture assumption rather than just filling in a blank, don't just flip the status flag — re-derive the design from the new constraint. Here that meant recognizing `monster-detail.ts`'s immediate-per-action sub-resource pattern (`createAttack(monsterId, ...)` etc.) structurally cannot work on a page where the monster doesn't exist yet, and needed a genuinely new mechanism, not a copy-paste.
- **Chose local draft arrays (`signal<T[]>`) + single batched submit over "create-then-reveal" (silently create bare entity, then reveal existing immediate-mutation panels).** The deciding signal was the stakeholder's own wording ("not deferred to a second navigation") — read literally, it rules out *any* gating step before sub-resources are reachable, including an automatic/silent one, not just a manual click-through. When a stakeholder's rejection of a prior design specifically calls out "a second navigation," check whether the alternative you're considering still has a hidden version of the same gating step before assuming it satisfies the constraint.
- The parked wizard (`mystery-create.store.ts`) already had a complete, proven precedent for exactly this shape (`AttackDraft`/`PowerDraft`/`WeaknessDraft`/`ArmorDraft` + `saveThreatCollections`/`runBatch`, create-entity-then-`forkJoin`-its-children) — read it for precedent (field shapes, sequencing) without importing or modifying it, keeping the "parked plan stays untouched" instruction intact while still not reinventing a working pattern from scratch. New page-scoped drafts were simpler than the wizard's own (no `id: string | null` needed — a fresh create page has no existing-baseline to diff against, unlike the wizard's edit-an-existing-mystery case).
- When a coordinator explicitly asks you to re-check an earlier decision's justification for internal consistency after a downstream decision changes, actually re-scope the claim rather than leaving it as-is or deleting it — decision 1's "both sides are immediate-mutation" claim was narrowed to "the shared component's own save contract is immediate-mutation on both sides" (still true) rather than "the whole page is" (no longer true once the page picked up a batch step). Naming the throughline principle explicitly ("share only when submission models match") let it consistently justify a share call (the core-fields component) and a don't-share call (the sub-resource panels) in the same document without reading as contradictory.
- Docs: `docs/updates/standalone-creation-phase1-monsters.md`, `.squad/decisions/inbox/yoshi-standalone-creation-phase1-monsters.md`.

### Standalone Creation Phase 2 — Minions (2026-08-05, same day as Phase 1 shipped)

- Phase 1 shipped/committed same-day; re-verifying a "what shipped" summary against the actual committed code (not trusting the coordinator's recap) is worth doing even for a same-day handoff — confirmed `monster-create.ts`/`monster-form.ts`/`monster-detail.ts`/`monsters.routes.ts` all matched the summary exactly, but this should never be skipped since summaries can drift from implementation details (here they didn't, but the check is cheap and the failure mode if skipped is expensive).
- **A required 1:N attachment (Minion→Monster) is not just "Monster's optional M:N attachment minus the blank option" — it changes which entry-point shapes are even reasonable.** An optional dropdown defaulting blank costs nothing extra when the user already knows the answer; a *required* dropdown over every row of a domain forces a real extra step even for the most common case (user already on the parent's own page). Recognized this as the reason Minion's entry-point question isn't a copy of Monster's mystery-scoping question despite looking structurally similar (both are "how does this child pick its parent") — worth checking whether a required-vs-optional flip changes the shape of a product question before assuming a prior phase's resolved answer transfers.
- Reused an existing, already-shipped mechanism (`minion-detail.ts`'s own param-presence-driven `mysteryId`/`monsterId`/neither branching for `backLink()`) as the architecture for a *new* problem (create-page entry-point locking) rather than inventing a new "context passed via query param" convention — worth checking whether a codebase already has an established mechanism for "same page, multiple ways to arrive, adjust behavior by which route params are present" before adding a new one.
- Confirmed a real, load-bearing asymmetry worth remembering: `UpsertMinionRequest` has no `monsterId` field in either direction (create takes it as a separate method param; update has no way to change it at all) — stronger than Monster's "mystery picker is create-only" reasoning, because here the shared form component's request contract has *nothing to attach a parent-id input to* even if someone wanted to add one. When deciding "does field X belong inside or outside a shared form component," check the actual request/response contract shape, not just the UX mode (create vs. edit) — sometimes the contract itself rules it out more strongly than the UX does.
- Verified a real "no gap" finding (not just absence of evidence): Minion needed zero new backend work for creation, unlike Monster's Phase 1 endpoint gap, confirmed by reading `IMinionService.cs`/`MinionService.cs`/`MinionsController.cs` in full and finding `CreateAsync(Guid monsterId, ...)` already does exactly what's needed. Worth remembering as a genuine possible outcome of a phase-by-phase initiative: not every phase needs the same shape of backend work just because the previous one did.
- Docs: `docs/updates/standalone-creation-phase2-minions.md`, `.squad/decisions/inbox/yoshi-standalone-creation-phase2-minions.md`.

### Standalone Creation Phase 3 — Locations (2026-08-05, same day as Phase 2 shipped)

- **Two of Phase 1/2's load-bearing findings did NOT transfer to Location, confirming per-domain verification is worth doing every time, not just for the first phase in an initiative.** (1) Location has zero interactive sub-resources — its only child collection (`CustomMoves`) has full backend CRUD but `location-detail.ts` renders it fully read-only, so the entire local-draft-array + batched-submit design (the dominant complexity in both Phase 1 and Phase 2) simply doesn't apply; the create page is a single form, one API call. (2) There's no entry-point fork the way Minion had one — Location has no non-Mystery parent to scope an alternate "already-in-context" entry point under, so unlike Minion's two-routes-one-component design, there's exactly one plausible entry point here. Both of these could easily have been assumed-away by pattern-matching on "M:N to Mystery, looks like Monster" — the sub-resource question in particular needed an explicit check of what the current frontend actually renders, not just what the backend contract supports.
- **Named a reusable principle after the third phase confirmed it's a real pattern, not a one-off:** when a new phase's relationship shape is structurally *identical* (verified at entity/contract/controller layer, not just "seems similar") to an already-decided phase's shape, re-deriving or re-asking the same question is redundant. Used this to decline reopening mystery-scoping (reused Monster's optional-dropdown-default-blank answer directly), entry-point placement, and the detail-page-rewire timing question (third time this exact decision shape has come up; Skyler answered identically twice already) — all cited by reference to the earlier phase's decision number rather than re-presented as open.
- **A genuinely empty "Open Questions for Skyler" section is a valid, sometimes-correct outcome** — stated so plainly with the reasoning for each would-be fork, rather than manufacturing a question to match the shape of the prior two docs. The coordinator's instruction anticipated this explicitly; worth remembering that padding a standard section for consistency across a doc series is worse than an honest "none, and here's why" when that's what the verification actually supports.
- **Declined a cheap, in-scope addition (a `mystery-detail.html`-scoped "Add Location" button) specifically because it would create a new cross-domain asymmetry** — Monster's Phase 1 already deferred the identical entry point for the identical relationship shape; adding it now for only one of two structurally-identical domains would be a worse outcome than deferring it consistently for both. Worth checking "would doing the locally-better thing here create an inconsistency with an already-shipped sibling decision" before taking an easy win.
- Flagged explicitly for the next phase (Bystander) that it *looks* structurally identical to Location at a glance (M:N to Mystery, `CustomMoves`-only child collection) but that resemblance is exactly the kind of assumption this phase was told not to make about Location itself — don't let this doc's conclusions become an unverified template.
- Docs: `docs/updates/standalone-creation-phase3-locations.md`, `.squad/decisions/inbox/yoshi-standalone-creation-phase3-locations.md`.

### Standalone Creation Phase 4 — Bystanders, Initiative Complete (2026-08-05, same day as Phase 3 shipped)

- Followed my own Phase 3 closing-note instruction literally: independently re-verified every claim about Bystander (entity, DTOs, controller, service, both frontend pages, routing) against current source rather than trusting the "looks structurally identical to Location" resemblance, even though the resemblance turned out to be exactly correct in the end. The value wasn't in expecting a different result — Location itself proved the resemblance heuristic can mislead (it "looked" like it might need Monster's sub-resource machinery and didn't) — the value was in not letting a correct guess substitute for verification a second time. Worth generalizing: when asked to re-verify something you already suspect will match a prior finding, the check is still worth doing in full; a confirmed match and an assumed match look identical in the final doc but aren't the same claim.
- **This is the first phase in the initiative where every dimension of the design (mystery-scoping, sub-resource shape, entry point, detail-rewire timing) matched an already-decided prior phase's answer.** Result: a fully populated Resolved Decisions table with zero fresh judgment calls (every row cites either a direct precedent or freshly-gathered evidence) and a second consecutive empty Open Questions section. Worth remembering as a legitimate shape for the *last* phase of a repeated-pattern initiative — a full verification pass that happens to conclude "nothing new to decide," not a shortened or templated document just because it's the final one.
- Caught a concrete implementation-relevant asymmetry the "looks identical" framing could easily have missed: `LocationServiceTests.cs` already exists in the test project, `BystanderServiceTests.cs` does not — the backend sub-phase needs a new test file, not just new cases appended to an existing one. Small, but exactly the kind of detail a template-copy approach (as opposed to fresh-per-domain verification) would have silently gotten wrong.
- Docs: `docs/updates/standalone-creation-phase4-bystanders.md`, `.squad/decisions/inbox/yoshi-standalone-creation-phase4-bystanders.md`. This closes the four-domain standalone-creation initiative (Monster/Minion/Location/Bystander all have phase docs now).

### Hunter Playbooks — Fresh Pass After Doc Deletion (2026-08-25)

- **When a stakeholder deletes a prior planning doc and says "start clean, more consideration needed," don't skim-reuse its content conclusions — only its verified-environment facts.** Reused: PDF is 28 playbooks, `pdftotext -layout` is the extraction tool. Discarded and re-derived from scratch: the data model, the seeding recommendation, and the standard-vs-bespoke split — because the new brief's actual sequencing (Admin-UI-driven CRUD before bulk import) changed what the *correct* answer was, not just what needed re-verifying. The seeding call flipped entirely: prior pass wanted a JSON seed file (assumed deploy-time bulk load); this pass's brief has Skyler entering data through a Phase-3 Admin UI against the live DB, which makes Playbooks ordinary admin-authored CRUD rows, not seed data — `MotwDbInitializer` needs zero changes.
- **A PDF's 2-3 column layout under `pdftotext -layout` produces two distinct, real failure modes, both hit this pass**: (1) two logically-unrelated static columns (blank rating boxes; the fixed 8-basic-move reference list) merged onto matching line numbers, reading as a false per-stat move pairing — confirmed spurious by checking the move list is word-for-word identical across every playbook read, not tied to any one playbook's stats. (2) Page-bleed: reading a raw offset range near a playbook's title header pulled trailing content from the *previous* playbook in the PDF. Caught by cross-checking wording (a suspicious "Change this hunter to a new playbook" vs. the other two playbooks' matching "Change this hunter to a new type") before trusting it. Both are worth checking for by name, not just "does this look weird," on any future multi-column PDF extraction in this project.
- **A section predicted by the stakeholder to need a standard/bespoke split can turn out to need zero schema-level split once actually read** — Improvements/Advanced Improvements varies entirely in *which text* appears (confirmed: Crooked is missing a Weird stat-boost option that Chosen/Divine both have; one bespoke bonus item differs per playbook), never in structural shape (always a flat numbered pick-list). Modeled as one flat `PlaybookImprovement(Text, IsAdvanced, SortOrder)` table — resisted the temptation to add semantic sub-typing (`IsStatBoost`/`IsMoveGrant`) from only 3 samples, since that's exactly the over-fit-to-a-small-sample risk the task itself warned about for a different section (gear).
- **A guessed structure ("gear = named presets of weapon tags") can be genuinely wrong in an interesting way without requiring a new schema concept.** Chosen's "special weapon" is a real build-your-own (pick 1 of 4 forms + 3 of 7 modifiers + 1 material) — not a flat preset list — but it decomposes cleanly into the *same* `PlaybookGearCategory`(nullable `PickCount` = null means auto-grant-all, `IsOptional` bool)→`PlaybookGearOption` shape already needed for the simpler cases (Crooked's pick-3-of-9, Divine's fixed no-choice armor grant). Worth the general lesson: before inventing a second schema shape for content that "looks different," check whether a nullable/flagged version of the first shape already covers it.
- **When a new phase's own brief contains an explicit Admin-UI-CRUD requirement for content a previous pass had scoped as deploy-time seed data, re-derive the seeding answer from scratch rather than carrying the old recommendation forward** — the "must work in a deployed cloud production environment too" constraint is trivially satisfied once the Admin UI *is* the production data-entry path (same mechanism as every other domain entity in this app, already proven in production), which fully dissolves what looked like a hard problem in the prior framing.
- Confirmed `Data/Migrations/` (not top-level `Migrations/`) is this project's actual migrations folder path, and `Program.cs`'s `MigrateAsync()` + `MotwDbInitializer.InitializeAsync()` both already run on every startup including the Railway production deploy (per the `ForwardedHeaders`/`CookieSecurePolicy` production-specific comments already in that file) — useful confirmation that "does this already work in production" questions for this app usually resolve by reading `Program.cs` directly rather than needing new investigation.
- Flagged for Phase 5 (not designed this pass, per Skyler's explicit deferral): Crooked alone has three distinct named bespoke concepts (Background/Heat/Underworld), not one, and at least one (Heat) has real per-slot structure (named enemy relationships), not just prose — the single `UniqueMechanicText` free-text placeholder column is very likely insufficient once Phase 5 is designed for real, stated as a hypothesis to re-validate against more playbooks, not a conclusion.
- Docs: `docs/hunter-playbooks/{README,architecture,phases,open-questions}.md`, `.squad/decisions/inbox/yoshi-hunter-playbooks-phase1-plan-v2.md` (supersedes `yoshi-hunter-playbooks-phase1-plan.md`, marked superseded in place rather than deleted).

### Hunter Playbooks — Seeding Recommendation Corrected by Skyler (2026-08-25, same day)

- **Real mistake, not just an ambiguity Skyler resolved**: the first pass concluded "Playbooks are ordinary Admin-UI-authored CRUD rows, no seed file" by reasoning from Phase 3/4's Admin-UI requirement alone, without checking it against the actual distribution requirement (present automatically in every environment, like the static lookup tables) that Skyler's brief had already implied via "possibly more added locally... eventually work in cloud production too." A Data Admin UI can never satisfy "present in every environment automatically" by itself, no matter how good the UI is — every environment's database is independent, so anything entered through a UI stays wherever it was entered until something else propagates it. Worth generalizing: "there's a UI for creating X" and "X needs to exist everywhere automatically" are separate requirements that don't imply each other, and conflating them is an easy mistake when a brief mentions both in the same phase list without stating the relationship.
- **Correction re-adopted the deleted prior pass's JSON-seed-file idea, but for a different, more specific justification** — not "volume is large" (true but doesn't by itself establish *why* a seed file, since a big UI form is also just effortful, not wrong) but "this table needs the same automatic-everywhere distribution as the existing seeded lookup tables, and `MotwDbInitializer` is this codebase's only mechanism that provides that." When a stakeholder correction points back toward an idea you (or a prior pass) already discarded, re-derive the justification from the new framing rather than either blindly re-adopting the old reasoning or resisting the idea because it was discarded once already — same conclusion, different and better-grounded reason.
- **Caught a real precedent gap while designing the fix**: every existing seeded table in `MotwDbInitializer` uses a single table-level `AnyAsync()` guard, correct only because those tables' contents are fixed forever. Playbooks' seed set is explicitly expected to grow across future phases (3 → 28 → "possibly more") — a blanket table-level guard would silently stop seeding new playbooks the moment the table had *any* rows. Needed a per-playbook (per-`Name`) idempotency check instead — worth checking, for any future "extend the existing seed mechanism" task, whether the new table's dataset is expected to grow post-initial-seed the way none of the existing ones do, since the existing pattern silently assumes it won't.
- **Split the Admin UI's purpose into two genuinely different paths once the seeding correction forced the question**: Path A (canonical, code-seeded via the JSON file, everywhere automatically) vs. Path B (Skyler's own future one-off additions via the UI directly, stays wherever created, same as any other reference-data addition today — not wrong, just a different category of content than the canonical 28). The UI itself doesn't change scope from the original plan; only which of these two things it's understood to be doing changes.
- Reused verified fact from earlier in this same task (not re-derived): `Program.cs`'s `MotwDbInitializer.InitializeAsync()` call and `MigrateAsync()` genuinely run unconditionally on every startup including production — this was already confirmed by reading `Program.cs` directly earlier in the session, so the correction only needed re-stating it, not re-verifying it from scratch.
- Docs: `docs/hunter-playbooks/architecture.md` Section 4 (full rewrite, not a patch), `docs/hunter-playbooks/phases.md` Phases 3/4/7, `docs/hunter-playbooks/README.md` Phase summary + status note, `docs/hunter-playbooks/open-questions.md` Q1 (reframed) + new Q8.

### Hunter Playbooks — All 8 Open Questions Resolved, Second Same-Day Correction (2026-08-25)

- **A stakeholder's answer to a narrow, specific open question (Q1a, export-tool shape) can invalidate a broader design decision two levels up if the timing implication isn't traced through.** Skyler's actual words only picked "one-off script, not a reusable tool" — but "run it once, when the 28-playbook effort is essentially done" also meant the conversion happens *after all 28 are authored*, not incrementally per phase (Phase 4 for 3, Phase 7 for 25 more) as the doc had assumed. That timing shift, once traced, made an entire other piece of my own design (the "deliberate deviation": a per-playbook idempotency guard instead of the existing blanket per-table `AnyAsync()` pattern) not just unnecessary but actively wrong — the blanket guard is correct again once seeding is a single one-time event. Worth generalizing: when a stakeholder answers a narrow question, re-derive every downstream design choice that narrow question's *literal reasoning* touches, not just the one line item it was nominally about.
- **Caught a second, larger wrong assumption while resolving the first correction's own follow-up questions** (Q1b: who/what actually authors the canonical 28). The Q1a/Q1b split I wrote myself already separated "how does authored data get exported" from "who authors it" — but I had only asked about the export mechanism as genuinely open; I'd left "authored via the Phase 3 Admin UI form" as a settled assumption carried over from the *first* correction, not re-questioned it. Skyler's answer showed it was wrong too: an AI agent authors via the real API, the form is a testing/manual-tweak surface only. Worth remembering: after a stakeholder correction lands, don't just re-verify the specific claim they flagged — check whether adjacent claims in the same section were built on the same now-suspect reasoning ("the Admin UI is the mechanism") even if phrased as a different sub-question.
- **When a stakeholder explicitly says "your judgment" on a design fork (one-by-one vs. bulk agent authoring) while also naming two considerations that look like they pull opposite ways (catches inconsistencies vs. context-window degradation), check whether they're actually in tension before picking a side.** They weren't: "one bounded task per playbook" (fresh context each time, not one growing session) satisfies both simultaneously — the tension only existed under a false binary ("one continuous session for all 28" vs. "one bulk prompt for all 28") neither of which was actually the only two options. Worth checking for a false binary before treating stated countervailing concerns as a trade-off that needs balancing.
- **A live-link-vs-snapshot schema question resolves in a way that's easy to get backwards without the stakeholder's own domain framing.** My reasoning ("Playbook is a template Skyler may edit; a Hunter's sheet shouldn't silently change") was internally consistent but wrong for *this* domain specifically — Skyler's actual expectation (playbooks rarely change; when they do, propagating to existing instances is a feature) is closer to how a shared master template naturally works in tabletop play, not how a typical "snapshot at creation time for stability" data-integrity instinct would default. Worth double-checking a live-link/snapshot call against the *domain's* actual expected change frequency and desired behavior, not just generic data-modeling caution.
- Docs: `docs/hunter-playbooks/architecture.md` Sections 0/2/3/4/6 (BasicMove firm, Hunter live-link via `PlaybookStatArrayOptionId` FK, Hunter–Mystery confirmation, full Path A rewrite for agent-driven authoring + one-time-conversion timing + dropped per-playbook guard, Path B tightened, Section 6 acknowledgment), `docs/hunter-playbooks/phases.md` Phases 2/3/4/6/7/8/9, `docs/hunter-playbooks/README.md`, `docs/hunter-playbooks/open-questions.md` (full rewrite: all 8 resolved with answers, 1 new question raised — Q9, agent-authoring review/verification checkpoint, genuinely still open).

### Hunter Playbooks — Q9 Resolved, Skill-vs-Documentation Recommendation (2026-08-25, third round same day)

- **Q9's answer (agent self-verification sufficient, extra rigor only for the 3 pilots) reframed Phase 4's actual purpose, not just its verification step.** The doc previously framed Phase 4 as "prove the model works against 3 playbooks" with verification as an open side-question. Skyler's answer made the verification question and the phase's core purpose the same thing: Phase 4 exists specifically to use low-stakes 3-playbook rigor to discover and fix the repeatable pattern's rough edges *before* Phase 7 runs it 25 more times at higher stakes. Worth generalizing: when a stakeholder's answer to what looked like a narrow implementation-detail question (verification checkpoint) turns out to restate the phase's actual goal, rewrite the phase's stated purpose, not just the narrow answer — the two were never actually separate questions.
- **First genuine "give me a recommendation on a tool/mechanism I have first-hand operational knowledge of" ask in this task.** Answered directly (package as a Claude Code Skill) rather than an options list, per this role's standing instruction — but explicitly separated "decide the mechanism now" (yes, answerable now, low regret either way) from "write the mechanism's content now" (no — Phase 4's own newly-clarified purpose is to discover that content, so writing it now would be exactly the over-specification mistake already avoided once in this same doc for Phase 5's bespoke-schema shape). Worth remembering as a reusable split for any future "should we build tool/process X" recommendation: the *mechanism* choice and the *content* of what goes in it are often separable, and separating them can resolve an apparent "isn't this premature" tension without deferring the whole question.
- **Named the honest limitation of the recommended mechanism rather than oversell it**: a Skill doesn't mechanically enforce the self-verification checklist any more than a markdown procedure doc would — both rely on the agent actually following written instructions. The real, defensible advantage is operational (reliable discovery/loading across ~28 separate bounded task invocations, and a clean split between `phases.md`'s "why" and the Skill's "how") — not a stronger guarantee. Stating a recommendation's real limitation alongside its real advantage is worth doing explicitly whenever the tempting framing ("do X and it'll be more rigorous") isn't quite true and a more honest framing ("do X and it'll be more reliably followed, though not more strictly enforced") is available instead.
- Docs: `docs/hunter-playbooks/open-questions.md` Q9 (resolved) + Q10 (new, recommendation given not Skyler-confirmed), `docs/hunter-playbooks/phases.md` Phase 4 (purpose reframed, Skill deliverable added) + Phase 6/7 (Skill inheritance noted), `docs/hunter-playbooks/architecture.md` Section 4 (mirrored resolution + recommendation pointer), `docs/hunter-playbooks/README.md`.

### Hunter Playbooks — Phase 5 Ideation, First Genuine "Help Me Think" Request (2026-08-25, fourth round same day)

- **Re-verifying against source text before ideating caught a real gap in the task brief's own framing.** Skyler's message used Crooked "as an example" implying (correctly) it wasn't the only pilot with bespoke content, but a fresh re-read of Chosen's Fate and Divine's Mission surfaced two structural shapes neither the brief nor my own earlier Section 6 hypothesis had named: a label-only "tag pick" shape (Chosen's Heroic/Doom tags — no description text at all, the inverse of Crooked's Background) and a flat description-only pick-1 shape (Divine's Mission — simpler than any of Crooked's three). Five confirmed shapes from 3 playbooks, not three. Worth the general lesson again (third time this exact pattern has paid off in this task): re-verify directly against source material before reasoning about it, even when a stakeholder's own framing already sounds correctly-scoped — "Crooked as an example" was right, but didn't tell me what the *other* examples actually looked like.
- **A genuinely open "help me think" request (first one in this task) is a different deliverable shape than every prior round** — candidate approaches with honest tradeoffs and a provisional recommendation, explicitly not a spec. Kept literal DDL and EF entity syntax out of the doc per the task's own instruction, using the same illustrative-field-list style already established elsewhere in `architecture.md` instead — worth remembering as the right register for "ideation" specifically, distinct from this task's other rounds which were specifying real decisions.
- **Found a way to answer "should X go in a generic attribute table" that isn't "yes" or "no" but "it depends on which X"** — recommended real typed columns for min/max-select (only ever two of them, every shape uses the same two — a generic mechanism buys nothing) while explicitly declining to model "blank" as a schema concept at all (the source PDF text already contains a literal blank marker; storing it verbatim and treating detection as a frontend concern avoids inventing a schema concept the source material doesn't need — the same principle already used for gear's mechanical text and Improvements' flat text in this same doc). Worth generalizing: "should this be generic/attribute-driven" doesn't have to be answered once per table — different candidate attributes can get different answers from the same reasoning framework (how many possible values, does every shape need it, does the source data already encode it structurally).
- **Noticed the recommended shape doesn't need a stored discriminator column at all** — "which shape is this" is fully derivable from which nullable fields are populated (title set vs. null, parent-FK set vs. null), the same way Gear's shape already emerges from `PickCount`/`IsOptional` without a `GearShapeKind` enum. Surfaced this as a simplification rather than assuming a named-shape-kind enum was needed just because Skyler's own working idea implied naming shapes — the naming request was answered as vocabulary for docs/conversation, kept separate from whether the schema itself needs to store that name.
- **Instance-side JSON-vs-bridge-table argument got stronger once traced against decisions already made earlier in the same conversation, not just general codebase precedent.** Hunter already has two FK-bridge-table selection mechanisms (`HunterMove`, `HunterGearSelection`, both resolved as live-linked when Q4 was answered) — recommending JSON for bespoke selections would be a third, different persistence shape on the same entity for conceptually the same kind of data, an internal inconsistency on top of the codebase-wide "no JSON precedent" argument already used once in this task. Worth checking whether a precedent argument can be sharpened from "the whole codebase does X" to "this exact entity, earlier in this same conversation, already committed to X" before defaulting to the broader/weaker version of the argument.
- Docs: `docs/hunter-playbooks/phase5-bespoke-ideation.md` (new file — re-verified 5-shape enumeration, 3 candidate approaches with tradeoffs table, provisional no-enum recommendation, 4 explicit open sub-questions), `docs/hunter-playbooks/architecture.md` Section 6 (pointer added, original hypothesis marked superseded-in-detail), `docs/hunter-playbooks/phases.md` Phase 5 (reframed as active ideation, summary + pointer), `docs/hunter-playbooks/README.md` (status note + file list + Phase 5 summary line).

### Hunter Playbooks — Phase 5 Ideation Round 2: Catalogue Extraction, Q1-Q4 Resolved, Formatting Requirement (2026-08-26)

- **A stakeholder's brief answer to a design question can quietly correct a scoping error the question itself was built on — worth tracing "why" all the way through, not just recording the answer.** Q4 asked "does the instance-side selection table need to distinguish blank-fill from wholly-custom-override." Skyler's answer ("only matters for Advanced Improvements") wasn't really answering the question as framed — it was pointing out the motivating example (Divine's "mission of your creation") belonged to a different, out-of-scope mechanism (Advanced Improvements, level-up-time) entirely, not the initial-pick table the question was about. Net result: `HunterBespokeSelection.BespokeOptionId` reverts from nullable back to required, a real simplification, not just a "keep one column" answer. Had to re-check Section 1's own earlier claim (which had stated this as a confirmed initial-pick requirement) and correct it there too, not just in the Q&A section — an answer that corrects framing needs to ripple to every place the original framing was asserted as fact, not just the question it was nominally answering.
- **Explicitly distinguished "confirming a reading of an ambiguous stakeholder answer" from "silently picking the reading that requires no rework."** Q1's answer ("no extra field needed... falls out of the existing structure") was genuinely ambiguous between two readings (confirm the already-drafted recursive field design vs. reject it for a hardcoded rule) that happened to differ in whether any doc rewrite was needed. Resolved via the stakeholder's own stated reasoning ("no special-casing... in UI or API" — a hardcoded rule would itself be special-casing) rather than defaulting to the reading that was less work, and stated the disambiguation logic explicitly in the doc per the stakeholder's own request to flag it if genuinely unresolved.
- **A "preserve formatting" requirement surfaced a real, previously-unexamined limitation of this task's own established tooling**: `pdftotext -layout` (confirmed multiple times earlier in this task as the right extraction tool) discards all bold/italic/font-weight information — plain-text extraction has no way to represent it. When asked to populate a catalogue with real content demonstrating a newly-agreed rich-text convention, caught that I could not actually verify which spans of Crooked's Background text were bold in the source (only that they existed, per the stakeholder's own claim) — populated the catalogue with fully accurate text but explicitly declined to invent markup positions, flagging the extraction-method gap explicitly rather than fabricating plausible-looking bold spans. Worth the general lesson: a new fidelity requirement can retroactively expose that an already-established, already-trusted tool/process doesn't actually support it, and that's worth surfacing as its own finding, not quietly worked around with invented content.
- **Checked the actual codebase (package.json + grep for `innerHTML`) before recommending HTML vs. Markdown for rich-text storage, rather than defaulting to whichever sounds more "standard."** Neither had any existing precedent one way or the other, which made the real deciding factor legible: Angular's `[innerHTML]` + built-in `DomSanitizer` renders a safe tag subset with zero new dependency, while Markdown would require adding a parser library for a narrow, fixed formatting need (bold/italic/bullets only). Recommended a small enumerated HTML tag allowlist over full Markdown syntax specifically because of this dependency asymmetry — verified, not assumed.
- **Flagged (didn't unilaterally act on) a cross-cutting consequence that reached outside the file I was asked to update**: Skyler's own formatting example (custom-move roll-result bullet lists) pointed directly at `PlaybookMove.DescriptionText`, a field already established in Phase 2's model (`architecture.md`, itself only "leaning towards" settled, not confirmed) — outside this round's explicit scope (`phase5-bespoke-ideation.md` + the new catalogue file). Noted it in-doc and flagged it for the report-back rather than editing `architecture.md` unilaterally, consistent with this task's repeated pattern of treating "asked to update file A" as a scope boundary, not an invitation to also fix file B's related-but-not-requested issue without flagging it first.
- No `.squad/decisions/inbox/` entry filed this round either, per the coordinator's explicit repeated instruction — Skyler is still "leaning towards" Section 3, not confirmed, and this round's work (Q1-Q4 resolution + the catalogue) is still squarely ideation, not a locked decision.
- Docs: `docs/hunter-playbooks/phase5-bespoke-ideation.md` (Section 1 Divine-mission correction, Section 4 expanded with formatting/markup guidance, Section 5 all 4 questions marked resolved with reasoning, new status/date markers), `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (new file — first real entries for all 3 pilots' bespoke rulesets, organized by playbook per the requested convention, explicit gap flagged on unverifiable bold/italic positions), `docs/hunter-playbooks/README.md` (status note + file list).

### Hunter Playbooks — Phase 5 Ideation Round 3: Pipeline Spike Dispatched, Two Real Decisions Land Inside a Still-Open Framework (2026-08-26, same day)

- **A flagged gap that gets escalated to another agent (Bowser's extraction spike) still needs its status changed everywhere it was described as open, not just acknowledged once.** The prior round's gap note lived in two places (`phase5-bespoke-ideation.md`'s cross-cutting-consequence paragraph and `bespoke-ruleset-catalogue.md`'s intro) — updated both to "active spike, owned by Bowser, here's the acceptance test" rather than leaving either to read as still-unaddressed once the coordinator reported it was being solved elsewhere. Worth the general check: when a flagged-but-unowned gap gets picked up by someone else, grep for every place this doc described it as open before considering the update done.
- **Distinguished "the overall framework is still ideation" from "specific sub-decisions inside that framework can still be genuinely locked."** Skyler's Fate-structure call and the Phase 2 formatting-gap/Phase-4-checkpoint process change are both stated in confirmed, non-tentative language ("Skyler decided, not asking you to re-derive"; "Skyler agrees... confirmed") — different in kind from Section 3's own "leaning towards, not confirmed" framing one round earlier. Filed a `.squad/decisions/inbox/` entry for these two specific items while explicitly keeping Section 3's broader schema out of it and still marked ideation — worth remembering that a coordinator not repeating a prior round's "no decisions/inbox yet" instruction can itself be a signal the constraint has narrowed, not necessarily lifted entirely; re-read what's actually being asserted as settled per-item rather than applying the last round's blanket caveat forward unchanged.
- **A structural decision justified by "the source material itself is exceptionally messy, and that's fine to handle by hand later" is a legitimate design closure, not a deferral to flag as unresolved.** Skyler's Fate reasoning explicitly invoked the already-established Q9 manual-review step (self-verify, then Skyler reviews) as the intended fix path for this specific playbook's layout oddity — recognized this as closing the loop on an already-agreed process rather than opening a new one, and cited that connection explicitly in the catalogue entry rather than treating the messiness as a fresh unresolved risk.
- **The Fate mandatory-pick-all-three-categories requirement (`MinSelect=MaxSelect=3` of exactly 3 options) validated the recursive `MinSelect`/`MaxSelect` design against a shape it hadn't been walked through yet** — "pick all of N" falls out for free as the degenerate case of "pick N of M" where N=M, no new flag or concept needed. Worth noting as a real (if small) confirmation that the schema generalizes correctly to a case not in the original 5-shape enumeration, found by applying a real stakeholder decision rather than hypothesizing edge cases in the abstract.
- Docs: `docs/hunter-playbooks/phase5-bespoke-ideation.md` (status/date update, Fate walkthrough rewritten to the confirmed single-section structure, cross-cutting-consequence paragraph updated to "confirmed" + Phase 4 checkpoint + Bowser spike note), `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (gap note updated to reference the active spike, Chosen/Fate entry rewritten to the confirmed structure), `docs/hunter-playbooks/phases.md` (Phase 4 new explicit checkpoint), `docs/hunter-playbooks/architecture.md` (Phase 2/Moves section flags the same formatting-fidelity gap), `docs/hunter-playbooks/README.md`, `.squad/decisions/inbox/yoshi-hunter-playbooks-formatting-fidelity-and-fate-structure.md` (new — first decisions/inbox entry for this Phase 5 ideation thread, scoped narrowly to the two settled items).

### Hunter Playbooks — Formatting-Fidelity Scope Narrowed to Moves Only, Same Day (2026-08-26)

- **A same-day "confirmed" note I wrote turned out to be overstated within hours — the correction wasn't in the reasoning, it was in the scope.** I'd generalized "Skyler confirmed formatting fidelity matters beyond bespoke content" into "...for any Description-type standard field," citing `PlaybookGearOption.MechanicalText`/`PlaybookImprovement.Text` as likely examples I hadn't actually been told needed it — only `PlaybookMove.DescriptionText` had a real, stated example (bulleted roll-result breakdowns). Skyler's actual scope was Moves only. Worth the general lesson: when generalizing from one confirmed example to "and probably these other similar-looking fields too," that's an inference being presented as if it were already confirmed — needed its own explicit confirmation, which it didn't have, and got corrected once actually checked.
- **The same overstatement had propagated into four separate files by the time the correction landed** (`architecture.md`, `phases.md`, `phase5-bespoke-ideation.md` — twice, in two different paragraphs — and `README.md`), plus a filed `.squad/decisions/inbox/` entry. Grepped for the exact overstated phrase across the whole `docs/hunter-playbooks/` directory to find every copy rather than trusting memory of where it had been written — found one occurrence the initial mental list would have missed (a second paragraph further down the same ideation file, not just the first one edited). Reinforces the pattern from the previous round's history entry: a corrected claim needs a directory-wide sweep, not a fix wherever it's first noticed.
- **Treated the already-filed `.squad/decisions/inbox/` entry as append-only once a stakeholder correction lands after it's filed** — added a dated correction note at the top rather than silently rewriting the original "What" text, matching the same convention already established earlier in this task for the superseded `hunter-playbooks-phase1-plan.md`. An inbox decision record should show what was believed true at the time it was filed, with corrections layered visibly on top, not retroactively edited to look like it was always right.
- Docs: `docs/hunter-playbooks/architecture.md` (Moves section narrowed to Moves-only + Bowser pipeline confirmed), `docs/hunter-playbooks/phases.md` (Phase 4 checkpoint narrowed to Moves-only), `docs/hunter-playbooks/phase5-bespoke-ideation.md` (both instances of the overstated cross-cutting note corrected, extraction-pipeline status updated from "spike" to "resolved"), `docs/hunter-playbooks/README.md`, `.squad/decisions/inbox/yoshi-hunter-playbooks-formatting-fidelity-and-fate-structure.md` (correction note appended, original left intact).

### Mystery Required-Field Validation — First Pass of a New Cross-Domain Initiative (2026-08-05, same day as standalone-creation initiative closed)

- **When a claim is about runtime behavior (does validation X actually fire), verify against the live app, not just the code shape.** Found an already-running dev API instance (`dotnet run` on port 5225, `docker compose` postgres already up) and curled it directly rather than trusting static analysis of `[ApiController]`/DataAnnotations wiring — confirmed empirically that ASP.NET's implicit-required inference for non-nullable reference types is active with zero explicit attributes (blocks null/missing `Name`), that it does NOT block whitespace-only strings (created two real `""`-named Mysteries live, then deleted them), and that the codebase's *existing* explicit `[Required]` precedent (`CreateAdventureTypeRequest`) already correctly rejects whitespace via `RequiredAttribute`'s default `AllowEmptyStrings=false` trim behavior. All three findings were load-bearing for the recommendation and none were safe to assume from reading the C# alone.
- **"Required" splits into two categories needing two different validation layers, and conflating them under one fix would have left a real gap unfixed.** Presence/shape of a scalar (string blank-check) is a pure syntax question DataAnnotations already handles for free at the model-binding layer; existence/validity of a referenced entity (FK Guid) needs a DB round-trip and can only live in the service layer via the existing `ServiceResult<T>` convention. A `[Required]` attribute on a non-nullable `Guid` is a silent no-op (`Guid.Empty` is valid) — this distinction is worth carrying into every future required-field pass, not just Mystery's.
- **Mystery is the one domain whose own service didn't consistently apply its own file's own convention**: `MysteryService.UpsertCountdownAsync` already uses `ServiceResult<T>` correctly; `CreateAsync`/`UpdateAsync` in the same file still return raw `MysteryDetailResponse` with zero FK-existence check on `AdventureTypeId` (confirmed live: a syntactically-valid-but-nonexistent `AdventureTypeId` 500s, unlike Monster's own two FKs which are checked and return clean 400s). The fix needs a `ServiceResult<T>` retrofit of `CreateAsync`/`UpdateAsync` as a precondition, not an optional cleanup alongside the actual fix.
- Mystery has no separate create/edit page unlike the other four domains (whose standalone-creation initiative just finished) — both `create` and `:id/edit` route to the same wizard component (`MysteryCreateComponent`); `mystery-detail.ts` is genuinely read-only, no form, no update call anywhere in the file. Worth remembering before assuming the `*-form`-shared-component precedent from the standalone-creation initiative transfers to Mystery — it doesn't, Mystery's only core-fields form lives inside the (still-active, not-parked) wizard.
- Found two gaps adjacent to but outside the required-field scope during the same inventory pass, both flagged rather than silently fixed: `Concept`'s DB `HasMaxLength(500)` has no client/server length guard anywhere (same `500`-on-overflow risk shape as the FK gap); Mystery-level custom moves (`MysteryCustomMove`) have full entity/DTO shape but zero API surface anywhere (no controller routes, no service/repository methods) — unlike all four other domains, which have full custom-move CRUD. Neither is a required-field question, both are real.
- Task explicitly asked for genuinely exhaustive Glob before generalizing the pattern (per `.squad/agents/Yoshi/history.md`'s own architecture-scan-completeness convention, itself sourced from user memory) — spot-checked `MonsterService`/`LocationService` in full and grepped `[ApiController]` presence across every controller before concluding the recommended mechanism needs zero new infrastructure for the four follow-up domain passes.
- Docs: `docs/updates/mystery-required-fields-validation.md`, `.squad/decisions/inbox/yoshi-mystery-required-fields-validation.md`.

### Monster Required-Field Validation — Phase 2 of the Cross-Domain Initiative (2026-08-06)

- **Re-verifying a "this domain is already fine" prediction from a prior phase is still worth doing in full, not skipping because the prior phase already said so.** Phase 1's cross-domain spot check predicted Monster's FK-existence checks needed no fix; this phase re-read `MonsterService.cs` in full and live-tested `Guid.Empty`/omitted/empty-string `monsterTypeId` against all three of `CreateAsync` (both overloads)/`UpdateAsync`, confirming none 500. Worth remembering: a whole new `CreateAsync` overload was added to that file by the Standalone Creation Phase 1 initiative *after* the original spot check ran — re-verification confirmed the new code inherited the correct pattern, but that wasn't guaranteed just because the old code had it.
- **A domain's sub-resources being in-scope for the same required-field pass (vs. a separate follow-up) is a real judgment call worth deciding explicitly with stated reasoning, not defaulting either way.** Called Monster's 4 sub-resource types (+ the shared `UpsertCustomMoveRequest`) in-scope because: identical gap shape live-verified on all 5 create endpoints, zero incremental FK-existence work needed (each already only checks its parent's existence), and the custom-move DTO is shared cross-domain infrastructure whose fix here pre-closes the same gap for Minion/Location/Bystander. Declined to extend scope to Minion's *own* sub-resources despite the structural resemblance — stays with Minion's own future phase, per the initiative's one-domain-per-phase framing.
- **Non-nullable value-type fields (`int`) raise a validity question DataAnnotations can still answer for free (`[Range]`), distinct from both halves of Phase 1's presence-vs-existence split.** An `int` is always "present" (no null/missing case), but can still be domain-invalid (negative harm). This is a second flavor of presence/shape validation, not a new third category — still belongs at the DataAnnotations layer, still zero DB round-trip. Mystery's all-string field list never had occasion to raise this; worth checking for numeric fields specifically in every future domain pass.
- **A DB length cap on a field with zero client/server DTO guard reproduces as a live, unhandled 500 — worth actually reproducing it, not just noting the cap exists.** Sent a 300-char Monster Name against the DB's `HasMaxLength(255)`; got a genuine 500 (Postgres `data too long`), confirmed no partial row persisted afterward. Same shape as Phase 1's `Concept`-length finding but stronger evidence for bundling since it's the *required* field itself, live-reproduced, not inferred from the schema alone.
- **A missing client-side validator on one sibling field while an identical sibling field one line away has it is a bug to fix outright, not a product question to defer to Skyler.** Found `MonsterFormComponent.monsterTypeId` has no `Validators.required` while `monsterArchetypeId` (next line, same shape, same required-at-every-other-layer status) does — confirmed the component's own spec file never asserts `monsterTypeId.touched`, which is why it went uncaught. Worth checking sibling form controls' validator arrays against each other, not just against the DTO, when auditing a shared form component.
- Docs: `docs/updates/monster-required-fields-validation.md`, `.squad/decisions/inbox/yoshi-monster-required-fields-validation.md`.

### Minion Required-Field Validation — Phase 3 of the Cross-Domain Initiative (2026-08-06)

- **A domain can already have "solved" a required-field gap via a mechanism this initiative previously rejected — worth checking the actual code path, not just the observed status code.** `MinionService.CreateAsync`/`UpdateAsync` already reject blank/whitespace `Name` with a clean 400, live-confirmed — but via a hand-rolled `string.IsNullOrWhiteSpace` check in the service, the exact "manual validation helper" shape Phase 1's Architecture Discussion explicitly declined in favor of DataAnnotations. The status code alone would have looked like "nothing to fix here"; reading `MinionService.cs` directly showed it was solved the disfavored way, and — more importantly — that mechanism only covers presence, not length (a live 300-char `Name` still 500s). Worth generalizing: when a live test shows correct behavior, still check *how* it's achieved before concluding there's no work, especially for domains touched by earlier ad-hoc fixes this initiative wasn't tracking.
- **Multi-minion support's status (parked vs. shipped) needed re-verification, not trust in the last doc's framing — and the answer was "partially shipped."** `docs/updates/multi-minion-support.md`'s MM-1 (a standalone, independently-useful `DELETE /api/minions/{id}` endpoint) shipped; its MM-2/MM-3 (the list-of-minions wizard rework, which would have replaced the conditional-required `Name` hack with a plain validator) did not — confirmed by grepping the store for `minionDrafts`/`editingDraftIndex` (absent) vs. `editingMinionId`/`minionSectionStarted` (present). A multi-part plan can ship partially without any doc being updated to say so; always re-check the actual store/entity state for a "parked" plan rather than trusting the parked status applies uniformly to every sub-phase.
- **Same live-reproduction rigor as Phase 1/2 (blank/whitespace/null Name, 300-char Name, negative harm, bad/empty/nonexistent FK) reproduced the identical result shape for Minion's own sub-resources (4 of 5 unfixed, 1 of 5 already-fixed via the shared custom-move DTO) and identical FK-existence correctness (both `MonsterId` and `MinionTypeId` already clean, never 500) — the third domain in a row confirming this codebase's service layer gets FK-existence right consistently, while the DataAnnotations-presence gap is the one that recurs every phase.** Worth continuing to verify both halves live every phase rather than assuming either one from precedent, even though the FK half in particular has now been correct three times running.
- **A gap flagged and explicitly deferred in one phase's addendum (Luigi's wizard-side `Validators.required` finding for `minionForm.harmCapacity`/`minionAttackForm.harm`/`minionArmorForm.harmSoak`, from the Monster-phase follow-up) is worth re-surfacing as a live open question in the next phase that actually owns the domain, not silently re-deferred a second time without comment.** Confirmed the gap is still present, cited it explicitly, and gave it its own Open Question with a stated lean (bundle) rather than letting it quietly persist a third time.
- Docs: `docs/updates/minion-required-fields-validation.md`, `.squad/decisions/inbox/yoshi-minion-required-fields-validation.md`.

### Location Required-Field Validation — Phase 4 of the Cross-Domain Initiative (2026-08-06)

- **Smallest-scope phase of the four so far, and worth stating as a real finding rather than a shortcut.** Location has: no sub-resource `Upsert*Request` types beyond the already-Phase-2-fixed shared `UpsertCustomMoveRequest`; no numeric fields at all (no `[Range]` question, structurally absent, not overlooked); no hand-rolled service-layer workaround to clean up (unlike Minion); and — the one genuine surprise — no missing client-side `Validators.required` on its FK (unlike both Monster and Minion). Net backend scope: one DataAnnotations attribute on one field of one record.
- **Traced *why* Location's FK validator was already correct rather than just noting that it was — the answer matters for how far the finding generalizes.** `LocationFormComponent.locationTypeId` already had `Validators.required` because `location-detail.ts`'s pre-existing form had it before Standalone Creation Phase 3's extraction, and that phase's plan doc explicitly preserved the shape field-for-field rather than harmonizing it toward Monster's/Minion's (buggy) precedent. This is downstream of an extraction-fidelity decision made for an unrelated reason, not evidence of more careful original engineering on Location specifically — don't assume Bystander (Phase 5) will or won't have the same gap either way without checking its own form/detail-page history directly.
- Confirmed FK-existence correctness a third domain in a row (Monster, Minion, now Location) — re-verified live anyway (`Guid.Empty`/omitted/nonexistent against `locationTypeId`, all clean 400s) rather than extrapolating from the streak; a repeated-true prediction is still worth re-checking every time, same point Phase 2 made re-verifying Phase 1's prediction.
- Docs: `docs/updates/location-required-fields-validation.md`, `.squad/decisions/inbox/yoshi-location-required-fields-validation.md`.

### Bystander Required-Field Validation — Phase 5, Final Domain of the Cross-Domain Initiative (2026-08-06)

- **Confirmed Bystander is the second (of five) domain matching Location's "smallest scope" shape, independently re-verified per Location's own instruction not to assume it.** Same result across every dimension: no sub-resource `Upsert*Request` types beyond the already-fixed shared `UpsertCustomMoveRequest`, no numeric fields, no hand-rolled service-layer workaround, and no missing client-side `Validators.required` on its FK (`bystanderTypeId`) — traced to the identical extraction-fidelity cause Location had (pre-existing `bystander-detail.ts` form already had the validator; Standalone Creation Phase 4's extraction preserved it field-for-field rather than harmonizing toward Monster's/Minion's buggy precedent, per `bystander-form.ts`'s own doc comment and `luigi-bystander-form-component.md`'s judgment call 1). Net backend scope: one DataAnnotations attribute on one field of one record — the smallest possible shape, tied with Location, not a new minimum.
- **Verified all four prior phases were actually shipped/committed, not just documented, before starting** — `git log` showed `689d484`/`5ca0e10`/`ec64b9b`/`983b3bd` (Mystery/Monster/Minion/Location validation commits) all present, confirmed by reading `ApiContracts.cs` directly (`UpsertLocationRequest`, `UpsertCustomMoveRequest`, `UpsertMinionRequest` already carry the expected attributes) rather than trusting each doc's "Revision" section as proof of the live state. Worth doing this check explicitly at the start of a phase that depends on priors' shipped state, not just their documented conclusions — a doc and the code can drift, and hadn't here, but that's a checked fact not an assumption.
- **This is the initiative's closing phase — wrote a cross-domain summary table + a 7-point future-domain-audit checklist rather than a shortened doc just because it's last.** Findings across all five: blank/whitespace-`Name` and oversized-`Name`-500 gaps recurred in literally every domain (100%, always fixed identically); FK-existence was already correct in 4/5 domains before this initiative touched anything (only Mystery needed a `ServiceResult<T>` retrofit); the hand-rolled-check-to-remove shape occurred in exactly 1/5 (Minion); the missing-client-FK-validator bug class occurred in exactly 2/5 (Monster, Minion — not Mystery, which has no separate create/edit form to have the bug in); numeric fields and interactive sub-resources existed in exactly 2/5 (Monster, Minion). Every one of these splits has a traced, non-arbitrary cause, not unexplained variance — worth stating that explicitly rather than letting a "mixed results" table read as noisy.
- Docs: `docs/updates/bystander-required-fields-validation.md`, `.squad/decisions/inbox/yoshi-bystander-required-fields-validation.md`. This closes the five-phase required-field-validation initiative.

### Authentication & Authorization Architecture — Public-Web Readiness (2026-08-08)

- **`MapIdentityApi<T>()` has no logout endpoint.** That single fact decided the whole mechanism question: the requirement said "sign out must actually revoke the session," and the .NET 8+ one-liner's bearer tokens are self-contained with no server-side revocation list. Worth remembering as the standard disqualifier any time `MapIdentityApi` looks like a free win — it also can't be trimmed (2FA/recovery-code surface is fixed) and its response contracts can't be reshaped to match a codebase's own DTO conventions. Landed on Identity services + cookie auth + hand-written controllers: framework for the crypto/state machines, our own HTTP surface for the conventions.
- **The decisive argument for EF Core global query filters over per-repository `.Where()` was not the 85 repository methods — it was the five `ISearchProvider`s.** `MonsterSearchProvider` et al. hold `MotwDbContext` directly and issue their own projections, bypassing repositories *and* services entirely. Any ownership convention living in the repository layer would have silently missed global search, which returns names and description snippets from every matching row. Generalizable check: before choosing an enforcement layer, grep for who else touches the DbContext directly — a layer-based convention is only as good as the layer's actual monopoly on data access.
- **"Parent-scoped" is not "owner-scoped," and this codebase is only ~60% parent-guarded.** Verified by method counts: `MonsterService` 31 public methods / 19 `*ExistsAsync(` guards, `MinionService` 29/15, `Location`/`Bystander` 12/7, `Mystery` 7/3. The unguarded ones are update/delete-by-child-id, which rely on the repository's `Where(x => x.Id == attackId && x.MonsterId == monsterId)` returning null — correct for existence, wrong for ownership once multi-tenant. ~30 one-line additions, enumerated as an explicit work item rather than left as an emergent property of the query filter.
- **`Minion` looks like a fifth owned root and isn't.** It has its own top-level routes, its own flat `GET /api/minions`, its own create page — but `MonsterId` is a required FK with cascade delete, so it cannot have an owner different from its monster's. Derive, don't denormalize. Worth remembering as the general test: *navigational* top-level-ness is not *ownership* top-level-ness; check whether the parent FK is required before treating a domain as a root.
- **Cookie auth's viability is a hosting-topology decision, not an auth decision.** `SameSite` is evaluated on the registrable domain (port-independent), so today's `localhost:4200 → localhost:5225` is already same-site; production is the risk. Recommended single-origin (API serves the SPA) specifically because it also makes Angular's built-in `withXsrfConfiguration` work — it deliberately skips absolute cross-origin URLs, a detail that quietly breaks the "just use the framework's XSRF support" plan under a sibling-subdomain or split-host deploy.
- `HealthService` (`core/health.ts`) calls `HttpClient` directly, bypassing `ApiService` — so `withCredentials` has to be an interceptor, not four small edits in `ApiService`. Same shape as the search-provider finding: check for the one caller that skips the chokepoint before treating the chokepoint as one.
- `environment.ts` hardcodes `http://localhost:5225` and there is **no** production environment file and **no** `fileReplacements` entry in `angular.json` — the app has never been built for a non-local API. Real deployment gap, not just an auth one.
- **The existing test suite structurally cannot prove ownership isolation.** All API tests are unit tests over hand-written fakes (`FakeMonsterRepository`), no `WebApplicationFactory`, no `Microsoft.AspNetCore.Mvc.Testing`. A fake has no query filter and no `ICurrentUser`, so "user A can't reach user B's row" is unobservable. This is the first feature in this repo that needs an integration test type it doesn't have — flagged as part of the high-risk phase, not as a later cleanup.
- Declined to put `OwnerId` assignment in a `SaveChanges` interceptor despite the existing `ApplyTimestamps()` precedent sitting right there: timestamps are cosmetic and belong in infrastructure, ownership is a security boundary and should be visible at the ~8 create call sites where a reviewer reads it. Worth remembering as the line for "should this be automatic" — automation that hides an authorization-relevant assignment is worse than eight explicit assignments.
- Data Protection key persistence belongs in the *first* phase, not the deployment phase — losing the key ring on restart fails silently as "users keep getting logged out," which is the worst class of bug to discover in production.
- `.squad/ceremonies.md`'s `security-review` ceremony (Boo) triggers on all three of its stated conditions here; noted in the doc that it should run before Phase 0, not after Phase 6.
- Docs: `docs/authentication-update/README.md`, `architecture.md`, `data-ownership.md`, `phases.md`, `open-questions.md`.

### Auth Architecture — Skyler's 16 Resolutions Folded In; Three Overrode My Default (2026-08-08, same day)

- **When a stakeholder removes a safety mechanism, find the mechanism that now has to carry the load and document it explicitly.** This was the whole shape of the revision. #7 removed email confirmation as a login gate → **password reset silently became the only proof-of-address-control in the system**, which forced a new rule (reset must stay available to *unconfirmed* accounts, or a squatted address is permanently unrecoverable) and made the reset token's 1h lifespan matter *more*, not less. #8 removed the super-admin bootstrap config → **security-stamp revalidation silently became the entire authorization-freshness story**, which forced documenting the 30-min propagation window and its two operational consequences. Neither mechanism was load-bearing in the original design; both are now. Generalizable: an override doesn't just delete a control, it relocates the responsibility — go find where.
- **Two "unrelated" resolutions can become coupled by a third.** #11 (30-min security-stamp `ValidationInterval`, chosen as a *security* bound on revocation) became the *functional* mechanism for role propagation once #8 made DB-assigned roles the only path. Same number, completely different justification, and now shortening it is a freshness/DB-read trade rather than a purely security one. Worth re-reading every already-settled decision after a late override to see which ones changed meaning without changing value.
- **A resolution can silently transfer weight onto a limit that was sized for a different job.** #9's 10-registrations/day cap was originally "reasonable anti-spam." Under #7 (every registration is an immediately usable account, no inert unconfirmed state) it became the *only* bound on account creation. Said so in-doc rather than leaving the number looking arbitrary.
- **Global vs. per-IP is a real fork for a rate limit, not a detail.** Chose a global 24h fixed window for registration (a per-IP cap of 10/day is near-useless against a distributed attacker, and "10 per day" reads as an absolute cap at this scale), then contained the self-DoS objection structurally — login/forgot/reset keep *separate* per-IP limiters, so a burned registration budget can never block existing users from signing in or recovering. The containment is what makes the global choice defensible; without it I'd have taken per-IP.
- **`IOptionsMonitor` inside a rate-limiter partitioner is the live-reload form.** `AddPolicy`'s partitioner receives the `HttpContext` and can resolve options from `context.RequestServices`, so a configurable limit can change without a restart — barely more code than reading `IConfiguration` once at startup. Worth defaulting to whenever a stakeholder says "configurable."
- **Resolving a brief's own internal ambiguity is my call to make, but it must be flagged in the doc, not just decided.** Skyler wrote "username + password" while enrollment/reset/Profile all key off email. Specified email — on the concrete evidence that **no `username` field exists anywhere in the codebase** (`ApiContracts.cs`, `core/models.ts`, every template) rather than on preference — and flagged it in three places with an explicit "correct me before Phase 1 ships the DTOs," since it's cheap to change now and a contract change afterwards.
- **A resolved open-questions file should become a decision record, not be deleted.** Kept all 16 questions with resolution + rationale, marked which three went against the recommendation, and added a "decided by reasoning, not asked" section so the calls that were never open don't get re-litigated later. Two sources of truth is the failure mode to avoid — the file explicitly says it's the audit trail, and the design docs are authoritative.
- **Retire a phase number, don't reuse it.** #13 folded the standalone Phase 5 (role management) into Phase 4; kept Phase 6 numbered 6 rather than shifting it down, because the coordinator and Skyler both already referred to "Phase 6" for the deployment analysis. A numbering gap with a one-line explanation beats silently renumbering something people have already named.
- **Edit tool was unavailable this session** — all five docs had to be rewritten in full via Write. Worth knowing: for a multi-file doc revision this makes full-file context retention (not re-reading) the practical constraint.
- Docs: `docs/authentication-update/` (all five files), `.squad/decisions/inbox/yoshi-authentication-architecture.md` (revision section appended).

### Auth Architecture — #7 Reverted, and What a Reverted Premise Actually Costs (2026-08-08, same day, third pass)

- **A stakeholder reversing an answer is not a flag flip when downstream reasoning was built on it — and the tell for "built on it" is that the consequence stops making sense, not that it stops being true.** Skyler's #7 (email confirmation not a login gate) reverted to my original recommendation. Five documented "trade-offs," one behavioural rule, one enumeration analysis, one rate-limit justification, and one UI affordance had all been derived from the wrong premise. Re-opened each from the corrected premise rather than reverting by assumption: two kept their conclusion with *new* reasoning, two flipped, one was deleted, one net-new interaction surfaced. The failure mode to avoid is a half-reversed document — prose asserting the gate while still carrying mitigations designed for its absence.
- **"Mitigates X" and "removes X from the threat model" are different claims and need different prose.** The five trade-offs (squatting, reclaim, data transfer, junk accounts, typo failure) weren't wrong-but-smaller under the gate — they stopped existing, because an unconfirmed account is inert. Deleting them and writing one sentence about what the gate *buys* was correct; shrinking them would have implied residual risk that isn't there.
- **When a rule's only justification dies, re-decide the rule — don't assume it flips.** "Password reset available to unconfirmed accounts" existed solely as the squatted-address reclaim path. With that gone I re-argued it from scratch and it *did* flip (blocked), but for three fresh reasons: it wouldn't help the user anyway (still can't sign in), it's a free mail-sending oracle, and `MapIdentityApi` gates on `IsEmailConfirmedAsync` for the same reasons. A dead justification doesn't automatically mean a dead rule.
- **The enumeration/UX interaction is the genuinely subtle one, and the resolving test is "is the password verified before this branch is reached."** Restoring the gate reintroduces a "correct password, unconfirmed" case. Collapsing it into the generic failure per #15 creates a real dead end (right password, told it's wrong, no hint a link is waiting). Resolved as four cases / two responses: distinct response for unconfirmed, because it's reachable only *after* the password is proven — not an unauthenticated oracle. Lockout stays collapsed under the same test, because `PasswordSignInAsync` returns `LockedOut` from `PreSignInCheck`, *before* the password is checked. Same-shaped question, opposite answers, one principle.
- **`SignInManager.PasswordSignInAsync` returns `NotAllowed` from `PreSignInCheck` — before the password is validated.** So the naive wiring (`if (result.IsNotAllowed) return EmailNotConfirmed()`) leaks "registered and unconfirmed" for *any* password, converting a deliberate, safe exception into a plain enumeration oracle. The safe form pairs it with an explicit `CheckPasswordAsync`. Exactly the kind of framework detail where the design's security property lives entirely in one implementation line, so it belongs in the doc and in the reviewer's focus list, not left to whoever writes the endpoint.
- **A conclusion surviving a premise change deserves an explicit re-derivation note, not silence.** The 10/day global registration cap kept its shape, but its stated justification ("only bound on usable account creation") had evaporated. Rewrote it around the resource actually at risk — outbound email quota and sender reputation, which is a *global* resource, hence a global limiter. Same answer, better reasoning, and the doc now says why it was re-checked rather than looking like it was never revisited.
- **Removing a mechanism elsewhere can silently delete a mitigation you're relying on.** #7's real cost is an ordering hazard (no email delivery means nobody can register, including the owner). My original design closed it with auto-confirm for allowlisted super-admin emails — but #8 had since deleted the allowlist. Two independently-reasonable resolutions combined into an unmitigated risk. Worth actively scanning for after any resolution that removes a mechanism: *what was depending on it?*
- **Check whether a UI affordance can still render a meaningful state after a policy change.** The Profile page's `emailConfirmed` display + resend action were correct without the gate and became unreachable dead UI with it (an authenticated user is confirmed by definition). The flow was still needed — it moved to the unauthenticated side, the only side a user who needs it can reach. Also dropped `emailConfirmed` from `/api/auth/me` for the same reason.
- **Keep a corrected answer's history visible in the decision record.** Wrote `open-questions.md` #7 with both the first answer and the correction, because the re-derivations elsewhere in the docs only make sense with that history — a silently-rewritten record would leave several "why is this re-argued?" passages unexplained.
- `Edit` was announced as available but was still disabled; all five docs required full rewrites again. `sed -i` remains viable for genuinely single-token fixes (used it for one stale decision cross-reference after renumbering). Multi-line heredoc appends via Bash failed on this content; writing the append to a scratchpad file and `cat`-ing it in worked.
- Docs: `docs/authentication-update/` (all five), `.squad/decisions/inbox/yoshi-authentication-architecture.md` (correction section appended).

### Auth Architecture — Luigi's Four Blocking Findings Adjudicated (2026-08-15)

- **All four findings were the same structural shape, and it's a shape worth watching for in any multi-section design doc: a requirement stated in one section with no wiring in the section (or phase step) that would implement it.** §7 required `GET /api/auth/csrf` at bootstrap → §6 and every Phase 3 step omitted it, so *nothing* fetched it and login was impossible as written. §6 required `authErrorInterceptor` not to genericise login's failures → no mechanism named anywhere, and no section stated login's failure status. Nothing in the design was *wrong*; four things were *unreachable*. A doc that argues its decisions well can still be unimplementable, and the review that catches it is the one that traces each requirement forward to a step rather than reading each section for correctness.
- **Two of the four fail silently, and both silences come from a success-shaped failure.** `ng serve`'s history fallback answers an unproxied `/health/live` with `index.html` and a **200**, and `HealthService.getLiveness()` uses `responseType: 'text'` — so the probe *resolves* on a blob of HTML and `isApiUnavailable()` can never become true. And a `401` login failure would be *handled* by `authErrorInterceptor` (session cleared, bounce to `/login`, error swallowed) rather than erroring. Generalizable: any `responseType: 'text'` health probe against a dev server with a history fallback is structurally undetectable, and any global 401 handler collides with an endpoint whose job is to *evaluate* credentials.
- **`CanMatchFn` returning a `UrlTree` is incompatible with the two-shell (`''` + `''` complementary guards) route pattern — declined Luigi's sub-recommendation on this.** Returning `false` is what makes the router fall through to the sibling shell; a `UrlTree` cancels the navigation and redirects *instead* of trying siblings. Since shell 1's `''` prefix-matches every URL, its guard also runs for `/login`, so `UrlTree('/login')` loops until Angular's redirect limit throws. The real fix for the dead-end was just the missing `{ path: '', pathMatch: 'full', redirectTo: 'login' }` child — which `PageLayoutComponent` has carried at `app.routes.ts:9` all along for exactly the same reason.
- **When choosing an HTTP failure status for a new endpoint family, check what the codebase's existing result-translation layer can already express before arguing semantics.** `ServiceErrorType` has exactly two members (`NotFound`, `Validation`) and every controller maps `Validation → BadRequest(new { … })`. So `400 { code }` for login failure costs nothing and `401` would cost a third enum member plus a new arm in every switch — a concrete, codebase-grounded argument that outranked the abstract "401 is the semantically correct code for bad credentials." Decision #37.
- **A "belt and braces" recommendation is worth accepting *as* belt and braces, and saying so in the doc.** Both the `400`-not-`401` contract and the interceptor's `/api/auth/` URL skip independently prevent the same silent failure; specifying both, and writing down that they're redundant on purpose, is what stops a future reader deleting one as duplication.
- Partial agreement on one item is a legitimate outcome and should be recorded at sub-recommendation granularity: adopted the empty-path child verbatim, declined the `UrlTree` half of the same finding, and left the doc silent on the guards' return type rather than pre-empting Skyler's ruling with prose arguing my side.
- Docs: `docs/authentication-update/` (all four of README/architecture/phases/open-questions), `.squad/decisions/inbox/yoshi-auth-luigi-four-blocking-findings.md`. New decision **#37** in `phases.md`; new "Luigi review dispositions" section in `open-questions.md`.

### Simple Authentication — A Minimal Stepping Stone Under an Existing Robust Plan (2026-08-18)

- **`AddAuthentication().AddCookie()` needs zero NuGet packages** — `Microsoft.AspNetCore.Authentication.Cookies` is in the `Microsoft.AspNetCore.App` shared framework, unlike `Microsoft.AspNetCore.Identity.EntityFrameworkCore`. That single fact decided the mechanism for a minimal pass: Identity's entire value (PBKDF2 hasher, lockout, token providers, user/role store, 7 tables, a `MotwDbContext` base-class change) maps one-to-one onto the features the owner explicitly excluded. Both paths produce the *same artefact* — a Data-Protection-encrypted ticket validated by the same `CookieAuthenticationHandler` — which is exactly why the later migration is cheap and why the whole Angular side is written once.
- **The right design move for a "smaller version of an existing plan" is to copy the plan's *values* verbatim wherever they're mechanism-independent, and only re-derive where the mechanism differs.** Cookie options, `SetFallbackPolicy`, the 401/403 override, Data Protection persistence, the `400 { code }` failure contract, the two-shell routes, `credentialsInterceptor`, and single-origin hosting all transfer unchanged. Only the things that are *about Identity* (security stamps, `PasswordSignInAsync`, `ValidationInterval`, antiforgery) don't exist yet. Stating that split explicitly is what makes the forward-compat table short and checkable rather than hand-wavy.
- **Best new finding: `MapFallbackToFile("index.html")` is an *endpoint*, so a global fallback authorization policy gates it — and the failure is an unrecoverable bootstrap deadlock.** Deep links 401 before any HTML is served, so the browser never loads the app that would let you log in. Worse, `/` keeps working, because `UseDefaultFiles` serves `index.html` from static-file *middleware*, which never reaches endpoint authorization — so it reads as intermittent and route-dependent. The robust plan lists it in one line; it's load-bearing on day one for anything that adopts single-origin hosting in the same pass. Same middleware-vs-endpoint distinction explains why JS/CSS stay public (fine, and worth saying out loud).
- **Two column/table names chosen purely to make a future migration cheap, and both are worth reusing as a rule.** (1) Name the minimal table `app_users`, *not* `users`, because the robust plan maps Identity's user table to `users` — distinct names mean the later migration is create-alongside-and-drop rather than rename-and-widen. (2) Never name a plaintext column `password_hash`; a column whose name asserts a property it doesn't have outlives whoever created it and will be misread by the next security reviewer.
- **Plaintext is the one storage format from which a rehash is possible without user involvement** — with N users the migration is a script calling `UserManager.CreateAsync(user, plaintext)`; with one user it's "register again and drop the table." Genuinely the only operational upside, worth stating so the trade-off reads as analysed rather than apologised for. Paired it with a *condition-shaped* upgrade trigger (a second account, or data worth losing) rather than "revisit later," per my own standing rule.
- **A trap that is harmless now and silent later deserves the mitigation now, not a note.** With a bare `AddCookie` there's nothing on `options.Events`, so `options.Events = new CookieAuthenticationEvents { … }` is safe — until `AddIdentityCookies()` lands and that assignment silently discards `OnValidatePrincipal`. Writing it as mutation from day one costs nothing and turns the migration into a no-op. Generalizable: when a future change converts a currently-benign idiom into a silent security hole, adopt the safe idiom immediately rather than documenting the hazard.
- **"Do we need the reviewed-and-ruled-on pattern for a one-page version?" is usually yes, and the argument is that the constraint is structural, not proportional.** Traced the single-route alternative to the two-shell routes and it still needs an empty-path landing route for the logged-out case and still needs `false`-not-`UrlTree` — i.e. it reproduces both of Luigi's findings from scratch. Re-deriving a route shape that already cost a review cycle gives its defects a second chance.
- Re-verified the robust plan's 10-day-old §0 inventory in full rather than citing it: **107 actions across 7 controllers still exact** (12/12/29/31/7/14/2), still no `username` anywhere in `src/`, `serve` target still has no `options` block, `OnModelCreating` still has no `base.` call (`MotwDbContext.cs:52`), `page-layout.html` Profile/Sign-out still dead at lines 104/111. Two facts the older inventory didn't carry that mattered here: **`Program.cs:9–10` hard-throws when `Cors:AllowedOrigins` is absent** (a production startup failure under a topology that doesn't use CORS at all), and the build emits to `dist/monster-of-the-week-web/browser` with **no `wwwroot/` in the API project**.
- **Found a real reduction in the robust plan's own deferred work:** with the dev proxy making development same-origin, `apiBaseUrl` is `''` in *both* environments, so `environment.prod.ts` and `angular.json` `fileReplacements` — both deferred to that plan's Phase 6 — are simply not needed for a same-origin deployment. Don't create a config seam that has nothing environment-specific to put through it.
- Docs: `docs/simple-authentication-update/` (README, architecture, phases, open-questions), `.squad/decisions/inbox/yoshi-simple-authentication-architecture.md`.

### Simple Auth — Skyler's Three Answers Folded In; Fallback Gap Turned Into a Worked Fix (2026-08-18, same day)

- **"Flag it" is not a deliverable when the fix is knowable — and the stakeholder's own framing usually contains the acceptance criterion that makes it decidable.** I had flagged `MapFallbackToFile` + a global fallback auth policy as a critical risk. Skyler's answer ("index.html should serve the app, which shows the login page itself; I'm not worried about anonymous visitors fetching the shell and bundles") supplied the missing premise — *the shell is public, only the API is gated* — which collapsed the whole thing to one `.AllowAnonymous()` on one endpoint. Worth internalising: when I catch a fail-closed hazard, work out the fix under each plausible stakeholder premise rather than stopping at the diagnosis; the premise is usually a one-line question, not a research task.
- **The mechanism to write up is the static-file-*middleware* vs. endpoint split, not the attribute.** `UseStaticFiles`/`UseDefaultFiles` short-circuit before authorization ever runs, so JS/CSS/`/` are anonymous by construction and can't be configured otherwise; `MapFallbackToFile` is an endpoint and is the only piece the fallback policy touches. That's what makes the bug present as *intermittent* (`/` works, `/login` and every deep link 401) rather than as an obviously missing attribute. Reusable diagnostic: any time authorization behaviour differs between `/` and a deep link in an SPA-hosting setup, it's this split.
- **Second-order consequence I nearly missed: `MapFallbackToFile` registers at `Order = int.MaxValue` with `{*path:nonfile}`, so a *misspelled* `/api` path gets answered with `index.html` and a `200`.** Same success-shaped-failure family as the `/health/live` + `responseType:'text'` trap already in these docs, and strictly more reachable under single-origin hosting where every frontend call is relative. Fix is one line — `app.Map("/api/{**rest}", () => Results.NotFound())` at default order, which beats the fallback on order and loses to every literal controller route on precedence — and deliberately **not** `AllowAnonymous`, so anonymous gets 401 and authenticated gets 404.
- **When a server-side gate is added, explicitly re-argue whether the client-side gate is now redundant — the answer here is no, and the argument is a two-column "what each catches that the other cannot" table.** `canMatch` is proactive (auth state is known pre-first-route because `provideAppInitializer` awaited `/api/auth/me`) and catches the logged-out deep link before any chunk downloads; the 401 interceptor is reactive and catches mid-session expiry, which a guard structurally cannot see because guards only run on navigation. Deleting the guard doesn't break access control — it produces a visible flash of the full signed-in chrome plus seven lazy bundles downloaded for nothing. That distinction (correctness vs. observable quality) is what makes the table the right form for the argument.
- **My "match the stakeholder's literal words" instinct lost to their "don't build the same screen twice" instinct, and the override was right.** I'd recommended `username` because Skyler said "username"; they overrode to email, restoring alignment with the robust plan's resolution #17. The tell I under-weighted: all four artefacts that would have churned (`LoginRequest`, `CurrentUserResponse`, the form control, `AuthService.login()`) sit on the *contract* boundary, which is where churn costs most. Generalizable rule — when a brief's wording conflicts with an already-settled decision in a sibling plan, weight the conflict by *which layer* the divergence lives in, not by how many lines it is.
- **`type="email"` is free in an Angular reactive form and is not "validation."** `FormGroupDirective` puts `novalidate` on the host `<form>`, so HTML5 constraint validation never blocks submit — the only effect is the correct mobile keyboard. Useful any time a no-validation constraint seems to rule out semantic input types. Same category as case-insensitive email lookup: normalisation, not validation.
- **A "three items answered" message from a coordinator is not necessarily *my* three open questions — diff them explicitly.** Skyler's three were the fallback gap, hosting confirmation, and the identifier; my open-questions #2 (Sign out) and #3 (local data migration) were untouched and would have been silently dropped if I'd rewritten the file as fully-resolved. Kept them open, and raised a genuinely new #4 (24h session vs. the app's total absence of draft persistence — `MysteryCreateStore` has no localStorage save, so a lapse mid-wizard loses work) that only became a real UX path once "the app renders its own login view on 4xx" was confirmed as designed behaviour.
- Docs: `docs/simple-authentication-update/` (all four; new `architecture.md` §2.3, §2.2–§2.5 renumbered), `.squad/decisions/inbox/yoshi-simple-authentication-architecture.md` (resolution section appended).

### Simple Auth — Final Four Resolutions; Plan Closed (2026-08-18, same day, third pass)

- **A stakeholder's confirmation sentence is worth parsing for conflated concepts, not just for the yes/no.** Skyler approved Sign out with *"we will need it for when a session expires and it's easy enough to enable a UI button to expire it manually/intentionally"* — which spans **two different mechanisms** (the automatic `401`-interceptor bounce when a session lapses on its own, and the manual button that ends a *live* session). Both were already in the design, but the sentence would have let an implementer believe the button was the expiry handler. Wrote them as a two-column table rather than banking the approval and moving on.
- **Best find of the pass, and it came from tracing an approved feature against an earlier decision: `AuthService.logout()` must act on its `error` path or Sign out is silently inert in exactly the case the owner named it for.** `POST /api/auth/logout` is authenticated → clicking it on an already-expired session returns `401` → but **both** interceptors skip `/api/auth/` by design (decision #9, adopted from the robust plan's #37) → nothing handles it. Two independently correct decisions combining into a silent dead end. Same shape as Luigi's four blocking findings on the robust plan (a requirement in one section with no wiring in another), and the same shape as my own "removing a mechanism elsewhere silently deletes a mitigation you're relying on" note from the #7 revert. **Generalizable: whenever a global handler gains an exemption list, re-walk every endpoint on that list and ask what now handles its failures.**
- **"Accepted" and "absent" are different risk-register states, and collapsing them loses information.** Skyler accepted the mystery-wizard draft-loss consequence outright (*"what's lost is lost"*). The wrong move is deleting the row; the right one is keeping it marked accepted **with an explicit instruction that no draft-persistence work should be proposed on the strength of it** — otherwise a future reader finds a live-looking risk with no mitigation and helpfully designs one. Same for the "no data migration" answer: recorded as a decision in the Decisions table (#18) and in §4.3 rather than as an absence, because an unexplained gap invites someone to fill it.
- **Resolving open questions is also the moment to kill your *own* remaining hedges, not just the stakeholder's.** A doc with zero open questions but three "optional / if wanted / say the word" phrases still reads as unresolved. Swept and decided each: the `/api/{**rest}` catch-all became in-scope (one line against a silent failure), the constant-time password compare became an explicit *decline* with reasoning rather than an offer, and Sign out lost its "say the word and it comes out." Each moved into the "decided by reasoning, not asked" section so they can't be re-litigated. Grepping my own docs for hedge words (`optional|if wanted|say the word|confirm-or-override|owner's call`) is a cheap, repeatable final check.
- **A "no open questions" claim needs a companion list of what's still outside the design, or it's misleading.** Closed the plan as fully resolved while stating plainly that two things still stand between it and a deployment — the infrastructure pass (already scoped out) and whether Boo's `security-review` ceremony runs (genuinely the owner's call, `.squad/ceremonies.md` triggers it on "handles auth"). Distinguishing "no open questions *about this design*" from "nothing left to decide *before shipping*" is the honest form.
- Added a review gate naming Luigi (Phase 2 in full), Bowser (Phases 0/1/3), and Boo (optional; §2.3's anonymous surface and §6's plaintext trade-off only — not the parts the owner excluded by instruction). Matches the robust plan's convention, scaled down.
- Final state: 4 docs, 1,405 lines (vs. the robust plan's 2,749), 18 decisions, all four questions resolved, every `§N.N` cross-reference verified to resolve against a real heading.

### Hunter Playbooks — Phase 1 Plan (2026-08-24)

- **`pdftotext -layout` is available in this environment's Git Bash (`/mingw64/bin/pdftotext`) and is the right tool for a large rules-PDF read** — the `Read` tool's PDF path needs `pdftoppm`/poppler page-rendering, which isn't installed, and errors immediately for anything not given an explicit `pages` range. Extracting to a scratchpad `.txt` once, then `Grep`/`Read`-ing that, is far cheaper than paging through the PDF tool 20 pages at a time for a 3,000-line, 28-playbook source document. Worth defaulting to this whenever a large PDF needs full-text search/grep rather than visual page inspection.
- **A brief's own example list can be wrong in both directions (missing real entries, and not flagging that it's incomplete) — always re-derive the authoritative set from the source, and say so explicitly when it diverges.** The task brief's 13-playbook example list undercounted by more than half; the actual consolidated PDF has 28. A naive `^To make your` anchor grep also silently dropped 4 real playbooks (Crooked, Curse-eater, Forged, Interface) because their "Getting Started" paragraph starts mid-column in the 2-column PDF layout, not at column 0 — broadening to an unanchored `pick a name` substring match caught them. Two different failure modes (wrong source-of-truth list, wrong extraction pattern) that would have both under-scoped the seed-data effort if not caught.
- **`-layout` column-preserving extraction can misattribute which visual column text belongs to**, and this is a real fidelity trap worth flagging in the deliverable rather than silently trusting: Mundane's basic-moves list appeared to reassign "Act Under Pressure" from Cool to Sharp purely from column-merge artifacts in the 2-column layout, not a real per-playbook rules variation. Called this out explicitly as a caveat in the plan rather than either hard-coding the apparent variation or silently assuming it was extraction noise — flagged for physical-rulebook confirmation before any seed data locks in stat-move pairings.
- **The concrete "one Hunter + JSON blob / one table per playbook / lookup table + child tables" fork the task posed was decidable by grepping the codebase for existing precedent, not by abstract database-design preference**: zero `HasColumnType`/`HasConversion`/`jsonb` usage anywhere in the API project, and an already-proven `TypeRef`-style reference-lookup + bridge-table pattern (`MonsterType`/`MonsterArchetype`/`WeaponTag`+`MonsterAttackWeaponTag`) at smaller scale. That evidence made the lookup-table-with-children recommendation load-bearing rather than a preference call — worth doing this grep-for-precedent step before any "which storage shape" architecture question in this codebase.
- **A recommendation can correctly be "mostly (c), with two named exceptions" rather than a clean single pick among the options a brief poses.** Found real, PDF-evidenced non-uniformity the brief's three options didn't anticipate: gear category/option shape genuinely differs per playbook (no shared harm/range/tag taxonomy the way Monster attacks have), and 5 of 28 playbooks (Envoy, Divine, Monstrous, Wronged, + Haven references) have genuinely bespoke mechanics with no shared schema between them. Normalizing 100% of this in Phase 1 would mean designing ~5 one-off mini-schemas before any hunter could be created; the stated trade-off (free-text for these two carve-outs, normalized for everything uniform) ships Phase 1 without inventing taxonomy the source material doesn't actually have. Worth generalizing: when a brief poses a clean N-option fork, check whether the actual source content is uniform enough for a clean pick before defaulting to the "obviously right" option for the majority case.
- **MOTW's own "History" character-creation step is structurally a cross-hunter relationship prompt** ("pick one for each of the other hunters") — this made the brief's "don't bind Hunters to other domain objects" exclusion bite on something not obviously implied by that phrase at first glance. Resolved as a single free-text `Background` field (mirroring `Monster.Description`/`Mystery.Notes`) rather than a half-built relationship table pointing nowhere yet. Worth checking any new domain's own source material for relationship-shaped prompts that could silently violate a "no cross-entity binding" scope boundary before assuming free-text prose is a downgrade rather than the correct Phase 1 answer.
- Confirmed `docs/seeds/*.sql` files are dev-reference-only and never read by the running app (absent from `Program.cs`/`MotwDbInitializer.cs`) — only `MotwDbInitializer`'s inline C# seeding actually runs at startup, idempotently guarded per-table via `AnyAsync`. This matters for any future high-volume reference-data domain: the existing dual-artifact convention (inline C# + hand-mirrored SQL) doesn't scale past a handful of rows per table, and for Hunter Playbooks (~1,000+ seed rows across nested tables) recommended breaking it — JSON seed file inside the API project as sole source of truth, `MotwDbInitializer` deserializes it, no hand-maintained SQL mirror — flagged explicitly as breaking precedent rather than silently doing so.
- Docs: `docs/hunter-playbooks/hunter-playbooks-plan.md`.

### Hunter Playbooks — Data-Loss Incident + Consolidated Recovery of Lost Rounds (2026-08-26)

**Incident, flagged for the squad, not just noted for myself**: mid-task, discovered this tracked file had been silently reverted to an earlier state (git diff showed only ~80 lines of my edits surviving against the last commit, matching just my first "Fresh Pass After Doc Deletion" append) — every subsequent round's detailed learnings (seeding correction, all-8-open-questions resolution, Skill recommendation, three rounds of Phase 5 ideation, the Moves-only formatting-scope narrowing) were gone from this file despite each `Edit` call reporting success at the time. Bowser's `history.md` showed the identical pattern (only one surviving entry). Root cause not directly observable from here, but the shape strongly implies a concurrent `git checkout`/`restore`/`reset` on this **shared, not per-agent, working tree** by another agent (Bowser was dispatched in parallel this same task) reverted uncommitted changes to *tracked* files — while every `docs/hunter-playbooks/*.md` file and every `.squad/decisions/inbox/*.md` file (all untracked, new-this-task) were completely unaffected, since `git checkout`/`reset` never touches untracked paths. **Worth flagging upward, not just logging here**: when multiple agents share one working tree, a tracked file with in-progress-but-uncommitted edits is genuinely at risk from any other agent's git safety operation, even one performed correctly by that agent's own standards — the auto-mode instruction to `git status`/stash before destructive git commands protects the *operator's own* uncommitted work, not necessarily a *different concurrent agent's* uncommitted work sitting in the same tree. Recommended mitigation (for the coordinator, not something I can act on myself): either give concurrently-dispatched agents isolated worktrees, or have agents commit tracked `.squad/` state more frequently than "at the end of a long task," so less is ever exposed to a sibling agent's git operation.

**Consolidated recovery of the lost rounds' key learnings** (condensed, not a verbatim reconstruction — full detail lives in the intact doc files, cited per bullet):

- **Seeding correction round**: a Data Admin UI existing for a domain doesn't imply it's how that domain's canonical data reaches every environment — those are separate requirements a brief can easily conflate when it mentions both without stating the relationship. Corrected recommendation: canonical playbooks get seeded via an extended `MotwDbInitializer` reading a checked-in JSON file, re-justified by the *distribution* requirement (same as the static lookup tables), not by data volume alone (`architecture.md` Section 4).
- **All-8-open-questions round**: `BasicMove` confirmed as a real DB table (Skyler wants to tweak it over time, even at 8 rows); Playbook gets zero sub-resource endpoints, one upsert-the-graph endpoint per playbook; Hunters stay **live-linked** to their Playbook via FK (not snapshotted) — Skyler doesn't expect playbooks to change much, and wants propagation to existing instances when they do; a Q4 answer ("only matters for Advanced Improvements") corrected a real scoping error — the motivating custom-override case (Divine's "mission of your creation") belongs to a not-yet-designed, out-of-scope leveling-up mechanism, not the initial bespoke pick (`architecture.md` Section 3, `open-questions.md`).
- **Skill recommendation round**: recommended packaging the playbook-authoring procedure as a Claude Code Skill — mechanism decided immediately, but content deliberately *not* written until Phase 4 actually runs and discovers what the repeatable pattern is (`open-questions.md` Q10, `phases.md` Phase 4).
- **Phase 5 ideation rounds (3 total)**: re-verified bespoke content directly against all 3 pilots and found **five** structural shapes, not the three Crooked alone suggested (Chosen's label-only "tag pick" groups, Divine's flat description-only Mission). Landed provisionally on a shared `BespokeSection`/`BespokeOption` table with **no rigid shape-kind enum** — the shape emerges from which nullable fields are populated (title vs. description vs. both vs. neither; self-referencing `ParentOptionId` for nesting; recursive `MinSelect`/`MaxSelect` on any option with children, validated against Skyler's own Chosen-Fate decision as "pick all of N" falling out for free as the N=M degenerate case). Instance-side: a `HunterBespokeSelection` bridge table (FK + optional `FreeformText` for blank-fills), not JSON — strengthened by Hunter already having two other FK-bridge selection mechanisms (`HunterMove`/`HunterGearSelection`) in this same design. Chosen's Fate settled by Skyler directly as **one** `BespokeSection` (not three), `MinSelect=MaxSelect=3` mandatory top-level categories, each with its own nested child count — an intentionally messy source-layout case Skyler is fine handling by hand later, not something the pipeline/schema needs to solve perfectly. Full detail: `phase5-bespoke-ideation.md`, `bespoke-ruleset-catalogue.md`.
- **Formatting-fidelity narrowing round**: an early "confirmed" note generalized one confirmed example (`PlaybookMove.DescriptionText` needs rich-text preservation) into "any Description-type standard field needs it" — an unstated inference presented as settled, corrected once actually checked against every other standard section. **General lesson, worth repeating**: generalizing from one confirmed example to "and probably these other similar fields too" is an inference, not a confirmation, and needs its own explicit check before being stated as resolved.
- **This round**: Bowser's pdf.js-based extraction pipeline (`docs/hunter-playbooks/pdf-extraction-pipeline.md`, `tools/pdf-extract/`) passed both validation passes (Crooked/`<b>`, Covenant/`<i>`+`<ul>`) and is now formalized into Phase 4 as the standing mechanism, not a spike. Reconciled a genuine ambiguity Skyler flagged themselves: `bespoke-ruleset-catalogue.md`'s purpose was ambiguous between "source of truth for real data injection" (my own status line originally said "reference data," Skyler's own request said "for the actual implementation" — directly conflicting signals) — resolved explicitly as the former, with the direct consequence that every entry needs real, pipeline-verified markup (or a verified "no markup in source" note) before counting as final, not plain/tagless placeholder text. Pulled Crooked's Background entry's real `<b>` markup directly from Bowser's already-produced `crooked-background-review.json` (no need to ask for a fresh dispatch); flagged that Chosen's Fate and Divine's Mission haven't been checked against the pipeline at all yet (genuinely unverified, not assumed clean) — concrete next Bowser dispatch needed if/when those playbooks come up in Skyler's new one-at-a-time walkthrough.
- Docs (all intact, untracked, unaffected by the incident above): `docs/hunter-playbooks/architecture.md`, `phases.md`, `open-questions.md`, `README.md`, `phase5-bespoke-ideation.md`, `bespoke-ruleset-catalogue.md`, `pdf-extraction-pipeline.md`; `.squad/decisions/inbox/yoshi-hunter-playbooks-{phase1-plan-v2,seeding-correction,questions-resolved,skill-recommendation,formatting-fidelity-and-fate-structure}.md`.

### Hunter Playbooks — Catalogue Backfill Complete, All 3 Pilots Finalized (2026-08-26, same day)

- **A "physically near section X in the PDF" fact is not the same as "belongs to section X's model" — this recurred a third time with a genuinely new twist.** Bowser flagged a bold Luck-spend-trigger aside sitting near Fate's column and correctly declined to decide its scope unilaterally. Ruled it out of Fate's bespoke entry entirely, same call as the already-excluded "[Playbook] special:" callouts elsewhere (standard `LuckSpecialText`, not bespoke content) — but this instance had a wrinkle the prior ones didn't: the aside's wording is a *second, differently-phrased* rendering of a trigger already captured elsewhere for the same playbook, not just a duplicate mention of the same text. Flagged that specific wording mismatch explicitly as its own small unresolved fidelity question rather than letting the scope ruling silently absorb it — a "this is out of scope" call and "the in-scope version of this same fact might itself have two inconsistent source renderings" are two different findings, worth stating separately even when they surface at the same moment.
- **Confirmed-plain is not always the actual result once real extraction runs — Divine's Mission proved the "shape suggests no markup, verify anyway" discipline isn't just defensive box-checking.** Heat, Underworld, and Fate's 33 tags all came back genuinely clean on verification; Mission's option 5 did not (`<i>any</i>`). Had the catalogue defaulted to "description-only shapes probably don't have inline formatting" as a shortcut instead of running every ruleset through the pipeline regardless of shape, this one would have shipped wrong under the (a) source-of-truth standard adopted the same day — worth remembering as the concrete payoff of a verification policy applied uniformly rather than selectively to the entries that "look risky."
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (Divine/Mission real `<i>` markup + verified-plain notes on options 1-4, Chosen/Fate finalized with two-way-verified plain confirmation + Luck-aside judgment call recorded in-doc, top status/progress line updated to "3 of 28, fully formatting-verified, nothing pending"), `.squad/decisions/inbox/yoshi-hunter-playbooks-pipeline-formalized-catalogue-purpose.md` (addendum).

### Hunter Playbooks — First Non-Pilot Playbook: The Action Scientist (2026-08-26/27)

- **Verified the actual PDF page number directly (`pdftotext -f N -l N`) rather than trusting a form-feed count in a shared scratchpad file** — the scratchpad turned out to contain other agents' artifacts (`page11.jsonl`, `runs-15.json`, etc. from Bowser), which was reason enough to distrust a derived page-number guess from a possibly-touched shared file and re-derive it from the source PDF directly instead. Confirmed cleanly: Action Scientist is genuinely PDF pages 1–2, no contamination, just content I hadn't read yet this early in the file.
- **My own pre-extraction prediction ("mechanical grant, no roll = probably plain") was wrong for 3 of 4 options I called plain** — Bowser verified all 7 directly rather than trusting the shape summary, and found real `<b>`/`<i>` in 6 of 7 (only Space genuinely plain). Concrete, first-hand evidence (not just a stated principle) that "this shape looks safe" is not a substitute for actually checking every option on every playbook — logged explicitly in the catalogue entry itself as a process note, not just here, since it's exactly the kind of miss a future faster pass might repeat.
- **A structurally-ambiguous nested bulleted list (Physics and Cosmology's 3 example questions) needed a source-intent judgment call, not a schema decision** — resolved as inline `<ul><li>` content (in-play optional flavor, "you may ask one of these") rather than a nested `BespokeOption` pick (which Underworld's structurally similar-looking sub-options actually are) by reading the wording carefully ("one of these as well as the usual questions" vs. Underworld's "Pick one:"). Both readings are schema-supported either way; this recurs the general lesson that visual/structural resemblance to an already-solved case doesn't mean the same resolution applies without checking the actual wording.
- First non-pilot playbook confirms the Section 3 model (specifically the Titled-Choice/Background shape) generalizes cleanly with zero new schema needed — worth noting as the first real external validation beyond the 3 playbooks the model was built against.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (new `## The Action Scientist` section, progress tracker updated to 4 of 28).

### Hunter Playbooks — The Celebrity: First Zero-Bespoke Playbook, a "New Gap" That Wasn't (2026-08-27)

- **A confirmed-zero result needed its own standing rule, not a one-off exception.** The Celebrity was the first playbook read with genuinely no bespoke ruleset — flagged it as a real catalogue-convention gap rather than silently omitting the playbook or silently including it under my own judgment, since Skyler had explicitly asked to be looped in on model-fit questions this round and "how do we record a checked negative" is exactly that kind of convention decision with 24 more playbooks riding on getting it consistent. Skyler's answer became a standing rule, written into the catalogue's own convention section, not just applied to Celebrity — worth the general reminder that a first-instance decision worth generalizing should be written as a rule immediately, not left to be inferred from one example.
- **The best catch of this round was mine to make against my own model, not a new stakeholder requirement in disguise.** Skyler (via the coordinator) proposed adding a `Required` property to the Moves model for Celebrity's always-granted "Fakelore" move. Before implementing anything, checked the existing schema first — `PlaybookMove.IsAutoGranted` already covered this exact concept, originally built and validated against Chosen's identical shape (2 pre-granted moves + pick-1-of-5 pool) back in the very first Phase 2 pass. Net result: a rename (`IsAutoGranted` → `Required`, Skyler's clearer proposed name), zero functional or schema change. Worth the general lesson, stated plainly: when a stakeholder asks for what sounds like new schema work, check whether the existing model already covers it under a different name before building anything — silently implementing a "new" field that duplicates an existing one would have been a worse outcome than surfacing the rename-not-addition finding, even though the stakeholder's own request read as if new work were needed.
- Also corrected a small factual slip in the request itself while verifying: the ask said "pick-2-of-7 from the other 6 named moves" (internally inconsistent — 7 and 6 don't match); recounted directly from my own source read and confirmed it's 7 named pickable moves, pick 2. Worth re-verifying a relayed count against your own source read even when the overall request is otherwise correct, since an inconsistency inside someone else's summary is a cheap, easy thing to silently propagate if not checked.
- Docs: `docs/hunter-playbooks/architecture.md` (Moves section: field renamed + Celebrity re-validation note, Section 3 schema block updated), `docs/hunter-playbooks/phases.md` (Phase 2: rename note), `docs/hunter-playbooks/phase5-bespoke-ideation.md` (new dated status paragraph, flagged-not-designed note on `BespokeOption` possibly needing an equivalent `Required` concept later), `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (new standing convention rule for confirmed-zero-bespoke playbooks, new `## The Celebrity` entry, progress tracker to 5 of 28).

### Hunter Playbooks — The Changeling: Two Precedent Extensions Resolved Without Escalating (2026-08-27, same day)

- **Distinguished "a token's literal original trigger condition" from "a token's actual underlying purpose" when deciding whether it still applies in a new case.** `{{blank}}` was introduced because Heat's source had a literal printed marker; Unknown Heritage's 4 truncated tags ("Allergy to") have no printed marker at all, verified directly against the PDF item stream. Resolved by going back to *why* the token exists in the first place ("marks where the UI renders an input" — a functional question) rather than treating "was there a printed glyph" as the deciding test. Worth the general lesson: when a mechanism's originating example and its actual purpose come apart, re-derive from the purpose, not the example, and say explicitly that's what happened rather than silently extending or silently declining.
- **Re-applied the Chosen-Fate "concatenate scattered source prose into one Description field" precedent a second time, after first ruling out the more likely-looking alternative** (that the trailing paragraph was a Luck-special-callout-adjacent aside, the pattern already excluded from bespoke scope twice before). Checked the content itself ("obstacles... based on these tags," not a different standard mechanic) before applying either precedent — worth the reminder that two prior patterns can look similarly applicable to a new case, and the source's own wording, not surface resemblance to either precedent, is what actually decides which one fits.
- Both resolved from precedent/principle without escalating to Skyler, as invited — flagged explicitly in the catalogue as judgment calls rather than presented as if self-evidently correct, so they're visible to revisit if wrong.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (new `## The Changeling` section with both resolutions stated in-doc, progress tracker to 6 of 28), `docs/hunter-playbooks/phase5-bespoke-ideation.md` (new dated note recording both precedent extensions for future reference), `.squad/decisions/inbox/yoshi-hunter-playbooks-changeling-blank-token-and-description-concatenation.md`.

### Hunter Playbooks — `BespokeSection.EffectText` Added: Position vs. Role, and a Split That Isn't Clean Twice (2026-08-27, same day)

- **A stakeholder correctly overrode a pattern I'd already resolved twice via precedent extension — worth recognizing that "I've already handled this case-by-case successfully" doesn't mean it shouldn't become real schema.** Concatenating Fate's and Changeling's scattered prose into one `Description` worked functionally both times, which is exactly why it was easy to keep doing rather than noticing it was actually the third occurrence of a recurring shape worth modeling. Skyler naming it after two instances (not waiting for a third) is a useful calibration point: "resolved with reasoning" and "should be schema" are different questions, and a pattern repeating twice with the same shape is often enough evidence, not something that needs a third confirmation before acting.
- **The most important finding of this round was discovering the split does NOT hold by source position, only by semantic role — and that distinction changes what actually goes in the new field for Fate.** A naive reading ("block 1 = Description, block 2 = EffectText") would have put Fate's literal pick-instruction sentences into the new field, since they're physically in the second source-position block. Reading for role instead of position: those sentences are pure pick-count restatement, fully redundant with `MinSelect`/`MaxSelect` already on the category options, and belong in neither field — they're simply dropped, matching how every other Section already omits restating its own pick-count in prose. Worth the general lesson: when generalizing a schema field from two examples, check whether the examples' surface commonality (both have "a second block") and the field's actual semantic purpose (what does this block *mean*) genuinely coincide — here they didn't, and forcing the field to match the surface pattern would have produced a wrong result for Fate specifically.
- **Stated an explicit, checkable split criterion (distinctly-positioned block in the source, not "any sentence with consequence-flavored wording") specifically so the retroactive review wouldn't drift into over-splitting.** Underworld's `Description` has one forward-looking clause ("Keep this in mind when you select your moves...") that could superficially read as EffectText-shaped if the criterion were purely semantic — kept it in `Description` because it's part of one continuous source paragraph, not a separately-printed block. Worth the general reminder: a new field justified by "this content plays a different role" still needs a mechanically-checkable boundary condition, or every future playbook's judgment call becomes a fresh, inconsistent re-derivation.
- Did the full retroactive pass across all 5 previously-catalogued rulesets (not just the 2 known cases) before declaring the schema change complete — found zero additional cases needing rework, all for the same reason (single continuous Description block, nothing separately positioned after the options in the source).
- Docs: `docs/hunter-playbooks/phase5-bespoke-ideation.md` (Section 3: `EffectText` added to the schema block, full semantic-role reasoning, split criterion stated explicitly, Fate bullet updated with the rework note), `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (Fate and Unknown Heritage entries reworked to `Description`/`EffectText`, new retroactive-review summary note covering all 5 other rulesets + Celebrity), `.squad/decisions/inbox/yoshi-hunter-playbooks-effecttext-schema-change.md`.

### Hunter Playbooks — The Covenant: Zero-Option `BespokeSection`, Structural Question Correctly Escalated (2026-08-27, same day)

- **Flagged a genuinely new structural wrinkle to Skyler instead of silently resolving it, even though I had a confident lean** — The Covenant's fixed, always-active "you protect your ally from death" ability had no picks at all and was headed by the *playbook's own name*, not the nearby "Friendship" ruleset's own title. My lean (fold it into Friendship's `EffectText`) would have avoided introducing a second new schema wrinkle in one round, but stretching `EffectText`'s role to content living under a differently-named header was a bigger reach than either prior `EffectText` case (both of which stayed within the same ruleset's own titled area). Skyler chose the other option — a standalone zero-option `BespokeSection` — confirming the reach would have been wrong to take silently. Worth the general lesson: "I can construct a defensible reading" and "this is still fine to resolve without asking" are different bars, and a structural choice that would set precedent for how future eponymous-ability cases get handled cleared the bar for asking, even with a reasoned lean in hand.
- **Resolved the null-vs-zero representation question for "no options exist" by re-deriving from the field's already-established meaning, not by picking whichever value felt intuitive.** `MinSelect`/`MaxSelect` = `0`/`0` would read as "a real, empty option set, pick none of it" — wrong model of what's actually true (there is no option set at all). `null`/`null` was already the convention for "this concept doesn't apply here" everywhere else nullable fields are used in this schema (`BespokeOption.MinSelect` on a childless option, `BespokeOption.Title`/`DescriptionText` when a shape doesn't need one) — extended the same convention to the Section level for the first fully-childless Section, rather than treating it as a fresh naming/values decision.
- Directly stated a consequence of the zero-option case for the instance-selection side (`HunterBespokeSelection` needs no rows for it at all) before being asked, since it's the kind of thing a future implementer could otherwise go looking for and not find — same instinct as flagging `Required` moves' equivalent non-need for a selection row earlier in this task.
- **Catalogue entry itself intentionally left unfinished this round** — Bowser's formatting extraction for the Covenant/Friendship column was dispatched in parallel by the coordinator and hadn't landed by the time the schema/structural side was ready; finalizing `## The Covenant` (both `BespokeSection`s) is correctly sequenced after that, not guessed at ahead of it.
- Docs: `docs/hunter-playbooks/phase5-bespoke-ideation.md` (Section 3: zero-option `BespokeSection` degenerate case documented — Skyler's structural decision, the `null`/`null` representation and its reasoning, the `HunterBespokeSelection` consequence). `bespoke-ruleset-catalogue.md` not yet updated for Covenant — pending Bowser's results.

### Hunter Playbooks — The Covenant Finalized: Blank-by-Intent Confirmed a Second Time (2026-08-27, same day)

- **A single-instance judgment call became a stated pattern once independently reproduced, not before.** Changeling's "no printed marker, add `{{blank}}` anyway" call was flagged as a new provenance category the first time, deliberately not generalized into a rule yet. Covenant's "Something else" landed on the exact same finding via an independent check (different playbook, different ruleset, same discipline: check the raw item stream, don't assume). Updated the language from "a judgment call, flagged in case it needs revisiting" to "an expected pattern for future playbooks" only now, after the second independent confirmation — worth the general discipline of not upgrading a one-off resolution to a standing rule until it's actually been reproduced, even when the reasoning behind the first instance was already solid.
- Finalized both `BespokeSection`s cleanly against Bowser's real data with no surprises relative to the structural analysis already done in the prior two rounds (Friendship's Type titles being bold was new information, not a new decision — same "check every option, no predictions" discipline, this time confirming rather than overturning a prediction).
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Covenant` — "Covenant" zero-option Section and "Friendship" 2-of-2 nested-category Section, both content-fidelity-verified; progress tracker to 7 of 28), `docs/hunter-playbooks/phase5-bespoke-ideation.md` (blank-by-intent pattern upgraded from single-instance judgment call to confirmed recurring convention, cross-reference note added marking the earlier Description-concatenation resolution as superseded by `EffectText`).

### Hunter Playbooks — The Curse-Eater: Two New Schema Structures (`PlaybookExtraTrack`, `BespokeJournal`), Both Approved as Proposed (2026-08-27, same day)

- **The biggest architectural fork of this whole effort so far, and the first round with two genuinely separate new structures needed at once — handled by keeping them in separate homes rather than merging them because they arrived together.** Corruption (a track) and Consumed Magic (a growing pick-less list) share one source paragraph and one playbook, but they're conceptually unrelated shapes — one is parallel to Luck/Harm/Experience, the other is parallel to (but structurally distinct from) `BespokeSection`/`BespokeOption`. Documented `PlaybookExtraTrack` in `architecture.md` next to the other tracks and `BespokeJournal` in `phase5-bespoke-ideation.md` next to the other bespoke structures, rather than inventing one combined "misc extra stuff" concept because they happened to show up in the same round. Worth the general lesson: two new things arriving together in one playbook's source text doesn't mean they're the same kind of thing architecturally — check each independently against what it's actually most similar to, even under time pressure to just "handle this playbook's oddities."
- **Chose additive-only over full generalization for Corruption, and stated the accepted inconsistency explicitly rather than letting it look like an oversight.** Luck/Harm/Experience stay hardcoded fields on `Playbook`; Corruption gets a new normalized table. This means the four conceptually-similar "tracks" are now modeled two different ways. Rather than treating that as a loose end, wrote it into `architecture.md` as a named, deliberate trade-off (migration cost now vs. one clean shape) — the same "state what's NOT being solved, don't let it read as forgotten" discipline already used for Fate's messiness and Corruption's own instance-value asymmetry.
- **`BespokeJournal`/`BespokeJournalField` reused the `Description`/`EffectText` role split from `BespokeSection` rather than inventing new field names for a structurally different table** — worth noting as the split's first reuse outside the table it was designed for, a sign the pattern (framing vs. consequence prose) generalizes beyond the one table it was invented against.
- One paragraph split three ways this round (Corruption's `Description`, Corruption's `EffectText`, and the *separate* `BespokeJournal.Description`) — the most granular content-fidelity split done so far, resolved the same way as every prior split: by what each clause actually instructs, not by which sentence came first.
- Docs: `docs/hunter-playbooks/architecture.md` (new "Extra Tracks" subsection, Section 3 schema block: `PlaybookExtraTrack`/`HunterExtraTrackValue` added), `docs/hunter-playbooks/phase5-bespoke-ideation.md` (`BespokeJournal`/`BespokeJournalField`/`HunterJournalEntry`/`HunterJournalEntryFieldValue` documented, "How Consuming Magic Works" confirmed as an ordinary Mission-shaped Section, content-fidelity split resolved). Catalogue entry (`## The Curse-Eater`) still pending Bowser's formatting results.

### Hunter Playbooks — The Curse-Eater Finalized: a Proposed Split Overturned by Direct Source Verification (2026-08-27, same day)

- **A content-attribution judgment call I'd made with real reasoning turned out to be factually wrong, not just debatable — and the fix was to recognize the *kind* of error, not just correct the one instance.** My original 3-way split attributed two sentences to the Consumed Magic journal because their *subject matter* was clearly about recording magic/power/downside. Bowser checked the actual PDF item stream and found there's no prose under "Consumed Magic" at all — the sentences I'd attributed there are physically part of Corruption's own paragraph, full stop. This wasn't "a reasonable call that could go either way," it was a factual premise (there are two source-positioned blocks to split) that turned out to be false. Worth distinguishing sharply from the EffectText "split by role, not position" principle this reasoning was modeled on — that principle assumed two *real* blocks existed and settled which field each belonged in; here, one of the two blocks I split against never existed to begin with.
- **Resolved by finding and applying the closest real precedent rather than re-deriving a new principle under time pressure.** Covenant had already established exactly this shape of question (content whose *subject* points at a nearby differently-headed structure) and Skyler's answer there was to respect the header boundary rather than stretch a field across it. Recognized the parallel and applied it consistently instead of treating this as a fresh case needing its own new reasoning — the same "check whether an existing decision already answers this before deriving a new one" discipline used repeatedly throughout this task, just applied here to my own prior output instead of a stakeholder's.
- **A genuinely empty `Description` field (`BespokeJournal.Description = null`) is the correct, honest result once the false premise was removed** — not a gap needing a workaround. Consistent with the file's own established discipline (don't invent content the source doesn't have) applied to a *field*, not just an *option*, for the first time.
- Corrected the wrong split in both `architecture.md` and `phase5-bespoke-ideation.md` before finalizing the catalogue, not just the catalogue entry alone — a wrong premise recorded in three places needed correcting in all three, not just the one a reviewer happened to flag.
- Docs: `docs/hunter-playbooks/architecture.md` (Corruption split corrected with full reconciliation reasoning), `docs/hunter-playbooks/phase5-bespoke-ideation.md` (`BespokeJournal.Description` correction), `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Curse-Eater` — Corruption/`PlaybookExtraTrack`, Consumed Magic/`BespokeJournal` with null `Description`, How Consuming Magic Works as an ordinary Section with the first genuinely-rendered blank marker; progress tracker to 8 of 28).

### Hunter Playbooks — The Envoy: the `EffectText`-Motivating Case Confirms the Existing Model, No New Schema Needed (2026-08-27, same day)

- **The playbook that originally motivated a schema field turned out not to need any further schema work — worth registering as a real, positive validation, not just "nothing happened this round."** `EffectText` was named specifically with Envoy's "Overseers" in mind, unread at the time. Reading it for real: Overseers fit the already-existing 2-mandatory-category nested shape (Fate/Friendship) exactly, and the two OTHER bespoke concepts on the same playbook (Task, Secret Wisdom) also each matched an already-established shape (Titled Choice; zero-option Section) with zero new work. Worth the general point: when a stakeholder names a future case as the reason for building something, actually reading that case later and finding it needs nothing further is itself a meaningful confirmation the design was scoped correctly — not a non-event to skip past in the report.
- **Reused the Covenant zero-option precedent for Secret Wisdom without re-litigating it, on the strength of the two cases being genuinely the same shape** (differently-headed content, no pick of its own, positioned near a nearby ruleset it's thematically related to) — not just because reusing a precedent is convenient, but because I checked the shape match first (fixed mechanic, its own header, zero options) before applying the resolution rather than the reasoning.
- **Named a real ambiguity risk in the write-up before it could cause confusion, without it rising to an actual modeling question**: Task's 4 options and Secret Wisdom's 4 sub-entries reuse the identical 4 labels (Guide/Herald/Watcher/Witness) for two structurally unrelated lists in the same column. Bowser flagged the extraction risk; added an explicit disambiguation note in the catalogue entry itself (Task's are real `BespokeOption` rows, Secret Wisdom's are plain-text labels inside free-form `EffectText`, no stored link between them) so a future reader doesn't assume a structural relationship the data doesn't actually encode.
- **Blank-marker rendering conventions are now confirmed at three, not two** — Envoy's "label on its own line, blank run on the next line" is genuinely different from both Curse-Eater's inline-same-line form and Changeling's/Covenant's absent-marker form. Recorded this explicitly as a third convention `{{blank}}` needs to abstract over, per Bowser's own flag that source-layout reasoning could matter later even though the token already handles semantic intent uniformly today.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Envoy` — Task/Secret Wisdom/Overseers, all three content-fidelity-verified with real markup captured, disambiguation note, third blank-marker convention noted; progress tracker to 9 of 28).

### Hunter Playbooks — The Expert: a Real Source Inconsistency Recorded, Not Normalized (2026-08-27, same day)

- **Distinguished two categories of source inconsistency that had been getting the same "normalize it" treatment by default, and gave them different resolutions on purpose.** Wording inconsistencies (Underworld's "(choose one)" vs. "Pick one:") get normalized because they affect *instructional meaning* — a downstream reader could genuinely be misled about how many options to pick. Armory's bold-instead-of-italic trigger clause carries no such functional difference; both stylings convey identical game meaning. Recorded it exactly as printed rather than defaulting to "this project normalizes inconsistencies" as a blanket rule. Worth the general lesson: a project-level pattern ("we normalize X") isn't actually one rule, it's shorthand for a *reason* (here: protecting meaning) that needs re-checking against each new case rather than applied mechanically to anything that looks like the same shape of problem.
- **Framed the decision correctly as "override a confirmed fact vs. preserve it," not "should the source have been consistent."** The pipeline's entire purpose is faithful transcription of the source's actual rendered typography — normalizing Armory's styling to match its siblings would mean substituting a guess about authorial intent for a verified fact, the same category of move already declined multiple times this task (guessing bold-span positions, inventing blank markers). Named that connection explicitly rather than treating this as an unrelated one-off call.
- Confirmed the "content shape doesn't predict PDF structure" lesson (first surfaced by Action Scientist) generalizes beyond inline emphasis to list structure too — Armory's roll-outcome breakdown looks identical in kind to Action Scientist's genuinely-bulleted options, but is flowing prose with zero list markers, verified directly rather than assumed from the resemblance.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Expert` — Haven, content-fidelity-verified with real markup, bold-vs-italic inconsistency recorded as-is with reasoning, prose-not-list finding documented; progress tracker to 10 of 28).

### Hunter Playbooks — The Flake: a Stakeholder's Own Prediction Verified Independently, Not Trusted (2026-08-27, same day)

- **Skyler predicted zero-bespoke this time; verified it the same way as every other playbook rather than fast-pathing the check because the answer was expected.** Read both pages in full, walked every section, checked Moves explicitly — same rigor as Celebrity's first zero-bespoke case, not a lighter pass because the stakeholder had already told me what I'd find. The one early signal worth noting (the page header row pattern, "Moves | Gear" with no third named concept, matching Celebrity's) was treated as a data point to check against, not a shortcut to skip the full read — stated that distinction explicitly in the write-up rather than silently confirming the prediction.
- **Flagged a genuine cross-playbook reference (Flake borrowing Expert's Haven via an Improvement) without letting it destabilize the zero-bespoke conclusion or get modeled as new structure it doesn't need.** The reference is real and worth naming, but it's flat Improvement text describing a level-up grant, not evidence of a bespoke ruleset belonging to Flake itself — same treatment already established for every other cross-playbook Improvement mention. Worth the general point: a stakeholder's prediction being confirmed doesn't mean nothing worth reporting was found; a genuinely interesting detail and a "does this change the answer" question are separate things to track.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Flake` — confirmed-zero entry per the standing convention, cross-playbook Haven reference noted, Moves checked and logged per the standing convention; progress tracker to 11 of 28).

### Hunter Playbooks — The Forged: Four Bespoke Concepts in One Playbook (a New High), Plus a Real Correction to a Stakeholder's Relayed Moves Layout (2026-08-27, same day)

- **The same source-header-boundary principle (Covenant precedent), applied consistently, produced two different structural outcomes on the same playbook — worth registering as confirmation the principle is doing real work, not just being invoked as a rubber stamp.** Bonds and Burdens sit under Partner's Move description with no shared header at all → modeled as two independent `BespokeSection`s. Origin has its own bare header line directly above both Forging and Partnering with nothing intervening → modeled as one umbrella `BespokeSection` with 2 nested mandatory categories, despite having no framing sentence of its own (`Description = null`, same as Consumed Magic). Dual Nature has a full framing sentence plus 3 nested categories, the cleanest case, no judgment call needed. The lesson: applying an old principle faithfully to genuinely different source text should sometimes yield different answers — treating that as a red flag ("didn't I already decide this?") would be the wrong instinct; the check is "does the *source* look the same," not "does the *shape* look the same."
- **Verified a stakeholder's relayed technical detail and found it wrong, reported the correction rather than reconciling silently.** Skyler's framing was "2 required + pick-two pool, like Curse-Eater." Direct read: "You get this one:" (singular) + "Then pick one of these:" (not "two") — 1 required (Partner) + pick 1 of 6, not 2 required + pick 2. Still nets out to the same stated total ("plus two Forged moves"), and still the same required-plus-pool *shape* — but the specific counts Skyler gave don't match the source. Reported as a stated correction rather than silently substituting the right numbers into the catalogue without calling it out, consistent with this task's standing "verify directly, never take a stakeholder's framing as given" discipline — this is exactly the kind of small factual mismatch that discipline exists to catch, not just the big "is there bespoke content at all" questions.
- **A title+description split with no font signal at all — the first of its kind in this catalogue, recorded as final but with an explicit lower-confidence provenance note rather than silently treated as equivalent to font-verified splits.** Benefits' 8 options (`"Magic: Add the \"magic\" tag"`) are single plain-font text items; the Title/DescriptionText boundary comes entirely from the extraction script's own colon-position parsing, unlike every prior Titled-Choice case (Background, Task, Type), which had a real bold-run boundary backing the split independently of the colon. Content here is simple enough that colon-splitting is safe and the result is recorded as final, not held back — but the provenance is genuinely different and worth flagging explicitly for any future case where a colon placement might be ambiguous.
- **Underscore-run length inside the "inline same-line" blank-marker convention is not fixed even between playbooks using the identical convention** — 30 underscores (Curse-Eater, Bonds, Burdens) vs. 27 (Forged's Origin, Forging and Partnering both). Three rendering *conventions* were already tracked (absent / inline-same-line / label-then-next-line); this adds the finding that even within one convention, the literal character count varies and shouldn't be assumed fixed — only the convention shape matters for how `{{blank}}` gets placed, never the count.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Forged` — Bonds, Burdens, Dual Nature, Origin, all four content-fidelity-verified with real markup captured, Moves-layout correction documented, Benefits' no-font-signal provenance flagged, third-underscore-count finding noted; progress tracker to 12 of 28). No new schema needed — all four concepts reused already-established shapes (Tag Pick, Titled Choice, Simple Choice, mandatory-multi-category umbrella).

### Hunter Playbooks — The Forged, Round Two: a Real Miscategorization, a Fabricated Quote Laundered Through a Relay, and an Independent Root-Cause Investigation (2026-08-27, same day)

- **A genuine scope miscall, not a source-reading error: Bonds/Burdens belongs to a `Required` Move (Partner), not this catalogue.** I'd correctly read the content and correctly applied the Covenant header-boundary precedent to justify treating Bonds/Burdens as independent `BespokeSection`s — but the entire premise was wrong at a higher level: Moves (including a Move's own pick-based sub-structure) are out of scope for this catalogue regardless of how cleanly they fit the `BespokeSection` shape. Worth the general lesson: a structural fit being clean is not evidence the content belongs in this catalogue at all — check category/scope before checking shape. Removed the `### Bonds`/`### Burdens` entries entirely rather than reworking them, and added a one-line Phase-4 flag on Partner's own Moves paragraph instead.
- **A fabricated quote reached the catalogue as if Skyler said it, and I recorded it as a direct quote without having any way to verify it against the actual source.** The coordinator invented "2 required + pick-two pool, like Curse-Eater" while paraphrasing Skyler's real (vaguer, correct) statement about a shared dual-column trait. I'd already independently verified the *numbers* were wrong (1 required + pick 1 of 6) — but I still wrote up the discrepancy as "Skyler's relayed description was X" without questioning whether X was really Skyler's own words or an intermediary's gloss. **The fix going forward: treat anything phrased as "Skyler said X" with the same skepticism as my own PDF extraction — a quoted claim needs the same "verify the literal source" discipline as a page read, and a relay (even a trusted one) is not the literal source.** This is a new, generalizable calibration, not a one-off correction: I can verify PDF content directly, but I have no independent channel to verify what Skyler actually said — which means specificity in a relayed quote is itself a signal worth treating with suspicion (the more precise and quotable something sounds, the more it's worth asking "did the source actually say this, or did someone sharpen it in transit").
- **Investigated the root-cause PDF defect independently rather than trusting Skyler's/Bowser's framing at face value, even though both were already correct.** Re-derived the swap myself via `pdftotext -raw` (poppler's literal content-stream mode, distinct from both `-layout` and bare/default mode) on both Forged pages — confirmed page 23's Col1→Col3→Col2 swap directly from the raw item order, confirmed page 24 has no swap. Then checked the tool I'd actually used throughout this whole task (bare `pdftotext -f N -l N`, never `-raw`) against the same defective page and found it already reconstructs correct visual order — poppler's default heuristic mode does independent geometric reading-order reconstruction, it doesn't follow the literal stream. This is a distinct, complementary finding to Bowser's pdf.js-based immunity argument (different tool, different mechanism, same practical conclusion) — worth having both because they cover different pipelines (my page-level structural reads vs. Bowser's coordinate-scoped formatting extraction) that could in principle have failed independently even if one was fine.
- **Did a real (not hand-waved) risk assessment of the 11 previously-processed playbooks rather than asserting "probably fine."** Ran `pdftotext -raw` against every page actually used for a structural read across all 11 prior playbooks (12 pages: 1,3,5,7,8,9,11,13,15,17,19,21) and checked each for the swap's own signature (a later-column heading preceding an earlier-column heading it should follow in the stream) — zero found, including Curse-eater (the playbook structurally closest to Forged's dual-column Moves layout, the single highest-risk candidate). Reported this as a completed check with its own evidence, not a reassurance — the coordinator's ask was explicitly "flag which ones, if any... rather than silently assuming everything else is fine," so the deliverable had to be the check itself, not a confidence statement.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (`## The Forged` — removed `### Bonds`/`### Burdens` entirely, corrected the fabricated-quote paragraph, added Partner's Phase-4 Moves flag, fixed two stale cross-references to the removed entries, progress tracker note added), `docs/hunter-playbooks/pdf-extraction-pipeline.md` (new "Addendum: Yoshi's structural-read risk assessment" section — independent `-raw` re-confirmation, bare-`pdftotext` immunity finding, full 11-playbook spot-check with results, top status paragraph updated).

### Hunter Playbooks — The Gumshoe: the Naked City Trap Caught in Real Time, a Third Free-Text Shape Needed, `BespokeSection.FreeTextLabel` Approved (2026-08-27, same day)

- **Applied the Forged lesson within the very next round, not just retroactively.** The Naked City (a `Required` Move) has its own internal "pick four contact types from 34 named areas" sub-structure — structurally identical to Forged's Bonds/Burdens under Partner. Recognized it immediately as Move content, flagged it for Phase 4, and did not model it as a `BespokeSection` at all. Worth noting as the actual test of whether a corrected mistake sticks: the same *shape* (Required Move with pick-based internal structure) showed up again one playbook later, and the fix held without needing re-derivation.
- **A third genuinely new bespoke shape, correctly *not* forced into either of the two existing "no fixed option list" patterns.** Gumshoe Code (free-authored, one value, set once, illustrative-only examples) is neither Covenant's zero-option fixed ability (no player input at all) nor `BespokeJournal` (growing, repeating, multi-field entries). Proposed a third: `BespokeSection.FreeTextLabel` (nullable string) — zero options, but *does* need to capture one player-authored value. Explicitly considered and rejected two alternatives with stated reasoning before proposing this: overloading `{{blank}}` to mean "whole-Section free text" (would give the token two different meanings depending on context) and reusing `BespokeJournal` (real unused structure — a template+instance pair built for repeating entries, applied to something that's neither repeating nor multi-field). Skyler approved as proposed.
- **Flagged, not resolved, a real loose end on the instance side**: `HunterBespokeSelection.BespokeOptionId` was deliberately made required (not nullable) earlier in this project specifically because every case seen until now attaches to a real option. A `FreeTextLabel` Section's answer has no option to attach to — proposed reusing `HunterBespokeSelection.FreeformText` with `BespokeOptionId` null as the *one* expected exception, but explicitly flagged this as worth confirming when Gumshoe Code is actually authored in Phase 4, not locked in now. Don't let a previously-firm "always required" rule get silently punched through by the first case that doesn't fit it — name the exception instead of quietly making the FK nullable everywhere.
- Docs: `docs/hunter-playbooks/phase5-bespoke-ideation.md` (Section 3 — new `BespokeSection.FreeTextLabel` subsection: schema, both declined alternatives with reasoning, the flagged instance-side loose end). `bespoke-ruleset-catalogue.md`'s `## The Gumshoe` entry still pending Bowser's formatting results before finalizing.

### Hunter Playbooks — The Gumshoe Finalized: a Fourth Provenance-Caveat Case (Sentence-Split, Not Font-Split), and Cross-Reference Bolding Extends to a Playbook's Own Moves (2026-08-27, same day)

- **A new content shape (one continuous italicized run covering 6 illustrative sentences, no list markers) got the same "record the real structure, flag the convenience split separately" treatment already established for Forged's Benefits.** Bowser's extraction included both the true structural signal (one undelimited italic run) and its own sentence-boundary parsing (a 6-item convenience array) — used the former for `Description`, explicitly noted the latter is script-derived text-parsing, not a PDF signal, naming the exact same category of caveat as Benefits' colon-based title split even though the underlying mechanism (sentence boundaries vs. colon position) differs. Worth the general pattern now: whenever an extraction script offers a "cleaned up" convenience view alongside the raw structural signal, catalogue the raw signal and flag the convenience view's provenance explicitly — this is the second time it's come up, confirming it's a recurring shape in how the pipeline reports ambiguous-structure findings, not a one-off.
- **Cross-reference bolding (previously: always a universal base move like "manipulate someone") extended to a playbook's own bespoke move names for the first time** — "The Postman Always Rings Twice" and "The Long Goodbye" both bold inside Gumshoe Code's `EffectText`. Recorded as found, flagged explicitly as a pattern extension rather than silently treated as a one-off, since the project-wide assumption ("bold cross-references point at universal base moves") was real but incomplete — worth checking both possibilities on any future bold cross-reference rather than assuming base-move-only.
- **"Example Codes:" (a confirmed-bold structural label in the source) was excluded from the persisted markup, applying the already-established "structural labels don't carry their bold into stored option text" rule** (same treatment as "Moves"/"Then pick one of these:" throughout this catalogue) — worth noting since this is the first time that rule needed applying to a label sitting *inside* a `Description` block's own prose, not just a page-level section heading.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Gumshoe` — Gumshoe Code with real `FreeTextLabel`/`Description`/`EffectText` content and full content-fidelity verification, Naked City's Move-internal pick structure flagged for Phase 4 and correctly excluded from the start this round; progress tracker to 13 of 28, noting Gumshoe as the second playbook needing schema beyond baseline `BespokeSection`/`BespokeOption`).

### Hunter Playbooks — The Hex: a Rejected Proposal Redesigned From Scratch (`BespokeJournal` → Repeatable `BespokeSection`), and Why the Redesign Held Up Better Than the Original (2026-08-27/28)

- **Skyler rejected my first-pass proposal outright ("I would not treat this like a BespokeJournal at all") and the fix was a genuine rethink, not a patch.** My original instinct pattern-matched Rotes to Consumed Magic on the surface trait "grows over time, player-authored." Skyler's correction identified the actual distinguishing axis I'd missed: `BespokeJournal` is for content with **no real structure** (Power/Downside are bare labels, nothing enumerable); Rotes has **real, heavily-structured** content (a genuine fixed pick-list, mandatory die-result-keyed slots) that just happens to also need multiple instances. Worth the general lesson: "grows over time" and "has no fixed structure" are two *different* properties that happened to co-occur in the one prior case (Consumed Magic) that motivated `BespokeJournal` — I'd conflated them into one signal because I'd only ever seen them together before. The fix was recognizing they're separable and that Rotes only has one of the two.
- **The redesign turned out cleaner than the rejected one, not just different** — confirming a rethink-from-scratch was the right call rather than a patch. `MinInstances`/`MaxInstances` on `BespokeSection`, mirroring `MinSelect`/`MaxSelect`'s own existing idiom one level up, needed zero new option-level schema (Requirements/Effect fit the *existing* `BespokeOption` model exactly, verified explicitly rather than assumed) — versus the `BespokeJournal` proposal, which would have needed a whole parallel pick-capable-field extension (`BespokeJournalFieldOption`, a new instance-side selection bridge) just to shoehorn Requirements' real pick-list into a table designed for pure free text. The corrected direction reused far more of the existing schema than the original guess did — a good signal, in retrospect, that the original framing was fighting the shape rather than fitting it.
- **Deliberately declined a discriminator boolean (`IsRepeatable`) a second time in as many rounds** (after declining one implicitly for `FreeTextLabel` too) — derived repeatability from whether `MinInstances`/`MaxInstances` are populated, consistent with the schema's standing "no `ShapeKind` enum, shape emerges from populated fields" philosophy. Worth naming as a recurring discipline now, not a one-off: every time a new "does this Section do X" question comes up, check whether it's derivable from already-planned fields before reaching for a new boolean flag.
- **A worksheet's physical page-space (8 printed blank Rote slots) was explicitly NOT treated as a game-rule cap** (`MaxInstances = null`, not `8`) — checked the source directly for a stated maximum and found none, only "you can learn more by taking improvements." Worth the general caution: a repeated print template's row count reflects how much room fit on the page, not a rule, and needs checking before being mistaken for one.
- Docs: `docs/hunter-playbooks/phase5-bespoke-ideation.md` (Section 3 — new repeatable-`BespokeSection` subsection: `MinInstances`/`MaxInstances`, the `HunterBespokeSectionInstance` table, both declined alternatives with reasoning, explicit confirmation that Requirements/Effect need zero `BespokeOption` changes). `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Hex` — Temptation confirmed as a clean standard-shape fit per Skyler's own expectation, Rotes fully modeled with real captured markup including a documented inconsistent-bolding case and Bowser's hand-corrected extraction pitfall; progress tracker to 14 of 28, Hex noted as the third playbook needing schema beyond baseline `BespokeSection`/`BespokeOption`).

### Hunter Playbooks — The Host: Distinguishing Two Superficially Similar "Improvement Grows It" Cases Without Reaching for Hex's New Mechanism (2026-08-28)

- **The most useful thing this round wasn't finding new content, it was correctly declining to reuse a just-built mechanism.** "Take another benefit from Symbiosis" (an Improvement) looks, on the surface, exactly like the signal that triggered Hex's repeatable-Section design ("a ruleset that grows via Improvements"). Traced through *why* Rotes actually needed `MinInstances`/`MaxInstances` — not because it grows, but because growth there means *multiple independent instances of a whole nested tree with colliding option-slot answers* (two Rotes both need their own distinct "10+" effect text against the same template option). Symbiosis's Benefits has no such collision: each of its 10 options is a one-time, non-repeating pick: "gaining another" just means picking one more *distinct* option from the same pool, structurally identical to how "Take another Host move" has always grown a Move pool past its creation-time count with zero schema for it. Resolved by precedent, not escalated, and stated the distinguishing test explicitly in the catalogue entry so it doesn't need re-deriving next time a similar Improvement shows up (and one will — this is a common MOTW playbook pattern).
- **Worth naming as a recurring meta-lesson now that it's happened three times in three rounds**: a new schema mechanism approved for one specific shape (repeatable Sections, `FreeTextLabel`, `EffectText` before that) creates real pressure to reach for it again on the next superficially-similar case. The actual discipline that's held up each time: check what specifically made the *prior* case need the mechanism, not just whether the *surface trait* (grows over time, needs a label, has two prose blocks) recurs — the surface trait recurring is necessary but not sufficient, and conflating the two is exactly the mistake Skyler corrected on Hex's first pass.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Host` — Symbiosis, a clean 2-mandatory-category Tag-Pick structure confirmed entirely plain; Defensive Adaptation's Move-internal pick structure (the third instance of the Forged/Naked-City trap) flagged for Phase 4 and correctly excluded; the Benefits-growth judgment call stated explicitly with its distinguishing test; progress tracker to 15 of 28).

### Hunter Playbooks — The Initiate: Two Genuinely New Out-of-Scope-But-Worth-Flagging Findings, and a Same-Word Bold False Positive Now a 4th-Time Standing Pattern (2026-08-28)

- **A cross-section mechanical dependency running the opposite direction from every prior case, correctly left unmodeled but explicitly flagged rather than silently noted-in-passing.** Every previous Move/bespoke cross-reference in this project has been a *Move* pointing at a *bespoke ruleset* (Forged's Partner → Bonds/Burdens, Gumshoe's Naked City, Host's Defensive Adaptation). The Initiate's Gear text ("If your Sect has fighting arts or obsolete gear... pick three old-fashioned weapons...") is the first case where a **standard section's own pick-count** (Gear) depends on a **bespoke ruleset's** picks (Sect's Traditions). Confirmed this doesn't change Sect's own modeling at all (still a clean 2-category Tag Pick, no new schema) — but it does mean Gear's `PlaybookGearCategory.PickCount` can't be authored as a flat fixed value for this playbook, which is a real Phase-4 authoring complication worth a name now rather than a surprise later. Worth the general pattern: a cross-reference's *direction* (which side depends on which) matters for scoping impact, not just whether one exists.
- **A genuinely unnamed Required Move — first of its kind across 16 playbooks — recorded as a finding, not silently worked around.** Every other Required move catalogued so far has a bold name preceding its description; this one has none, verified via both default and `-raw` extraction (not just a first-read assumption). Since Moves are out of this catalogue's scope, resisted the urge to invent a name for it just to make the note read more cleanly — flagged the absence itself as the Phase-4-relevant fact (will need either a synthesized name or a schema accommodation for a null `PlaybookMove.Name`), left the actual resolution for whoever authors Moves.
- **The same-word bold false positive (a section's own bold heading colliding with the identical ordinary word appearing in its body prose) hit a 4th independent case** (Curse-Eater, Envoy, Hex, now Initiate's "Sect" heading vs. "Sect" in the flavor text) — worth registering explicitly that this is no longer a one-off bug pattern worth re-discovering each time; Bowser's already made it a standing checklist item, and it's reliable enough now that seeing it *not* flagged on a future page dense with repeated section-name words would itself be worth double-checking.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Initiate` — Sect, a clean 2-mandatory-category Tag-Pick structure confirmed entirely plain; the conditional Gear-depends-on-Sect-Traditions dependency and the unnamed Required Move both flagged explicitly for Phase 4; checked and confirmed no repeatable-Section signal per the standing post-Hex discipline; progress tracker to 16 of 28).
- **Follow-up, same day**: Skyler named the previously-unnamed Required Move "One of Us." Updated the catalogue's note to record the name while marking it explicitly as Skyler-assigned, not source-printed — the one line in this whole catalogue that isn't a PDF-transcribed fact, worth keeping visibly distinct from everything else in the file rather than blending it in as if it read that way on page 33. Checked `phases.md`/`phase5-bespoke-ideation.md` for any other place this was tracked as an open item — found none; the only other mention was Bowser's own pipeline-pass log (`pdf-extraction-pipeline.md`), which is a historical record of that dispatch's scope, not a pending flag, so left it untouched rather than editing history to look pre-resolved.

### Hunter Playbooks — The Interface: a Clean, Uneventful Pass — Worth Noting as Confirmation the Established Toolkit Now Generalizes, Not Just a Non-Event (2026-08-28)

- **First playbook in several rounds with no new schema question, no false-positive class, and no escalation needed at any step** — Integration mapped directly onto the 3-mandatory-category shape already proven by Dual Nature (two Tag-Pick children + one Titled-Choice child), the repeatable-Section check came back negative on the first pass (Upgrades/Faults growth is ordinary Symbiosis-shaped pool growth, not Rotes-shaped instance growth), and Bowser's extraction found zero new false-positive classes. Worth registering explicitly rather than letting an uneventful round pass without comment: after ~7 consecutive playbooks each surfacing at least one genuinely new wrinkle (a fabricated quote, a rejected proposal, three new schema fields, two unnamed/synthesized-title cases), a clean pass is itself a data point that the accumulated shape vocabulary (Titled Choice, Tag Pick, Nested/mandatory-multi-category, repeatable-Section, `FreeTextLabel`) is now covering real variety, not just the original 3 pilots' shapes.
- **A second synthesized (not source-printed) Title, handled with the exact same explicit-flagging discipline as "One of Us."** Integration's third category ("Then pick how you gained these abilities.") has no short header in the source the way Upgrades/Faults do — gave it a synthesized `Title` ("Origin") and flagged it as such in the same sentence, rather than letting a second unflagged synthesized label quietly join the first. Worth the general point: once a discipline like "mark self-assigned content as distinct from source content" gets established for one case, the next structurally-similar case should reflexively get the same treatment, not require re-deciding whether it's warranted.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Interface` — Integration, a clean 3-mandatory-category structure with real captured content and a fully plain, first-attempt-clean content-fidelity result; the synthesized "Origin" title flagged explicitly; checked and confirmed no repeatable-Section signal and no Move-internal-pick trap (no Required move exists on this playbook at all); progress tracker to 17 of 28).

### Hunter Playbooks — The Monstrous: a Scope-Boundary Correction to Skyler's Own Framing, an Accepted Unenforced Rule, and a New Zero-Option Section Pattern-Reused for Orphaned Framing Text (2026-08-28)

- **Skyler's own framing ("Monster Breed is informed by three rule sets — Curses, Natural Attacks, and Moves") didn't survive direct verification, and the fix was catching it before modeling around it rather than after.** "Moves" turned out to be the playbook's completely ordinary standard Move-granting section, structurally and verbally identical to every other playbook's — narratively grouped with Curses/Natural Attacks in the flavor text ("picking a curse, moves, and natural attacks") but not actually a third bespoke concept. Caught this at the scope-boundary-check step the coordinator explicitly asked for, before doing any modeling work on it, not after building a "Moves" `BespokeSection` and having to unwind it. Redirected Skyler's custom-blank-Move-slots idea to a `PlaybookMove`-schema flag for Phase 4 instead of silently either modeling it here or dropping it. Same "verify a stakeholder's framing directly, don't build on top of an unverified premise" discipline as the Forged Moves-count correction, just caught earlier in the process this time.
- **A schema-enforcement question got a real proposed answer this round (leave the either/or rule unenforced, stated in prose only) — and the coordinator caught that Skyler's first-pass response hadn't actually engaged with the tradeoff, requiring a re-ask before it counted as resolved.** Worth registering as a coordinator-side catch, not mine, but relevant to how "Skyler decided X" gets verified going forward: an answer that just restates the already-proposed structure isn't the same as an answer that addresses the flagged gap, and the coordinator (not me) is who caught and re-surfaced that distinction here. Filed for the general point: a stakeholder's response should be checked for whether it actually answers the specific tradeoff raised, not just whether a response arrived.
- **Reused the Covenant zero-option-Section pattern for a genuinely new purpose (orphaned framing text spanning multiple sibling Sections), not just a repeat of "fixed ability with no picks."** Every prior zero-option Section (Covenant's ability, Envoy's Secret Wisdom) held a fixed passive mechanic. "Monster Breed" holds pure introductory/thematic prose that doesn't belong to any one of its sibling Sections specifically — the same *shape* (zero options, Description+EffectText only) solving a different *problem* (a content-placement gap created by Skyler's explicit "no umbrella" structural preference, not a mechanic with no picks). Worth the general note: an existing shape being reusable for a new purpose is worth flagging as such, not just applying it silently as if it were the same case as before.
- **Departed from the established sequence on explicit instruction, and marked the departure clearly rather than letting it look like the normal finalize-after-Bowser flow.** Wrote the full structural catalogue entry before Bowser's dispatch (per the coordinator's direct request, since both open questions were resolved and they wanted the handoff spec immediately) — flagged this prominently at the top of `## The Monstrous` itself ("structurally final, content fidelity not yet verified") and deliberately held the playbook out of the progress tracker's main count/shape-tally sentence until the real check comes back, rather than letting an unverified entry blend in with 17 fully-verified ones.
- One source typo fixed on sight and flagged: the intro flavor text's "if you *you* were originally" (a genuine duplicated-word error, confirmed present in both default and `-layout` extraction, not a column-boundary artifact) — treated the same as a line-wrap artifact, not preserved literally.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (added `## The Monstrous` — Monster Breed/Curses/Natural Attacks structurally recorded with real source content, explicitly marked pending Bowser; progress tracker note added without incrementing the main count). Bowser handoff spec given directly to the coordinator for dispatch.

### Hunter Playbooks — The Monstrous Finalized: a Structural Assumption Corrected by the Real Extraction, and Two More Recurring Bug Classes Confirmed (2026-08-28, same day)

- **A structural claim written into the "pending" entry turned out wrong once real formatting data arrived — caught and fixed before it could harden into an unverified fact.** The draft entry described Natural Attacks' Base/Extra options as "title + description each," inferred from the visual shape of "Base: teeth (3-harm intimate)" without a font-level check (since that check hadn't happened yet — the whole entry was written ahead of Bowser's dispatch, by design, per the coordinator's request). Bowser's real extraction showed `Title: null` for every one of the 7 — no font-derived split exists at all, matching Forged's Benefits precedent, not Background's. Corrected on fold-in rather than left inconsistent with the newly-confirmed data. Worth the general lesson specific to this round's unusual sequencing: writing structure ahead of formatting verification is fine when explicitly requested, but anything inferred from *visual shape alone* (not a confirmed font signal) needs to be treated as provisional even in the structural pass, not stated as flatly as a font-confirmed case would be — this is the second time "looks like title+description" turned out wrong on inspection (first: assuming Background's shape from resemblance instead of checking, walked back same way here).
- **Two more instances of already-tracked recurring bug classes, not new discoveries — correctly described as extensions of known patterns rather than new one-offs.** Curses' leading-colon leftover (mirror image of Haven's leading-period bug) and the Monster Breed Suggestions cross-call-run leakage (second occurrence of the class first found on Hex) both got attributed to their existing lineage in the catalogue entry rather than written up as fresh findings — keeps the "how many distinct bug classes exist" count honest for anyone auditing pipeline maturity later.
- **Inline bold/italic markup inside a zero-option Section's `EffectText` free text (Monster Breed Suggestions' bold archetype names) is not subject to the "titles never persist markup" rule** — that rule is specifically about `BespokeOption.Title`, and there's no `Title` field involved in a block of free-form prose. Worth stating explicitly since the two could be conflated at a glance (both are "a short bold phrase decorating a piece of content"), but they're structurally unrelated cases with opposite fidelity treatment.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Monstrous` — pending-status note removed, real captured markup folded into Monster Breed/Curses/Natural Attacks, the Base/Extra title-shape correction applied, two bug-class attributions recorded; progress tracker to 18 of 28).

### Hunter Playbooks — The Mundane: a Second Zero-Bespoke Confirmation, Skyler's Prediction Verified Rather Than Assumed From the Flake Precedent (2026-08-28, same day)

- **Skyler's own expectation ("like The Flake") was checked as its own independent finding, not treated as pre-confirmed by the earlier case it was compared to.** Read both pages in full, walked every section, checked Moves explicitly — identical rigor to Celebrity's and Flake's own zero-bespoke passes, not a lighter pass because both the stakeholder's prediction *and* a structurally similar precedent both pointed the same way. Two independent things could each be wrong in the same direction; verifying anyway is what actually rules that out.
- **Getting Started's own enumeration of what a Hunter needs to decide continues to be a reliable, cheap early signal — used as a data point to check against, not a shortcut.** "Decide your look, ratings, moves, and gear" (no third named item) matched the zero-bespoke prediction before the rest of the page was even read closely, consistent with Celebrity's/Flake's own versions of this same tell. Restated explicitly, again, that this is corroborating evidence checked against the full read, not a substitute for it.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Mundane` — confirmed-zero entry per the standing convention, Moves checked and logged (0 required, pick 3 of 8, no Move-internal-pick trap); progress tracker to 19 of 28).

### Hunter Playbooks — The Pararomantic: a Stakeholder's Own Restructuring Proposal Partly Overridden, Backed by the Same ExtraTrack Precedent That Would Have Been Reinvented Otherwise (2026-08-28)

- **The most substantive pushback on a Skyler proposal so far, and it held up because the reasoning traced back to an existing precedent rather than a fresh judgment call.** Skyler proposed merging Relationship Status (a tracker) and the Gear-embedded gift picks into one bespoke Section, flagging themselves that the two felt conceptually different but offering the merge as a starting point. Recognized Relationship Status ("Loving `bbbbbbb` Broken") as structurally identical to Luck/Harm/Corruption — a box-track, not a pick-list — meaning it doesn't fit `BespokeSection` *at all*, not just "would be cleaner split out." This wasn't a stylistic preference; forcing a track into a Section/Option shape would have been the exact category error `PlaybookExtraTrack` was invented to prevent in the first place. Worth the general lesson: when a stakeholder's own proposal bundles two things and flags uncertainty about whether they belong together, check whether one of them actually belongs to a *different established shape entirely* before evaluating whether the bundle is well-organized — the answer here wasn't "reorganize," it was "one of these isn't the kind of thing being organized."
- **Caught a naming collision a stakeholder couldn't have seen without reading Moves closely — "Supernatural Guide" was already the Required Move's own name.** Skyler's proposed name for the merged Section was, unbeknownst to them, identical to a completely different, already-out-of-scope concept on the same playbook. Renamed the relocated Gear content "Guide's Gift" (drawn from the source's own phrasing) instead. Worth registering as a concrete argument for reading the *whole* page before reacting to a structural proposal, even when the proposal itself seems to be about a different part of the page — the collision was invisible from the Luck/Gear-focused framing alone.
- **`PlaybookExtraTrack.StartLabel` — a third case of "one prior instance never needed a field because its value was always the same generic default."** Every track before this one used "Okay" as its start label, so no field ever captured it — same shape as `BespokeSection` never needing per-option `Required` until a case demonstrated it, or `HunterBespokeSelection.BespokeOptionId` never needing to be nullable until Gumshoe Code. Added as nullable, defaulting to the implicit "Okay," with zero retroactive change to Corruption's already-final entry — consistent with how every additive field in this schema gets introduced.
- **A new provenance tier for `BespokeOption.Title`, distinguished explicitly from Forged's Benefits rather than lumped in with it.** Guide's Gift's option titles have literally nothing to key off in the source — no font signal, no colon, nothing — a step below Benefits (which at least had a colon). Recorded as externally-supplied labels assigned during authoring, not extracted content, and flagged as needing the same review a fresh authoring pass would get if `Title` is ever load-bearing. Worth the general point: "lower confidence than the last lowest-confidence case" is itself worth a distinct label, not folded into an existing caveat just because both involve an inferred title.
- **Cross-ruleset bolding (not just cross-Move bolding) confirmed as a real, generalizable pattern, not a one-off.** Guide's Gift bolds "bond abuse" (pointing at this same playbook's own Bond Abuse ruleset) and Bond Abuse bolds "fate of your love" (pointing at Fate of Your Love) — the established "bold marks a cross-reference" convention already covered base moves and, since Gumshoe, a playbook's own bespoke moves; this extends it a third way, to a playbook's own *other bespoke rulesets*. Named explicitly rather than treated as two isolated findings.
- Docs: `docs/hunter-playbooks/architecture.md` ("Extra Tracks" section — `PlaybookExtraTrack.StartLabel` added with full reasoning, Section 3 schema listing updated). `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (added and finalized `## The Pararomantic` — Relationship Status as `PlaybookExtraTrack`, Guide's Gift/Bond Abuse/Fate of Your Love as three `BespokeSection`s with real captured markup, the merge-proposal correction and naming-collision catch both documented in-entry; progress tracker to 20 of 28, noting Pararomantic as the fourth playbook needing schema beyond baseline).

### Hunter Playbooks — The Professional: a Third Unenumerated-Answer Gap Fixed, the Move-Internal-Pick Trap Confirmed on an Optional Move, and a Second Unnamed Required Move (2026-08-29)

- **Skyler caught a real structural gap in Agency (a Description asking "who do you work for" with nowhere for the answer to live) and the fix generalized cleanly from an existing pattern rather than needing a new one.** The single-child "Agency name:" mandatory category is structurally identical to Dual Nature's Effect category, just with 1 child instead of 3 — the same `{{blank}}`-in-`DescriptionText` mechanism, no new schema at all. The one real judgment call was field placement (`Title` null vs. populated for the lone child) — resolved by contrast with two existing precedents rather than from scratch: Curses' custom slot needed a blank `Title` (no sibling-distinguishing parent label to lean on, sits flat among named siblings), Dual Nature's Effect children needed real `Title`s (each needs to distinguish itself from 2 siblings sharing the same blank-`DescriptionText` mechanism), and Agency name's lone child needs neither (no siblings at all, parent already supplies the only label) — three points on the same spectrum, not three unrelated calls.
- **Flagged, not fixed, a likely-recurring version of the same gap across already-catalogued playbooks (Sect, Task, Origin) — deliberately not retrofitted without a direct instruction.** Skyler asked for this fix on Agency specifically, having noticed it there; extending it to every catalogued Section with an unanswered "who/what/where" framing question would be a real, if well-intentioned, unrequested scope expansion. Flagged explicitly in the catalogue and left for the coordinator to raise with Skyler as its own decision, same sequencing as `EffectText`'s retroactive review (which only happened after Skyler explicitly asked for one, not proactively).
- **The Move-internal-pick trap's known scope confirmed to include optional (non-Required) moves, not just Required ones** — Mobility (an ordinary pick-3-pool option, not auto-granted) has its own embedded "choose two good things and one bad thing" pick structure. Applied the identical out-of-scope-for-this-catalogue treatment regardless of Required-vs-optional, since the underlying reason (it's Move content, period) never depended on granting mechanism in the first place — worth registering as confirmation the trap's real boundary is "any Move with internal picks," not "any *Required* Move with internal picks," a distinction that had never actually been tested until this round.
- **A second unnamed Required Move** (after The Initiate's, later named "One of Us") — flagged the same way, no name invented to smooth over the gap.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (added and finalized `## The Professional` — Agency's Resources/Red Tape confirmed entirely plain, the new "Agency name:" category added with the field-placement reasoning stated explicitly, both Phase-4 flags (unnamed move, optional-move trap instance) recorded, the broader-gap flag raised for the coordinator rather than acted on; progress tracker to 21 of 28).

### Hunter Playbooks — The Searcher: Fifth Confirmed-Zero Playbook, First to Pair Zero-Bespoke With a Move-Internal-Pick Trap Finding (2026-08-29)

- **Fresh process, full continuity from disk — reconstructed context from `.squad/agents/Yoshi/history.md`, `README.md`, `architecture.md`, `phase5-bespoke-ideation.md`, `bespoke-ruleset-catalogue.md`, and `pdf-extraction-pipeline.md` after a session restart, per the orchestrator's brief. No re-derivation needed; the catalogue and this history file are the durable record and both were intact (no repeat of the earlier git-revert incident this round).**
- **The carried-over finding (First Encounter's name, confirmed 3 ways in an earlier round) held up on a 4th direct check** — page 45's own bold header reads "`B First Encounter:`," matching the Searcher Special trigger text and Getting Started's summary line exactly. Treated as confirmed, not re-litigated, per the brief's instruction — but still spot-checked directly against the page rather than taken purely on the brief's word, consistent with this project's standing "verify everything fresh" discipline even for carried-over facts.
- **First playbook to combine two previously-separate outcome categories: confirmed-zero bespoke content (Celebrity/Flake/Mundane's shape) *and* a Move-internal-pick trap finding (Forged/Gumshoe/Host/Professional's shape).** Every candidate bespoke-looking structure on this playbook — First Encounter's own "pick a category, take the associated move" list (7 categories: Cryptid Sighting, Zone of Strangeness, Psychic Event, Higher power, Strange Dangers, Abduction, Cosmic Insight) and the ordinary pick-2-of-7 Moves pool — lives entirely inside Moves, which stays categorically out of this catalogue's scope regardless of how cleanly it would fit `BespokeSection` (the Forged/Monstrous-established discipline: check category/scope before checking shape). Net result: zero `BespokeSection` rows, same as Celebrity/Flake/Mundane, but worth a distinguishing note in the entry and the tracker rather than silently folded into "another clean zero" — the *reason* it's zero is different (nothing bespoke exists at all, vs. here something bespoke-shaped exists but is Move content), and a future reader scanning for Move-trap instances needs to find this one.
- **Fourth Required-Move instance of the trap (after Forged's Partner, Gumshoe's Naked City, Host's Defensive Adaptation), fifth overall including Professional's optional-move case** — worth the specific count since the trap's boundary (any Move with internal picks, Required or not) was only actually tested by Professional's case, and this confirms the Required-Move version is still by far the more common shape, not a coincidence.
- **No pipeline dispatch made** — since nothing catalogue-scope exists to verify, consistent with how Forged/Gumshoe/Host/Professional's own trap instances were flagged and excluded without a formatting pass. Stated explicitly in the entry (not just implied by absence) so a future reader doesn't mistake "no content-fidelity note" for an oversight.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (added `## The Searcher` — confirmed-zero-outside-Moves entry with the Move-trap finding recorded in full, `pdftotext -raw` column-swap spot-check clean; progress tracker to 22 of 28, updated to note Searcher as a fourth confirmed-zero playbook distinct from the other three, and as the fourth Required-Move trap instance in the Move-internal-pick-structure sentence).

### Hunter Playbooks — The Snoop: First Round Driven by a Stakeholder's Own Pre-Made Structural Call ("Instructions-Only"), Pipeline Run Solo Without a Separate Bowser Dispatch (2026-08-29, same day)

- **Ran `tools/pdf-extract`'s scripts directly via Bash myself rather than waiting on a fresh Bowser dispatch** — no sub-agent-dispatch tool is available to me, and the pipeline is deterministic Node CLI tooling, not an LLM judgment call, so operating it directly (same `dump-page.mjs`/`list-fonts.mjs`/`extract-runs.mjs` sequence Bowser's own README documents) produces an identical, independently-reproducible result. Verified the font-id→style mapping fresh via `list-fonts.mjs` before trusting anything (`g_d0_f1` = `WarnockPro-Bold`, confirmed, not assumed from a prior playbook's mapping — font ids aren't stable page-to-page, per the pipeline's own standing warning) and re-ran both scoped `extract-runs.mjs` commands after drafting the catalogue text to confirm the exact documented output, rather than trusting my own paraphrase of an earlier combined-range run. Worth logging as a reusable fact for next time: this pipeline doesn't require a separate persona to operate correctly, only the same rigor (coordinate scoping, programmatic 0-collision checks, re-verifying documented commands actually produce what's written) Bowser's own worked examples already model.
- **Checked whether "instructions-only" (a structural call Skyler made, not left for me to derive) actually maps onto existing schema before treating it as settled** — per the coordinator's explicit ask. It does, cleanly, onto the zero-option `BespokeSection` shape (Covenant/Secret-Wisdom/Bond-Abuse/Fate-of-Your-Love/Monster-Breed precedent: both `MinSelect`/`MaxSelect` null, zero `BespokeOption` rows) for both Crew and Team Concept — no new schema, no escalation needed. Named explicitly, though, that Skyler's instruction is a deliberate simplification, not the only possible reading: Crew's "entourage vs. hunters" branch and its "up to three, each with a name and job" slots *could* have been decomposed into real nested picks (closer to Fate/Friendship's shape) had Skyler not said to keep it flat. Worth the general point: when a stakeholder's own explicit structural call happens to match an existing pattern, still name what a stricter read would have done differently — otherwise a future reader can't tell whether "instructions-only" was a modeling *finding* (nothing to decompose) or a modeling *choice* (something decomposable, deliberately left flat), which matters if a UI designer later wonders why Crew doesn't have real form fields.
- **Team Concept required one step further than any prior zero-option Section — flavor copy with no player-facing task of any kind, not just an "orphaned prose, no picks" fixed mechanic.** Monster Breed (Monstrous) was the closest precedent (zero-option Section reused for framing/reference text rather than a mechanic), but even Monster Breed's prose still supports the *sibling* Sections' real picks. Team Concept doesn't support anything — it's genuinely standalone example content, present in the catalogue purely because Skyler wants the copy tracked. Flagged this as a real, if minor, Phase 9 open question (does a Hunter-creation UI even need to render a Section with nothing to decide) rather than resolving it myself, since it's a UI-scope call, not a schema one.
- **A verified source anomaly (missing opening quotation mark before "Monster Revelations") checked against the raw PDF item stream and deliberately left unfixed**, distinguished explicitly from Monstrous's duplicated-word typo (an unambiguous, no-guessing-required fix) — correcting a missing punctuation mark would require guessing its exact position, which this project's standing discipline treats as a line past "fix on sight" and into "would be inventing content."
- **A new sub-pattern of the cross-reference-bolding convention**: Crew's `<b>bystanders</b>` is the first instance pointing at a core MOTW rules term (an NPC classification) rather than a Move name or another bespoke ruleset — the convention has now been seen pointing at base moves (many playbooks), a playbook's own bespoke moves (Gumshoe), a playbook's own other bespoke rulesets (Pararomantic), and now a core rules-term cross-reference. Named as a fourth extension rather than folded silently into the existing three.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (added and finalized `## The Snoop` — Crew and Team Concept: Monster Revelations both content-fidelity-verified via directly-run `extract-runs.mjs`, `pdftotext -raw` column-swap spot-check clean, Skyler's exact framing quoted verbatim, the schema-fit check and Phase 9 flag both stated explicitly; progress tracker to 23 of 28, bespoke-content tally to 20 of 23, new sentence added crediting Snoop's reuse of the Covenant zero-option pattern alongside Monstrous's).

### Hunter Playbooks — The Spell-Slinger: First Genuinely New Pick-Shape With No Fitting Precedent, Escalated Rather Than Forced Into Fate's or Natural Attacks' Molds (2026-08-29, same day)

- **No stakeholder framing this time ("Go ahead with The Spell-Slinger," nothing else) — went in cold and found the richest single-playbook round so far**: a genuinely new bespoke-Section pick-shape, two Move-internal-pick-trap instances on one playbook for the first time, and a page with a font (`WarnockPro-BoldIt`) not seen on any prior playbook. Ran the pipeline solo again (`dump-page.mjs`/`list-fonts.mjs`/`extract-runs.mjs`), same as Snoop's round — no Bowser dispatch needed, since Combat Magic's shape (flat title+description, real font-derived bold titles, no nested lists) is one this pipeline already handles reliably; explicitly checked the new bold-italic font wasn't actually used anywhere in the column I cared about rather than assuming a richer font table meant a harder extraction.
- **The real finding of this round: Combat Magic's "pick 3 from 10, with at least 1 of the 3 from a specific 4-item sub-group" is a shared-budget-with-a-floor shape that neither existing nested-category precedent actually covers, and forcing it into either one produces materially different failure modes worth naming explicitly rather than picking silently.** Fate/Friendship's mandatory-multi-category shape assumes each category's count is independent — no mechanism for a budget shared *across* categories exists anywhere in the schema. Natural Attacks' "leave the real either/or rule in prose, accept the schema gap" precedent is the closest analogue, but re-running the same technique here doesn't just port a small accepted gap forward — it more than doubles it (a naive Bases-1-to-3/Effects-0-to-3 reading would allow 6 total picks against a true cap of 3, versus Natural Attacks' worst case of 1-over). Recognizing that "this looks like the same kind of gap I've already gotten sign-off on" and "this is actually a bigger version of that gap" are different findings was the key check here — reused the framing (state the real rule in prose, don't invent a schema mechanism silently) but did not reuse the conclusion (that the resulting gap is small enough to not need asking).
- **Recommended a specific resolution (flat 10-option list, `MinSelect=3`/`MaxSelect=3`, Bases/Effects grouping preserved only via display order) rather than presenting a bare either/or** — the task brief asked to raise unclear cases as questions rather than guess, but a well-reasoned recommendation alongside the question is still more useful to Skyler than an undifferentiated list of options, consistent with how every other escalation in this project has been framed (Covenant's ability, Hex's rejected-then-redesigned proposal). Named the accepted trade-off explicitly (loses the source's visual Bases/Effects grouping for UI purposes) rather than presenting the recommendation as cost-free.
- **Held the whole playbook out of the progress tracker's main count, matching Covenant's precedent exactly** — all standard sections, both Moves, and Combat Magic's actual content are fully verified and captured (nothing here is provisional or guessed), but the one open structural question means the entry isn't "finalized" in the sense every fully-counted playbook has been. Added an explicit note in the tracker line itself (not just in this history) stating what's pending and why, so a future reader scanning the tracker alone — not just this file — can tell the count is deliberately short one, not stale.
- **Second-in-a-row playbook whose Getting Started line independently confirms a bespoke component's peer status** (Combat Magic named alongside look/ratings/moves/gear, same tell as Snoop's Crew one round earlier) — worth noting this specific cross-check is now proving reliable across consecutive rounds, not just a one-off coincidence with Snoop.
- **First playbook with both Move-internal-pick-trap shapes at once**: Tools and Techniques (Required — fifth such instance) and Could've Been Worse (optional, nested inside the pick-3 pool — second such instance after Professional's Mobility). Both correctly excluded from the catalogue, per the standing "Move content is out of scope regardless of how cleanly it fits the shape" discipline, re-applied without hesitation now that it's been tested five-plus times.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (added `## The Spell-Slinger` — standard sections and both Moves fully verified and logged, Combat Magic's real content fully captured with markup, the pick-shape question stated explicitly with a recommendation, both Move-trap instances recorded; progress tracker note added flagging the playbook as drafted-but-not-yet-counted, pending Skyler; Move-internal-pick-structure recurrence sentence updated with the fifth Required-Move instance and second optional-move instance).

### Hunter Playbooks — The Spell-Slinger Finalized: Skyler Chose the Alternative I Didn't Recommend, Recorded Verbatim Not Second-Guessed (2026-08-29, same day)

- **Skyler picked nested Bases/Effects categories over my recommended flat list, with no rationale given beyond the choice itself — implemented exactly as decided, no re-litigating.** My own writeup had explicitly named the trade-off both ways (grouping fidelity vs. total-cap fidelity); Skyler's silence on *why* isn't a gap to fill in with my own inferred justification — recorded the decision as what it is (a direct structural call) rather than manufacturing a rationale to make the entry read more complete. Consistent with the standing project discipline of never presenting an inference as if it were the stakeholder's own stated reasoning.
- **The one real follow-through obligation from Skyler's own instruction: document the now-larger enforcement gap explicitly, not just implement the structure.** Choosing nested categories didn't just pick a modeling shape, it reopened the exact risk the recommendation had been built to avoid (a naive `MinSelect`/`MaxSelect` reader permitting up to 6 picks against a true cap of 3, versus Natural Attacks' 1-over worst case). Named this in three places — the catalogue entry itself, the tracker-line summary, and the decision record — rather than only the entry, since a future implementer could plausibly encounter any one of the three first and needs the same warning wherever they land.
- **Updated the decision record to show both the road not taken and the one actually chosen, not just overwritten to reflect only the final state.** Restructured it into "My recommendation (not chosen)" / "Skyler's decision" / "Why" sections specifically so a future reader can see what trade-off was surfaced and which side Skyler landed on, rather than reading a record that looks like nested-categories was the only option ever considered.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Spell-Slinger` — Combat Magic remodeled as 2 mandatory nested categories per Skyler's decision, enforcement gap documented explicitly in the entry; progress tracker to 24 of 28, bespoke-content tally to 21 of 24, pending-note replaced with the resolved summary), `.squad/decisions/inbox/yoshi-hunter-playbooks-spell-slinger-combat-magic-pick-shape.md` (updated to record Skyler's actual decision and reasoning-so-far, status changed from Open to Resolved).

### Hunter Playbooks — The Spooktacular: First Track Conditional on a Single Bespoke Pick, Not Universal to Its Playbook (2026-08-29, same day)

- **Went in cold again ("Go ahead with The Spooktacular," no framing) and found a genuinely new track shape — checked whether the existing `PlaybookExtraTrack` concept actually covers it before reusing it, rather than assuming a fourth track is just "more of the same."** "An Infernal Power" (1 of 5 Show specialties) grants a real, printed 3-box track functioning exactly like Luck — but only for the subset of Hunters who picked that specific option, unlike Corruption/Relationship Status, both universal to their whole playbook. The check that mattered: does `PlaybookExtraTrack`'s current shape (one row per playbook, implicitly "every Hunter of this playbook has it") actually accommodate "only Hunters who made a specific bespoke pick have it"? It doesn't — there's no conditionality concept at all in the table today. Recognizing that "this resembles a track I've already built" and "this track's conditionality is actually a structural difference the existing table can't represent" are different findings was the key step, not just noticing the resemblance.
- **Bundled two genuinely separate questions rather than presenting only the "how" once "whether" was assumed**: should Infernal Favour be tracked as real state at all (vs. left as prose, the resource exists narratively but the app renders no checkbox control), and if yes, how does option-conditionality fit the schema. Corruption's precedent argues for real tracking on resource-shape grounds, but Corruption's universality was never actually tested as "why it needed a `HunterExtraTrackValue` row" — worth naming explicitly that reusing a precedent's conclusion (track it) without re-checking whether the precedent's own justification still transfers (universal vs. conditional) would have been the same mistake as blindly reusing Natural Attacks' "small accepted gap" conclusion for Combat Magic's much bigger one, one round earlier. Same discipline, second time in two playbooks.
- **Recommended a specific mechanism (nullable `TriggeringBespokeOptionId` FK on `PlaybookExtraTrack`) rather than a bare fork**, on the reasoning that "conditional on this playbook" (already implicit in every track's `PlaybookId`) and "conditional on this playbook *and* this specific pick" are the same *kind* of conditionality at different granularity, not two different kinds of thing needing separate tables — consistent with this project's standing preference for extending an existing mechanism over inventing a parallel one when the underlying concept genuinely generalizes (same reasoning already used for `EffectText`, `FreeTextLabel`, `MinInstances`/`MaxInstances`).
- **A second "genuinely absent, not defaulted" field finding on the same track**: no printed start/end label at all, not even Luck/Harm's own literal "Okay" — checked directly rather than assumed, and distinguished from Pararomantic's `StartLabel=null`-means-"Okay" convention (that convention still applies here for `StartLabel`, but `EndLabel=null` would be the first genuinely label-less end-state case in this catalogue, not just an unlabeled default).
- **Also flagged a smaller, non-blocking wrinkle in passing**: "Supernatural Creatures" (a different Show option) grants a cross-playbook Move pick *at character creation*, not via a later Improvement — every prior cross-playbook Move reference in this catalogue has been a level-up-time grant. Still Move content, still out of scope, but the timing difference is worth Phase 4 knowing about even though the scope call itself doesn't change.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (added `## The Spooktacular` — standard sections and Moves fully verified and logged, The Show's full content captured with markup and finalized [no schema question on the Section itself], the Infernal Favour question stated explicitly with a recommendation, the Move-internal in-play-menu distinction noted for The Game Is Fixed, the creation-time cross-playbook Move-grant wrinkle flagged; progress tracker note added flagging the playbook as drafted-but-not-yet-counted, pending Skyler), `.squad/decisions/inbox/yoshi-hunter-playbooks-spooktacular-infernal-favour-track.md` (new decision record).

### Hunter Playbooks — The Spooktacular Finalized: Skyler's Counter-Proposal Evaluated Honestly and Adopted, Not Just Implemented Because Skyler Said So (2026-08-29, same day)

- **Asked explicitly to evaluate Skyler's proposal "on its merits," not just implement it — actually did that check rather than treating the request as a formality.** Traced the new "numeric-option-with-min/max" idea against the full existing model before agreeing: checked it didn't collide with `BespokeOption.MinSelect`/`MaxSelect` (different concept, no naming risk), checked it fit the "shape emerges from populated fields" philosophy already governing every other leaf kind, and worked out a concrete attachment mechanism (reusing the Agency-name single-child pattern) rather than leaving "how does this actually attach to an option" as a hand-wave. This is the same rigor given to every other schema proposal in this project (Curse-Eater's `PlaybookExtraTrack`/`BespokeJournal`, Hex's rejected-then-redesigned repeatable-Section) — evaluating a stakeholder's own idea doesn't get a lighter pass just because they proposed it themselves.
- **Found and named a real, non-obvious echo worth checking before agreeing: the *word* "min/max" had already appeared once earlier in this exact doc, attached to a *different* concept (pick-count bounds, Section 4, declined as a generic attribute table years — well, rounds — ago).** Went back and confirmed directly that this new proposal isn't quietly reopening that earlier declined idea under a new name — it shares only the word, not the underlying question (value bounds vs. selection-count bounds). Worth the general lesson: when a new proposal's vocabulary echoes something already decided earlier in the same document, check whether it's actually the same question before treating the earlier decision as settled precedent either for or against the new one.
- **Recognized the proposal as genuinely *better* than my own declined recommendation on a real axis, not just "different but acceptable" — and said so plainly rather than underselling it to make my own prior recommendation look more reasonable by comparison.** Keeping the numeric bounds directly on the option that gates them (vs. my own cross-table FK) is structurally tighter and generalizes further (any bounded numeric input, not just tracks). Named this honestly in both the catalogue and the decision record instead of just recording "Skyler chose the other option" the way Combat Magic's resolution (a round earlier, where I genuinely still preferred my own recommendation) was framed — these are two different postures and reading the record should make clear which one applied each time.
- **No questions surfaced back to Skyler** — the one real structural decision (parent/child attachment vs. new fields directly on the parent) was resolvable from an already-proven precedent (Agency's synthesized single-child pattern) with no genuine trade-off requiring a stakeholder call, unlike Combat Magic's pick-shape question or the original conditionality question this proposal itself made moot.
- Docs: `docs/hunter-playbooks/phase5-bespoke-ideation.md` (new `BespokeOption.NumericMin`/`NumericMax` leaf-shape subsection: schema, the Section-4-echo check, alternatives declined including my own original FK proposal, `HunterBespokeSelection.NumericValue` instance-side addition, initialization convention), `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (finalized `## The Spooktacular` — Infernal Favour modeled as a nested numeric child option under "An Infernal Power"; progress tracker to 25 of 28, schema-needing-playbooks list updated), `.squad/decisions/inbox/yoshi-hunter-playbooks-spooktacular-infernal-favour-track.md` (updated to record Skyler's actual proposal, my evaluation, and the adoption reasoning; status changed from Open to Resolved).

### Hunter Playbooks — The Spooky: a Font-ID Mapping Trap Caught in My Own Manual Verification Before It Could Ship, Numeric Leaf Shape Correctly Not Forced (2026-08-29, same day)

- **The coordinator explicitly invited reusing Spooktacular's brand-new `NumericMin`/`NumericMax` leaf shape "if it genuinely fits — don't force it." Checked for it deliberately and confirmed it doesn't apply, stated that check explicitly rather than silently saying nothing about it.** The Dark Side has no box-track or bounded-numeric mechanic anywhere in its text — a clean Tag Pick (16 label-only tags, pick 3) plus an `EffectText`-shaped consequence paragraph, both already-established shapes. Worth the general point: when a coordinator flags a brand-new mechanism as "fair game if it fits," the right response to "it doesn't fit" is a stated negative check, not silence that could later read as "wasn't considered."
- **Caught and corrected a real error in my own manual verification process before it reached the catalogue, not after.** A first-pass manual read (cross-referencing a `list-fonts.mjs` run scoped to *both* pages 53–54 against `dump-page.mjs`'s per-item font ids) concluded the 16 Dark Side tags were italic and the pick-instruction sentence was plain — backwards from the truth. Re-running `list-fonts.mjs` scoped to page 54 alone (matching the actual scope `extract-runs.mjs` uses) reversed the mapping and matched `extract-runs.mjs`'s own programmatic output exactly. This is a fresh, concrete instance of the pipeline's own standing warning ("font ids are NOT stable across pages") — worth naming plainly as a mistake I made and caught, not just a rule I already knew, since the near-miss is the more useful data point than the rule restated in the abstract. **General lesson for next time**: when manually cross-referencing `dump-page.mjs` output against a `list-fonts.mjs` font table, always scope `list-fonts.mjs` to the exact same single page, and treat any manual font-based conclusion as provisional until confirmed against `extract-runs.mjs`'s own output — which is exactly what caught this before it shipped.
- **A third instance of the "in-play menu, not a one-time pick" Move-content pattern, and the first to recur three times on one playbook** (Hex, Tune In, Jinx) — same shape as Action Scientist's Physics and Cosmology and Spooktacular's The Game Is Fixed, still entirely out of catalogue scope regardless of how many times it recurs on a single page. Checked each explicitly against the permanent-pick trap (Forged/Gumshoe/Host/Professional/Searcher/Spell-Slinger's shape) and confirmed none of the three are that — worth the explicit distinction since both shapes involve a Move with an embedded bulleted list and could otherwise blur together.
- **A second minor duplicate-Luck-trigger-wording finding**, same category as Chosen's Fate/Luck-aside case from early in this project: Dark Side's own `EffectText` restates "As you mark off Luck boxes, these requests will get bigger and nastier" — nearly identical to the standard `Playbook.LuckSpecialText` ("your dark side's needs will get nastier") captured separately. Noted in passing, not treated as a modeling problem — same non-issue as the earlier instance.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (added and finalized `## The Spooky` — Moves and Dark Side fully verified and logged, the numeric-leaf non-applicability explicitly stated, the font-ID trap documented for future reference; progress tracker to 26 of 28, bespoke-content tally to 23 of 26).

### Hunter Playbooks — The Visitor: Skyler's Own Structural Request Verified Before Modeling, a Genuinely Different Shape From Combat Magic Distinguished Rather Than Assumed Similar, First Real 3-Level Nesting (2026-08-29, same day)

- **Verified Skyler's own summary against the literal source before building anything on top of it, per the coordinator's explicit instruction — and it held up exactly, not just approximately.** "Pick one or more from at least two lines" is the source's own exact printed sentence, not a paraphrase. Worth restating the general discipline even when a stakeholder's relay turns out accurate: the check has to happen either way, since "this time it was exactly right" is only knowable after checking, not a reason to have skipped it.
- **The core analytical task — does this hit Combat Magic's gap — required distinguishing two constraint shapes that both involve "multiple categories" on the surface but are structurally unrelated, and getting this right was the whole ballgame.** Combat Magic: a *shared numeric budget* (3 total items) split across categories, which independent per-category bounds genuinely cannot express — a real, accepted gap. Expatriation: *no* shared budget at all (each line is independently uncapped, "one or more"), only a *categorical participation minimum* (at least 2 of 3 lines touched) — which turns out to be exactly what the already-proven "pick M of N categories" mechanism (Fate/Friendship/Combat Magic's own top-level shape) already expresses, just with a range (`MinSelect=2, MaxSelect=3`) instead of every prior case's fixed `MinSelect == MaxSelect == count`. Getting from "these both involve multiple categories" to "these are actually different kinds of constraint" was the real work this round — surface resemblance to a recently-solved problem is exactly the trap flagged repeatedly throughout this project (most recently for Infernal Favour's track-conditionality question), and it would have been easy to either wrongly claim a gap here or wrongly assume no gap without actually separating the two constraint types.
- **A design principle stated back in Section 3 of the ideation doc ("this generalizes past 2 levels for free... untested, but the shape doesn't care") got its first real test, and held.** Modeled Expatriation as Section → category divider ("Home Culture") → line → tag item, a genuine 3-level structure, with zero new schema needed — closed the loop on that 2026-08-26 prediction explicitly in the ideation doc rather than letting a confirmed prediction go unrecorded.
- **Found and resolved, without escalating, a genuinely untested instance-side question the coordinator's framing didn't explicitly ask about but which the same structural change exposed**: whether "is this category engaged" should be a stored fact (its own `HunterBespokeSelection` row) or a derived one. Every prior nested-category case had 100% mandatory category engagement, so this was never actually load-bearing before. Resolved as derived, consistent with the schema's own stated "don't store what's derivable" philosophy (already applied to declining `IsRepeatable` and preemptive `Required`-on-`BespokeOption` flags) — stated the reasoning explicitly and flagged it for visibility rather than treating a confident, precedent-backed resolution as something that had to be blocked on Skyler's sign-off, while still keeping the coordinator's stronger, explicitly-asked question (the Combat-Magic-gap check) squarely as the thing that got the full "checked and here's why it's different" treatment.
- **A new synthesized-title sub-case**: the three unnamed lines got deliberately positional labels ("Line 1"/"Line 2"/"Line 3") rather than thematic ones, distinguished explicitly from "Origin"/"Agency name:"'s thematic synthesis — Line 3 alone mixes governance and tech-level traits, so no single clean theme actually fits, and inventing one would misrepresent the source rather than clarify it. Worth the general point: the "flag self-assigned content" discipline doesn't stop at *whether* to synthesize a label, it extends to *how* — a thematic label is sometimes simply the wrong kind of synthesis for genuinely heterogeneous content.
- **Cleanest content-fidelity result in the catalogue so far**: zero bold, zero italic anywhere across the entire Expatriation block, verified with page-56-scoped font resolution (applying The Spooky's lesson from one round earlier without needing to be reminded).
- Docs: `docs/hunter-playbooks/phase5-bespoke-ideation.md` (Underworld nesting section: both 2026-08-26 predictions — deep nesting, and the recursive `MinSelect`/`MaxSelect` mechanism's generality — confirmed exercised for the first time, with the Combat-Magic-gap distinction and the derived-vs-stored engagement question both recorded), `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (added and finalized `## The Visitor` — Moves fully verified and logged [including both flagged Move-internal items], Expatriation modeled as a 3-level nested structure with full option content and markup verification; progress tracker to 27 of 28, bespoke-content tally to 24 of 27).

### Hunter Playbooks — The Wronged: the 28th and Final Playbook, Walkthrough Complete (2026-08-29, same day)

- **Went in cold, and this time the outcome itself was the finding: three already-established shapes, reused cleanly, zero new questions.** "Who You Lost" is an umbrella Section with 3 mandatory sub-blocks (Fate/Origin/Expatriation's "pick all N of N" shape), one of which ("What did it?") is a single-mandatory-child free-text leaf (Agency's "Agency name:" pattern, reused a second time), and two of which are uncapped-minimum Tag Picks (Heat's/Expatriation's "one or more, no stated max" shape). After 4 consecutive rounds each surfacing at least one genuinely new schema question or judgment call (Combat Magic, Infernal Favour, the font-ID trap, Expatriation's category-engagement nuance), a completely uneventful final round is itself worth registering rather than passing over — it's a second, stronger confirmation (after The Interface's own "clean pass" round mid-project) that the accumulated shape vocabulary now covers real variety across the full 28-playbook set, not just the easy majority.
- **Positively distinguished, for the first time explicitly, a third category of Move-embedded bulleted list from the two already-named traps.** DIY Surgery's "On a 7-9... plus one of these, your choice" is ordinary PbtA partial-success roll-outcome branching — a shape used throughout the base game and dozens of already-catalogued playbooks' own Moves without ever needing its own name, because it was never ambiguous enough to be confused with the two real traps (the permanent character-build pick, and the in-play repeatable menu). Naming it explicitly now, at the very end of the walkthrough, closes out the trap taxonomy cleanly: two real traps, correctly excluded every time they appeared, and one look-alike that was never actually one of them.
- **Confirmed the Advanced Improvement "Change the target of your vengeful rage... I know my prey now applies to them instead" as positive evidence the chosen model was right, not just noted it in passing.** This Improvement *replaces* the "My prey" value rather than growing a collection — independent confirmation that modeling "What did it?" as a single mutable leaf (not a repeatable or growing structure) was the correct read, the same kind of "the Improvements list itself validates the model" check already used for Spell-Slinger's Combat Magic and Spooky's Dark Side.
- **Closed out the walkthrough's own bookkeeping properly, not just the last playbook's entry**: updated the catalogue's own top-of-file status line from "growing" to "COMPLETE," the progress tracker to 28 of 28 with an explicit milestone marker, and both `README.md` and `phases.md`'s Phase 5 sections to reflect that the model is now validated against all 28 real playbooks — closing the loop on `phases.md`'s own standing flag from 2026-08-25 ("whatever this converges to will need re-validation against all 28 playbooks before it's trustworthy, not just the three read so far"). Explicitly did *not* overclaim Phase 5 as "landed" in the sense Phases 4/6/7 use that word — the catalogue is a validated design-and-data artifact, not yet running EF Core code, and said so plainly rather than letting "the walkthrough is done" blur into "the feature is done."
- **Two items remain open across the whole project, both already filed, neither invented new by this closing round**: Expatriation's category-engagement (stored vs. derived) nuance, resolved with reasoning but not yet explicitly confirmed by Skyler; and the standing, deliberately-out-of-scope Move-internal-pick-trap list for Phase 4. Flagged both explicitly in the closing summary rather than letting "walkthrough complete" imply "nothing left to check."
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (added and finalized `## The Wronged`; top-of-file status changed to COMPLETE; progress tracker to 28 of 28 with an explicit milestone marker; bespoke-content tally to 25 of 28), `docs/hunter-playbooks/README.md` (new dated completion entry, Phase 5 bullet and catalogue-file bullet updated), `docs/hunter-playbooks/phases.md` (Phase 5 heading and body rewritten to reflect the closed re-validation flag and the still-open EF Core implementation gap; Phase 7's reading-prerequisite note updated).

### Hunter Playbooks — Closing Follow-Ups on Skyler's Five Responses (2026-08-29, same day)

- **Skyler named The Professional's previously-unnamed Required Move "Agency politics."** Updated `## The Professional` and the progress-tracker line with the same "Skyler-assigned, not source-printed" flag already used for The Initiate's "One of Us" — now both unnamed-Required-Move cases in this catalogue are closed out identically.
- **Skyler didn't understand my derived-vs-stored explanation for Expatriation's category-engagement question and asked for a concrete, non-jargon re-explanation** — a genuinely useful signal: the original write-up (in `## The Visitor` and the tracker line) leaned on schema vocabulary — "HunterBespokeSelection row," "derived fact" — without ever grounding it in what a person actually sees or does. Worth the general lesson for any future stakeholder-facing explanation of an internal-only implementation choice: lead with the concrete before-and-after (what does the database/UI actually show under each option), not the mechanism name, especially when the choice has no visible behavioral difference to the end product either way — that "no visible difference" fact is itself the thing most likely to be missing from a first-pass technical summary, and is probably why "why does this matter" didn't land.
- **Skyler confirmed no further action needed on 3 of the 5 items**: a future Moves-focused walkthrough is coming (Skyler's own initiative, likely its own phase) — noted, no action from me; the accumulated Section 3 schema shape reviewed and confirmed "sits in a good place" — no action; Forged's Bonds/Burdens history and Combat Magic's enforcement gap — "not concerned about these," no appendix needed.
- Docs: `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (`## The Professional` and progress-tracker line updated with "Agency politics"). Re-explanation of the Expatriation derived-vs-stored question relayed to the coordinator directly (not written into any doc — a plain-language answer to a clarifying question, not new project content).

### Hunter Playbooks — Phase Renumber + New Phase 6 (Custom Move Modeling) Inserted, Skyler Confirmed the Full-Renumber Recommendation (2026-08-29, same day)

- **Planning-only round preceded this one** — Skyler asked for a recommendation (not an implementation) on how to slot a new phase between Phase 5 and the old Phase 6, with a rough scope sketch. Recommended full renumbering over a decimal/lettered scheme, sized the actual cost first (grepped every "Phase N" reference across `docs/hunter-playbooks/` rather than guessing at scale — 32 hits across 5 files, ~15–20 distinct edits once multi-hit paragraphs are accounted for) so the trade-off being presented was concrete, not abstract. Skyler confirmed the full renumber directly and asked for exactly that recommendation implemented.
- **Rebuilt `phases.md` via a full-file rewrite rather than ~20 sequential `Edit` calls** — given the number of interdependent phase-number references within single paragraphs (e.g. "Phase 5/6's model," "Phases 2–6 having proven out"), a full reconstruction from the already-read file content was safer than chained edits risking a stale intermediate state mid-sequence. Read every one of the other 4 files' phase references in context before changing them (not blind find-replace) — several needed real re-derivation, not just incrementing a digit (e.g. old "Phase 5/6" compound references needed to become "Phase 5/7" *and*, where the sentence was actually about bespoke+Move-internal data jointly, "Phase 5/6/7" to include the new phase rather than just shifting the old one).
- **New Phase 6 write-up folded in everything from the planning round's sketch, not just a stub heading**: the explicit scope boundary (internal pick-structure only, not the Moves container model or formatting fidelity), both named patterns (permanent creation-time picks vs. in-play repeatable menus) with their full confirmed-instance lists, the existing trap list stated explicitly as a seed-not-census with the concrete gap named (Chosen/Crooked/Divine's own Moves were never checked against this pattern at all), and the Curse-Eater/Forged dual-column layout quirk as a related-but-distinct loose end. Also sketched the likely deliverable shape (ideation doc, catalogue doc, architecture.md subsection) explicitly marked as not-yet-confirmed, since Skyler said the real design work starts later.
- **Did not create `custom-moves-ideation.md` or a catalogue doc** — per Skyler's own explicit instruction ("I will do this work soon... that detailed work starts when Skyler is ready to begin it"), the same sequencing Phase 5 itself followed (no `phase5-bespoke-ideation.md` until Skyler opened that phase for real). Only the phase's existence, position, and known scope are recorded now.
- **Added a same-day-related fix while in the file anyway, not a scope-creep addition**: `bespoke-ruleset-catalogue.md`'s own closing tracker line still read "not yet explicitly confirmed by Skyler" for the Expatriation category-engagement question, even though Skyler confirmed it in the immediately preceding round — fixed alongside pointing the Move-trap-list note at the new Phase 6, since both edits touched the same sentence and leaving one stale while fixing the other in the same paragraph would have been a strange half-measure.
- **Deliberately did not retrofit every "flagged for Phase 4" Move-trap note throughout the catalogue to say "Phase 6" instead** — considered it, since Phase 6 is now the more semantically precise pointer for "who designs the modeling," but it wasn't asked for, the catalogue is marked COMPLETE, and this project's own standing discipline is not to retrofit already-finalized content without a direct instruction (the same restraint already applied to Agency's broader "unanswered worldbuilding question" gap and `EffectText`'s retroactive review, both left alone until Skyler asked). Flagged the general pointer clearly at the one place that needed it (the tracker line) instead.
- **New phase numbering, for the record**: old Phase 6 (Import bespoke data for the three playbooks) → **Phase 7**; old Phase 7 (Import remaining 25 + seed conversion) → **Phase 8**; old Phase 8 (Hunter instance list UI) → **Phase 9**; old Phase 9 (Hunter create/edit UI) → **Phase 10**. New **Phase 6** = Custom Move modeling, inserted between Phase 5 and (new) Phase 7.
- Docs: `docs/hunter-playbooks/phases.md` (full rewrite — new Phase 6 inserted, old 6–9 renumbered to 7–10, every cross-reference within the file corrected), `docs/hunter-playbooks/architecture.md` (all Phase 6/7/8/9 references renumbered, new Section 6.7 added pointing at Phase 6 as the explicit out-of-scope boundary for Move-internal content, Section 6.8 renumbered from the old 6.7), `docs/hunter-playbooks/open-questions.md` (all phase-number references and section headings renumbered, new top-of-file status note), `docs/hunter-playbooks/README.md` (Phase-summary numbered list renumbered with new item 6 inserted, early-history entries corrected), `docs/hunter-playbooks/bespoke-ruleset-catalogue.md` (3 stale "Phase 9" UI-concern references renumbered to Phase 10, closing tracker paragraph updated to reflect both the Expatriation confirmation and the new Phase 6 as the Move-trap list's home).

### Hunter Playbooks — Phase 6 Design Pass: Full Moves Census Across All 28 Playbooks, and My Own Pre-Census Fork Framing Turned Out Wrong (2026-08-30)

- **The single most valuable thing this pass produced was proving the pre-existing flag list was ~half complete — and the miss landed on a Phase 4 pilot playbook.** `phases.md` Phase 6 already warned (my own words, one round earlier) that the trap list was a seed, not a census, and named Chosen/Crooked/Divine as never-checked. Actually checking: **The Crooked has two creation-time in-move picks (Artifact, Deal with the Devil), never flagged anywhere** — and Crooked is one of the three playbooks Phase 4 authors first. 7 of the 14 total creation-time instances were new to this census. Worth the general lesson: a warning that a list is incomplete is cheap to write and easy to under-weight later; actually running the exhaustive pass is what converts it from a caveat into a finding. The concrete sequencing consequence (Phase 4 will hit Crooked before Phase 6 resolves) was only visible *because* the census ran.
- **My own architecture fork, stated confidently in the planning round, was wrong once real content was read — flagged that plainly in the doc rather than quietly picking the better of two bad options.** I'd framed it as "nullable `PlaybookMoveId` on `BespokeOption`" vs. "parallel `PlaybookMoveOption` table." Reading Forged's Partner and Professional's Mobility (each: two named categories under one Move, each with its own pick count) showed both are the wrong attachment point — the natural one is a nullable **`BespokeSection.PlaybookMoveId`**, because "how many of my direct categories are mandatory" is a Section-level field, and the Option-level fork would have needed a new field invented immediately to express something the schema already handles one level up. Worth the general lesson: naming a fork before doing the census is useful for scoping the work, but the fork's own framing is itself a hypothesis that the census can falsify — say so when it does, rather than defending the original framing's terms.
- **Recommending option C reuses all of Section 6 rather than half of it — one nullable FK, zero `BespokeOption` changes, zero new tables, zero instance-side changes.** This is the literal reading of Skyler's "same kind of modeling we're doing for the bespoke rulesets," and it's a strictly better fit than either originally-named option. Named the accepted trade-off explicitly (`BespokeSection` becomes polymorphic in its owner; every read site must filter `PlaybookMoveId IS NULL` for top-level rulesets) rather than presenting it as cost-free.
- **The scope question is the deliverable's real fork, and it's genuinely Skyler's, not mine.** In-play menus (~35 moves — roll-result-gated pick counts, hold-spend menus, additive question lists) are 2–3× the creation-time population and would roughly triple the phase. Recommended leaving them as prose, on the concrete grounds that nothing else in this design models live-play state at all (no holds, no roll outcomes, no forward/ongoing) and modeling them would be the first such machinery — but explicitly framed it as a **product-intent** call, not an architecture one, since the answer flips entirely if the eventual vision includes a play-session view. Worth the discipline: when a recommendation's whole justification is "the product doesn't do X today," that's exactly the kind of premise a stakeholder can overturn with information I don't have.
- **A genuinely new structural shape with no schema equivalent at all: roll-result-gated pick counts** ("On a 10+ pick three, on a 7-9 pick one"). `MinSelect`/`MaxSelect` are static — there's no concept of a count that varies by outcome. Worth registering as the one shape in this census that would need real new mechanism rather than a reuse, and it lives entirely inside the in-play population, which is part of why Q1's answer changes the phase's size so much.
- **Two systematic extraction findings, both verified via the pipeline rather than asserted**: (a) **in-move option lists never carry a font-derived title signal** — checked an inline case (Crooked p11) and a bulleted case (Host p31), and in both the Move's own name is bold while *every* option name inside is regular weight, because the typography spends its bold on the Move name. So the lower-confidence colon-derived `Title` split already flagged as an exception for Forged's Benefits is, for this whole content class, the rule. (b) **Presentation doesn't track structure** — the same pick appears bulleted, comma-separated, and semicolon-separated, meaning `extract-moves.mjs` (which keys on bullet glyphs) will silently miss every inline case. That's a concrete tooling prerequisite for Phase 6's authoring, not just an authoring note.
- **Also confirmed a third category that is neither named trap**: ordinary PbtA roll-outcome branching (10+/7-9/miss consequence text with no choice) is the *majority* of bulleted lists inside Moves and needs no modeling at all. The Wronged's DIY Surgery forced this distinction at the end of Phase 5; the census confirms it generalizes, which matters because it's the most likely source of future over-modeling.
- Docs: `docs/hunter-playbooks/custom-moves-ideation.md` (new — method/coverage claim, 6-part census, modeling proposal with the re-derived fork, 4 questions for Skyler each with a recommendation, suggested next steps incl. the Crooked/Phase-4 sequencing flag), `docs/hunter-playbooks/architecture.md` (Section 6.7 updated to point at the new doc and state the proposed resolution as pending), `docs/hunter-playbooks/phases.md` (Phase 6 heading + new lead paragraph with headline results; original scope note retained below it), `docs/hunter-playbooks/README.md` (new file-list entry, Phase-summary item 6 updated).
