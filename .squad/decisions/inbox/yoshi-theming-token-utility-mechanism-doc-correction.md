### 2026-08-02: Theming Plan — Formalized Luigi's Token→Utility Mechanism Correction, Resolved Four Open Items
**By:** Yoshi (Architect)
**What:** Folded Luigi's `luigi-theming-token-utility-mechanism.md` investigation into `docs/theming/theming-plan.md` as a real edit pass, not a status-note append:
- Decision B now has three addenda: (1) the Category A (role-named, multi-family, auto-generated) vs. Category B (role *is* a family, hand-written `@utility`) split, framed as Tailwind's own documented `@utility` mechanism (decoupling class name from custom property name), not a workaround — no token renamed, no target class name changed; (2) `@theme static` documented as load-bearing (a bare `@theme` tree-shakes unreferenced tokens out of `:root` while `.dark` always emits in full — 5 tokens were silently undefined in light mode before this fix) and flagged as a standing hygiene rule for every future token; (3) the `@reference "tailwindcss"` → relative-path-to-`styles.css` fix, with exact per-file relative paths added to Phases 2, 3, 4, 5, 6 (verified via grep against the actual 10 `.scss` files using `@apply` — **Phase 4's `monsters-list.scss` is also affected**, not just 2/3/5/6 as the task briefing assumed; confirmed by checking Phase 4's own Inspection Points, which already plan to repoint that file's `@apply` rule).
- Token Catalogue's naming subsection gained a "companion fix" paragraph tying `on-*` (fixes multi-family collisions by naming) and `@utility` (fixes single-family collisions by mechanism) together, plus a new "Conventions for Future Tokens" subsection as the practical checklist for whoever adds the next token.
- `header-search.html`/`.scss` added to Phase 1's Files-changed list and given a full re-pointing summary, with a one-line note that it predates this plan and was missed by the original repo scan.
- Resolved four open items from Luigi's decision file: (a) **kept `--color-sidebar-text` un-renamed** (Luigi's `text-sidebar` `@utility` stays) — my call, because in practice this token is only ever consumed as a text color (including via `currentColor` for sidebar icons), so it belongs with `text-primary`/`text-secondary`/`text-muted` (single-family, region-scoped text roles, none `on-*`), not with the fill/on-fill pairs `on-*` exists for; (b) flagged `text-sidebar-muted`'s inline `color-mix` (no dedicated token) for Rosalina, not resolved myself — it's a values/precedent call; (c) **resolved the stale API-modal `bg-surface` vs. `surface-raised` inconsistency directly** rather than deferring a third time — corrected the plan's instruction to `bg-surface-raised` (matching the palette doc, the Catalogue's own role description, and the file's two sibling overlay panels), and flagged the actual one-word template fix in `page-layout.html` as a pending zero-risk follow-up commit, since I did not touch code in this documentation-only pass; (d) added the "a stuttering name silently emits nothing, not an error" line to the new Conventions subsection.
- Overview gained a dated correction paragraph pointing to all of the above; did a coherence pass confirming no other Phase 2-8 text still implies auto-generation would produce a clean name for a Category B token.

**Why:** This was a load-bearing mechanism fix (silently missing CSS in three distinct ways), not a cosmetic naming tweak — it needed to live in the plan itself, at the sections future phase implementers will actually read, not just in Luigi's investigation note. Item (c) specifically had already been deferred twice; a third deferral serves nobody once every design signal (palette doc, Catalogue role description, sibling panels) already points the same direction.

**Docs:** `docs/theming/theming-plan.md` (Overview, Decision B, Token Catalogue naming subsection + new Conventions subsection, Phases 1-7, Risk Register). `docs/theming/dark-theme-palette.md` unaffected, per Luigi's investigation.
