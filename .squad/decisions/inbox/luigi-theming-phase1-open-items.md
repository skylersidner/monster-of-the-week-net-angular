### 2026-08-02: Theming Phase 1 — Three Open Items Surfaced During Implementation
**By:** Luigi (Frontend Developer)
**What:**

1. **API-unavailable modal panel: `bg-surface` (as instructed) vs. `--color-surface-raised` (as designed).** *(Still open. Now additionally inconsistent with the header-search dropdown panel, which took `bg-surface-raised` when it was re-pointed — so this file currently has two overlay panels on `surface-raised` and one on `surface`.)* `theming-plan.md`'s Phase 1 says "Panel converts to `bg-surface`"; `dark-theme-palette.md`'s Modals & Overlays says "Modal panel and dropdown/user-menu panels use `--color-surface-raised`" — and the Token Catalogue's own description of `--color-surface-raised` literally names "modal" as its role. I implemented `bg-surface` because that's the plan's explicit Phase 1 instruction and the task restated it. **This is invisible today** (both tokens are `white` in light mode and Phase 0's `.dark` block is still a light-value duplicate) and only becomes a real difference when Phase 7 swaps in the real dark fills (`slate-900` vs `slate-800`), where the modal would sit flush against a `slate-900` page with no elevation separation. One-word fix in `page-layout.html` whenever it's decided. The user-menu panel in the same file *did* get `bg-surface-raised`, per the plan's own re-pointing map — so the two overlay panels are currently inconsistent with each other.

2. ~~**`shared/header-search/` is in no phase's Files-changed list.**~~ **RESOLVED 2026-08-02 — done, folded into Phase 1.** Skyler corrected the premise: it is not a late addition, it shipped in the search feature's "Phase 2 (UI first pass)" commit (confirmed via `git log`) and was simply missed by the theming plan's original repo scan. It renders inside the shell header, so Phase 1 owns it. Re-pointed onto the token/`@utility` layer in the same pass as the mechanism fix — see `luigi-theming-token-utility-mechanism.md` §10 for the full class-by-class map and the three sanctioned light-mode shade shifts. My "the search input stays a white box in an otherwise dark header" observation was also wrong on its own terms: Tailwind preflight makes the input `background-color: transparent`, so it was only white because the header was. Yoshi still needs to add `header-search.html`/`.scss` to Phase 1's Files-changed list in the plan.

3. **The "Soon" nav badge is dead markup at runtime.** Every entry in `PageLayoutComponent.navItems` now has a `route`, so the `@else` branch that renders the badge never executes. Its inline-style→`bg-white/20` conversion was still made per Phase 1, and I verified class-vs-inline-style parity by injecting both variants into the live sidebar and diffing computed styles (identical background/radius/size/weight/tracking/margin/padding) — but it cannot be verified by looking at the running app, because it never renders. Worth knowing before anyone hunts for it during a Phase 1 visual review.

**Why:** (1) is a direct conflict between two governing docs that I resolved by following the explicit instruction rather than silently preferring the design doc; (2) is a scope gap no phase owns; (3) is a verification caveat that would otherwise read as "Luigi didn't check it."
**Docs:** `docs/theming/theming-plan.md`, `docs/theming/dark-theme-palette.md`.
