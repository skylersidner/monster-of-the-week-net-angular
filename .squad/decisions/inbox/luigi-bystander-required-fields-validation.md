### 2026-08-06: Bystander Required-Field Validation — Frontend (Phase 5, Final Domain)
**By:** Luigi (Frontend Developer)

**What:** Implemented the unconditional frontend scope from `docs/updates/bystander-required-fields-validation.md`'s "Revision — Skyler's 2 Answers Resolved (2026-08-06)" section (paired with Bowser's `ApiContracts.cs`/`BystanderServiceTests.cs` DataAnnotations pass, no other backend files touched). Smallest of the five phases, same shape as Location's — no validator bug to fix (`bystander-form.ts`'s `name`/`bystanderTypeId` already both carry `Validators.required`, confirmed by the doc and re-confirmed by reading `bystander-form.ts` myself before touching anything), no sub-resource forms, no conditional-required shape, no numeric fields. Patterns copied verbatim from my own already-shipped Location phase (`luigi-location-required-fields-validation.md`) — the doc explicitly called this out as "same smallest-possible-scope shape."

1. **`maxlength="255"`** added to the single Name `<input>` in `bystander-form.html` — the only Name input in this feature (Bystander has no sub-resource authoring UI beyond a read-only `CustomMoves` list).
2. **Asterisk convention.** Checked `bystander-form.html`'s label markup first, per the task's explicit instruction not to assume: confirmed it uses the same `grid font-medium gap-1` class Monster's/Minion's/Location's do. Used the wrapper-span trick (`<span>Name <span class="text-danger">*</span></span>`) from the start — same discipline as Location's phase, no shipping-then-fixing-a-layout-bug detour. Applied to `Name` and `Bystander Type` (the only two required fields in this form); `Description` left untouched.
3. **No validator changes.** Confirmed `bystander-form.ts`'s `bystanderTypeId` already has `Validators.required` — did not touch `bystander-form.ts` at all, per the doc's explicit instruction not to "fix" anything there.

**Why / judgment calls:**

1. **Verified the label class before writing markup** rather than assuming resemblance to Location — the task explicitly asked for this and it held true, but it was checked, not inherited.
2. **Spec assertions added**, mirroring Location's own `label.querySelector('span.text-danger')`/`input.maxLength` idiom in `bystander-form.spec.ts`: one asterisk-presence spec (Name + Bystander Type get `span.text-danger`, Description doesn't) and one `maxlength` spec. No validator-touched-state assertion needed or added, since no validator changed (the doc explicitly noted this, and the spec file already had a pre-existing "treats a blank bystanderTypeId as invalid" test from the standalone-creation phase that this work didn't need to touch).
3. **No `.scss` changes** — pure template/spec edits, matching every prior phase's finding that this convention needs no stylesheet work.

**Files:**
- `features/bystanders/shared/bystander-form/bystander-form.html` — asterisks (wrapper-span form) on Name/Bystander Type labels, `maxlength="255"` on Name
- `features/bystanders/shared/bystander-form/bystander-form.spec.ts` — 2 new specs (asterisk presence on Name/Bystander Type + absence on Description, maxlength)

**Not touched:** `bystander-form.ts` (no validator gap exists here — confirmed, not assumed), `bystander-create.ts`/`.html`, `bystander-detail.ts`/`.html` (both consume the shared `BystanderFormComponent`, so no page-level template changes were needed — same reasoning as the doc's own framing that this is a one-file change covering both pages), `Contracts/ApiContracts.cs`, `BystanderServiceTests.cs`, or any other `.cs` file — Bowser's parallel backend pass, confirmed via `git status` to be pre-existing changes I didn't make.

**Verification:** `npm run build` clean (same 2 pre-existing component-style budget warnings: `custom-select.component.scss`, `mystery-create.scss`, unrelated). `npm run test -- --watch=false`: 38 files / 278 tests passed (276 baseline + 2 new specs in `bystander-form.spec.ts`). Visually verified via a throwaway Playwright script against the already-running dev `dotnet run` (port 5225) + `ng serve --port 4200` (both confirmed live via `curl` before testing, neither started nor killed by me — pre-existing from the session) on both `/bystanders/new` (create) and `/bystanders/:id` (edit, an existing seeded bystander "Deputy Marsh"): on both pages, the Name and Bystander Type asterisks rendered on the same line as their label text (bounding-box `y` matched within 2px), and the Name input reported `maxlength="255"`. Screenshot-confirmed on both pages, then deleted. Cleaned up: deleted the throwaway `.mjs` script and screenshots; did not touch the running `dotnet run`/`ng serve` processes.

**This closes the frontend side of the five-phase required-field-validation initiative** (Mystery/Monster/Minion/Location/Bystander). All five domains' shared form components now have consistent server-matching `maxlength` guards and the asterisk required-field convention on every field that's actually required at every layer. Combined with Bowser's parallel `[Required, MaxLength(255)]` DataAnnotations pass on `UpsertBystanderRequest.Name`, this is the last implementation step Yoshi's closing note in `docs/updates/bystander-required-fields-validation.md` was waiting on.
