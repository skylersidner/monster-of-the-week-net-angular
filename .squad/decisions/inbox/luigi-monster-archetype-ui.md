### 2026-08-01: Monster Archetype — Angular UI Wiring
**By:** Luigi (Frontend Developer)
**What:** Wired the new `monster_archetypes` backend table into all Angular monster views. Added `MonsterArchetypeResponse` interface to `core/models.ts`. Updated `MonsterListItemResponse`, `MonsterDetailResponse`, and `UpsertMonsterRequest` with the new archetype fields. Added `getMonsterArchetypes()` cached observable to `ReferenceDataService`. Updated `monster-detail` page (TS + HTML) and `monsters-list` HTML to display purple archetype badges. Updated `mystery-create.store.ts` and `mystery-create-monster-phase.html` to load archetypes, expose them as a signal, include the `monsterArchetypeId` form control (required), patch it in edit mode, and include it in Phase 1 submission. Phase 1 step-complete gate now requires both monster name AND archetype selection.
**Why:** Backend API now requires `monsterArchetypeId` on all monster create/update requests. All views that create or edit monsters must include the archetype dropdown to remain functional.
