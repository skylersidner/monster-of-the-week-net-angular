# Hunter Playbooks — Bespoke Ruleset Catalogue

**Status: COMPLETE — reopened once and corrected, 2026-08-30.** All 28 playbooks were processed via Skyler's one-playbook-at-a-time walkthrough (2026-08-26 to 2026-08-29). Every playbook in the source PDF has been read directly, verified against the literal source, and catalogued (or confirmed to have zero bespoke content, per the standing convention below). Future edits should be corrections/refinements to existing entries, not new playbook ingestion, unless Skyler introduces a 29th playbook or revises the source material.

**Reopened 2026-08-30 on a known-real gap, with Skyler's explicit sanction — one content correction applied.** Phase 6's tooling work gave the extraction pipeline the ability to detect **inline** comma/semicolon option runs, which it previously could not see at all (it only recognized bulleted lists). Since every entry in this catalogue was authored with the older tooling, Skyler directed a full re-sweep of all 28 playbooks' bespoke content to find picks that had been transcribed as prose purely because the tooling couldn't see them (Bowser's sweep: `tools/pdf-extract/phase5-inline-pick-sweep.json`/`.html`, `.squad/decisions/inbox/bowser-hunter-playbooks-phase5-inline-pick-sweep.md`).

**Sweep result: 123 raw hits across all 58 pages → 14 in this catalogue's scope → exactly 1 genuine gap.** The single correction is **The Monstrous / Curses / Pure Drive**, whose nested pick-1-of-11 was recorded as prose and is now modeled as nested options (see `## The Monstrous`). Everything else was adjudicated and rejected, for four distinct reasons — recorded here so the sweep's own findings can be audited rather than taken on trust, and so the same candidates aren't re-raised later:

| Candidate | Adjudication |
|---|---|
| Crooked / Underworld ×4 (p12) | **Already modeled** — all four nested pick-1-of-4 sub-choices are structured sub-options in this file. The sweep detects source structure and deliberately never read this catalogue, so it cannot tell "present in source" from "missing from catalogue." |
| Visitor / Expatriation lines ×3 (p56, 20 descriptors) | **Already modeled** — all 20 tags are title-only children under Line 1/2/3. |
| Monstrous / Pure Drive (p37, 11) | **Genuine gap — corrected.** |
| Snoop / Crew jobs (p48, 9) | **Deliberate stakeholder decision, not a gap** — Skyler's explicit "instructions-only" call keeps the 9-item job list as free text in `Description`. Left unchanged; flagged to Skyler in case the sweep is reason to revisit it, but not overridden. |
| Chosen / Special Weapon "Material" (p7, 9) | **Out of this catalogue's scope** — Special Weapon is **Gear**, a standard section, already modeled as three `PlaybookGearCategory` rows (Form / Business-end / Material) in `architecture.md` Section 2, with its "or anything else you want" escape covered by that section's existing "from a list or freeform" handling. Its option content is Phase 4 authoring, not bespoke-catalogue content. |
| Crooked / Burglar, Fixer (p11) | **Correctly prose** — both are roll-outcome-gated in-play menus ("On a 10+ pick three, on a 7-9 pick two"), which Phase 6's Q1 settled as prose. Bowser downgraded rather than dropped these so the call stayed here; confirmed. |
| Gumshoe / Code sentence (p25) | **Not a pick** — the commas separate adjectives within one clause ("any sort of supernatural, alien, demonic entity or item"), not options. |
| Professional / Agency goal question (p44) | **Not remodeled** — a real instance of the already-known "Description asks a worldbuilding question with no structural place for the answer" pattern, which Skyler addressed for Agency's *name* specifically and explicitly declined to retrofit more broadly. Consistent with that standing decision; re-raised to Skyler rather than acted on. |

**Nine of the ten high-confidence hits needed no change** — the adjudication step, not the detection step, is what separated the one real gap from the rest. Worth stating plainly since a raw hit count reads alarming on its own. Design reasoning, candidate approaches, and open questions live in `phase5-bespoke-ideation.md`; this file only holds concrete per-playbook, per-ruleset definitions once their shape is worked out there. Section 3 of that file explains the schema shape these entries map onto (`BespokeSection`/`BespokeOption`, no rigid shape-kind enum) — this catalogue doesn't repeat that reasoning, just applies it.

**Purpose, reconciled 2026-08-26 — this is (a), the actual source of truth for data injection, not (b) a structural-shape reference only.** Skyler flagged this as a genuine, not-yet-settled ambiguity: this file's own original status line described it as "reference data," which reads as (b), while Skyler's request that created it ("catalogue the definition within this framework here for the actual implementation") reads as (a). Resolving explicitly rather than leaving it assumed: **it's (a).** Concrete consequence: every entry here needs to be real, authoritative content an agent can author a `Playbook` record directly from later — not just enough to prove the shape works. This has two direct implications for how entries get built from now on:
- **Structure** (section boundaries, min/max-select, parent/child nesting) still comes from reading the extracted plain text (`pdftotext -layout`) and applying the Section 3 framework, same as it always has.
- **Content fidelity**, including bold/italic/bulleted-list markup where the source has it, goes through the formatting-preserving extraction pipeline before an entry is recorded as final — plain/tagless text is only correct when the source genuinely has no formatting to preserve (verified, not assumed), not a placeholder to "fill in later."

**Formatting pipeline, resolved 2026-08-26 — no longer a gap.** `pdftotext -layout` alone can't see bold/italic/font-weight; that limitation is now solved. Full writeup: `phase5-bespoke-ideation.md` and `docs/hunter-playbooks/pdf-extraction-pipeline.md`. Concrete tools: `tools/pdf-extract/extract-runs.mjs` + `splice-formatting.mjs` for flat option text with inline emphasis (used for Background/Mission); `tools/pdf-extract/extract-moves.mjs` for list-containing content. Every entry below reflects real, pipeline-verified markup where applicable, or an explicit verified-plain confirmation — each ruleset's own content-fidelity note states which. As of 2026-08-26, all 3 pilot playbooks are fully checked, nothing pending; future playbook entries should carry the same note (checked-with-markup, checked-and-plain, or explicitly not-yet-checked) rather than silently omitting it.

**Convention**: one `##` heading per playbook, one `###` heading per bespoke ruleset that playbook has. Don't add placeholder headings for playbooks or rulesets not yet analyzed — a playbook simply doesn't appear here until it's been read and ingested. Keep per-entry provenance notes (structural decisions, normalization calls, content-fidelity status) terse by default as this file grows across 28 playbooks — reserve a full explanatory paragraph for genuinely unusual cases (Fate's messy source layout, Underworld's inconsistent wording), not routine ones.

**Standing rule, confirmed by Skyler 2026-08-27 — playbooks with confirmed-zero bespoke content still get a `##` heading, not an omission.** A playbook that's been read in full and genuinely has no bespoke ruleset (e.g. The Celebrity) is a real, checked result — different from "not yet processed" — and needs to be distinguishable as such by anyone scanning this file, not just inferable from the progress-tracker line. Record it as `## The [Playbook]` with a single line: "No bespoke ruleset found — confirmed by reading both pages in full." No `###` sub-heading underneath (there's nothing to give one to). This applies to every future playbook in the walkthrough that turns out the same way, not just Celebrity.

**`BespokeSection.EffectText` retroactive review, 2026-08-27 — every already-catalogued ruleset checked, so this doesn't need re-auditing later.** New field added (`phase5-bespoke-ideation.md` Section 3) after Fate's and Unknown Heritage's second prose blocks showed the same shape recurring twice. Checked every ruleset processed before the field existed against the split criterion stated in that section (applies only when the source presents follow-through/consequence content as a *distinctly, separately-positioned* block — not merely a forward-looking clause inside one continuous paragraph):
- **Crooked's Background, Heat, Underworld; Divine's Mission; Action Scientist's Area of Study** — each has exactly one continuous `Description` block immediately followed by its options, with nothing positioned after in the source. `EffectText` doesn't apply to any of them; no rework needed. (Underworld's `Description` contains one forward-looking clause — "Keep this in mind when you select your moves in the next section, so that everything fits together" — but it's part of the same single continuous sentence as the pick instruction, not a separately-positioned block, so per the stated criterion it stays in `Description` unchanged, not split out.)
- **The Celebrity** — confirmed-zero bespoke content; no ruleset exists to check `EffectText` against.
- **Chosen's Fate, Changeling's Unknown Heritage** — reworked below/above to use the new field (superseding the earlier concatenated versions).

**Progress: 28 of 28 playbooks processed — WALKTHROUGH COMPLETE, 2026-08-29.** The Crooked, The Chosen, The Divine, The Action Scientist, The Celebrity, The Changeling, The Covenant, The Curse-Eater, The Envoy, The Expert, The Flake, The Forged, The Gumshoe, The Hex, The Host, The Initiate, The Interface, The Monstrous, The Mundane, The Pararomantic, The Professional, The Searcher, The Snoop, The Spell-Slinger, The Spooktacular, The Spooky, The Visitor, The Wronged — all 28 playbooks in the source PDF have now been read directly, verified against the literal source (including a `pdftotext -raw` column-swap spot-check on every playbook since The Forged incident), and catalogued. 25 of the 28 have bespoke content, fully formatting-verified (Background, Heat, Underworld, Fate, Mission, Area of Study, Unknown Heritage, Covenant, Friendship, Corruption, Consumed Magic, How Consuming Magic Works, Task, Secret Wisdom, Overseers, Haven, Dual Nature, Origin, Gumshoe Code, Temptation, Rotes, Symbiosis, Sect, Integration, Monster Breed, Curses, Natural Attacks, Relationship Status, Guide's Gift, Bond Abuse, Fate of Your Love, Agency, Crew, Team Concept: Monster Revelations, Combat Magic, The Show, The Dark Side, Expatriation, Who You Lost — all through the pipeline or confirmed to have no prose to verify, finalized under this file's (a) source-of-truth purpose, nothing pending). The Celebrity, The Flake, and The Mundane have confirmed-zero bespoke content per the standing rule above — no formatting verification needed since there's no bespoke text to verify. The Searcher is a fourth confirmed-zero-bespoke-content playbook by the same standard (nothing outside Moves fits `BespokeSection`/`BespokeOption`), but distinct from the other three in one respect: its Required Move (First Encounter) carries a genuine Move-internal-pick trap, flagged below and for Phase 4, not modeled here — the first confirmed-zero playbook to also surface that trap. Curse-Eater, The Gumshoe, The Hex, The Pararomantic, and now The Spooktacular are the only playbooks so far needing schema beyond baseline `BespokeSection`/`BespokeOption` (`PlaybookExtraTrack`/`BespokeJournal` for Curse-Eater, `BespokeSection.FreeTextLabel` for Gumshoe, `BespokeSection.MinInstances`/`MaxInstances` + `HunterBespokeSectionInstance` for Hex's Rotes, `PlaybookExtraTrack.StartLabel` for Pararomantic's Relationship Status, `BespokeOption.NumericMin`/`NumericMax` + `HunterBespokeSelection.NumericValue` for Spooktacular's Infernal Favour — see `phase5-bespoke-ideation.md`/`architecture.md`); Envoy's, Expert's, Forged's, Host's, Initiate's, Interface's, Monstrous's, and Professional's bespoke rulesets all reused already-established shapes with no new schema (Monstrous's "Monster Breed" reuses the Covenant zero-option pattern for a new purpose — orphaned framing text — rather than introducing a new structure). **The Snoop's Crew and Team Concept: Monster Revelations both reuse the same Covenant zero-option pattern too, per Skyler's own explicit "instructions-only" framing** — Crew is a real, if deliberately unmodeled, creation-time task (Skyler chose to keep it flat rather than decompose its entourage-vs-hunters/name-and-job structure into real `BespokeOption` picks); Team Concept is pure reference/example copy with no player-facing task at all, one step further than Monster Breed's own "orphaned flavor text" extension of the pattern — flagged as a Phase 10 UI-rendering question (does Hunter creation even need to surface a Section with nothing to decide) rather than a schema gap. **Forged's Bonds/Burdens were originally miscatalogued here as a `BespokeSection` and have been removed — that content belongs to Partner, a `Required` Move, and is out of this catalogue's scope (Phase 4's job); see `## The Forged` for the correction.** The same Move-internal-pick-structure shape recurred on The Gumshoe (Naked City), The Host (Defensive Adaptation), The Professional (Mobility — the first instance on an *optional*, not Required, Move), and now The Searcher (First Encounter — back on a Required Move, the fourth such Required-Move instance after Forged/Gumshoe/Host), all correctly excluded; checked on The Hex, The Initiate, The Interface, The Monstrous, and The Mundane too, none found there (The Interface and The Mundane have no Required move at all; The Monstrous's "Moves" component turned out to be the standard Move-granting section, not bespoke — see `## The Monstrous`'s scope-boundary note). **The Spell-Slinger is the first playbook with both trap shapes at once** — Tools and Techniques (Required, the fifth Required-Move instance) and Could've Been Worse (optional, the second optional-move instance after Professional's Mobility), both correctly excluded; see `## The Spell-Slinger` for the full accounting. **The Pararomantic is the first playbook where a stakeholder's own proposed bespoke-Section merge was directly evaluated and partly overridden** — Relationship Status turned out to be `PlaybookExtraTrack`-shaped (a box-track, not a pick-list) and was modeled separately rather than folded into any Section; see `## The Pararomantic` for the full reasoning. **The Professional's Agency is the first case of a Section's own `Description` asking a question with no structural place for the answer to live** — fixed by adding a third, synthesized "Agency name:" mandatory category with a single `{{blank}}`-only child; the same gap likely exists on other already-catalogued playbooks (Sect, Task, Origin) but hasn't been retrofitted anywhere else pending Skyler's call on a broader pass. A second Unnamed-Required-Move case (The Professional, after The Initiate) was also resolved — Skyler named it "Agency politics," 2026-08-29. **The Spell-Slinger's Combat Magic is the first "shared budget across two sub-groups with a floor constraint" shape in this catalogue — resolved by Skyler 2026-08-29 as nested Bases/Effects categories** (not the flat 10-option list recommended), with the resulting enforcement gap documented explicitly in the entry (a naive reading of `MinSelect`/`MaxSelect` alone permits up to 6 total picks against the true cap of 3 — a bigger version of Natural Attacks' own accepted either/or gap, same "state the real rule in prose, don't invent schema for it" resolution, just a larger gap this time); see `## The Spell-Slinger` for the full accounting. **The Spooktacular's "An Infernal Power" is the first bespoke pick to grant a trackable resource conditional on that specific pick, not universal to the whole playbook — resolved by Skyler 2026-08-29 as a new `BespokeOption` leaf shape (`NumericMin`/`NumericMax`), not a `PlaybookExtraTrack` extension.** Skyler's own proposal ("a new type of bespoke option... a numeric input value with a minimum and maximum value"), evaluated on its merits and adopted over my original nullable-FK-on-`PlaybookExtraTrack` recommendation — attaching the bounds directly to the option that gates them makes the conditionality question moot by construction. Modeled as a single mandatory child option ("Infernal Favour", `NumericMin=0`/`NumericMax=3`) under "An Infernal Power," the same attachment shape as Professional's "Agency name:" single-child pattern; see `## The Spooktacular` and `phase5-bespoke-ideation.md` for the full reasoning and the new `HunterBespokeSelection.NumericValue` instance-side field. **The Spooky's Dark Side reused two already-established shapes cleanly (Tag Pick, `EffectText`'s framing/consequence split) with no new schema** — checked explicitly against the fresh `NumericMin`/`NumericMax` leaf shape from the immediately preceding round and confirmed it doesn't apply here, per the coordinator's explicit invitation to reuse it if it fit (it didn't, and wasn't forced). This round's real catch was a font-ID mapping trap in my own manual verification (resolving a combined 2-page font table instead of the single page actually being extracted), caught by cross-checking against `extract-runs.mjs`'s own programmatic output before it could ship a reversed bold/italic result; see `## The Spooky` for the full account. **The Visitor's Expatriation is the first 3-level-deep nested `BespokeOption` structure, and the first real test of whether a range (not a fixed count) works at the "how many of my direct categories" level — confirmed clean, no new schema, no Combat-Magic-style gap.** Skyler explicitly asked to model the ruleset's three unlabeled comma-item lines as a real nested pick structure (parent options with child options, not flattened copy) — verified directly against the source first (an exact match to Skyler's own summary), then checked whether "one or more per line, from at least two of three lines" hits the same shared-budget problem Combat Magic did. It doesn't: there's no cross-category numeric budget here at all, only a categorical participation minimum, which the existing recursive `MinSelect`/`MaxSelect` mechanism (already proven for Fate/Friendship/Combat Magic's "pick M of N categories") already expresses exactly via `MinSelect=2, MaxSelect=3` — the first case needing an actual range instead of every prior case's `MinSelect == MaxSelect == category count`. One implementation nuance (whether a category counts as "engaged" via a stored selection row or a derived fact) was resolved with reasoning, not escalated, since every prior nested-category case never actually tested it; see `## The Visitor` for the full account. **The Wronged, the 28th and final playbook, reused three already-established shapes cleanly (an umbrella Section with 3 mandatory sub-blocks per Fate/Origin/Expatriation; a single-mandatory-child free-text leaf per Agency's "Agency name:"; an uncapped-minimum Tag Pick per Heat/Expatriation's Lines) with no new schema at all** — see `## The Wronged` for the full accounting, including a positive confirmation that not every Move-embedded bulleted list is one of the two named traps (DIY Surgery's roll-outcome branch is ordinary PbtA partial-success content, explicitly distinguished from both). **Nothing outstanding from this final round.** **One post-completion correction, 2026-08-30**: The Monstrous's Pure Drive nested pick-1-of-11 was remodeled from prose to structured options after Phase 6's tooling gained inline-run detection and a sanctioned full re-sweep found it — the only genuine gap out of 14 in-scope candidates. Full adjudication table in the status block at the top of this file. **Both items originally flagged as open here are now resolved, 2026-08-29.** The Expatriation category-engagement resolution (derived vs. stored) was confirmed directly by Skyler ("I agree that this should be logically derived, so good call") — see `architecture.md` Section 6.4. The standing Move-internal-pick-trap list (every instance flagged in-place across the relevant playbook entries below, never resolved here since Moves were always out of this catalogue's scope by design) now has a dedicated home: **Phase 6** (`phases.md`), inserted between bespoke rulesets and playbook imports specifically to work through custom Move modeling — this catalogue's scattered flags are its starting seed, not a census; see Phase 6's own note for what's still needed before real modeling starts. (This line no longer needs updating — the walkthrough is complete.)

---

## The Crooked

### Background

- **Section**: Title "Background". Description: "You worked a less-than-legal job before you became a monster hunter. What did you do?"
- **Select**: 1 of 7 (inferred — no explicit "pick one" instruction found near this section; defaulted to 1 per the now-agreed convention).
- **Options** (title + description both populated, flat — no nesting). **Content fidelity: verified via `tools/pdf-extract/build-crooked-review.mjs`, real `<b>` markup below matches `crooked-background-review.json` exactly** (this is the pipeline's own acceptance-test entry, "Hoodlum," plus the other 6 confirmed the same way):
  1. **Hoodlum** — You can use Tough instead of Charm to `<b>`manipulate someone`</b>` with threats of violence.
  2. **Burglar** — When you `<b>`break into a secure location`</b>`, roll +Sharp. On a 10+ pick three, on a 7-9 pick two: you get in undetected, you get out undetected, you don't leave a mess, you find what you were after.
  3. **Grifter** — When you are about to `<b>`manipulate someone`</b>`, you can ask the Keeper "What will convince this person to do what I want?" The Keeper must answer honestly, but not necessarily completely.
  4. **Fixer** — If you `<b>`need to buy something, sell something, or hire someone`</b>`, roll +Charm. On a 10+ you know just the person who will be interested. On a 7-9 you know the only person who can do it, but there's a complication. Pick one: you owe them; they screwed you over; you screwed them over. On a miss, the only person who can help is someone who absolutely hates you.
  5. **Assassin** — When you take your first shot at an unsuspecting target, do +2 Harm. *(no bold in source — verified, not a gap)*
  6. **Charlatan** — When you want people to think you are using magic, roll +Cool. On a 10 or more, your audience is amazed and fooled by your illusion. On a 7-9 you tripped up a couple of times, maybe someone will notice. You may also `<b>`manipulate people`</b>` with fortune telling. When you do that, ask "What are they hoping for right now?" as a free question (even on a miss).
  7. **Pickpocket** — When you `<b>`steal something small`</b>`, roll +Charm. On a 10 or more, you get it and they didn't notice you taking it. On a 7-9 either you don't grab it, you grab the wrong thing, or they remember you later: your choice.

  (Backtick-wrapped `<b>`/`</b>` above purely so the literal tags render visibly in Markdown preview rather than being interpreted as HTML by whatever renders this file — the actual `DescriptionText` value has no backticks, just the raw tags, exactly as in `crooked-background-review.json`.)

### Heat

- **Section**: Title "Heat". Description: "You didn't get here without making enemies."
- **Select**: at least 2 of 5, no stated max (**explicit**: "Pick at least two of these and name the people involved").
- **Content fidelity: verified via the pipeline — no bold/italic present in the source for this ruleset** (page 12 has no italic font at all, and its only bold text is section headings, none inside option bodies). Plain text below is the correct, final result, not a gap.
- **Options** (description-only, no title, each with an embedded `{{blank}}` marking the name to fill in — position varies per option, preserved as-is per the source):
  1. A police detective, `{{blank}}`, has made it a personal goal to put you away.
  2. You have a rival from your background, `{{blank}}`, who never misses a chance to screw you over.
  3. You pissed off a well-connected criminal, `{{blank}}`, and they'll do whatever they can to destroy you.
  4. `{{blank}}` is someone with special powers, a person or monster, who you took advantage of.
  5. `{{blank}}` is an old partner you betrayed in the middle of a job.

### Underworld

- **Section**: Title "Underworld". Description: "Pick how you discovered about the real underworld. Keep this in mind when you select your moves in the next section, so that everything fits together."
- **Select**: 1 of 4 top-level options (inferred, same convention as Background).
- **Content fidelity: verified via the pipeline — no bold/italic present in the source for this ruleset** (same page-12 check as Heat). Plain text below is the correct, final result, not a gap.
- **Top-level options** (description-only, no title; each has its own nested pick-1-of-4 sub-choice):
  1. The target of a job was a dangerous creature. Pick one: → **vampire** / **werewolf** / **troll** / **reptiloid**
  2. You worked with someone who was more than they seemed. Pick one: → **sorcerer** / **demon** / **faerie** / **psychic**
  3. You were hired by something weird. Pick one: → **immortal** / **god** / **outsider** / **witch**
  4. Things went south on a job, including but not limited to running into. Pick one: → **a horde of goblins** / **a hunger of ghouls** / **a dream-eater** / **a salamander**

  Sub-option labels only, no description text on any of them.

  **Normalization applied, per Skyler's explicit example**: option 4's source wording read "Things went south on a job--including, but not limited to, running into (choose one): a horde of goblins, a hunger of ghouls, a dream-eater, a salamander" — inconsistent with the other three options' "Pick one:" phrasing. Normalized to match here, working from assumed intent rather than preserving the literal inconsistency, per Skyler's explicit instruction that this exact example should be corrected during import.

---

## The Chosen

### Fate

**Structural decision made by Skyler, 2026-08-26 — one `BespokeSection`, not three independent ones** (supersedes the earlier three-sections capture). Rationale, kept here for context: the source PDF's own layout for Fate is unusually messy (the flavor blurb sits right after Experience, the actual option content is on the next page, and the closing description text only makes sense referencing the last two categories) — Skyler is explicitly fine handling that messiness by hand later via the Admin UI during per-playbook review, not solving it perfectly here.

**Content fidelity: verified via the pipeline, finalized 2026-08-26 — all 33 tags confirmed plain.** `tools/pdf-extract/chosen-fate-review.json`: Bowser checked all 33 tags across the 3 groups two ways (manual font inspection + a programmatic diff of every detected bold/italic run in the column against every tag label — 0 collisions) and confirmed zero bold/italic. The 5 bold/italic runs the extractor *did* detect in this column are all accounted for as non-option content (the section headings "Your Fate"/"How You Found Out"/"Heroic"/"Doom", plus one rules-explanation aside — see below) — none are false negatives on a tag. Tag labels below stay plain text, correctly, not a gap.

**REVERSED BY SKYLER, 2026-08-30 — the Luck aside IS part of this ruleset. It is now stored in `EffectText`.** The two paragraphs below (the original out-of-scope judgment and the flag that followed it) are retained as the reasoning trail, but their conclusion no longer holds. Skyler's rationale, which the original analysis did not consider: **"the intent with the second statement is to remind the player that these systems are connected."** The earlier call classified the aside purely by *what kind of rule it is* (a Luck-spend trigger, therefore `Playbook.LuckSpecialText`), and on that basis excluded it. It is better read by *what it is doing where it is printed* — it sits inside Fate deliberately, to tell a player choosing fate tags that Luck will bring those tags into play. That is precisely `EffectText`'s role per `architecture.md` Section 6.1: what having made these picks means during ongoing play.

**Resolved, and both beats now exist** — this is the "treat as intentionally two separate beats" option the flag below anticipated, not a merge and not a move:
- `Playbook.LuckSpecialText` keeps its own wording, unchanged: "When you spend a point of Luck, the Keeper will bring your fate into play."
- Fate's `EffectText` gains a second block, appended after the existing "contradictory tags" text and separated the same way the source separates them:

  `<b>`Whenever you mark off a point of Luck,`</b>` the Keeper will throw something from your fate at you.

**Content fidelity: bold span verified programmatically against page 8, not inferred.** `extract-runs.mjs` reports exactly one bold run in this aside — "Whenever you mark off a point of Luck," — with "the Keeper will throw something from your fate at you." in regular weight. The page-8 font table was resolved for page 8 alone, avoiding the combined-multi-page font-ID trap recorded in `## The Spooky`. Note the sentence spans a line break in the source with "the Keeper" ending the bold line as regular text, so a naive read of the two visible lines drops those two words entirely.

**Original judgment, superseded above — retained for the trail.**

**Judgment call on the Luck-related aside near the Fate column — out of scope for this entry, reasoning stated.** Bowser flagged bold text reading roughly "Whenever you mark off a point of Luck..." sitting near the Fate column, deliberately not included or excluded without my call. This is the same shape as the "Crooked special:"/"Chosen special:" Luck-spend-trigger callouts already excluded everywhere else in this catalogue — those are `Playbook.LuckSpecialText` (a **standard** field, `architecture.md`'s Luck section), not bespoke-ruleset content, regardless of which column of the PDF they're physically printed in. This aside is the same kind of content, printed near Fate only because of the PDF's own layout, not because it's a rule about picking among Fate's tags. **Correctly out of scope for this entry**, same call as the other playbooks' Luck-special text.

**One thing worth flagging, not resolved here**: this aside's wording ("Whenever you mark off a point of Luck, the Keeper will throw something from your fate at you") reads as a *second*, differently-worded rendering of the same Luck-spend trigger already captured elsewhere in Chosen's own Luck section (recorded earlier in this project's PDF read as "Chosen special: When you spend a point of Luck, the Keeper will bring your fate into play."). Similar meaning, not identical text — a real, small source-fidelity question for whoever finalizes Chosen's actual `LuckSpecialText` value during Phase 4 authoring (pick one, merge them, or treat as intentionally two separate beats), not something this catalogue entry needs to resolve, since it's out of Fate's scope either way.

- **Section**: Title "Fate".
  - **Description**: "You get to decide what sort of fate is in store for you. Pick how you found out about your fate on the reverse side of this sheet." — the early intro blurb only.
  - **EffectText** (new field, reworked 2026-08-27 — see `phase5-bespoke-ideation.md` Section 3 for the schema change and full reasoning; **extended 2026-08-30**, see the reversal note above): two blocks, separated as the source separates them —
    1. "This is how your fate will unfold. It's okay to pick contradictory tags: that means your fate is pulling you both ways." — the closing thematic statement about what having made contradictory picks means for the character.
    2. "`<b>`Whenever you mark off a point of Luck,`</b>` the Keeper will throw something from your fate at you." — the Luck-connection reminder, added at Skyler's direction. Bold span verified programmatically.
  - **Dropped from free text entirely, not placed in either field**: the literal pick-instruction sentences ("How You Found Out (pick one):", "Then pick two heroic and two doom tags for your fate from the lists below") — fully redundant with the `MinSelect`/`MaxSelect` values already on each category option below, matching how no other Section restates its own pick-count in prose.
  - **Reworked, superseding the earlier "best-effort composite" version** (which concatenated all of the above into one `Description` field before `EffectText` existed). **Still only partially resolves Fate's messiness, stated plainly**: this gives the one genuinely freestanding piece of consequence text a proper home, but the underlying cause of the messiness — the source's own disjointed layout (blurb separated from options by a full page, closing text positioned to read naturally only against the last two categories) — isn't something a schema field fixes. Skyler is still explicitly fine handling that by hand during per-playbook review.
  - Still deliberately excludes the Luck-related aside per the judgment call below (unaffected by this rework).
- **Select**: 3 of 3 at the top level — all three categories below are mandatory, not a subset choice.
- **Top-level options** (act as category dividers — title populated, no description, each with its own nested children and its own select count):
  1. **How You Found Out** — select 1 of 7: **Nightmares and visions** / **Some weirdo told you** / **An ancient cult found you** / **Sought out by your nemesis** / **Attacked by monsters** / **Trained from birth** / **You found the prophecy**
  2. **Heroic** — select 2 of ~12 (**explicit**: "pick two heroic and two doom tags"): **True love** / **Sacrifice** / **You are the Champion** / **Visions** / **Secret training** / **Magical powers** / **Mystical inheritance** / **A normal life** / **You can save the world** / **Hidden allies** / **The end of monsters** / **Divine help**
  3. **Doom** — select 2 of ~14 (explicit, same sentence as Heroic): **Death** / **You can't save everyone** / **Impossible love** / **Failure** / **A nemesis** / **No normal life** / **Loss of loved ones** / **Treachery** / **Doubt** / **Sympathy with the enemy** / **Damnation** / **Hosts of monsters** / **The end of days** / **The source of Evil**

  All child options (in all three groups) are title-only, no description — just the tag phrase itself, confirmed plain per the content-fidelity note above.

---

## The Divine

### Mission

- **Section**: Title "Mission". Description: "You have been put on Earth for a purpose."
- **Select**: 1 of 5 (**explicit**: "Pick one").
- **Content fidelity: verified via the pipeline, finalized 2026-08-26.** `tools/pdf-extract/divine-mission-review.json`: one genuine formatting instance found — option 5 has inline italic. Options 1–4 confirmed plain from the raw PDF, not assumed (same "shape suggests no markup, but verify anyway" discipline already applied to Heat/Underworld — this time it turned up a real hit, not just a confirmed absence).
- **Options** (description-only, no title, flat — no nesting):
  1. You are here to fight the schemes of an Adversary.
  2. The End of Days approaches. Your role is to guide these hunters and prevent it from coming to pass.
  3. The End of Days approaches. Your role is to guide these hunters and ensure it comes to pass.
  4. You have been exiled. You must work for the cause of Good without drawing attention from your brothers and sisters, as they are bound to execute you for your crimes.
  5. One of the other hunters has a crucial role to play in events to come. You must prepare them for their role, and protect them at `<i>`any`</i>` cost.

  (Backtick-wrapping on `<i>`/`</i>` in option 5 is the same Markdown-preview convention used for Background's `<b>` tags above — the actual `DescriptionText` value has no backticks.)

**Not part of this ruleset — flagged, not included**: Divine's Advanced Improvements list separately offers "Change your mission... or a new mission of your creation." That's a level-up-time concern (Advanced Improvement selection tracking), not part of the Mission bespoke ruleset itself — see `phase5-bespoke-ideation.md` Section 1/5 for the scoping correction.

---

## The Action Scientist

### Area of Study

Only bespoke ruleset on this playbook (PDF pages 1–2, confirmed directly via `pdftotext -f 1 -l 1`/`-f 2 -l 2`, not inferred from the flattened multi-page extraction). Everything else — Ratings, Luck (including the standard `Playbook.LuckSpecialText`-shaped "Action scientist special:" trigger text), Harm, Experience, the 5-move Moves list (pick 2, no auto-grants), the 3-category Gear section (fixed toolkit grant + pick-2-of-10 weapons + pick-1-of-5 protective equipment), Pronouns/Looks, History, Improvements, and Advanced Improvements — is standard-section content fitting the Phase 2 model with no new decisions needed.

- **Section**: Title "Area of Study". Description: "Pick one focus for your research and training:"
- **Select**: 1 of 7 — **explicit** ("Pick one focus"), not inferred.
- **Options** (title + description both populated, flat — no nesting).

**Content fidelity: verified via the pipeline, finalized 2026-08-27** (`tools/pdf-extract/action-scientist-area-of-study-review.json`). **Process note worth flagging**: my own pre-extraction prediction, based on which options read as plain mechanical grants with no roll, called 4 of the 7 options plain — that prediction was wrong for 3 of those 4 (only Space actually held). "No roll attached" turned out to be a bad predictor of "no inline formatting" here; the PDF's typesetting bolds/italicizes trigger phrases and cross-references independent of whether the option has dice-roll mechanics. Worth carrying forward as a reason to keep verifying every option on every future playbook regardless of how safe the shape looks — already standard practice in this catalogue, and this is a concrete case where skipping it would have shipped 3 wrong entries.

1. **Physics and Cosmology** — You can tell when something is altering the basic laws of reality around you. When you `<b>`investigate a mystery`</b>`, you may ask one of these as well as the usual questions:`<ul><li>`What direction is the source?`</li><li>`Is this static or changing?`</li><li>`What equipment would tell me more about this?`</li></ul>`
2. **Biology and Chemistry** — When you `<i>`examine a mysterious substance,`</i>` ask the Keeper what the source is. They'll tell you if it's biological or not, and if the source is mundane, supernatural, extraterrestrial, or extra-dimensional.
3. **Neurology and Psychology** — When you're `<i>`talking to someone and assessing their motives,`</i>` roll +Sharp.`<ul><li>`On a 10+, the Keeper will tell you if they're really human and whether they're an ally, bystander, villain, minion, or monster.`</li><li>`On a 7–9, the Keeper will tell you if there's something off about them but you're not sure what it is yet.`</li><li>`On a miss, you reveal too much about yourself instead.`</li></ul>`
4. **Computers and Electronics** — When you `<i>`access a secure computer system or electronic device, or change what it does`</i>`, roll +Cool.`<ul><li>`On a 10+, you get access, or change it (within the limits of the system).`</li><li>`On a 7–9, either it takes extra time or something unexpected happens.`</li><li>`On a miss, it all goes wrong in the worst way possible.`</li></ul>`
5. **Violence** — You may roll +Sharp to `<b>`kick some ass`</b>` instead of +Tough. When you make a scientific attack, either do +1 harm or take +1 forward.
6. **Mechanics and Engineering** — When you `<i>`fix machinery`</i>`, it takes much less time than expected. You also don't need all the proper tools to work on machines (sometimes a thump in the right spot is all that was needed). You may also `<i>`combine any two devices, rebuilding them together`</i>`. The new gadget has all the abilities of each component, and is the size of the larger one. It has all the tags of both components. You may add, delete, or change one tag.
7. **Space** — You have +1 ongoing with rocketry or astronaut stuff. You have access to a space launch facility (including use of spacecraft). *(confirmed genuinely plain — the only one of the 4 "plain" predictions that held)*

  (Backtick-wrapped tags above are the same Markdown-preview convention used throughout this file — actual `DescriptionText` values have no backticks. Two small fixes applied during transcription, same review discipline already established for this pipeline: typographic curly quotes normalized to straight quotes to match this file's existing style, and option 6's source line-wrap artifact "com- ponent" corrected to "component" — the same known, accepted `pdftotext -layout`/pdf.js line-wrap limitation already documented in `pdf-extraction-pipeline.md`, fixed on sight rather than preserved literally.)

**Judgment call, resolved with reasoning (flagged when first raised, restated here for the record)**: Physics and Cosmology's 3-item question list reads as optional in-play flavor ("you may ask **one of these** as well as the usual questions" — i.e. during an existing move, not a one-time character-creation pick), not a nested `BespokeOption` pick the way Underworld's sub-options are. Modeled as inline `<ul><li>` content within the option's own `DescriptionText`, not as child options. Both readings are supported by the existing schema either way — this was a source-intent call, not a modeling gap.

**Not part of this ruleset — flagged, not included**: Advanced Improvements separately offers "New interest: add a second area of study" — a level-up-time reference back to this ruleset, structurally identical to Divine's "mission of your creation" case. Out of scope, same reasoning as Divine's note above.

**Bowser's pipeline notes, for the record**: two real bugs (a line-wrap boundary dropping a joining space, and a follow-on stray-space regression) were found and fixed in the extraction script during this pass — confirmed not to have affected any previously-approved output (Crooked/Covenant/Divine/Chosen all re-verified unchanged after the fix).

---

## The Celebrity

No bespoke ruleset found — confirmed by reading both pages in full (PDF pages 3–4, verified directly via `pdftotext -f 3 -l 3`/`-f 4 -l 4`). Every section on this playbook (Ratings, Luck — including the standard `Playbook.LuckSpecialText`-shaped "Celebrity Special:" trigger text, Harm, Experience, Moves, Gear, Getting Started, Pronouns, Looks, Introductions, History, Improvements, Advanced Improvements) is standard-section content fitting the Phase 2 model as-is. The one thing that looked like it might be bespoke on first read — "Fakelore," an always-granted Move with a nested roll-outcome-gated effects list — turned out to be standard Moves content (the same shape already validated for Chosen and Divine), not a bespoke ruleset; it did surface a real Moves-schema naming clarification (`PlaybookMove.IsAutoGranted` → `Required`), tracked in `architecture.md`/`phases.md`, not here.

---

## The Changeling

### Unknown Heritage

Only bespoke ruleset on this playbook (PDF pages 5–6, confirmed directly via `pdftotext -f 5 -l 5`/`-f 6 -l 6`). Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Changeling Special:" trigger), Harm, Experience, the Moves list (**checked explicitly for `Required` vs. pick-N**: "Glamour" is `Required = true`, plus a pick-2-of-7 pool — Inhuman Talent, They Are My People, Force of Nature, Lockdown, Faerie Gossip, Renewal, Strange Paths — same already-validated shape as Chosen/Celebrity, no new case), the 3-category Gear section, Pronouns, Looks (4 categories this time, still the standard shape), History, Improvements, and Advanced Improvements (includes "Remove one Unknown Heritage tag," a level-up-time reference to this ruleset, correctly out of scope per the same precedent as Divine/Celebrity) — is standard-section content fitting the Phase 2 model with no new decisions needed.

- **Section**: Title "Unknown Heritage".
  - **Description**: "Your non-human heritage has pitfalls you were never taught about. Pick three:" — the intro/framing block, positioned before the options.
  - **EffectText** (new field, reworked 2026-08-27 — un-concatenated from `Description`, see `phase5-bespoke-ideation.md` Section 3 for the schema change and full reasoning): "The Keeper may introduce obstacles or distractions based on these tags. If you push through or ignore them, things can spiral out of control, and you may need to `<b>`act under pressure`</b>`. Alternatively, devote time to dealing with it and put other concerns (like the current crisis, ongoing mystery, or personal issues) on hold. If you do, mark experience. You always have the temptation to find out about your nature. When `<b>`investigating a mystery`</b>` with a source of supernatural knowledge, you may ask, "What do I learn about myself?" as one of your questions. If the character gains any useful knowledge of themself, word will get out among the supernatural world that they're asking questions." — the mechanical follow-through block, positioned after the options. This is the clean case the new field was designed around: two genuinely distinct, source-separated blocks, one framing (Description) and one consequence (EffectText), no leftover redundant instructional text unlike Fate.
- **Select**: 3 of 10 — **explicit** ("Pick three").
- **Options** (title-only, no description, flat — no nesting; matches the Tag Pick shape already established for Chosen's Fate):
  1. Dietary restriction
  2. Hygienic need
  3. Unearned reputation
  4. Erratic power
  5. Strange thoughts
  6. Sensory bombardment
  7. Allergy to `{{blank}}`
  8. Repulsion from `{{blank}}`
  9. Attraction to `{{blank}}`
  10. Obsession with `{{blank}}`

**Content fidelity: verified via the pipeline, finalized 2026-08-27** (`tools/pdf-extract/changeling-unknown-heritage-review.json`). All 10 tag titles and the short `Description` sentence confirmed plain — no bold/italic. `EffectText` is **not** plain: two real bold cross-reference spans (`<b>act under pressure</b>`, `<b>investigating a mystery</b>`, both references to base moves), captured above. No italic anywhere on this page (confirmed both from the embedded font table and from the extraction), no bulleted list despite the paragraph's visual indentation (checked directly for the list-marker glyph — none present).

**Decision 1, resolved from precedent, not escalated to Skyler — `{{blank}}` recorded despite no printed marker in the source.** Bowser checked the raw PDF item stream directly (not just the plain-text extraction) and confirmed nothing follows "Allergy to"/"Repulsion from"/"Attraction to"/"Obsession with" — no underscore run, no glyph of any kind, unlike Heat's literal inline `________`. This is a genuinely different provenance for the token than every prior use: Heat's blanks were a typographic marker in the source, transcribed and normalized into the token; these 4 are added on **semantic/functional grounds** — the phrases are grammatically incomplete on their own ("Allergy to" isn't a pickable, self-contained tag the way "Dietary restriction" is), and a Hunter creation form has no way to capture what a player is allergic to without an input somewhere, regardless of what the PDF's typesetting happened to print. The token's own original purpose ("marking exactly where the UI should render an input control") is functional, not purely transcriptional — it was always about answering "does the frontend need an input here," and the answer for these 4 is unambiguously yes independent of source markup. Flagged explicitly as a new category (inferred-from-grammar vs. transcribed-from-glyph) rather than silently treated the same as Heat's case, in case Skyler wants to weigh in later — confident enough in the reasoning to resolve it now rather than block the walkthrough on it.

**Decision 2, superseded 2026-08-27 — the two prose blocks are now `Description` + `EffectText`, not a concatenated `Description`.** Originally resolved by concatenating (following the precedent set for Chosen's Fate), after checking the trailing paragraph wasn't a "special:"-callout-adjacent aside (the pattern already excluded from bespoke scope elsewhere) — it isn't: that exclusion applies to text about a *different* standard mechanic (Luck-spending) sharing page proximity with a bespoke ruleset, and this paragraph is explicitly about Unknown Heritage's own tags. That finding still holds (the paragraph belongs with this ruleset). What changed: Skyler asked for this recurring shape to be modeled as a real second field rather than resolved case-by-case via concatenation — see `phase5-bespoke-ideation.md` Section 3 for the schema change (`BespokeSection.EffectText`), its naming rationale, and the semantic split criterion (framing vs. consequence, not source position) applied here.

---

## The Covenant

Two bespoke rulesets on this playbook (PDF pages 9–10, confirmed directly via `pdftotext -f 9 -l 9`/`-f 10 -l 10`; page 9's Moves column was already touched by an earlier, unrelated Bowser tag-validation pass — re-verified the bespoke content lives in a *different* column on the same page, not reused blindly). Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Covenant Special:" trigger), Harm, Experience, the 3-category Gear section, Pronouns, Looks, History, Improvements (11 this time, more than the usual 10, still flat — includes "You gain a second ally. Pick a different style for them.," a level-up reference to Friendship, correctly out of scope per the established precedent), and Advanced Improvements (8, includes "Your Covenant now works twice per session," same out-of-scope precedent) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all the basic moves, plus two Covenant moves. Pick two of these:" — **no auto-grant sentence at all**, the first playbook processed with zero `Required` moves. 7 named options (Get Down!, Fast Friends, Smash Cut, The Geek in the Chair, Acolyte, Who Said I Was Alone?, Opening Doors), all `Required = false`, `MoveGrantCount = 2`. Same already-validated shape (N required + pick from pool, here N=0), not a new case.

### Covenant

**Structural decision made by Skyler, 2026-08-27 — a standalone, zero-option `BespokeSection`**, not folded into Friendship's `EffectText` (the alternative I'd leaned toward). See `phase5-bespoke-ideation.md` Section 3 for the full reasoning and the new degenerate-case documentation this required (representation of "no picks": `MinSelect`/`MaxSelect` both `null`, not `0`/`0`).

- **Section**: Title "Covenant". `Description`: "You have a knack for keeping allies safe. Once per session, if an ally would be killed, describe how you help them survive and escape. They return, fully or mostly recovered, at the start of the next mystery." No `EffectText` — nothing left over once the whole paragraph sits in `Description` for this shape.
- **Select**: not applicable — `MinSelect`/`MaxSelect` both `null`.
- **Options**: none. Zero `BespokeOption` rows.

**Content fidelity: verified via the pipeline, finalized 2026-08-27** (`tools/pdf-extract/covenant-friendship-review.json`). Entirely plain — 0 bold/italic.

### Friendship

- **Section**: Title "Friendship". `Description`: "You start with an ally. Pick a type (`<i>`Monster of the Week`</i>` hardcover, page 131) and a style:"
- **Select**: 2 of 2 at the top level — both Type and Style mandatory ("Pick a type... **and** a style," matching Fate's precedent for reading a conjunction as mandatory-both). Same "mandatory multi-category" shape as Fate, N=2 instead of 3.
- **Top-level options** (category dividers, title populated, no description, each with its own nested children and select count):
  1. **Type** — select 1 of 3, **Titled-Choice shaped** (title + real description each — the first time a *nested* category's children use this shape rather than title-only):
     - **Watson** — An individual, extra-competent ally.
     - **Rolodex** — An ally team with various skills; tends to operate individually.
     - **Unit** — A small group that usually operates as a team.
  2. **Style** (source label: "Describe the ally") — select 1 of 8, title-only, Tag Pick shape:
     - Long-time coworker
     - Good buddy
     - Supernatural creature
     - Romantic interest
     - Friendly employer
     - Loyal retainer
     - Mutually cursed
     - Something else `{{blank}}`

**Style ordering corrected 2026-08-31 during Phase 8 group 1 — this entry was wrong.** Both this file and `covenant-friendship-review.json` originally listed **Loyal retainer** first. The source prints these 8 tags in a two-column grid, and the item coordinates (verified directly on page 9: column 1 at x=534, y=395→345; column 2 at x=654, y=395→370) put Loyal retainer at the *top of the second column* — position 6 read column-major, position 2 read row-major, and first under neither. The set was right; only the order was wrong. Skyler settled the general rule the same day: **multi-column option grids are transcribed column-major**, matching `pdftotext -raw`, the Skill's existing "transcribe lists from `-raw`" instruction, and the "top to bottom" phrasing of the improvement-ordering rule. The Curse-Eater's 3-column Vehicles grid was settled by the same call. Worth noting as a failure mode for the remaining playbooks: a two-column list is easy to read correctly *as a set* and get wrong *as a sequence*, and nothing downstream catches it — the content-fidelity check passes either way, because every individual string is still on the page.

**Content fidelity: verified via the pipeline, finalized 2026-08-27** (`tools/pdf-extract/covenant-friendship-review.json`). `Description` carries one genuine italic span (`<i>Monster of the Week</i>` — the game's own title, a bibliographic cross-reference, not previously predicted). All 3 Type titles bold (title styling isn't persisted — same convention as every prior playbook's option titles), their description bodies plain. All 8 Style tags plain, confirmed programmatically.

**`{{blank}}` on "Something else" — same semantic-grounds reasoning as Changeling, now independently confirmed a second time.** No PDF-rendered marker follows "Something else" (checked directly against the raw item stream, same as Changeling's 4 tags) — only a trailing colon implies a fill-in, no glyph actually renders. This is the second, independent ruleset landing on the identical finding (a grammatically/typographically implied blank with zero printed marker), which meaningfully strengthens confidence in the original call: it's not a one-off interpretation of Changeling's specific case, it's a real, recurring source convention (MOTW's own character-sheet typesetting apparently doesn't always print a blank line for short trailing fill-ins the way it does for Heat's embedded mid-sentence blanks) — worth carrying forward as an expected pattern for future playbooks' trailing "something else"/"other"-style options, not re-litigated from scratch each time it recurs.

---

## The Curse-Eater

Three bespoke-adjacent concepts on this playbook (PDF pages 13–14, confirmed directly via `pdftotext -f 13 -l 13`/`-f 14 -l 14`) — the most of any playbook processed so far, and the first to need genuinely new schema beyond `BespokeSection`/`BespokeOption`. Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Curse-eater special:" trigger), Harm, Experience, Gear (**3**-category: handy gear pick 3 of 10, practical weapons pick 2 of **5**, vehicles pick 1 of **5** — counts corrected 2026-08-31 during Phase 8 authoring; the original line said "2-category" and then listed three, and overcounted both of the last two lists), Pronouns, Looks, History, Improvements (**10**, not 11 — corrected 2026-08-31; the list is Sharp/Weird/Tough/Charm, two "Take another Curse-eater move", the consumed-magic and corruption entries, and two "Take a move from another playbook". It does include "Make a consumed magic into a permanent custom move... then clear it" and "Clear all corruption, without clearing consumed magics" — level-up-time references to both new concepts below, correctly out of scope per the established precedent), and Advanced Improvements (mirrors Improvements' two consumed-magic/corruption references, same out-of-scope precedent) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all the basic moves, and four Curse-eater moves. You get these two:" **Devour Evil** + **Unleash Corruption** (`Required = true` both) + "Then pick two of these:" 7 named options (Devour Harm, Reach, Curse Whispers, Ropes of Fate, Feel the Burn, Fortune's Fool, Let It Go). `MoveGrantCount = 4` (2 required + pick 2 of 7). Same already-validated shape, not a new case. (Two-column Moves layout noted for Phase 4 authoring per Skyler's instruction — required moves in one column, the pick-2 pool in the other — not acted on this round.)

### Corruption

**New schema, approved by Skyler 2026-08-27 — `PlaybookExtraTrack`, additive only, doesn't touch Luck/Harm/Experience.** Full reasoning: `architecture.md`'s "Extra Tracks" section.

- **Track**: Name "Corruption". `BoxCount`: 7. `EndLabel`: "Lost" (vs. Luck's "Doomed", Harm's "Dying"/"Unstable").
- **`Description`**: "When you consume evil magic, you gain corruption as well as the ability to use some of those powers. Record what the magic was, the power it offers you, and the downside it asks of you. The Keeper will provide these, and can also confirm whether you are in the presence of a consumable curse."
- **`EffectText`**: "Repeated `<b>`devouring evil`</b>` may give the same or different effects. Other moves, starting with `<b>`unleash corruption`</b>`, depend on these corruptions. If you `<i>`should`</i>` take corruption, but the track is full, you become a monster under the Keeper's control." Both bold spans cross-reference this playbook's own Required moves (Devour Evil, Unleash Corruption) — same pattern already seen in Action Scientist and Changeling.

**Content fidelity: verified via the pipeline, finalized 2026-08-27** (`tools/pdf-extract/curse-eater-review.json`). 2 bold cross-reference spans + 1 italic, all in `EffectText`, captured above; `Description` is plain.

**Split corrected from the original proposal — see `architecture.md`/`phase5-bespoke-ideation.md` for the full reconciliation.** Originally proposed splitting the middle two sentences ("Record what the magic was...") into the Consumed Magic journal's `Description` on semantic grounds. Bowser verified directly against the PDF item stream that "Consumed MagiC (Power, Downside)" has zero body text of its own — the whole paragraph sits under "Corruption" alone. Resolved by respecting the source's own header boundary rather than stretching content across it, consistent with how Skyler already resolved the analogous question for Covenant's "Covenant" ability.

### Consumed Magic

**New schema, approved by Skyler 2026-08-27 — `BespokeJournal`/`BespokeJournalField` (template) + `HunterJournalEntry`/`HunterJournalEntryFieldValue` (instance), for a growing, player-authored list added to during play, not a fixed pick from a predefined option list.** Full reasoning: `phase5-bespoke-ideation.md`'s `BespokeJournal` section.

- **Journal**: Title "Consumed Magic".
- **`Description`**: `null`. The source gives this journal nothing beyond its own bare table heading ("Consumed MagiC (Power, Downside)") — confirmed by Bowser directly against the item stream, not assumed. A real, honest null, not a placeholder to fill in later.
- **Fields** (`BespokeJournalField`, derived directly from the heading): **Power**, **Downside**.

**Content fidelity**: nothing to verify — there's no prose for this journal at all, only the heading (which supplies `Title` and the two field labels, already captured above).

### How Consuming Magic Works

Ordinary, already-modeled `BespokeSection` — no new concept, distinct from both Corruption and Consumed Magic.

- **Section**: Title "How Consuming Magic Works". `Description`: "When you consume/absorb evil magic, how does it work?"
- **Select**: 1 of 5 — inferred from the singular framing question, same confidence as Background's implicit pick-1.
- **Options** (description-only, no title, flat — matches the Mission shape):
  1. You lay your hands on it and a visible glowing smoke transports the curse.
  2. You ritually eat some part of it.
  3. You closely embrace the cursed thing and spend several minutes bonding with it.
  4. You have an amulet that you must hold against it.
  5. Something else `{{blank}}`

**Content fidelity: verified via the pipeline, finalized 2026-08-27** (`tools/pdf-extract/curse-eater-review.json`). Entirely plain — 0 bold/italic across the intro and all 5 options.

**First case with a genuinely rendered blank marker, exact form confirmed.** Unlike Changeling's 4 tags and Covenant's "Something else" (both confirmed absent), this one is real: `"Something else: "` (label, colon, one space) followed by exactly 30 consecutive underscore characters, all one PDF text item, plain font — no bold/italic on the marker itself. Good corroborating data point either way: the `{{blank}}` token renders correctly whether the source prints a marker or not, and this confirms the token isn't papering over a systematic absence — sometimes MOTW's own typesetting does print the blank, sometimes it doesn't, and the token handles both uniformly.

---

## The Envoy

Three bespoke concepts (PDF pages 17–18, confirmed directly via `pdftotext -f 17 -l 17`/`-f 18 -l 18`) — all resolved by shapes already established elsewhere in this catalogue, no new schema needed. This is the playbook that originally motivated `EffectText`'s design ("Overseers," flagged by Skyler when the field was proposed) — confirmed here to need nothing beyond what already exists. Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Envoy Special:" trigger), Harm, Experience, Gear (2 categories, Resources pick-1-of-4 + Weapon pick-1-of-4), Pronouns, Looks, History, Improvements (11, includes 3 level-up references to Task/Overseers/Gear, correctly out of scope), and Advanced Improvements (9, includes 2 more level-up references to Task/Overseers, same out-of-scope precedent) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all the basic moves and two Envoy moves. Pick two of these:" — no auto-grant sentence, all 7 named options optional (Strangely Normal, Too Much Has Been Lost, The Council Has Made a Decision, Prime Directive, Servant of the Secret Fire, From a Certain Point of View, No One Listens to Zathras). `Required = false` for all 7, `MoveGrantCount = 2`. Same already-validated zero-required shape as Covenant.

### Task

Clean Titled Choice, same shape as Background.

- **Section**: Title "Task". `Description`: "You have been given a task by the Overseers. Pick one:"
- **Select**: 1 of 4 — explicit.
- **Options** (title + description both populated):
  1. **Guide** — You are meant to guide people towards a desired end. What is it?
  2. **Herald** — You are meant to bring instructions to other people. What do you teach?
  3. **Watcher** — You are meant to search for signs of something. What are the signs?
  4. **Witness** — You are meant to record and observe ongoing events. What do you record?

**Content fidelity: verified via the pipeline, finalized 2026-08-27** (`tools/pdf-extract/envoy-review.json`). All 4 options entirely plain — no bold/italic in the description bodies (titles carry bold, per convention titles never persist markup).

### Secret Wisdom

Fixed, zero-choice mechanic whose sub-rules vary by which Task was picked, but with no pick of its own. Applied the already-settled Covenant precedent directly (differently-headed content stays in its own standalone Section, not folded into a nearby ruleset's `EffectText`) — a direct reuse of an already-answered question, not a new judgment call.

- **Section**: Title "Secret Wisdom". Zero `BespokeOption` rows — `Select`/`MinSelect`/`MaxSelect` all `null`, same representation as Covenant.
- **`Description`**: "You've received insight from your Task. `<i>`At the start of each session`</i>`, roll +Cool. On a 10+, hold 2. On a 7-9, hold 1. On a miss, hold 1, but the Keeper introduces a cruel choice or terrible truth related to your Task. Hold can be spent in different ways based on your Task:"
- **`EffectText`**: "Guide: When you `<i>`advise as instructed by the Overseers`</i>`, spend 1 hold to `<b>`help out`</b>` without needing to be in the same place, or spend 2 hold to `<b>`help out`</b>` on a start-of-mystery or start-of-session move. Herald: When you `<i>`announce your Overseers' demands`</i>`, spend 1 hold to `<b>`manipulate`</b>` a monster, if it is capable of communication. Watcher: When you `<i>`examine the patterns of Fate`</i>`, spend 1 hold to `<b>`protect someone`</b>` even when you are not in the same place. Witness: When you `<i>`report to the Overseers`</i>`, spend 1 hold to grant any hunter one question as if they just `<b>`investigated a mystery`</b>`."

**Content fidelity: verified via the pipeline, finalized 2026-08-27.** `Description` has one italic span, not predicted. `EffectText` has real markup throughout — every one of the 4 sub-entries has an italic trigger clause plus at least one bold cross-reference span (Guide's has two separate `<b>help out</b>` spans). One line-wrap artifact fixed on sight, same established discipline as Action Scientist's "com- ponent": Witness's "investi- gated" corrected to "investigated." Typographic curly apostrophe in "Overseers' demands" normalized to straight, matching this file's existing style.

**Disambiguation note, since it's easy to conflate**: Task's 4 options and Secret Wisdom's 4 sub-entries share the same 4 labels (Guide/Herald/Watcher/Witness) in the same column, but are two separate lists for two separate concepts — Task's are real `BespokeOption` rows (picked once, at creation); Secret Wisdom's are plain-text labels *inside* `EffectText`'s free text (not modeled as options at all, since Secret Wisdom itself has zero options). No structural link between them is stored — the correspondence is narrative only, same as how Fakelore's roll-outcome list wasn't modeled as linked options either.

### Overseers

Clean 2-mandatory-category nested structure — the same shape already validated twice (Fate's 3 categories, Friendship's 2).

- **Section**: Title "Overseers". `Description`: "The Envoy was sent or given a task by a higher power. Describe your Overseers according to their Values and your Concerns." `EffectText`: "The Envoy doesn't have a means to contact their Overseers easily in the way the Initiate and Professional can. This is by design—the Overseers are hands-off and esoteric compared to an Agency or Sect. You can `<b>`use magic`</b>` to reach out, otherwise contact is always at the Overseers' whim."
- **Select**: 2 of 2 (both mandatory).
- **Top-level options**:
  1. **Values** — select 2 of 11 (explicit "pick two"): Order, Freedom, Safety, Compassion, Secrecy, Knowledge, Honesty, Power, Growth, Truth, Something else `{{blank}}`
  2. **Concerns** — select 1 of 10 (explicit "pick one"): Overseers' internal politics, Cryptic communication, Strict rules, Alien perspective, Distant presence, Secret underlying motives, Purity, Narrow-focused, Big picture, Something else `{{blank}}`

**Content fidelity: verified via the pipeline, finalized 2026-08-27.** Intro plain; all 21 Values+Concerns tags plain (confirmed 0 collisions); `EffectText` has one bold cross-reference (`<b>use magic</b>`). One false positive caught and fixed during extraction, for the record: the bold "Overseers" section heading initially collided with the ordinary word "Overseers" appearing in both prose blocks — resolved by narrowing the extraction's Y-bound to exclude the heading, the same category of false positive already documented for Crooked's Background (Assassin/Fixer) in `pdf-extraction-pipeline.md`.

**Blank markers: both present, independently confirmed, and a third distinct rendering convention.** Values' and Concerns' "Something else" markers are each exactly 21 underscore characters — but rendered differently from every prior case: "Something else:" prints as its own line, with the underscore run as a *separate* text item on the *next* line, left-aligned under the label — not appended inline on the same line (Curse-Eater's convention) and not absent entirely (Changeling's/Covenant's convention). `{{blank}}` now has three confirmed source-rendering conventions to handle uniformly (absent / inline-same-line / label-then-next-line) — worth noting since this could matter if the eventual authoring process ever needs to reason about source layout directly rather than just final semantic intent, even though the token itself already abstracts over all three at the data-model level.

---

## The Expert

One bespoke ruleset (PDF pages 19–20, confirmed directly via `pdftotext -f 19 -l 19`/`-f 20 -l 20`) — clean Titled Choice, same shape as Background, no new schema. Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Expert special:" trigger), Harm, Experience, Gear (1 category — Monster-slaying weapons, pick 3 of 9), Pronouns, Looks (2 categories this time, still the standard shape), History (7 prompts, standard defer), Improvements (10, includes "Add an option to your haven" x2 — a level-up reference to Haven, correctly out of scope), and Advanced Improvements (7, all flat) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all of the basic moves, plus pick two Expert moves:" — no auto-grant sentence, all 7 named options optional (I've Read About This Sort Of Thing, Often Right, Preparedness, It Wasn't As Bad As It Looked, Precise Strike, The Woman (or Man) With The Plan, Dark Past). `Required = false` for all 7, `MoveGrantCount = 2`. Same already-validated zero-required shape as Covenant/Envoy. ("Dark Past" has a nested 4-question list — ordinary Move content, same shape as Fakelore/Doors of Perception, no new modeling.)

### Haven

- **Section**: Title "Haven". `Description`: "You have set up a haven, a safe place to work. Pick three of the options below for your haven:"
- **Select**: 3 of 9 — explicit ("Pick three").
- **Options** (title + description both populated, flat — no nesting):
  1. **Lore Library** — When you hit the books, take +1 forward to `<b>`investigate the mystery`</b>` (as long as historical or reference works are appropriate).
  2. **Mystical Library** — If you use your library's occult tomes and grimoires, preparing with your tomes and grimoires, take +1 forward for `<b>`use magic`</b>`.
  3. **Protection Spells** — Your haven is safe from monsters—they cannot enter. Monsters might be able to do something special to evade the wards, but not easily.
  4. **Armory** — You have a stockpile of mystical and rare monster-killing weapons and items. If you `<b>`need a special weapon`</b>`, roll +Weird. On a 10+ you have it (and plenty if that matters). On a 7-9 you have it, but only the minimum. On a miss, you've got the wrong thing.
  5. **Infirmary** — You can heal people, and have the space for one or two to recuperate. The Keeper will tell you how long any patient's recovery is likely to take, and if you need extra supplies or help.
  6. **Workshop** — You have a space for building and repairing guns, cars and other gadgets. Work out with the Keeper how long any repair or construction will take, and if you need extra supplies or help.
  7. **Oubliette** — This room is isolated from every kind of monster, spirit and magic that you know about. Anything you stash in there can't be found, can't do any magic, and can't get out.
  8. **Panic Room** — This has essential supplies and is protected by normal and mystical means. You can hide out there for a few days, safe from pretty much anything.
  9. **Magical Laboratory** — You have a mystical lab with all kinds of weird ingredients and tools useful for casting spells (like the `<b>`use magic`</b>` move, big magic, and any other magical moves).

**Content fidelity: verified via the pipeline, finalized 2026-08-27** (`tools/pdf-extract/expert-haven-review.json`). Zero italic anywhere in the column, confirmed programmatically. 3 bold cross-reference spans (Lore Library → investigate the mystery; Mystical Library and Magical Laboratory → use magic each), same base-move-reference-bolding pattern already seen throughout this catalogue. Line-wrap artifacts fixed on sight, same established discipline (Protection Spells' "mon- sters," Armory's "mon- ster-killing," Oubliette's "Any- thing," Panic Room's "pro- tected"), and typographic curly quotes/apostrophes normalized to straight, matching this file's existing style.

**Armory's bold trigger clause — recorded as-is, not normalized, reasoning stated.** "Need a special weapon" is styled bold, where most other playbooks' equivalent trigger clauses (e.g. Envoy's Secret Wisdom `EffectText`) use italic — a real, confirmed book-wide inconsistency in the source's own typesetting, not an extraction error. **Not normalized to italic.** This is a different category of inconsistency than the wording normalization already applied elsewhere (e.g. Crooked's Underworld "(choose one)" → "Pick one:"): that case resolved an ambiguity in *instructional meaning* where getting it wrong would functionally mislead a reader about how many options to pick. Bold vs. italic carries no such functional difference here — both convey the identical game meaning, purely a stylistic choice, and the entire point of this formatting-preservation pipeline is faithful transcription of the source's *actual* rendered typography, not an idealized, self-consistent version of it. Overriding a confirmed fact about the source with a guess at "what it should probably have been" would be the same category of fabrication already declined for guessing bold-span positions before real extraction existed. Recorded exactly as printed.

**Armory's roll-outcome breakdown — confirmed flowing prose, not a list, despite matching a shape seen elsewhere.** Checked directly against the raw PDF item stream (not pattern-matched from Action Scientist's similarly-shaped Neurology and Psychology/Computers and Electronics options, which *do* use real bulleted lists for their own 10+/7-9/miss breakdowns): zero `•` bullet-marker characters anywhere in Armory's range — plain sentences within the same paragraph, no `<ul>`/`<li>` structure. Confirms the standing lesson from Action Scientist (don't predict formatting from content shape) generalizes to structure too, not just inline emphasis.

---

## The Flake

No bespoke ruleset found — confirmed by reading both pages in full (PDF pages 21–22, verified directly via `pdftotext -f 21 -l 21`/`-f 22 -l 22`). Skyler's own expectation going in ("no bespoke rulesets, just more custom moves") checked directly rather than taken on trust — confirmed correct. Every section (Ratings, Luck — including the standard `Playbook.LuckSpecialText`-shaped "Flake special:" trigger, Harm, Experience, Moves, Gear, Getting Started, Pronouns, Looks, Introductions, History, Improvements, Advanced Improvements) is standard-section content fitting the Phase 2 model as-is. One thing worth naming even though it didn't change the zero-bespoke result: Improvements includes a genuine cross-playbook reference ("Get a haven, like the Expert has, with two options" / "Gain another option for your haven") — borrows Expert's Haven concept via a level-up grant, but it's flat Improvement text describing what the grant does, not a bespoke ruleset belonging to Flake itself, same treatment as every other cross-playbook Improvement reference already seen (Gumshoe's naked city move, etc.).

**Moves, checked explicitly for `Required` vs. pick-N, per the standing convention**: "You get all of the basic moves, plus pick three Flake moves:" — no auto-grant sentence, all 8 named options optional (Connect the Dots, Crazy Eyes, See It All Fits Together, Suspicious Mind, Often Overlooked, Contrary, Net Friends, Sneaky). `Required = false` for all 8, `MoveGrantCount = 3`. Same already-validated zero-required shape as Covenant/Envoy/Expert — noted for Phase 4, not acted on now.

---

## The Forged

Two bespoke rulesets (PDF pages 23–24, confirmed directly via `pdftotext -f 23 -l 23`/`-f 24 -l 24`) — both resolved by shapes already established elsewhere in this catalogue, no new schema needed. Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped trigger), Harm, Experience, Gear (2 categories, Sentimental objects pick-1-of-5 + Human-form self-defence method pick-1-of-4), Pronouns, Looks, Introductions, History, Improvements (11, flat, includes "Pick another benefit for your weapon form" and "You can choose to count your current wielder as your partner..." — level-up references to Dual Nature/Partner, correctly out of scope), and Advanced Improvements (10, flat, includes "Get a second partner" and "You are able to wield yourself in weapon form permanently..." — same out-of-scope precedent) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Correction, 2026-08-27 — Bonds/Burdens is Move content, not a bespoke ruleset, and doesn't belong in this catalogue.** Originally miscatalogued as two independent `BespokeSection`s. Skyler's correction: Bonds/Burdens falls entirely under **Partner**, the `Required` Move every Forged hunter takes — a customized Move exclusive to this playbook with pick-based needs beyond a plain description, same category as any other Move. Moves are explicitly out of scope for this bespoke-ruleset catalogue (that's Phase 4's job); removed the `### Bonds`/`### Burdens` entries entirely rather than reworking them into a different shape. Noted for Phase 4 instead, in the Moves paragraph below.

**Correction, 2026-08-27 — a fabricated quote in a relayed brief was recorded in this catalogue as something Skyler said. It wasn't.** An earlier version of this entry stated: "Skyler's relayed description of the Moves layout was '2 required + pick-two pool, like Curse-Eater.'" Skyler never said this — the "2 required + pick two" specifics were invented during paraphrasing upstream of this entry, not stated by Skyler. What Skyler actually said: The Forged has a similar dual-column setup for Moves as The Curse-Eater, flagged specifically so this wouldn't be misread as incomplete Moves content; Dual Nature exists entirely within a single column. Corrected below — nothing about required/pick counts was ever claimed by Skyler for Forged; the actual counts are this entry's own independently-derived finding, not a confirmation of anything relayed.

**Moves, checked explicitly for `Required` vs. pick-N, per the standing convention**: "You get all the basic moves, plus two Forged moves. You get **this one**:" (singular) — only **Partner** is auto-granted (`Required = true`). "Then pick **one** of these:" follows, a 6-option pool (Tactical Advice, My Outlet, To My Side, Ritual Use, Don't Worry About Me, Pointing the Way — "Don't Worr y About Me" line-wrap artifact corrected on sight). `MoveGrantCount = 2` (1 required + pick 1 of 6) — this entry's own direct finding, not a confirmation of relayed guidance (see correction above). Same already-validated required-plus-pool shape, just spread across two of the page's three columns rather than one — matching Curse-Eater's own two-column Moves layout (required move(s) in one column, the pick-pool in another), the trait Skyler flagged to avoid misreading it as incomplete. **Partner's own `DescriptionText` references "pick two bonds and one burden," naming 5 Bonds options (pick 2) and 6 Burdens options (pick 1) with `{{blank}}`-marked "Something else" entries on both — flagged here for Phase 4, since this Move will need handling beyond a plain description when Moves are modeled. Not modeled as a `BespokeSection` in this catalogue**; see the correction above.

### Dual Nature

- **Section**: Title "Dual Nature". `Description`: "You may freely transform between your human and weapon shapes. You can't `<b>`kick some ass`</b>` when in weapon form, but when wielded as a weapon, your bearer adds harm equal to your Weird. Pick your base range, benefits, and flaw."
- **Select**: 3 of 3 (all three categories mandatory).
- **Top-level options**:
  1. **Range** — select 1 of 4, title-only: Intimate, Hand, Close, Far. *(Order corrected 2026-08-31: printed as a two-column grid — col 1 at x=294 is Intimate/Hand, col 2 at x=374 is Close/Far — so column-major is Intimate, Hand, Close, Far. This entry originally recorded it row-major.)*
  2. **Benefits** — select 2 of 8, title + description (Titled Choice, same shape as Type in Friendship):
     - **Magic** — Add the "magic" tag
     - **Vicious** — You deal +1 harm
     - **Precise** — You deal +1 harm
     - **Life Drain** — Add the "life-drain" tag; you may heal your wielder instead of yourself
     - **Reach** — Add another range
     - **Defensive** — Add 1-armour for your wielder
     - **Sweeping** — Add the "area" tag
     - **Elemental** — Add matching tags such as "fire," "wind," "lightning," "mind," or "darkness"
  3. **Flaws** — select 1 of 5, title-only: Conspicuous, Distinctive, Restricted, Unwieldy, Charging. *(Order corrected 2026-08-31, same two-column grid and same reason as Range above.)*

**Content fidelity: verified via the pipeline, finalized 2026-08-27** (`tools/pdf-extract/forged-review.json`). `Description` carries one bold cross-reference (`<b>kick some ass</b>`, a base-move reference — same recurring pattern seen throughout this catalogue). Range's 4 tags and Flaws' 5 tags confirmed plain, programmatically. Zero italic anywhere in this scope.

**Benefits' title/description split — recorded with a lower-confidence provenance note, unlike every other Titled-Choice case so far.** Every prior title+description split in this catalogue (Background, Task, Type) was recovered from a real font-weight boundary — the title is a distinct bold run, verified independently of the colon. Benefits is the first exception: each option (e.g. `"Magic: Add the \"magic\" tag"`) is a single plain-font text item with no bold run at all: the Title/DescriptionText split above comes from the extraction script's own colon-position parsing, not a font signal. The content here is simple enough (short label, then a short clause) that this is safe, and it's recorded as final rather than held back — but flagging the provenance explicitly, since it's a different kind of confidence than the rest of this catalogue's titled options, in case a future entry's colon placement is ever genuinely ambiguous.

### Origin

- **Section**: Title "Origin". `Description`: `null` (bare header, no framing sentence — same null treatment as Consumed Magic's bare heading). A real, if minimal, printed umbrella regardless: "Origin" sits directly above both Forging and Partnering with nothing else intervening, applying the Covenant "respect the source's printed header boundaries" precedent — the same reasoning that keeps Dual Nature's 3 categories under one Section here bundles Origin's 2.
- **Select**: 2 of 2 (both Forging and Partnering mandatory).
- **Top-level options** (both description-only, no title — Simple Choice shape, matching Mission/How Consuming Magic Works):
  1. **Forging** — select 1 of 7:
     - You always thought you were human.
     - You gained sentience by the perfection of your maker's craft.
     - You used to be a supernatural being.
     - A magical accident resulted in your creation.
     - You were created to fulfil an obligation.
     - "You know what? I don't remember."
     - Something else `{{blank}}`
  2. **Partnering** — select 1 of 7:
     - Your partner displayed great faith and devotion.
     - Your partner solved puzzles requiring wit and wisdom.
     - Your partner won your allegiance through power and might.
     - You were created specifically for your partner.
     - Your partner swore to perform a task and you are to aid them.
     - You met through sheer luck and happenstance.
     - Something else `{{blank}}`

**Content fidelity: verified via the pipeline, finalized 2026-08-27** (`tools/pdf-extract/forged-review.json`). All 14 Forging+Partnering options confirmed plain, programmatically — zero bold/italic on any option, only the structural headings carry formatting. Both "Something else" markers present: `"Something else: "` followed by 27 underscores each, identical to each other — same inline-same-line convention as Curse-Eater's marker (and Partner's own Bonds/Burdens markers, out of this catalogue's scope but observed on the same page), but a third distinct underscore count (27, vs. 30 on Curse-Eater/Bonds/Burdens, vs. 21 on Envoy's separate label-then-next-line convention). Underscore run length isn't consistent even within the same rendering convention — worth carrying forward as a reason not to assume a fixed count when normalizing, only the convention (inline vs. next-line vs. absent) matters for how `{{blank}}` is placed, not the literal underscore count.

**Not part of this ruleset — flagged, not included**: Advanced Improvements' "Get a second partner" and "You are able to wield yourself in weapon form permanently..." are level-up-time references to Partner/Dual Nature, correctly out of scope per the established Divine/Celebrity precedent.

---

## The Gumshoe

One bespoke ruleset (PDF pages 25–26, confirmed directly via `pdftotext -f 25 -l 25`/`-f 26 -l 26`; both pages spot-checked with `pdftotext -raw` for the Forged-style column-order swap — clean, normal stream order throughout, no defect on this playbook). Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Gumshoe Special:" trigger), Harm, Experience, Gear (2 categories, Recording devices pick-2-of-8 + P.I. Weapons pick-1-of-6), Pronouns, Looks (2 categories), Introductions, History (8 prompts + a closing flavor quote), Improvements (9, flat, includes "Get a haven, like the Expert has, with two options" — a cross-playbook reference, correctly out of scope — and "Add four additional or new contacts for your Naked City move" — a level-up reference to Naked City, correctly out of scope), and Advanced Improvements (8, flat) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get three Gumshoe moves. You get these two:" — **Occult Confidential** + **The Naked City** (`Required = true` both) + "Then pick one of these:" — 7 named options (The Postman Always Rings Twice, The Long Goodbye, Jessica Jones Entry, Out of the Past, Asphalt Jungle, Hacker with a Dragon Tattoo, "Just one more thing"), pick 1. `MoveGrantCount = 3` (2 required + pick 1 of 7), matching "three Gumshoe moves" exactly. **The Naked City's own `DescriptionText` includes "Pick four contact types from the following areas," naming 34 contact types (Academics, Accountants, Artists, Bartenders, Clergy, Conspiracy Theorists, Construction, Courts, Criminals (organised), Criminals (street), Cultists, Engineers, Espionage, Film and TV, Forensic Scientists, Fringe Scientists, Hackers, Journalists, Lawyers, Mechanics, Media, Medical Practioners, Military, Morgue, Occult, Police (local), Police (national), Politicians, Prisons, Private Security, Property Developers, Stage Magicians, Technologists, Transportation) — same Move-internal pick-structure shape as Forged's Partner/Bonds/Burdens, flagged here for Phase 4 rather than modeled as a `BespokeSection`.** Caught and correctly excluded this round rather than repeating the Forged miscategorization.

### Gumshoe Code

**New field, `BespokeSection.FreeTextLabel`, approved by Skyler 2026-08-27 — see `phase5-bespoke-ideation.md` Section 3 for the full schema addition and the two declined alternatives (`{{blank}}` reuse, `BespokeJournal` reuse).** The only case so far where a `BespokeSection` needs to capture a single player-authored value with no fixed option list — the "Example Codes" are explicitly illustrative, not a pick list.

- **Section**: Title "Gumshoe Code". `FreeTextLabel`: "Your Code" (a synthesized UI label — no literal short label is printed in the source; the game just says "pick a one-sentence Code").
  - **`Description`**: "With the agreement of the Keeper, pick a one-sentence Code that your Gumshoe adheres to. Example Codes: `<i>`Murders must be punished. Monsters must be destroyed. Innocents must be saved. Laws must be enforced. Evil must be exposed. The weak must be protected from the powerful.`</i>`"
  - **`EffectText`**: "This Code defines your Gumshoe. Any time you violate your code you forfeit all Code related moves (`<b>`The Postman Always Rings Twice`</b>`, `<b>`The Long Goodbye`</b>`) and the ability to spend Luck points. These forfeits last either until the next mystery or you make amends. As long as you follow the Code people will sense your sincerity: you receive +1 ongoing for `<b>`manipulate someone`</b>` and you may not be possessed or charmed by any sort of supernatural, alien, or demonic entity or item."
- **Select**: not applicable — zero `BespokeOption` rows, `MinSelect`/`MaxSelect` both `null`, same representation as Covenant/Secret Wisdom's zero-option case (this shape has no options either, just a free-text slot instead of a fixed ability).

**Content fidelity: verified via the pipeline, finalized 2026-08-27** (`tools/pdf-extract/gumshoe-code-review.json`). Framing sentence entirely plain. `EffectText` carries 3 bold spans: `<b>manipulate someone</b>` (the expected base-move cross-reference) plus `<b>The Postman Always Rings Twice</b>` and `<b>The Long Goodbye</b>` — **the first time this project's cross-reference-bolding pattern has extended to a playbook's own bespoke move names rather than only universal base moves.** Worth watching for on future playbooks rather than assuming base-move-only going forward.

**A genuinely new content shape for this pipeline, provenance recorded precisely rather than glossed over.** The 6 example codes render in the source as **one continuous italic run**, not a bulleted list and not individually-delimited sentences — confirmed no list-marker glyph anywhere in the block. The `<i>...</i>` wrapping the whole block above reflects that real structure. Bowser's own extraction also produced a convenience 6-sentence split (`sentences` array in the review JSON) — **that split is the script's own sentence-boundary parsing, not a PDF-derived structural signal**, and is not what's recorded in `Description` above, same "don't present a lower-confidence provenance as equivalent to a real structural signal" discipline already applied to Forged's Benefits title/description split (there: a colon-based text split with no font backing; here: a sentence-based text split with no list-marker backing — same category of caveat, different specific mechanism). "Example Codes:" itself is a structural bold label in the source (confirmed) — not carried into `Description`'s markup, consistent with how this catalogue already excludes other structural/section-heading bold (e.g. "Moves," "Then pick one of these:") from persisted option text.

**Instance-side note, flagged not resolved, carried from the schema doc**: a Hunter's answer for this Section is proposed to live on `HunterBespokeSelection.FreeformText` with `BespokeOptionId` null — the first expected exception to that FK's otherwise-universal required-ness. Not locked in; worth confirming when this Section is actually authored in Phase 4.

---

## The Hex

Two bespoke rulesets (PDF pages 27–30 — an unusually long spread for this playbook: 27 front, 28 back/standard, 29–30 a dedicated "Rotes (The Hex)" worksheet insert; all pages spot-checked with `pdftotext -raw` for the Forged-style column-order swap — clean, normal stream order throughout, no defect). Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Hex Special:" trigger), Harm, Experience, Gear (1 category, Wizardly weapons pick-2-of-6), Pronouns, Looks (2 categories), Introductions, History (8 prompts), Improvements (11, flat — includes "Take another Rote" ×3 and "Take another Hex move, or an additional Rote" ×2, both level-up references to Rotes' own growth mechanic, correctly out of scope here — logged instead as the direct evidence for Rotes' repeatable-Section shape below — plus "Take a Haven, like the Expert has, with two options," a cross-playbook reference, correctly out of scope), and Advanced Improvements (9, flat — includes "Gain another two Rotes," same out-of-scope precedent, plus a genuine playbook-specific fork, "Choose one advanced Hex move," between two named advanced moves, Apotheosis/Synthesis, both flat text, no new modeling needed) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You have three Hex moves. You have this one:" — **Bad Luck Charm** (`Required = true`) + "Plus pick two of these:" — 7 named options (Burn Everything, Cast the Bones, Force of Will, Luck of the Damned, Sympathetic Token, This Might Sting, Wise Soul), pick 2. `MoveGrantCount = 3` (1 required + pick 2 of 7), matching "three Hex moves" exactly. Checked explicitly for the Naked-City-style Move-internal pick trap — none present; no Move on this playbook has its own embedded sub-picks.

### Temptation

Clean Titled Choice, same shape as Background — confirmed to fit the standard framework exactly as Skyler expected going in, verified rather than assumed.

- **Section**: Title "Temptation".
  - **`Description`**: "You have a dangerous drive that you pursue, sometimes to the exclusion of your own safety. Decide if your Temptation drove you to learn magic, or if learning magic drove you to it." — the framing block.
  - **`EffectText`**: "Whenever you give in to your Temptation and act accordingly, you mark experience. You need to `<b>`act under pressure`</b>` to resist giving in to your temptation, if a perfect opportunity presents itself; if you fail this roll, you don't mark experience like you would have if you'd willingly acted out your desires." — the ongoing-play consequence block, same framing/consequence split criterion as everywhere else in this catalogue.
  - The literal "Choose one Temptation:" pick-instruction is dropped from free text, redundant with `MinSelect`/`MaxSelect`, same established precedent.
- **Select**: 1 of 7 — explicit ("Choose one Temptation").
- **Options** (title + description both populated, flat — no nesting):
  1. **Vengeance** — Use magic to inflict disproportionate retribution on someone who wronged you.
  2. **Power** — Use magic to exert your dominance over another.
  3. **Addiction** — Use magic to do what you could do without it.
  4. **Callousness** — Use magic without regard for the safety of others.
  5. **Carnage** — Use magic to inflict gruesome violence.
  6. **Secrets** — Use magic to discover forbidden, dangerous knowledge.
  7. **Glory** — Use magic to steal someone's thunder.

**Content fidelity: verified via the pipeline, finalized 2026-08-28** (`tools/pdf-extract/hex-review.json`). `EffectText` carries one bold cross-reference, `<b>act under pressure</b>` (the expected base-move pattern). All 7 options: bold titles (titles never persist markup, per convention), fully plain descriptions.

### Rotes

**New concept, approved by Skyler 2026-08-27 — a repeatable `BespokeSection` (`MinInstances`/`MaxInstances`), not a `BespokeJournal`.** Originally proposed as a `BespokeJournal` (matching Consumed Magic's growing-content shape); Skyler corrected this directly — Rotes has real, heavily-structured content (a genuine pick-2-of-5 Requirements list, 3 mandatory die-result-keyed Effect slots), unlike Consumed Magic's pure free-text fields. The only genuinely new thing is that a Hunter can end up with an open-ended, growing number of independent *instances* of that same structure. Full schema reasoning, the declined `IsRepeatable`-boolean and bare-instance-number alternatives, and the new `HunterBespokeSectionInstance` table: `phase5-bespoke-ideation.md` Section 3.

**Confirmed directly from the source, not assumed — a Hex does NOT start with a fixed number of Rotes.** Page 29's own "How many rotes?" sub-section: "You start out knowing up to one rote, which you can choose when creating your character or during play. You can learn more by taking improvements—when you do, you can choose the new rote right away or in play." Corroborated by page 28's Improvements ("Take another Rote" ×3, "Take another Hex move, or an additional Rote" ×2) and Advanced Improvements ("Gain another two Rotes").

- **Section**: Title "Rotes". `MinInstances = 0` (verbatim "up to one" — not a hard starting requirement), `MaxInstances = null` (unbounded — the worksheet's 8 printed blank Rote slots, confirmed by direct count across pages 29–30, are a page-space artifact, not a stated rule; no explicit cap found in the source).
  - **`Description`**: "Whenever you use magic, you can decide afterwards that a particular spell is a rote that you know. Write down in detail what the spell does, and what it requires. Give your new rote a name, and decide specifically what the requirements are (which words, gestures, objects, symbols, and procedures are required). Unlike regular `<b>`use magic`</b>`, a rote's cost and the consequences for failing it are known to you in advance. After you cast it for the first time, treat each rote as a custom move—a specialised version of use magic, which is built with the Keeper. Write down what it does on a 10+, a 7-9, and a miss. Also, a rote is a bit more powerful than a basic `<b>`use magic`</b>` spell: its glitches are less onerous and its effect may be a little bigger. Casting a rote requires you to have the needed items at hand and the ability to physically use them. You roll +Weird to cast it, as you would when `<b>`using magic`</b>` normally." — the literal "you choose two requirements from this list" enumeration instruction is dropped from free text; the 5 Requirements themselves live as real `BespokeOption` rows below, not restated in prose (same precedent as every other Section not repeating its own option content in `Description`).
  - **`EffectText`**: "You start out knowing up to one rote, which you can choose when creating your character or during play. You can learn more by taking improvements—when you do, you can choose the new rote right away or in play." — the "How many rotes?" text, per Skyler's own original proposal for this field; a clean fit, not contested.
- **Select**: 2 of 2 at the top level (Requirements and Effect both mandatory per instance).
- **Top-level options**:
  1. **Requirements** — select 2 of 5, title + description each (Titled Choice, same shape as Dual Nature's Benefits):
     - **Magic words, ritual gestures** — Magic words and ritual gestures.
     - **Object of power which must be wielded** — An object of power (wand, talisman, orb, staff, etc) which must be wielded.
     - **Expendable component destroyed or scattered** — An expendable component such as sulfur, sage, or incense, which must be burned, blown, or scattered during the casting.
     - **Runes or symbols written or engraved on a surface** — Runes or symbols written or engraved on a surface (which must be prepared).
     - **Spilling of blood (1-harm to you or willing person)** — A spilling of blood, which inflicts 1-harm upon you or a willing participant.
  2. **Effect** — select 3 of 3 (all mandatory), title populated, `DescriptionText` = the literal token `{{blank}}` alone on each (a new usage of the existing mechanism — previously always embedded in fixed sentence text or as one optional catch-all's full content; here it's the entire content of a mandatory option):
     - **10+**
     - **7-9**
     - **Miss**

**Instance-side note, per-Hunter free text**: a Rote's own name ("Give your new rote a name") lives on `HunterBespokeSectionInstance.Name`, not as a `BespokeOption` — confirmed per-repetition, per-Hunter data, not template content.

**Content fidelity: verified via the pipeline, finalized 2026-08-28** (`tools/pdf-extract/hex-review.json`). `Description` carries 4 bold spans across its assembled text: `<b>use magic</b>`/`<b>using magic</b>` (base-move cross-reference, matching the established pattern) appearing multiple times, though **not on every occurrence** — page 29's full explanation has 5 total occurrences of "use magic"/"using magic" across the page, only 4 bolded; captured exactly as found, not normalized to "always bold" or "never bold." The page 27 short blurb (not modeled above, since it's fully redundant with page 29's fuller version — "See the separate Rotes sheet for more details") also bolds "use magic" and, separately, "rote" on its own first mention — a **term-definition bold**, a different kind of bold usage than the established move-cross-reference pattern, noted for the record though not persisted (that blurb isn't part of the modeled `Description`). The 5 Requirements' long-form descriptions are a **real bulleted `<ul>` list** in the source (literal `•` markers) — confirmed, not assumed from shape; all 5 entirely plain. The short worksheet-form Requirement titles (repeated identically across all 8 printed worksheet slots) are also plain. Worksheet field labels: "Requirements, pick two:" and "Effect:" are bold (structural labels, not persisted into option text, same established treatment as "Moves"/"Then pick one of these:" elsewhere); "On a 10+"/"On a 7-9:"/"On a miss:" are plain; "Rote:" uses a distinct decorative display font (the same one used for playbook titles) rather than true bold or italic — correctly excluded from the `<b>`/`<i>` subset, flagged explicitly since it visually reads as "styled" without meeting either classification.

**A real extraction pitfall caught and hand-corrected this round, worth carrying forward.** "Use magic" repeats 3 times within one paragraph of page 29's explanation, bold on occurrences 1 and 3 but plain on occurrence 2 — Bowser's automated run initially matched the wrong (plain) occurrence, didn't trust it, verified by hand against the raw PDF item dump, and corrected it before this entry was built from the result. Recorded as a durable technique limitation (inconsistently-styled repeated phrases need manual verification, not just automated matching), not a one-off — worth watching for on any future playbook with the same shape (a phrase repeated several times with inconsistent styling across occurrences).

---

## The Host

One bespoke ruleset (PDF pages 31–32, confirmed directly via `pdftotext -f 31 -l 31`/`-f 32 -l 32` — verified this is the standard 2-page spread, not a Hex-style exception, before proceeding; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect). Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Host Special:" trigger), Harm, Experience, Gear (2 categories, Personal item pick-1-of-8 + Weapon pick-1-of-8), Pronouns, Looks (3 categories), Introductions, History (8 prompts), Improvements (12, flat, includes "Get a haven like the Expert has, with two options" — a cross-playbook reference, correctly out of scope — plus "Take another Host move," "Take another benefit from Symbiosis," and "Gain another option from defensive adaptation," all level-up references, correctly out of scope), and Advanced Improvements (10, flat, includes "Add a third option to your haven" and a second "Take another benefit from Symbiosis," same out-of-scope precedent) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all the basic moves, plus three Host moves. You get this one:" — **Defensive Adaptation** (`Required = true`) + "Then pick two of these:" — 6 named options (Mutual Survival, Balanced Neurochemistry, Open Your Mind, Predator and Prey, Collaborative Effort, Separation Anxiety). `MoveGrantCount = 3` (1 required + pick 2 of 6), matching "three Host moves" exactly. **Defensive Adaptation's own `DescriptionText` includes "Pick one:" over 6 named sub-options (Silk threads, Acid spray, Soothe thoughts, Scales, Camouflage, Whispered advice)** — same Move-internal pick-structure shape as Forged's Partner and Gumshoe's Naked City, flagged here for Phase 4 rather than modeled as a `BespokeSection`. (Checked Hex explicitly for the same trap last round — none was present there.) "Gain another option from defensive adaptation" (an Advanced Improvement) is the matching level-up reference, same out-of-scope treatment.

### Symbiosis

Clean 2-mandatory-category nested structure, same shape as Friendship/Overseers — both children Tag-Pick this time (title-only), rather than one Titled-Choice + one Tag-Pick.

- **Section**: Title "Symbiosis". `Description`: "Choose the Benefits and Downside of your symbiosis." No `EffectText` — confirmed directly from the raw item stream (not assumed from a plain-text read) that nothing sits between the Downsides list and "Moves"; the item immediately following the last Downside tag is "Moves" itself, in a different column.
- **Select**: 2 of 2 (both categories mandatory).
- **Top-level options**:
  1. **Benefits** — select 2 of 10, title-only: Aligned motivations, Unaging, Can survive vacuum, pressure, and lack of air, Immune to radiation, Disease, drug, and poison resistance, Subtle physical improvements, Can't be possessed, Can climb walls, Efficient metabolism, Non-physical symbiote.
  2. **Downsides** — select 1 of 7, title-only: Limited communication, Body timeshare, Biological needs, Personality conflicts, Obvious mutations, Distracting chatter, Magical aura.

**Judgment call, resolved from precedent, not escalated**: "Take another benefit from Symbiosis" (Improvements and Advanced Improvements both) does not imply Benefits needs the repeatable-Section mechanism from Hex's Rotes. That mechanism exists for *multiple independent instances of a whole nested tree* with colliding option-slot answers (Rote #1's "10+" text differs from Rote #2's). Benefits has no such collision risk — each option is pickable at most once ever; "gaining another" means picking one more *distinct* option from the same fixed 10-item pool, the same shape as "Take another Host move" growing the Moves pool beyond its creation-time `MoveGrantCount`. Neither has ever needed new schema in this project — Improvement-driven growth beyond a Section's/Move-pool's creation-time count is consistently an out-of-scope Leveling-Up concern.

**Content fidelity: verified via the pipeline, finalized 2026-08-28** (`tools/pdf-extract/host-symbiosis-review.json`). Entirely plain throughout — framing sentence, all 10 Benefits tags, and all 7 Downsides tags confirmed plain, programmatically, zero collisions. Only bold text in this column is the 3 structural section headings ("Symbiosis," "Benefits (pick two):," "Downsides (pick one):"). Zero italic anywhere.

---

## The Initiate

One bespoke ruleset (PDF pages 33–34, confirmed directly via `pdftotext -f 33 -l 33`/`-f 34 -l 34` — verified this is the standard 2-page spread before proceeding; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect). Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Initiate special:" trigger), Harm, Experience, Pronouns, Looks (2 categories), Introductions, History (8 prompts), Improvements (10, flat, includes "Take another Initiate move" ×2 and "Get command of your chapter of the Sect"/"Get a Sect team under your command" — level-up references to Sect, correctly out of scope), and Advanced Improvements (8, flat, includes "Become the leader, or effective leader, of the whole Sect," same out-of-scope precedent) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all of the basic moves, plus four Initiate moves. You get this one:" — one `Required` move, **unnamed in the source** (verified via both default and `-raw` extraction — no bold name precedes its description, unlike every other Required move catalogued so far) + "Then pick three of these:" — 8 named options (Ancient Fighting Arts, Mystic, Fortunes, Sacred Oath, Mentor, Apprentice, Helping Hand, That Old Black Magic). `MoveGrantCount = 4` (1 required + pick 3 of 8), matching "four Initiate moves" exactly. Checked explicitly for the Naked-City-style Move-internal-pick trap — none present. **Resolved 2026-08-28 — Skyler named it "One of Us."** Not a PDF-derived fact like every other name in this catalogue — flagging that distinction explicitly since this file's whole convention is source-transcribed content: "One of Us" is a Skyler-assigned name for a Move the source itself never names, not something printed on page 33. `PlaybookMove.Name` for this Move should be authored as `"One of Us"` in Phase 4.

**Flagged for Phase 4, not a bespoke-schema question**: page 33's Gear text is explicitly conditional on Sect's Traditions — "If your Sect has fighting arts or obsolete gear (see next page) then pick three old-fashioned weapons. If the Sect has modernised or nifty gadgets, you may pick two modern weapons. Otherwise, pick two old-fashioned weapons and one modern weapon." "Fighting arts"/"obsolete gear"/"modernised"/"nifty gadgets" are 4 of Sect's own Tradition options — a real mechanical dependency between a **standard** section's pick-count and a **bespoke** ruleset's picks, the first case of that direction (previously always a Move referencing bespoke content, e.g. Forged's Partner). No new schema needed for Sect itself; a flat fixed `PlaybookGearCategory.PickCount` won't capture this conditional logic on its own, so whoever authors this playbook's Gear categories in Phase 4 needs to read the conditional text carefully.

### Sect

Clean 2-mandatory-category nested structure, same shape as Symbiosis/Friendship/Overseers.

- **Section**: Title "Sect". `Description`: "You are part of an ancient, secret order that slays monsters. Where are they from? How old are they? Are they religious? Why do they stay secret? How do they recruit? You also need to pick the Sect's traditions (used by the Keeper to determine the Sect's methods and actions):" — the worldbuilding flavor questions stay in `Description` as framing (about the Sect itself, distinct from History's separate relationship prompts elsewhere on the same page). No `EffectText` — nothing found between the Bad Traditions list and "Introductions."
- **Select**: 2 of 2 (both categories mandatory). Checked explicitly for a repeatable-Section signal (per the standing discipline since Host) — none present: the two Improvements referencing Sect are narrative status upgrades ("Get command of your chapter," "Become the leader..."), not additional Tradition picks, so there's no instance-collision question to evaluate at all.
- **Top-level options** (both title-only, Tag Pick):
  1. **Good Traditions** — select 2 of 13: Knowledgable, Ancient lore, Magical lore, Fighting arts, Modernised, Chapters everywhere, Secular power, Flexible tactics, Open hierarchy, Integrated in society, Rich, Nifty gadgets, Magical items.
  2. **Bad Traditions** — select 1 of 12: Dubious motives, Tradition-bound, Short-sighted, Paranoid and secretive, Closed hierarchy, Factionalised, Strict laws, Mystical oaths, Total obedience, Tyrannical leaders, Obsolete gear, Poor.

**Content fidelity: verified via the pipeline, finalized 2026-08-28** (`tools/pdf-extract/initiate-sect-review.json`). Entirely plain throughout — the framing/flavor block and all 13 Good Traditions + all 12 Bad Traditions tags confirmed plain, programmatically. Only the 2 structural headings ("Sect," "Good Traditions (pick two):"/"Bad Traditions (pick one):" collectively counted as the section's own heading pair) are bold; zero italic anywhere. A false-positive bold match (the "Sect" section heading bleeding into the ordinary word "Sect" inside the body text) was caught and fixed before this result — the 4th instance of this exact class of bug across the pipeline's run (Curse-Eater, Envoy, Hex, now Initiate), now a standing checklist item on Bowser's side rather than a one-off. One minor styling quirk noted, not affecting the output: the "(pick two):"/"(pick one):" suffixes are separate plain runs from their bold heading phrases, book-wide.

---

## The Interface

One bespoke ruleset (PDF pages 35–36, confirmed directly via `pdftotext -f 35 -l 35`/`-f 36 -l 36` — verified this is the standard 2-page spread before proceeding; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect). Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Interface Special:" trigger), Harm, Experience, Gear (3 categories: Machines pick-3-of-7, Weapons pick-2-of-8 — including a cross-sourcebook reference, "Monster of the Week hardcover, page 101," recorded flat per established precedent — and Quality of life pick-2-of-5), Pronouns, Looks (3 categories), Introductions, History (8 prompts), Improvements (10, flat, includes "Add, remove, or change three tags on gear" ×2), and Advanced Improvements (**11**, flat — count corrected 2026-08-31 during Phase 8 authoring; this entry originally said 7 — includes "Add an Upgrade or change a Fault" — a level-up reference to Integration, correctly out of scope — "Get a haven like the Expert with workshop, supercomputer, and two other options" — a cross-playbook reference — and two cross-sourcebook tag references, "SSK page 188," all recorded flat) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all the basic moves, plus three Interface moves. Pick three of these:" — **no auto-grant sentence at all**, 6 named options (Technomancer, Expert Troll, Keep Going and Going, Take the Shot, Hack the Planet, Virus Whisperer). `Required = false` for all 6, `MoveGrantCount = 3`. Same already-validated zero-required shape as Covenant/Envoy/Expert/Flake. Technomancer and Virus Whisperer both have nested roll-outcome-gated effect lists — ordinary Move `DescriptionText` content, same shape as Fakelore/Action Scientist's nested lists, not the Naked-City-style Move-internal-pick trap (moot here regardless, since there's no Required move at all on this playbook).

### Integration

Clean 3-mandatory-category nested structure, same shape as Dual Nature — two Tag-Pick children plus one Titled-Choice child.

- **Section**: Title "Integration". `Description`: "Pick Upgrades and Faults to describe how you interact with machines." No `EffectText` — nothing sits between the last Origin option and "Moves."
- **Select**: 3 of 3 (all three categories mandatory). Checked explicitly for a repeatable-Section signal, per the standing discipline since Host — none present: "Add an Upgrade or change a Fault" (Advanced Improvements) is structurally identical to Symbiosis's "Take another benefit" — each Upgrade/Fault is pickable once from a small fixed pool, no per-instance answer collision.
- **Top-level options**:
  1. **Upgrades** — select 2 of 8, title-only: Always connected, Specialised tools, Security specialist, Intelligence database, Aim-assist, Heads-up display, Everything's compatible, Speed typing.
  2. **Faults** — select 2 of 7, title-only: Need my gear, Hackable brain, Specialised maintenance, Undisciplined, Overhyped rep, Overconfidence, Buggy implants.
  3. **Origin** *(synthesized title, not source-printed — flagged explicitly, same discipline as "One of Us")* — select 1 of 5, title + description (Titled Choice):
     - **We Can Rebuild Them** — You were rebuilt after a near-death, or actual death, experience. Who thought you were important enough to keep alive?
     - **Technopath** — You have a magical relationship with machines. How did you gain or discover your powers?
     - **Volunteer** — You volunteered to be upgraded. Who had the technology to do this to you?
     - **Skillz** — Sheer genius and hard work. What event made your reputation, and how does it cause you trouble?
     - **Artificial** — You were created this way. What was your original purpose? Have you transcended it?

  The source's only label for this category is a full sentence ("Then pick how you gained these abilities.") rather than a short header like "Upgrades (pick two):" — that sentence is dropped from free text (redundant with `MinSelect`/`MaxSelect=1/1`, matching how every other Section already omits restating its own pick-count in prose), and the option gets a short synthesized `Title` instead.

**Content fidelity: verified via the pipeline, finalized 2026-08-28** (`tools/pdf-extract/interface-integration-review.json`). Entirely plain throughout — framing sentence, transition sentence, all 8 Upgrades tags, all 7 Faults tags, and all 5 Origin option descriptions (bold titles only, per convention; plain description bodies, each ending in a narrative prompt question). Zero italic anywhere. A clean run — no new false-positive class this round, first-attempt confirmation that the established extraction technique set handles this shape correctly.

---

## The Monstrous

Three bespoke rulesets (PDF pages 37–38, confirmed directly via `pdftotext -f 37 -l 37`/`-f 38 -l 38` — standard 2-page spread; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect). Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Monstrous special:" trigger), Harm, Experience, Gear (1 category, optional, pick-1-of-8 handy weapon), Pronouns, Looks (3 categories), Introductions, History (8 prompts), Improvements (10, flat, includes "Take another Monstrous move" ×2, "Gain a haven, like the Expert has, with two options" — a cross-playbook reference, correctly out of scope — and "Take a natural attacks pick" — a level-up reference to Natural Attacks, correctly out of scope, checked explicitly and confirmed *not* a repeatable-Section signal, same ordinary fixed-pool-growth shape as Symbiosis's Benefits), and Advanced Improvements (9, flat, includes "Free yourself from the curse of your kind. Your curse no longer applies, but you lose 1 Weird." — a level-up reference to Curses, correctly out of scope — and "You turn evil (again). Retire this character, they become one of the Keeper's threats.," flat narrative text) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Scope-boundary check, resolved — "Moves" here is the standard Move-granting mechanism, not a bespoke ruleset, despite being narratively described as one of "three things that define your breed."** "You get all the basic moves, plus pick two Monstrous moves:" — no auto-grant sentence, 12 named options (Immortal, Unnatural Appeal, Unholy Strength, Incorporeal, Preternatural Speed, Claws of the Beast, Mental Dominion, Unquenchable Vitality, Dark Negotiator, Flight, Shapeshifter, Something Borrowed), all `Required = false`, `MoveGrantCount = 2`. Structurally and verbally identical to every other playbook's standard Moves section — stays out of this catalogue, same as everywhere else. Skyler's "2 custom blank Move slots" idea is redirected to a `PlaybookMove`-schema flag for Phase 4, not modeled here. Checked explicitly for the Naked-City-style Move-internal-pick trap — none present.

### Monster Breed

**New 4th Section, approved by Skyler 2026-08-28 — a zero-option `BespokeSection` (Covenant/Secret-Wisdom precedent), added specifically to give the intro flavor text and the "Monster Breed Suggestions" reference list a home, without reintroducing the umbrella-with-3-nested-mandatory-categories structure Skyler explicitly declined for Curses/Natural Attacks/Moves.** Curses and Natural Attacks remain fully independent Sections with no structural relationship to this one or each other — this Section exists purely for shared thematic framing content that doesn't belong to either specific pick-list.

- **Section**: Title "Monster Breed". `MinSelect`/`MaxSelect` both `null` — zero `BespokeOption` rows, same representation as Covenant.
  - **`Description`**: "You're half-human, half-monster: decide if you were always this way or if you were originally human and transformed somehow. Now decide if you were always fighting to be good, or if you were evil and changed sides. Define your monstrous breed by picking a curse, moves, and natural attacks. Create the monster you want to be: whatever you choose defines your breed in the game. Some classic monsters with suggestions for picks are listed on the back of this sheet. These are `<i>`only`</i>` suggestions: feel free to make a different version!" — one word fixed on sight: the source has a genuine duplicated-word typo ("if you *you* were originally"), confirmed present in both default and `-layout` extraction (not a pdftotext column-boundary artifact — it sits mid-sentence, nowhere near a column edge), treated the same as a line-wrap artifact rather than preserved literally. Bowser confirmed the typo verbatim in its own raw extraction (per instruction, unaltered) — the correction is a catalogue-side content-fidelity call, not a pipeline finding.
  - **`EffectText`**: the full "Monster Breed Suggestions" list, verbatim — each entry a bold archetype name followed by 3 italicized field sub-labels ("Curse"/"Natural attacks"/"Moves"), confirmed programmatically across all 7:
    - `<b>`Vampire`</b>`: `<i>`Curse`</i>`: feed (blood or life-force). `<i>`Natural attacks`</i>`: Base: life-drain or Base: teeth; add +1 harm to base attack. `<i>`Moves`</i>`: immortal or unquenchable vitality; mental domination.
    - `<b>`Werewolf`</b>`: `<i>`Curse`</i>`: vulnerability (silver). `<i>`Natural attacks`</i>`: Base: claws; Base: teeth. `<i>`Moves`</i>`: shapeshifter (wolf and/or wolfman); claws of the beast or unholy strength.
    - `<b>`Ghost`</b>`: `<i>`Curse`</i>`: vulnerability (rock salt). `<i>`Natural attacks`</i>`: Base: magical force; add hand range to magical force. `<i>`Moves`</i>`: incorporeal; immortal.
    - `<b>`Faerie`</b>`: `<i>`Curse`</i>`: pure drive (joy). `<i>`Natural attacks`</i>`: Base: magical force; add ignore-armour to magical force. `<i>`Moves`</i>`: flight; preternatural speed.
    - `<b>`Demon`</b>`: `<i>`Curse`</i>`: pure drive (cruelty). `<i>`Natural attacks`</i>`: Base: claws; +1 harm to claws. `<i>`Moves`</i>`: dark negotiator; unquenchable vitality.
    - `<b>`Orc`</b>`: `<i>`Curse`</i>`: dark master (the orc overlord). `<i>`Natural attacks`</i>`: Base: teeth; add ignore-armour to teeth. `<i>`Moves`</i>`: Unholy strength; dark negotiator.
    - `<b>`Zombie`</b>`: `<i>`Curse`</i>`: pure drive (hunger), feed (flesh or brains). `<i>`Natural attacks`</i>`: Base: teeth; +1 harm to teeth. `<i>`Moves`</i>`: immortal; unquenchable vitality.

**Content fidelity: verified via the pipeline, finalized 2026-08-28** (`tools/pdf-extract/monstrous-review.json`). Intro carries one italic span ("only"). Suggestions list carries a new formatting pattern for this pipeline — italicized structural sub-labels inline within an otherwise plain summary line, confirmed identical across all 7 entries — plus bold archetype names (inline markup within `EffectText`'s own free text, not a `Title` field, so the "titles never persist markup" convention doesn't apply here). Bowser hit and fixed a second occurrence of the cross-call-run-leakage bug class first found on Hex while extracting this block.

### Curses

**Source-fidelity fix, approved by Skyler 2026-08-28 — the trailing comma in "Curses, pick one:" dropped, `Title`/`Description` split to match this catalogue's normal pattern.** Verified the comma is genuinely present in the source before applying the fix (matching Underworld's "(choose one)" normalization precedent).

- **Section**: Title "Curses" (comma dropped). `Description`: "Pick one." — a minimal, literal relocation of the fragment rather than invented flavor text; there's no other framing sentence in the source to draw from. Confirmed by Bowser: the exact source heading is "Curses, pick one:" (bold, structural) with no separate sentence beyond it — nothing richer was left uncaptured.
- **Select**: 1 of 5 (4 fixed options + 1 custom slot).
- **Options** (title + description both populated, flat):
  1. **Feed** — You must subsist on living humans—it might take the form of blood, brains, or spiritual essence but it must be from people. You need to `<b>`act under pressure`</b>` to resist feeding whenever a perfect opportunity presents itself.
  2. **Vulnerability** — Pick a substance. You suffer +1 harm when you suffer harm from it. If you are bound or surrounded by it, you must `<b>`act under pressure`</b>` to use your powers.
  3. **Pure Drive** — One emotion rules you. Whenever you have a chance to indulge that emotion, you must do so immediately, or `<b>`act under pressure`</b>` to resist.
     - **Nested child pick — `MinSelect=1`/`MaxSelect=1`, 11 title-only children** *(remodeled 2026-08-30, see the correction note below — was previously recorded as prose inside this option's own `DescriptionText`)*: hunger / hate / anger / fear / jealousy / greed / joy / pride / envy / lust / cruelty. The literal instruction "Pick from:" is dropped from `DescriptionText`, redundant with the counts now carried structurally — matching how every other Section omits restating its own pick-count in prose.

**Correction, 2026-08-30 — Pure Drive's nested pick was missed on the original pass and is now modeled.** The source reads "One emotion rules you. **Pick from:** hunger, hate, anger, fear, jealousy, greed, joy, pride, envy, lust, or cruelty." — a genuine creation-time pick-1-of-11 nested one level inside a `BespokeOption`, structurally identical to Crooked's Underworld sub-picks (a top-level option with its own nested pick-1-of-N children) and needing **no new schema**. It was originally transcribed as prose because the extraction tooling of the time could only see bulleted lists, and this option's list is an inline comma run inside a sentence — so it read as ordinary description text. Found by Bowser's inline-pick sweep (2026-08-30) once the tooling could detect inline runs; verified against page 37 directly before remodeling. **This is the only genuine content gap the full 28-playbook sweep turned up** — see the catalogue status note at the top of this file.
  4. **Dark Master** — You have an evil lord who doesn't know you changed sides. They still give you orders, and they do not tolerate refusal. Or failure. *(no bold beyond the title — confirmed, not assumed)*
  5. **`{{blank}}`** — `{{blank}}` (custom slot, per Skyler's request — both `Title` and `DescriptionText` are the blank token, reusing the existing per-field mechanism combined on one option, matching the pick-count of 1).

**Custom-slot mechanism decision, resolved 2026-08-28 — plain `BespokeOption`, not `BespokeJournal`.** Checked Improvements/Advanced Improvements directly: no "take another curse" growth mechanic exists (only "Free yourself from the curse," which *removes* it). Curses is picked exactly once, from a pool, at creation — the same one-time-pick signal that ruled `BespokeJournal` in for nothing and out for Rotes. No new schema. The "needs a text-area, not a single-line input" detail is treated as a pure Phase 10 UI concern, not a schema-level flag — `DescriptionText` has never distinguished expected content length anywhere in this schema (Background's descriptions are already multi-sentence prose with no special flag), so a blank inside `DescriptionText` rendering as a textarea is a reasonable frontend default derivable from which field it's in, needing no new column.

**Content fidelity: verified via the pipeline, finalized 2026-08-28** (`tools/pdf-extract/monstrous-review.json`). All 4 options have bold titles (per convention) and a `<b>act under pressure</b>` cross-reference each, except Dark Master, which has none beyond its title. Two line-wrap artifacts fixed on sight, same established discipline as prior playbooks: "pres- sure" → "pressure" (Feed), "sur- rounded" → "surrounded" (Vulnerability). **A new generalizable extraction bug found and fixed this round**: 3 of the 4 options have a colon-free bold title immediately followed by a plain-styled run starting with the leading ": " — the mirror image of Haven's leading-period bug (Ninth pass, `pdf-extraction-pipeline.md`) — fixed generically in the extraction script and verified as a no-op against all 16 previously-processed playbooks.

### Natural Attacks

Confirmed the Base/Extra category structure exists in the source exactly as Skyler's proposal assumed.

- **Section**: Title "Natural Attacks". `Description`: "`<i>`Pick a Base and add an extra to it, or two Bases.`</i>`" — kept verbatim (not dropped as a redundant pick-instruction) since it's the only place the real either/or rule is stated; confirmed entirely italicized, the whole sentence.
- **Select**: 2 of 2 at the top level (both categories present — see the unenforced-relationship note below).
- **Top-level options**:
  1. **Base** — `MinSelect=1, MaxSelect=2`, description-only, flat (Simple Choice shape — corrected from an earlier draft's "title + description": confirmed no font-derived title signal exists, `Title` is `null` and the whole line, including the "Base:" label, stays in one continuous `DescriptionText`, same treatment as Forged's Benefits): "Base: teeth (3-harm intimate)", "Base: claws (2-harm hand)", "Base: magical force (1-harm magical close)", "Base: life-drain (1-harm intimate life-drain)" — **plus two custom slots** (`Title`/`DescriptionText` both `{{blank}}`, per Skyler's request — two, not one, since up to 2 Bases can be picked simultaneously and a single shared "something else" option could only ever be selected once).
  2. **Extra** — `MinSelect=0, MaxSelect=1`, description-only, same flat shape: "Extra: Add +1 harm to a base", "Extra: Add ignore-armour to a base", "Extra: Add an extra range to a base (add intimate, hand, or close)." — **plus one custom slot**.

**Either/or relationship — resolved by Skyler 2026-08-28: leave unenforced, stated in prose only, no new schema.** The real rule ("1 Base + 1 Extra, OR 2 Bases + 0 Extra") isn't strictly representable by independent per-category `MinSelect`/`MaxSelect` bounds — `Base(1-2)`/`Extra(0-1)` as modeled would also technically permit the invalid combo "2 Bases + 1 Extra." Skyler confirmed this gap is acceptable: the rule stays faithfully written in the Section's own `Description`, not mechanically validated — same category of accepted, hand-reviewable gap as Fate's messy source layout, not a new schema mechanism.

**Checked explicitly for a repeatable-Section signal, per the standing discipline since Host** — none needed. "Take a natural attacks pick" (Improvements) is ordinary fixed-pool growth, the same shape as Symbiosis's Benefits, not Rotes' instance-collision case.

**Content fidelity: verified via the pipeline, finalized 2026-08-28** (`tools/pdf-extract/monstrous-review.json`). Framing sentence entirely italicized (the whole sentence, not a partial span). All 4 Base and all 3 Extra descriptions confirmed entirely plain — zero bold/italic, and zero font-derived title signal, matching the Forged Benefits precedent exactly rather than being assumed from that resemblance.

---

## The Mundane

No bespoke ruleset found — confirmed by reading both pages in full (PDF pages 39–40, verified directly via `pdftotext -f 39 -l 39`/`-f 40 -l 40`; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect). Skyler's own expectation going in ("like The Flake, no bespoke rulesets, just additional Move selections") checked directly rather than taken on trust, same rigor as every prior zero-bespoke confirmation — confirmed correct. Every section (Ratings, Luck — including the standard `Playbook.LuckSpecialText`-shaped "Mundane special:" trigger, Harm, Experience, Moves, Gear, Getting Started, Pronouns, Looks, Introductions, History, Improvements, Advanced Improvements) is standard-section content fitting the Phase 2 model as-is. Getting Started's own wording is the clearest signal: "decide your look, ratings, moves, and gear" — no third named component, unlike every other recent playbook's Getting Started text (compare Monstrous's "...breed, moves, and gear," Interface's "...integration, moves..."). No cross-playbook or bespoke-adjacent references found in Improvements/Advanced Improvements either (both repeat "Get back one used Luck point" ×2, otherwise entirely flat and generic).

**Moves, checked explicitly for `Required` vs. pick-N, per the standing convention**: "You get all the basic moves, plus pick three Mundane moves:" — no auto-grant sentence, all 8 named options optional (Always The Victim, Oops!, Let's Get Out Of Here!, Panic Button, The Power of Heart, Trust Me, What Could Go Wrong?, Don't Worry, I'll Check It Out). `Required = false` for all 8, `MoveGrantCount = 3`. Same already-validated zero-required shape as Covenant/Envoy/Expert/Flake/Interface. Checked explicitly for the Naked-City-style Move-internal-pick trap — none present (Trust Me and What Could Go Wrong? have ordinary conditional roll-outcome/hold-spend content, not a fixed permanent sub-pick) — noted for Phase 4, not acted on now.

---

## The Pararomantic

Three bespoke `BespokeSection`s plus one `PlaybookExtraTrack` (PDF pages 41–42, confirmed directly via `pdftotext -f 41 -l 41`/`-f 42 -l 42` — standard 2-page spread; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect). Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Pararomantic Special:" trigger, fully standard and unchanged — coexists with, doesn't get replaced by, Relationship Status below), Harm, Experience, Gear (the ordinary weapon/vehicle portion — "Choose two of these normal things," pick 2 of 6), Pronouns, Looks (2 categories), Introductions, History (7 prompts), Improvements (9, flat, includes "Gain an ally," a bespoke grant no different in kind from other playbooks' one-off Improvement items), and Advanced Improvements (10, flat, includes "Take a second gift from your guide or add another property to an existing gift" and "Erase all the marks from the Relationship Status track" — level-up references to Guide's Gift and Relationship Status, correctly out of scope, plus "Change your Guide, as you fall for another creature" and "Gain a second Guide..." — flat narrative text) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Structural correction to a stakeholder's own restructuring proposal, resolved 2026-08-28 — not a rubber stamp.** Skyler's original suggestion was one merged bespoke Section ("Supernatural Guide") holding both Relationship Status and the Gear-embedded gift picks. Verified directly and pushed back on two points, both accepted: (1) "Supernatural Guide" is already the playbook's own Required Move name — reusing it for a Section would create a real naming collision, so the relocated Gear content is named "Guide's Gift" instead. (2) Relationship Status is a labeled box-track ("Loving `bbbbbbb` Broken"), structurally identical to Luck/Harm/Corruption, not a pick-list — it doesn't fit `BespokeSection` at all and was pulled out as its own `PlaybookExtraTrack`, not folded into any bespoke Section, merged or otherwise.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all of the basic moves, plus three Pararomantic moves. You get this one:" — **Supernatural Guide** (`Required = true`) + "Then pick two of these:" — 7 named options (Bonding Time, Dark Desires, The Power of Love, Do As The Supernatural Do, I Am Theirs And They Are Mine, Monster Empathy, Spirit Touched). `MoveGrantCount = 3` (1 required + pick 2 of 7), matching "three Pararomantic moves" exactly. Checked Supernatural Guide's own text explicitly for the Naked-City-style Move-internal-pick trap — none present (its "choose if secret or not"/"determine what kind of creature" content is freeform narrative authoring, not an enumerated fixed list). Skyler's own "encoded in a single column but visually split into two" observation is a print-layout detail, confirmed not to affect the logical Moves sequence either way — noted for Phase 4 alongside Curse-Eater's/Forged's existing dual-column notes.

### Relationship Status

**New field, `PlaybookExtraTrack.StartLabel`, approved by Skyler 2026-08-28 — see `architecture.md`'s "Extra Tracks" section for the full schema addition and reasoning.** The first track whose start label carries real thematic meaning rather than the universal "Okay" default every prior track (Luck, Harm, Corruption) has used.

- **Track**: Name "Relationship Status". `BoxCount`: 7. `StartLabel`: "Loving". `EndLabel`: "Broken".
- **`Description`**/**`EffectText`**: both `null` — no dedicated explanatory paragraph exists for this track in the source beyond its own bare header + box-track; the two mechanics that *trigger* it (Luck's spend-trigger, Fate of Your Love's Luck-tied unraveling) live in their own respective places, not attributed here.

**Content fidelity: verified via the pipeline, finalized 2026-08-28** (`tools/pdf-extract/pararomantic-review.json`). Both "Loving" and "Broken" confirmed entirely plain, programmatically — checked directly rather than assumed plain just because most short tags in this pipeline turn out that way.

### Guide's Gift

Relocated from Gear, per Skyler's original instinct — confirmed as genuine bespoke content (real narrative/mechanical weight, not mundane equipment) sitting inside a standard section's own text.

- **Section**: Title "Guide's Gift". `Description`: "And choose one gift from your Guide. Whatever gift you choose, it must be periodically recharged by interactions with your Guide."
- **Select**: 1 of 4 — explicit ("choose one gift").
- **Options** (title + description both populated):
  1. **Part of their body** — Part of their body, e.g. a vial of blood, lock of hair, tears in a phial, fairy dust. It either helps you heal, or counts as a weakness against a specific sort of monster.
  2. **Piece of jewelery** — Piece of jewelery, e.g. a golden ring, tribal pendant, silver locket. It either provides 1-armour magic or protects you from the powers of a specific sort of monster.
  3. **A memento of the time when they were human** — A memento of the time when they were human, e.g. a portrait/photo from life, diary, favourite hat, doll, or lighter. Provides +1 on `<b>`bond abuse`</b>` rolls.
  4. **A strange or antique weapon** — A strange or antique weapon, e.g. a family sword, ancient staff, holy mace (2-harm hand messy). Additionally, add "magic," "silver," "holy," or "iron" to the weapon.

**Content fidelity: verified via the pipeline, finalized 2026-08-28** (`tools/pdf-extract/pararomantic-review.json`). One bold cross-reference found: `<b>bond abuse</b>` in the memento option — a back-reference to this playbook's own Bond Abuse ruleset (not a base-move reference, a new sub-pattern of the established cross-reference-bolding convention: bolding can point at another bespoke ruleset on the same playbook, not only at Moves).

**Title provenance — a genuinely lower-confidence case than Forged's Benefits, recorded distinctly rather than reusing that caveat's language.** Benefits at least had a colon delimiter the extraction script could split on. These 4 options have **no delimiter of any kind** in the source — no colon, no font signal, nothing. The titles above (short item names like "Part of their body") are **externally supplied labels, assigned during this catalogue's own authoring, not derived from any PDF signal whatsoever** — `DescriptionText` is the full, unsplit sentence exactly as extracted. If `BespokeOption.Title` is ever needed as a clean short label distinct from the full description, treat this as manually-authored content requiring the same review a fresh authoring pass would get, not as a verified transcription.

### Bond Abuse

Zero-option `BespokeSection` (Covenant/Secret Wisdom precedent) — pure ongoing-mechanic prose, no creation-time pick to introduce or follow up on, so `EffectText` stays null and everything sits in `Description`.

- **Section**: Title "Bond Abuse". `MinSelect`/`MaxSelect` both `null` — zero `BespokeOption` rows.
- **`Description`**: "For details, see `<b>`fate of your love`</b>` on the reverse side. `<b>`Bond Abuse`</b>`: Whenever you do something to abuse the bond with your Guide, like acting against them, or not giving them enough attention, roll +Charm:" followed by a nested list (`<ul><li>On 10+, your action has no effect beyond the exchange of some angry words.</li><li>On 7-9 choose one of the bond abuse options below.</li><li>On a miss, the Keeper may choose two of the bond abuse options below, or something worse.</li></ul>`), then "`<b>`Bond abuse options:`</b>`" followed by a second nested list (`<ul><li>Mark off a box in your Relationship Status track.</li><li>You may not channel the power of your bond for the rest of the mystery.</li><li>You gain the unwelcome attention of others of your Guide's kind.</li><li>Someone who shouldn't know finds a clue about your relationship with your Guide.</li></ul>`).

**Content fidelity: verified via the pipeline, finalized 2026-08-28** (`tools/pdf-extract/pararomantic-review.json`). A genuinely new shape for this pipeline — flowing prose containing two real nested `<ul>` lists but zero top-level bulleted entries (unlike every prior Moves-shaped extraction, which always had at least one top-level bullet to key off) — required a new generic fallback path in the extraction tooling rather than a scoping workaround, verified purely additive against a full regression of every prior playbook. Two bold spans found: `<b>fate of your love</b>` (a cross-reference to this playbook's own Fate of Your Love ruleset — the same cross-ruleset-bolding pattern just confirmed on Guide's Gift) and term-definition bold on `<b>Bond Abuse</b>`/`<b>Bond abuse options</b>` themselves (structural labels within the flowing prose, kept — unlike a Section's own page-level heading, these sit inline within the body text being transcribed, not a separate Title field, so the "structural headings don't persist markup" exclusion doesn't apply here).

### Fate of Your Love

Zero-option `BespokeSection`, same shape as Bond Abuse — pure ongoing-mechanic/narrative prose, `EffectText` null.

- **Section**: Title "Fate of Your Love". `MinSelect`/`MaxSelect` both `null` — zero `BespokeOption` rows.
- **`Description`**: "There is a reason why your love with your Guide (the supernatural being you have a connection with) is forbidden or doomed. Invent this reason or leave it to the Keeper. Whenever you mark off a point of Luck, the truth of your love's Fate slowly unfolds: mark off a box in your Relationship Status track. When your last point of Luck is used it becomes clear you cannot be together. Your Guide might even end up as an enemy, depending on how things go. Whenever you put yourself or somebody else in serious danger in order to conceal the truth about you and your Guide, mark experience."

**Content fidelity: verified via the pipeline, finalized 2026-08-28** (`tools/pdf-extract/pararomantic-review.json`). Entirely plain, confirmed programmatically — zero bold/italic. One line-wrap artifact fixed on sight, same established discipline as Monstrous's "pres- sure": "is for- bidden" → "is forbidden." Source has a real paragraph break mid-block (before "Whenever you put yourself..."), collapsed to a single space in the stored text — the same flat-text convention already used for every other multi-paragraph block in this catalogue, not treated as a structured paragraph-array field.

---

## The Professional

One bespoke ruleset (PDF pages 43–44, confirmed directly via `pdftotext -f 43 -l 43`/`-f 44 -l 44` — standard 2-page spread; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect). Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Professional special:" trigger), Harm, Experience, Gear (3 categories: a fixed armor-choice grant plus Serious weapons pick-1-of-5 and Normal weapons pick-2-of-5), Pronouns, Looks (2 categories), Introductions, History (8 prompts), Improvements (10, flat, includes "Add a new resource tag for your Agency or change a red tape tag" and "Get command of an Agency team of monster hunters" — level-up references to Agency, correctly out of scope, checked explicitly and confirmed *not* a repeatable-Section signal, same ordinary fixed-pool-growth shape as Symbiosis/Sect), and Advanced Improvements (8, flat, includes "Get some or all of the other players' hunters hired by your agency. They get the deal with the agency move..." — a flat narrative reference to the Required Move's own thematic content) — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all the basic moves, plus four Professional moves. Pick three of these:" — 7 named options (Bottle It Up, Unfazeable, Battlefield Awareness, Leave No One Behind, Tactical Genius, Medic, Mobility), pick 3, plus "And you get this one:" — one `Required` move. `MoveGrantCount = 4` (3 picked + 1 required), matching "four Professional moves" exactly.

1. **The Required Move is unnamed in the source — the second such case in this project, after The Initiate's "One of Us."** "B When you deal with the Agency, requesting help or gear, or making excuses for a failure, roll +Sharp..." — no bold name precedes it, verified via both default and `-raw` extraction. **Resolved 2026-08-29 — Skyler named it "Agency politics."** Not a PDF-derived fact like every other name in this catalogue — flagging that distinction explicitly, same as "One of Us": "Agency politics" is a Skyler-assigned name for a Move the source itself never names, not something printed on page 44. `PlaybookMove.Name` for this Move should be authored as `"Agency politics"` in Phase 4.
2. **The Move-internal-pick trap showed up on an *optional* move for the first time**, not a Required one. **Mobility** (one of the 7 pick-3 pool options) has its own embedded pick structure: "Choose two good things and one bad thing about it. Good things: [14 named traits]. Bad things: [8 named traits]." Same treatment as every prior instance (Forged's Partner, Gumshoe's Naked City, Host's Defensive Adaptation) applies regardless of Required-vs-optional — Move content, correctly excluded, flagged for Phase 4 only.

### Agency

Clean 2-mandatory-category Tag Pick structure at first read (matching Symbiosis/Sect), **extended to 3 categories after Skyler caught a real gap**: the Section's own `Description` asks "Decide who it is you work for," but nothing in the original 2-category structure gave that answer anywhere to live.

- **Section**: Title "Agency". `Description`: "Decide who it is you work for. Are they a black-budget government department, a secret military unit, a clandestine police team, a private individual's crusade, a corporation, a scientific team, or what? Is the Agency's goal to: destroy monsters, study the supernatural, protect people, gain power, or something else? Pick two resource tags for the Agency, and two red tape tags:" — the worldbuilding flavor questions stay in `Description` as framing, same treatment as Sect's own intro questions. No `EffectText` — nothing found after the Red Tape list before "Introductions."
- **Select**: 3 of 3 (all three categories mandatory, per Skyler's fix — bumped from 2 of 2).
- **Top-level options**:
  1. **Resources** — select 2 of 10, title-only: Well-armed, Well-financed, Rigorous training, Official pull, Cover identities, Offices all over, Good intel, Recognised authority, Weird tech gadgets, Support teams.
  2. **Red Tape** — select 2 of 9, title-only: Dubious motives, Bureaucratic, Secretive hierarchy, Cryptic missions, Hostile superiors, Inter-departmental rivalry, Budget cuts, Take no prisoners policy, Live capture policy.
  3. **Agency name:** *(synthesized, not source-printed — Skyler's own fix for the gap above, flagged explicitly per the "One of Us"/"Origin" discipline)* — `MinSelect=1, MaxSelect=1`, exactly one child option: `Title = null`, `DescriptionText = "{{blank}}"`.

**Field-placement call for the "Agency name:" child, stated explicitly per the standing discipline for every `{{blank}}` decision**: `{{blank}}` goes in `DescriptionText` only, `Title` stays `null` — not both, unlike Monstrous's Curses/Natural-Attacks custom slots (`Title`/`DescriptionText` both `{{blank}}`). The difference: Curses' custom slot sits as one of several *sibling* options in a flat pick-list with no parent label to lean on, so it needed its own blank `Title` to be identifiable in a picker UI alongside Feed/Vulnerability/etc. "Agency name:"'s single child has no siblings (`MaxSelect=1` of exactly 1 child) and its parent option already carries the label — the UI renders "Agency name: [text input]" directly from the parent's own `Title`, with the child's `DescriptionText` blank triggering the input. Same reasoning as Dual Nature's Effect children, just the mirror case: there, each of 3 children needed its own `Title` (10+/7-9/Miss) to distinguish itself from siblings while sharing the blank-`DescriptionText` mechanism; here, there's exactly one child with nothing to distinguish itself from, so no `Title` is needed at all.

**Content fidelity: verified via the pipeline, finalized 2026-08-29** (`tools/pdf-extract/professional-agency-review.json`). Framing block, all 10 Resources tags, and all 9 Red Tape tags confirmed entirely plain, programmatically — zero collisions. Only the "Resources"/"Red Tape" structural headings are bold (their own "(pick two):" suffixes are separate plain runs, same minor stylistic detail already seen on Initiate's Sect). A false-positive bold match (the "Agency" section heading bleeding into the ordinary word "Agency" inside the framing text) was caught and fixed before this result — the 5th instance of this exact class of bug (Curse-Eater, Envoy, Hex, Initiate, now Professional). Zero italic anywhere.

**Flagged separately for the coordinator/Skyler, not acted on here**: this same gap — a Section's `Description` asking a "who/what/where" worldbuilding question with no structural place recording the answer — likely exists on other already-catalogued playbooks too (Sect's "Where are they from? How old are they? Are they religious?", Task's narrative prompts, Origin's "who thought you were important enough to keep alive?" questions, and others). Skyler asked for this fix on Agency specifically, having noticed it there — not a decision to retrofit it everywhere. Not retrofitting anything else without a direct instruction to do so, same as `EffectText`'s retroactive review only happened after Skyler explicitly asked for one.

---

## The Searcher

No bespoke ruleset found outside of Moves — confirmed by reading both pages in full (PDF pages 45–46, verified directly via `pdftotext -f 45 -l 45`/`-f 46 -l 46`; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect). Every standard section (Ratings, Luck — including the standard `Playbook.LuckSpecialText`-shaped "Searcher Special:" trigger, Harm, Experience, Gear — a fixed grant [laptop, car/motorcycle, camera, binoculars, two investigation-tool sets, one self-defence weapon] plus Investigation tools pick-2-of-**7** (count corrected 2026-08-31; this entry said 6) and Self-defence weapons pick-1-of-6, Pronouns, Looks [2 categories], Getting Started, Introductions, History [7 prompts], Improvements [10, flat, includes "Take a second first encounter move, based on a recent mystery" and "Take another Searcher move" ×2 — level-up references to Moves/First Encounter, correctly out of scope, checked explicitly and confirmed *not* a repeatable-Section signal, same ordinary fixed-pool-growth shape as Symbiosis/Sect/Agency], and Advanced Improvements [9, flat, includes "Resolve your first encounter. The Keeper makes the next mystery about this event..." — a level-up narrative reference to First Encounter, correctly out of scope]) is standard-section content fitting the Phase 2 model as-is.

**The Required Move's name, "First Encounter," is confirmed rather than re-derived** — carried over from an earlier verification pass (the Searcher Special's trigger text, the move's own bold header, and the Getting Started summary line all independently named it; re-confirmed again here directly against page 45's bold header "`B First Encounter:`").

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all of the basic moves, plus three Searcher moves. You get this one:" — **First Encounter** (`Required = true`) + "Then pick two of these:" 7 named options (Prepared to Defend, Fellow Believer, Guardian, Just Another Day, Network, Ockham's Broadsword, The Things I've Seen). `MoveGrantCount = 3` (1 required + pick 2 of 7), matching "three Searcher moves" exactly.

**Move-internal-pick trap confirmed on this playbook's own Required Move — the fourth Required-Move instance (after Forged's Partner, Gumshoe's Naked City, Host's Defensive Adaptation), and fifth overall counting Professional's optional-move case.** First Encounter's own description reads: "One strange event started you down this path... Decide what that event was: pick a category below and take the associated move." Seven named categories follow, each with its own embedded "associated move" description text (Cryptid Sighting, Zone of Strangeness, Psychic Event, Higher power, Strange Dangers, Abduction, Cosmic Insight) — structurally identical to every prior instance of this trap: a fixed fully-enumerated pick-list living entirely inside one Move's own text, granting a specific mechanical effect per pick. Same treatment as every prior case regardless of Required-vs-optional status — Move content, correctly excluded from this catalogue's scope, flagged for Phase 4 authoring only, not modeled here as a `BespokeSection`.

**Content fidelity/formatting pipeline not run — out of scope, per established precedent.** Since every candidate bespoke-looking structure on this playbook (First Encounter's category list, the pick-2 pool) is Move content, not catalogue-scope content, no `tools/pdf-extract` dispatch was made for The Searcher — same treatment as Forged/Gumshoe/Host/Professional, whose own Move-internal-pick instances were flagged and excluded without a formatting pass, reserving that work for whoever authors Moves in Phase 4.

---

## The Snoop

Two bespoke rulesets (PDF pages 47–48, confirmed directly via `pdftotext -f 47 -l 47`/`-f 48 -l 48`; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect), both modeled per Skyler's own explicit framing rather than derived independently. **Skyler's exact words**: "The Snoop does have a bespoke ruleset, but it's instructions-only (Crew). The other deviation from the typical playbook sections is 'Team Concept: Monster Revelations' which isn't actually a bespoke ruleset, just a suggestion. Add this in as another instructions-only ruleset just to track the copy." Everything else — Ratings, Luck (standard `Playbook.LuckSpecialText`-shaped "Snoop special:" trigger), Harm, Experience, Gear (a fixed grant [laptop, three recording devices, two detectors, one subtle weapon] plus Recording devices pick-3-of-14 [13 named + 1 custom blank slot], Detectors pick-2-of-13 [12 named + 1 custom blank slot], and Subtle weapons pick-1-of-5), Pronouns, Looks [2 categories], Getting Started (its own wording — "decide your look, ratings, crew, moves, and gear" — independently confirms Crew sits as a peer component alongside the standard sections, matching Skyler's framing), Introductions, History [8 prompts], Improvements [10, flat, includes "Gain an ally: one of your existing crew members or the whole crew as an ally team" — a level-up reference to Crew, correctly out of scope, checked explicitly and confirmed *not* a repeatable-Section signal, same ordinary fixed-pool-growth shape as Symbiosis/Sect/Agency; also "Get a haven, like the Expert has..." — a cross-playbook reference, correctly out of scope], and Advanced Improvements [8, flat, includes "Make it big. You're a superstar now!," flat narrative text] — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all the basic moves, plus three Snoop moves. Pick three:" — no auto-grant sentence, 7 named options optional ("What Does That Feel Like?", Minor Celebrity, We'll Fix It In Post, Press Accreditation, Truthiness, The Mojo Wire, Relaxed Producer). `Required = false` for all 7, `MoveGrantCount = 3`. Same already-validated zero-required shape as Covenant/Envoy/Expert/Flake/Interface/Mundane. Checked explicitly for the Naked-City-style Move-internal-pick trap — none present.

**Schema-fit check, resolved — both map cleanly onto the existing zero-option `BespokeSection` pattern (Covenant/Secret-Wisdom/Bond-Abuse/Fate-of-Your-Love/Monster-Breed precedent: `MinSelect`/`MaxSelect` both `null`, zero `BespokeOption` rows), no new schema, no escalation back to Skyler needed.** Skyler's own "instructions-only" framing is a deliberate simplification of content a stricter read could have decomposed further — Crew's "entourage vs. other-hunters" choice and its "up to three, each with a name and job" structure could, in principle, have been modeled as real nested `BespokeOption` picks (closer to Fate's or Friendship's shape); the instruction is to keep it flat instead, the same treatment already given to every other zero-option Section in this catalogue. Team Concept is a closer match to Monster Breed's precedent specifically — reusing the zero-option shape for orphaned flavor/reference prose rather than a fixed mechanic — extended one step further here, since Team Concept isn't even framing text for a sibling ruleset, it's standalone example copy with **zero player-facing task of any kind**, not even a "decide" instruction. Worth flagging as a Phase 10 UI-rendering nuance (does a Hunter-creation flow need to surface a Section with nothing to decide, or does it only ever belong in the Playbook reference view?) rather than a schema gap — noted for whoever builds Phase 10, not resolved here.

### Crew

- **Section**: Title "Crew". `Description`: "Decide if your crew is the other hunters, or if you have an entourage. If they are not the hunters, there are up to three of them. Pick a name and job for each. Tell the Keeper–your crew are `<b>`bystanders`</b>`. If it's the other hunters, decide together who gets which job. Crew jobs: camera, sound, editing, dogsbody, researcher, driver, director, producer, bodyguard." No `EffectText` — one continuous instructional block, nothing separately positioned after (same shape as Covenant's own ability).
- **Select**: not applicable — `MinSelect`/`MaxSelect` both `null`, per Skyler's explicit "instructions-only" instruction. Zero `BespokeOption` rows — the "up to three" entourage slots and the 9-item crew-jobs list stay inside `Description` as free text, not modeled as pickable options.

**Content fidelity: verified via the pipeline, finalized 2026-08-29** (`node extract-runs.mjs ... --minX 25 --maxX 270 --minY 195 --maxY 315`, scoped to Crew's own body range, excluding both its own heading and Team Concept's). 0 italic. 1 genuine bold span found and confirmed programmatically: `<b>bystanders</b>` — a real inline emphasis, checked directly and confirmed *not* a heading-collision false positive (the "Crew" heading never recurs as a capitalized word inside the body, and the lowercase body word "crew" is never itself bold). First instance in this catalogue of a bold cross-reference pointing at a core MOTW rules term ("bystander," an NPC classification) rather than a Move name or another bespoke ruleset — the established cross-reference-bolding convention extended a fourth way.

### Team Concept: Monster Revelations

- **Section**: Title "Team Concept: Monster Revelations". `Description`: "This is a quick example series setup based around the Snoop. The team has an Internet video channel, Monster Revelations,” that covers their monster hunts and teaches viewers about the monsters that are really out there. A Snoop is required to run things; a Flake or Expert makes a good researcher; a Mundane can make the coffee; everyone else can hold a camera or microphone or carry things." No `EffectText` — one continuous block.
- **Select**: not applicable — `MinSelect`/`MaxSelect` both `null`, zero `BespokeOption` rows, per Skyler's explicit instruction to track this purely as copy.

**A genuine, verified source punctuation anomaly, preserved rather than fixed.** The closing curly quote after "Monster Revelations,” has no matching opening quote anywhere in the source — checked directly against the PDF's raw item stream, not just the plain-text extraction: no quote glyph of any kind precedes "Monster" on that line or anywhere earlier in the sentence. Unlike Monstrous's duplicated-word typo (an unambiguous fix, no invented content needed), correcting this would require guessing exactly where an opening quote belongs — treated as a verified, real source oddity and preserved as printed, not silently patched. One pure line-wrap hyphenation artifact fixed on sight, same established discipline as every prior instance: "Revela- tions" → "Revelations."

**Content fidelity: verified via the pipeline, finalized 2026-08-29** (`node extract-runs.mjs ... --minX 25 --maxX 270 --minY 60 --maxY 185`, scoped to Team Concept's own body range, excluding its own heading). 0 bold, 0 italic. Entirely plain.

---

## The Spell-Slinger

(PDF pages 49–50, confirmed directly via `pdftotext -f 49 -l 49`/`-f 50 -l 50`; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect.) Everything except Combat Magic and Moves' two internal-pick traps is ordinary standard-section content: Ratings [5 lines], Luck (standard `Playbook.LuckSpecialText`-shaped "Spell-slinger special:" trigger), Harm, Experience, Gear (pick-1-of-3, no fixed grant beyond "you don't need much"), Pronouns, Looks [2 categories], Getting Started (its own wording — "decide your look, ratings, Combat Magic, moves, and gear" — independently confirms Combat Magic sits as a peer component alongside the standard sections, the same tell already seen for Snoop's Crew), Introductions, History [8 prompts], Improvements [9, flat, includes "Take another Combat Magic pick" — a level-up reference to Combat Magic, correctly out of scope, checked explicitly and confirmed *not* a repeatable-Section signal (each "another pick" is one more *distinct* option from the same fixed 10-item pool, the same ordinary fixed-pool-growth shape as Symbiosis/Sect/Agency, not a Rotes-style instance-collision case)], and Advanced Improvements [8, flat, includes a second "Take another Combat Magic pick" and "You may cross off another option from your Tools and Techniques" — a level-up reference to the Required Move's own internal pick, correctly out of scope] — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all the basic moves and four Spell-slinger moves. You have this one:" — **Tools and Techniques** (`Required = true`) + "Plus pick three of these:" 10 named options (Advanced Arcane Training, Arcane Reputation, Could've Been Worse, Enchanted Clothing, Forensic Divination, Go Big or Go Home, Not My Fault, Practitioner, Shield Spell, Third Eye). `MoveGrantCount = 4` (1 required + pick 3 of 10), matching "four Spell-slinger moves" exactly.

**First playbook with both Move-internal-pick trap shapes on the same page, not just one.** (1) **Tools and Techniques** (the Required Move itself): "Cross off one; you'll need the rest," followed by 4 named sub-items (Consumables, Foci, Gestures, Incantations), each with its own mechanical effect — the fifth Required-Move instance of this trap (after Forged's Partner, Gumshoe's Naked City, Host's Defensive Adaptation, The Searcher's First Encounter). (2) **Could've Been Worse** (one of the 10 optional pool moves): "you can choose one of the following options instead of losing control of the magic," followed by 2 named sub-items (Fizzle, This Is Gonna Suck) — the second optional-move instance (after The Professional's Mobility). Both correctly excluded from this catalogue — Move content, flagged for Phase 4 authoring only, not modeled here.

### Combat Magic

**A genuinely new pick-shape, not covered by any existing precedent — escalated rather than resolved unilaterally.** Source text: "Combat magic, pick three (with at least one base):" followed by two labeled sub-groups — **Bases** (4 named options: Blast, Ball, Missile, Wall) and **Effects** (6 named options: Fire, Force or Wind, Lightning or Entropy, Frost or Ice, Earth, Necromantic) — from which exactly 3 total are picked, with the constraint that at least 1 of the 3 must come from Bases. This is a **shared budget across two sub-groups with a floor constraint on one of them** — structurally distinct from every existing nested-category shape in this catalogue:
- **Fate/Friendship's mandatory-multi-category shape** (`Select N of M categories`, each category with its own independent `MinSelect`/`MaxSelect`) has no mechanism for a budget *shared* across categories — each category's count is independent of the others.
- **Natural Attacks' "either/or, left unenforced" precedent** (Base `MinSelect=1/MaxSelect=2` + Extra `MinSelect=0/MaxSelect=1`) is the closest analogue, but that gap was small (the naive reading allows at most 1 pick over the real cap: 2 Bases + 1 Extra = 3 vs. the true max of 2).

**Resolved by Skyler 2026-08-29 — nested Bases/Effects categories, not the flat 10-option list I'd recommended.** No additional rationale given beyond selecting that option; recorded as Skyler's direct structural call, not re-derived or second-guessed. This preserves the source's own visual Bases/Effects grouping for a future UI, the trade-off my recommendation would have given up.

**Enforcement gap, documented explicitly per Skyler's instruction — same treatment as Natural Attacks' own either/or gap.** The real rule ("pick 3 total, at least 1 from Bases") is **not** fully representable by independent per-category `MinSelect`/`MaxSelect` bounds. As modeled below — Bases `MinSelect=1/MaxSelect=3`, Effects `MinSelect=0/MaxSelect=3` — a naive consumer of just those bounds could permit up to **6** total picks (3 Bases + 3 Effects) against the true cap of **3**, a materially larger gap than Natural Attacks' own accepted version (whose worst case was 1-over, not 3-over). The real rule stays faithfully written in the Section's own `Description` (see below), not mechanically validated — same category of accepted, hand-reviewable gap as Natural Attacks and Fate's messy source layout, just a bigger one this time; whoever builds the Hunter-creation UI/validation layer needs to enforce the total-and-floor constraint explicitly from `Description`/business logic, not assume `MinSelect`/`MaxSelect` alone are sufficient.

- **Section**: Title "Combat Magic". `Description`: "You have a few attack spells you can use as weapons. When you use these spells to `<b>`kick some ass`</b>`, roll +Weird instead of +Tough. Sometimes the situation may require you to `<b>`act under pressure`</b>` to cast your spell without problems. Your combat spells can combine any of your base spells with any of your effects. `<b>`Combat magic, pick three (with at least one base):`</b>`" — the pick-instruction sentence is kept (not dropped as redundant, unlike most Sections' own pick-count restatements) specifically because of the "with at least one base" qualifier, which is otherwise unrepresented anywhere in the schema — same reasoning as Natural Attacks' own kept framing sentence. No `EffectText` — nothing separately positioned after the option list in the source.
- **Select**: 2 of 2 at the top level (both Bases and Effects categories present and mandatory, same top-level shape as Natural Attacks' Base/Extra).
- **Top-level options** (category dividers — title populated from the source's own bold "Bases:"/"Effects:" sub-headers per convention, no description, each with its own nested children and its own select count):
  1. **Bases** — select 1 of 4 (`MinSelect=1`, `MaxSelect=3` — see the enforcement-gap note above; a hand-reviewable UI/validation layer must also cap the combined Bases+Effects total at 3), title + description each, real font-derived bold titles (matches Background's/Task's Titled-Choice shape, not Forged's Benefits' colon-split-only shape):
     - **Blast** — 2-harm magic close obvious loud
     - **Ball** — 1-harm magic area close obvious loud
     - **Missile** — 1-harm magic far obvious loud
     - **Wall** — 1-harm magic barrier close 1-armour obvious loud
  2. **Effects** — select 0 of 6 (`MinSelect=0`, `MaxSelect=3` — same enforcement-gap caveat), same Titled-Choice shape:
     - **Fire** — Add "+2 harm fire" to a base. If you get a 10+ on a combat magic roll, the fire won't spread.
     - **Force or Wind** — Add "+1 harm forceful" to a base, or "+1 armour" to a wall.
     - **Lightning or Entropy** — Add "+1 harm messy" to a base.
     - **Frost or Ice** — Adds "-1 harm +2 armour" to a wall, or "+1 harm restraining" to other bases.
     - **Earth** — Add "forceful restraining" to a base.
     - **Necromantic** — Add "life-drain" to a base.

**Content fidelity: verified via the pipeline (ran solo — `dump-page.mjs`/`list-fonts.mjs`/`extract-runs.mjs` directly, same as The Snoop's round; no separate Bowser dispatch needed given the shape is flat title+description with no nested lists, a shape this pipeline already handles reliably), finalized 2026-08-29** (`node extract-runs.mjs ... --minX 280 --maxX 520 --minY 295 --maxY 570`). Confirmed programmatically: 15 bold runs total (`Combat Magic` heading, `kick some ass`, `act under pressure`, the pick-instruction sentence + `Bases:`, all 4 Base titles, `Effects:`, all 6 Effect titles), **zero italic anywhere** — notable since this page's font table includes a bold-italic face (`WarnockPro-BoldIt`, unique among all playbooks processed so far) that turned out unused within Combat Magic's own column. All 10 description bodies confirmed plain — the embedded quoted mechanical tags ("+2 harm fire," "+1 armour," etc.) are literal curly-quoted text in the source, not markup, preserved verbatim as printed (curly double quotes kept as-is, matching this catalogue's general "preserve as printed" default — no established precedent yet for normalizing curly double quotes the way curly apostrophes are normalized). "Bases:"/"Effects:" bold sub-labels confirmed real, not a heading-collision false positive (the "Combat Magic" page heading, capitalized, never recurs in the body; the lowercase phrase "combat magic" appears often in body text but is never itself bold) — now finalized as the two top-level category `Title`s (Bases/Effects) per Skyler's nested-categories decision; their bold styling doesn't persist into the stored `Title` string, same "titles never carry markup" convention already applied everywhere else in this catalogue (Background, Task, Type, etc.).

---

## The Spooktacular

(PDF pages 51–52, confirmed directly via `pdftotext -f 51 -l 51`/`-f 52 -l 52`; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect.) Everything except The Show and Moves' one internal-pick trap is ordinary standard-section content: Ratings [5 lines], Luck (standard `Playbook.LuckSpecialText`-shaped "Spooktacular special:" trigger), Harm, Experience, Gear (fixed paraphernalia grant + Camp tools pick-1-of-6 + Vehicles pick-1-of-4 + Mystical item pick-1-of-5, each mystical item with real flavor description), Pronouns, Looks [3 categories], Getting Started (its own wording — "decide your look, ratings, Show, moves, and gear" — independently confirms The Show sits as a peer component alongside the standard sections, the third playbook in a row with this exact tell after Snoop's Crew and Spell-Slinger's Combat Magic), Introductions, History [8 prompts], Improvements [10, flat, includes "Take another Show option" — a level-up reference to The Show, correctly out of scope, checked explicitly and confirmed *not* a repeatable-Section signal (ordinary fixed-pool growth, same shape as Symbiosis/Sect/Agency/Combat Magic), and "Take another Spooktacular move" ×2], and Advanced Improvements [8, flat, includes "Get your own traveling show. Maybe you take over your old one, maybe you start anew" — flat narrative text, not a mechanical pick] — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all of the basic moves, and two Spooktacular moves. Pick two of these:" — no auto-grant sentence, 6 named options optional (Put On A Show, A Negligible Price, Easygoin', Pay It Backward, The Old Crew, The Game Is Fixed). `Required = false` for all 6, `MoveGrantCount = 2`. Same already-validated zero-required shape as Covenant/Envoy/Expert/Flake/Interface/Mundane/Snoop.

**The Game Is Fixed (one of the 6 optional pool moves) has its own embedded menu, checked against the Move-internal-pick trap and against Action Scientist's "in-play menu, not a one-time pick" precedent — correctly excluded either way, flagged for Phase 4 with the distinction noted.** "You may also choose the following effects whenever you use magic:" followed by 5 bulleted items (Someone fails a skilled task; You disappear unnoticed and untraceable in a crowded and chaotic scene; Instantly set up or pack away a carnival stall or camp site; Track someone; Get a bunch of cash). Unlike the Forged/Gumshoe/Host/Professional/Searcher/Spell-Slinger trap instances (all one-time, permanent character-build picks), this reads as a repeatable in-play menu ("whenever you use magic") — the same shape already resolved for Action Scientist's Physics and Cosmology ("you may ask one of these... as well as the usual questions"). Move content either way, out of this catalogue's scope regardless of which reading is correct — noted for whoever authors Moves in Phase 4, not resolved here.

### The Show

**Structure**: Title normalized to "The Show" — the source prints it in stylized full caps ("The SHOW," a genuine bold-font emphasis choice, not the playbook-name's decorative display font) — same normalization precedent as Curse-Eater's "Consumed MagiC" → "Consumed Magic."

- **Section**: Title "The Show". `Description`: "You spent a long time with a traveling show and get an ability based on its style. Are you still with them? If so, how do you balance work and monster hunting? If you left, why was that?" No `EffectText` — nothing separately positioned after the option list in the source.
- **Dropped from free text, not placed in either field**: "Pick your show's specialty:" — the literal pick-instruction sentence, italicized in the source but fully redundant with `MinSelect`/`MaxSelect` below (unlike Combat Magic's kept instruction, there's no unenforceable extra qualifier here needing prose to carry it) — same treatment as every other Section's own pick-count restatement.
- **Select**: 1 of 5 (inferred — no explicit "pick one" number stated, same confidence level as Background's/Mission's/How-Consuming-Magic-Works' inferred pick-1).
- **Options** (title + description both populated, real font-derived bold titles — Titled-Choice shape, matching Background/Task):
  1. **An Infernal Power** — A Big Bad operated the Show for evil purposes. You signed the contract–take a three-box `<b>`infernal favour`</b>` track. Spend these as if they were Luck points. The Big Bad may restore these points when they wish, but first you must do something unforgivably terrible. What sin have you already committed?
     - **Nested child, `MinSelect=1`/`MaxSelect=1` on the parent, exactly one child** (structurally identical to Professional's synthesized "Agency name:" single-child pattern): `Title="Infernal Favour"`, `DescriptionText=null`, `NumericMin=0`, `NumericMax=3` — see the resolved schema note below.
  2. **Magic & Illusions** — You can cast confounding spells. When you `<b>`weave an illusion`</b>`, roll +Weird. On a 10+ everyone is fooled, they are either led in a direction of your choice or you and those you choose escape undetected. On a 7-9, it doesn't quite work: misdirect or escape as a 10+ result, but the Keeper chooses one: someone isn't fooled, or you gain unwelcome attention. On a miss, it goes badly as magic always does.
  3. **Making Money** — You've got a lot of practice bamboozling folks. When you `<b>`mess with someone`</b>`, roll +Charm. On a 10+, they don't know what's going on and overlook something of your choice. On a 7-9 they still overlook something but they're more angry than baffled. On a miss, you better run.
  4. **Problem Solvers** — The Show rolls into town, people visit and reveal their difficulties. The crew arrange to make things better, leaving each town a little happier than when you arrived. Well, when things go smoothly anyhow. When you `<b>`chat to someone, or observe them for a few minutes`</b>`, the Keeper will tell you what their biggest problem is right now.
  5. **Supernatural Creatures** — You're not entirely human yourself. Pick a single supernatural move from any hunter playbook (the Monstrous is a good place to start). How does your inhuman nature show?

**Not part of this option's stored text — dropped as structural/redundant, same treatment as Corruption's own trailing box-count**: option 1's source ends with a literal drawn checkbox track, "Infernal Favour: b b b." — this becomes the nested numeric child option above, not verbatim ASCII-art inside `DescriptionText`.

**Not part of this ruleset — flagged, not included**: option 5's "pick a single supernatural move from any hunter playbook" is a genuine character-creation-time cross-playbook Move grant (unusual — every prior cross-playbook Move reference in this catalogue has been a *later* Improvement/Advanced-Improvement grant, not a creation-time bespoke pick), but it's still Move content and stays out of this catalogue's scope, same as every other Move reference — flagged for Phase 4's awareness since the *timing* (creation-time, not level-up) is a genuinely new wrinkle worth knowing about, even though the scope call itself isn't.

**Content fidelity: verified via the pipeline (ran solo — `dump-page.mjs`/`list-fonts.mjs`/`extract-runs.mjs`), finalized 2026-08-29** (`node extract-runs.mjs ... --minX 275 --maxX 530 --minY 90 --maxY 570`). Confirmed programmatically: "The Show" heading and all 5 option titles bold (as expected); the pick-instruction sentence italic (dropped per above, not a gap); 4 inline bold spans inside option bodies — `infernal favour` (An Infernal Power), `weave an illusion` (Magic & Illusions), `mess with someone` (Making Money), `chat to someone, or observe them for a few minutes` (Problem Solvers). **Worth naming as a related but distinct pattern from established cross-reference bolding**: none of these four phrases are actual base-move names (unlike Action Scientist's/Envoy's "manipulate someone"/"act under pressure" bolding) — they're each option's own custom trigger clause ("When you X, roll +Y"), bolded for typographic emphasis rather than as a defined-term cross-reference. Recorded as found, not force-fit into the cross-reference category. Zero italic beyond the one dropped pick-instruction sentence.

### Resolved: "Infernal Favour" is a new `BespokeOption` leaf shape — `NumericMin`/`NumericMax`, not a `PlaybookExtraTrack` extension

**A genuinely new wrinkle, resolved by Skyler 2026-08-29 — not my own original recommendation, evaluated on its merits and adopted.** "An Infernal Power" grants a real, printed 3-box track ("Infernal Favour: b b b.") that functions exactly like Luck (spend the boxes, restorable by an external party — here the Big Bad, elsewhere the Keeper) — the same category of resource that justified building `PlaybookExtraTrack` for Curse-Eater's Corruption and Pararomantic's Relationship Status. But unlike every track modeled so far, Infernal Favour is **not universal to the playbook — it only exists for the subset of Spooktacular Hunters who specifically picked "An Infernal Power."** I originally proposed extending `PlaybookExtraTrack` with a nullable FK pointing at the triggering `BespokeOption`. **Skyler's counter-proposal, adopted instead**: "a new type of bespoke option, which could be as simple as a numeric input value with a minimum and maximum value" — attach the bounds directly to the `BespokeOption` that gates them, making the conditionality question moot by construction (no separate FK needed when the relationship is already direct).

**Modeled as**: `BespokeOption` gains two new nullable ints, `NumericMin`/`NumericMax`. "An Infernal Power" gets `MinSelect=1, MaxSelect=1` with exactly one child option — `Title="Infernal Favour"`, `DescriptionText=null`, `NumericMin=0`, `NumericMax=3` — the identical attachment shape already used for Professional's synthesized "Agency name:" single-child pattern, just with a numeric-range leaf instead of a `{{blank}}` text leaf. Full reasoning, alternatives considered and declined (including my own original FK proposal), and the instance-side `HunterBespokeSelection.NumericValue` addition: `phase5-bespoke-ideation.md`.

**No printed start/end labels for this track at all** — unlike every prior track (Luck/Harm's literal "Okay"/"Doomed"/"Dying," Corruption's "Lost," Relationship Status's "Loving"/"Broken"), the source shows only "Infernal Favour: b b b." with no label text anywhere near it. This new leaf shape deliberately has no `StartLabel`/`EndLabel` fields at all yet — not needed by this case, and not added speculatively ahead of a real demonstrated need (same discipline as `PlaybookExtraTrack.StartLabel` itself, only added once Pararomantic showed a real case).

**Instance-side value starts full and spends down** — `HunterBespokeSelection.NumericValue` (new nullable int, populated only for numeric-leaf options) initializes to `NumericMax` (3) for Infernal Favour and is spent toward `NumericMin` (0) during play, mutable via the standard Hunter-edit mechanism — an authoring/UI convention, not a schema flag, same as this catalogue's other "default unless the source implies otherwise" rules.

---

## The Spooky

One bespoke ruleset (PDF pages 53–54, confirmed directly via `pdftotext -f 53 -l 53`/`-f 54 -l 54`; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect; the raw stream's own tag order differs from the visual reading order for The Dark Side's 16 tags — a column-major-vs-row-major artifact of a 2-column tag list, not a swap defect, checked and confirmed harmless, same non-issue class as any other 2-column list in this book). Everything except The Dark Side is ordinary standard-section content: Ratings [5 lines], Luck (standard `Playbook.LuckSpecialText`-shaped "Spooky special:" trigger — "As you mark off Luck boxes, your dark side's needs will get nastier," worth flagging since Dark Side's own `EffectText` restates the same fact in slightly different words, the same kind of two-renderings-of-one-trigger duplication first noted for Chosen's Fate), Harm, Experience, Gear (fixed grant of 2 unspecified normal weapons + any magical items/amulets, plus a Normal weapons pick-2-of-5 category), Pronouns, Looks [3 categories], Getting Started (its own wording — "decide your look, ratings, moves, dark side and gear" — independently confirms The Dark Side sits as a peer component alongside the standard sections, the fourth playbook in a row with this exact tell after Snoop's Crew, Spell-Slinger's Combat Magic, and Spooktacular's The Show), Introductions, History [8 prompts], Improvements [10, flat, includes "Change some, or all, your dark side tags" — a level-up reference to The Dark Side, correctly out of scope, checked explicitly and confirmed *not* a repeatable-Section signal (a re-pick/swap of the existing 3 tags, not growth beyond 3 — doesn't even match the ordinary fixed-pool-growth shape, let alone Rotes' instance-collision shape), and "Get a mystical library, like the Expert's haven option" — a cross-playbook reference, correctly out of scope], and Advanced Improvements [8, flat, includes "You discover how to use your powers at a lower price. Delete one dark side tag permanently." — a level-up reference that *removes* a tag, correctly out of scope, matching Monstrous's "Free yourself from the curse of your kind" precedent] — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all the basic moves, plus pick three Spooky moves:" — no auto-grant sentence, 8 named options optional (Telepathy, Hex, The Sight, Premonitions, Hunches, Tune In, The Big Whammy, Jinx). `Required = false` for all 8, `MoveGrantCount = 3`. Same already-validated zero-required shape as Covenant/Envoy/Expert/Flake/Interface/Mundane/Snoop/Spooktacular.

**Richest concentration yet of the "in-play menu, not a one-time pick" Move-content pattern — three separate instances on one playbook, all correctly excluded, none needing the numeric-leaf shape.** Hex ("you may pick from the following" — 3 bulleted effects, chosen fresh each time the move triggers), Tune In ("spend one hold to ask the Keeper one of the following questions" — 5 bulleted questions), and Jinx ("Spend your hold to:" — 6 bulleted spend options) all embed a menu selected repeatedly during play, not a permanent character-build pick — the same shape already resolved for Action Scientist's Physics and Cosmology and Spooktacular's The Game Is Fixed, now confirmed 3x on a single playbook. Move content regardless, out of this catalogue's scope, flagged for Phase 4 only. **Checked explicitly and confirmed none of these are the Forged/Gumshoe/Host/Professional/Searcher/Spell-Slinger-style permanent-pick trap** — no fixed, one-time, mutually-exclusive selection among them.

### The Dark Side

- **Section**: Title "The Dark Side". `Description`: "Your powers have an unsavory source, and sometimes you get tempted to do things you shouldn't. These could be orders from whatever granted your power, or urges that bubble up from your subconscious. Something like that. Whatever it is, it's unsettling." `EffectText`: "The Keeper can ask you to do nasty things (in accordance with the tags), when your powers need you to. If you do whatever is asked, mark experience. If you don't do it, then your powers are unavailable until the end of the mystery (or until you cave). As you mark off Luck boxes, these requests will get bigger and nastier." — a distinctly-positioned consequence block after the options, the same split criterion already applied to Changeling's Unknown Heritage and Chosen's Fate.
- **Dropped from free text, not placed in either field**: "Pick three tags for your dark side:" — the literal pick-instruction sentence, italicized in the source (confirmed via the pipeline) but fully redundant with `MinSelect`/`MaxSelect` below — same treatment as every other Section's own pick-count restatement, regardless of its typography.
- **Select**: 3 of 16 — **explicit** ("Pick three tags").
- **Options** (title-only, no description, flat — Tag Pick shape, matching Chosen's Fate/Changeling's Unknown Heritage; **order corrected 2026-08-31 to column-major** — down the left column, then down the right — per Skyler's standing rule for every multi-column list, verified against item coordinates (col 1 at x=50 runs Violence → Addiction, col 2 at x=171 runs Mood swings → Paranoia); this entry originally recorded the row-major reading order):
  1. Violence
  2. Depression
  3. Secrets
  4. Lust
  5. Dark bargain
  6. Guilt
  7. Soulless
  8. Addiction
  9. Mood swings
  10. Rage
  11. Self-destruction
  12. Greed for power
  13. Poor impulse control
  14. Hallucinations
  15. Pain
  16. Paranoia

**Content fidelity: verified via the pipeline (ran solo — `dump-page.mjs`/`list-fonts.mjs`/`extract-runs.mjs`), finalized 2026-08-29** (`node extract-runs.mjs ... --minX 25 --maxX 270 --minY 85 --maxY 305`, covering the intro, pick-instruction, all 16 tags, and the trailing consequence paragraph in one pass). Confirmed programmatically: exactly one italic run in the whole block (the dropped pick-instruction sentence), zero bold anywhere, all 16 tags and both prose blocks (`Description`/`EffectText`) entirely plain. **A real font-ID mapping trap caught before it produced a wrong result, worth logging as a fresh instance of the standing warning**: an initial manual cross-check (resolving font ids via a combined `list-fonts.mjs 53 54` run) wrongly suggested the 16 tags were italic and the pick-instruction plain — the *opposite* of the truth. Re-running `list-fonts.mjs` scoped to page 54 alone (matching the scope `dump-page.mjs`/`extract-runs.mjs` actually use) showed font ids `g_d0_f2`/`g_d0_f4` resolve to *different* faces depending on whether the font table is built from one page or two — the exact "font ids are NOT stable across pages" hazard the pipeline's own README already warns about, this time caught by cross-checking against `extract-runs.mjs`'s own programmatic output before trusting the manual read, not just asserted from a single dump pass.

**No numeric-leaf option needed here** — checked explicitly per the coordinator's note, since Spooktacular's new `NumericMin`/`NumericMax` shape was fresh from the prior round. The Dark Side has no box-track or bounded-numeric mechanic of any kind; the only track referenced (Luck) is the standard field, unaffected. The new leaf shape doesn't apply and wasn't forced onto anything here.

---

## The Visitor

One bespoke ruleset, "Expatriation" (PDF pages 55–56, confirmed directly via `pdftotext -f 55 -l 55`/`-f 56 -l 56`; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect). Everything except Expatriation and Moves' two flagged items is ordinary standard-section content: Ratings [5 lines], Luck (standard `Playbook.LuckSpecialText`-shaped "Visitor Special:" trigger), Harm, Experience, Gear (a vehicle-flavor grant + Alien gear pick-2-of-8 + Alien weapon pick-1-of-6 + Local gear pick-2-of-9), Pronouns, Looks [3 categories, each with a real printed blank — standard-section blanks, out of this catalogue's scope per the established Look/History UI-presentation carve-out], Getting Started (its own wording — "decide your look, ratings, expatriation, moves, and gear" — independently confirms Expatriation sits as a peer component alongside the standard sections, the fifth playbook in a row with this exact tell after Snoop's Crew, Spell-Slinger's Combat Magic, Spooktacular's The Show, and Spooky's The Dark Side), Introductions, History [8 prompts], Improvements [**11**, flat — count corrected 2026-08-31 — includes "Take another Visitor move" ×2, checked and confirmed ordinary, and no Expatriation-growth reference of any kind — unlike every other recent bespoke ruleset, nothing in Improvements/Advanced Improvements points back at Expatriation at all], and Advanced Improvements [8, flat, includes several narrative "retire/change hunter" options, none referencing Expatriation] — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all the basic moves, plus three Visitor moves. Choose three of these:" — no auto-grant sentence, 7 named options optional (Something Strange, Always Learning, Being Neighbourly, Different World Different Rules, Taste of Home, Alien Anatomy, Otherworldly Techniques). `Required = false` for all 7, `MoveGrantCount = 3`.

**Two flagged Move-internal items, both correctly excluded, both already-established shapes.** (1) **Something Strange** (optional pool move) has its own embedded permanent pick — "Pick one:" 4 fixed physical-adaptation options plus "Something else (with the group's agreement):" — the Move-internal-pick trap on an optional move, same shape as Professional's Mobility/Spell-Slinger's Could've Been Worse. (2) **Taste of Home** (optional pool move) has an in-play menu — "The other hunter picks one:" 3 bulleted narrative options, chosen fresh each time the move triggers — the same "in-play menu, not a one-time pick" shape as Action Scientist's Physics and Cosmology, Spooktacular's The Game Is Fixed, and Spooky's Hex/Tune In/Jinx. Both Move content, out of this catalogue's scope, flagged for Phase 4 only.

### Expatriation

**Skyler's own structural request, verified directly against the source before modeling — confirmed accurate, not just taken on trust.** Skyler's summary: "Pick one or more from at least two lines," three bullet sections of comma-separated items, a blank slot per section for "something else." Checked against page 56's literal printed text: "What was your home culture like? Pick one or more from at least two lines:" — an exact match — followed by three bulleted lines (7, 6, and 7 comma-separated items respectively), each ending in a bare trailing comma with **nothing printed after it** — no "Something else:" label, no blank-marker glyph, on any of the three lines (confirmed directly against the item stream, not just the plain-text extraction). This is a variant of the already-established "blank implied by trailing punctuation, no printed marker" pattern (Changeling's `{{blank}}`-by-intent, Covenant's independent confirmation) — here the printed cue is a bare trailing comma rather than a colon-terminated "Something else," so `{{blank}}` is added on the same semantic grounds but with even less printed signal than either prior instance.

**Does the existing nested-category `MinSelect`/`MaxSelect` mechanism cover "one or more per line, from at least two of three lines" — checked directly against Combat Magic's own gap, not assumed similar because both involve multiple categories.** Answer: **yes, cleanly, no gap, no new schema.** This is a structurally different constraint from Combat Magic's, not a scaled-down version of the same problem:
- **Combat Magic's gap was a *shared numeric budget* across categories** (a hard total of 3 items split between Bases and Effects) — independent per-category `MinSelect`/`MaxSelect` bounds can't express a cross-category sum constraint, which is why that gap was real and had to be accepted, not solved.
- **Expatriation has no shared budget at all** — "one or more" per line is *uncapped* (no stated max, ever), and "at least two lines" is a purely **categorical participation** constraint (how many of the 3 line-categories get touched, not how many items total). This is exactly what the *already-proven* mechanism for "pick M of N categories, then pick independently within each" (Fate's 3-of-3, Friendship's 2-of-2, Combat Magic's own 2-of-2) already expresses — the only difference is that every prior case had `MinSelect == MaxSelect == category count` (every category mandatory); Expatriation is simply the **first case needing a real range** (`MinSelect=2, MaxSelect=3` — 2 or 3 of the 3 lines, not all 3 required) at that level. Nothing about the mechanism's own definition ("how many of my direct options must/may be picked") assumes a fixed count — a range was always structurally valid, just never previously exercised.

**Modeled as a 3-level nested structure — the first real exercise of a depth the schema already predicted it could handle.** The Underworld nesting design (Section 3 of `phase5-bespoke-ideation.md`) explicitly noted the adjacency-list (`ParentOptionId`) approach "generalizes past 2 levels for free... untested, but the shape doesn't care." Expatriation is that test, and it holds:

- **Section**: Title "Expatriation". `Description`: `null` — the only candidate framing text ("What was your home culture like?") is specific to one of Expatriation's four sub-blocks, not the whole ruleset, so it moves down to that sub-block's own option rather than being force-fit onto the Section (same principle as Forged's Origin umbrella, whose `Description` is also `null` for a related but distinct reason — Origin had no framing text anywhere at all; here real framing text exists but belongs one level down). No `EffectText` — nothing follows the last sub-block in the source.
- **Select**: 4 of 4 at the top level — all four sub-blocks mandatory (the "pick all N of N" idiom already used for Fate/Origin/Dual Nature).
- **Top-level options** (category dividers, `Title` populated, `DescriptionText` null except where noted, each with its own nested children and select count):
  1. **Home Culture** *(synthesized title — the source has no name for this sub-block, only its question; drawn directly from that question's own wording, lower-risk than a from-scratch invention)* — `DescriptionText`: "What was your home culture like?" (the one piece of genuine per-branch framing text; the redundant pick-count restatement, "Pick one or more from at least two lines," is dropped, fully captured structurally below). `MinSelect=2, MaxSelect=3` (at least two of the three Lines must be engaged).
     - **Line 1**, **Line 2**, **Line 3** *(synthesized, deliberately positional/non-thematic — the source calls them only "lines," never names them, and unlike "Origin" or "Agency name:" no single clean theme fits: Line 3 alone mixes governance traits with tech-level traits, so a thematic label would misrepresent the source rather than clarify it)* — each `MinSelect=1, MaxSelect=null` ("one or more," no stated cap, same representation as Heat's own uncapped minimum), title-only children (Tag Pick shape):
       - **Line 1**: Feudal, imperial, democratic, theocratic, mercantile, egalitarian, meritocratic, `{{blank}}`
       - **Line 2**: Lone homeworld, lone system, space habitats, interstellar, nomadic, scattered worlds, `{{blank}}`
       - **Line 3**: Caste system, wartorn, tyrannical, peaceful, rationalist, high-tech, low-tech, `{{blank}}`
  2. **Why did you leave?** — `MinSelect=1, MaxSelect=1` (inferred, no explicit count stated — same confidence as Background's/Mission's inferred pick-1), title-only children: You had to escape / You're a scout, exploring new worlds / You're a tourist, looking for interesting experiences / You're an emissary / Something else `{{blank}}`.
  3. **Why have you stayed on Earth?** — `MinSelect=1, MaxSelect=1` (inferred), title-only children: To befriend / To teach / To learn / To protect it / Something else `{{blank}}`.
  4. **What pulls you back home?** — `MinSelect=1, MaxSelect=1` (inferred), title-only children: Relationships / Enemies / Obligations / Homesickness / Something else `{{blank}}`.

**One implementation-side nuance resolved with reasoning, not escalated — flagged for visibility since it's genuinely untested territory, not because it's blocking.** Every prior nested-category case (Fate, Friendship, Combat Magic) has `MinSelect == MaxSelect == category count`, meaning every category is *always* engaged — so whether a category-divider option needed its own `HunterBespokeSelection` row (independent of its children's own rows) was never actually load-bearing. Home Culture is the first case where a category can legitimately have **zero** engagement (1 of its 3 Lines may go untouched), making "is this category engaged" a real question for the first time. Resolved as a **derived fact** (a Line counts as engaged if and only if ≥1 of its own children has a `HunterBespokeSelection` row), not a separately stored one — consistent with this schema's standing "shape emerges from populated fields, don't store what's derivable" philosophy (already applied to declining an `IsRepeatable` flag and to `Required`-on-`BespokeOption`). No new field, no separate category-level selection row needed.

**Content fidelity: verified via the pipeline (ran solo — `dump-page.mjs`/`list-fonts.mjs`/`extract-runs.mjs`, font ids re-confirmed scoped to page 56 alone after The Spooky's mapping-trap lesson), finalized 2026-08-29** (`node extract-runs.mjs ... --minX 25 --maxX 270 --minY 255 --maxY 566`, covering all of Expatriation including the three other sub-blocks). Confirmed programmatically: **zero bold, zero italic anywhere** in the entire block — the cleanest (least-marked-up) bespoke ruleset content found so far in this catalogue.

**No numeric-leaf option needed here** — checked explicitly, consistent with the standing discipline since Spooktacular. Expatriation has no bounded-numeric or track mechanic of any kind.

---

## The Wronged

**The 28th and final playbook in Skyler's one-at-a-time walkthrough.** One bespoke ruleset, "Who You Lost" (PDF pages 57–58, confirmed directly via `pdftotext -f 57 -l 57`/`-f 58 -l 58`; both pages spot-checked with `pdftotext -raw` for the column-order swap — clean, normal stream order throughout, no defect). Everything except Who You Lost is ordinary standard-section content: Ratings [5 lines], Luck (standard `Playbook.LuckSpecialText`-shaped "Wronged special:" trigger), Harm, Experience, Gear (a fixed protective-wear grant + an optional vehicle callout + Signature Weapons pick-1-of-7 + Practical weapons pick-2-of-7), Pronouns, Looks [2 categories], Getting Started (its own wording — "decide your look, ratings, who you lost, moves, and gear" — independently confirms Who You Lost sits as a peer component alongside the standard sections, the sixth playbook in a row with this exact tell after Snoop, Spell-Slinger, Spooktacular, Spooky, and The Visitor), Introductions, History [8 prompts], Improvements [10, flat, includes "Gain a haven, like the Expert has, with two options" and "Add one more option to your haven" — cross-playbook references, correctly out of scope, and "Take another Wronged move" ×2], and Advanced Improvements [9, flat, includes "You track down the specific monster(s) responsible for your loss. The Keeper must make the next mystery about them." — a level-up narrative reference to "My prey," correctly out of scope, and "Change the target of your vengeful rage. Pick a new monster breed: I know my prey now applies to them instead." — a level-up *replacement* of the existing "My prey" value, not growth, matching Spooky's "Change some, or all, your dark side tags"/Monstrous's curse-removal precedent, and incidentally confirming "My prey" really is modeled correctly as a single mutable value rather than a growing collection] — is standard-section content fitting the Phase 2 model with no new decisions needed.

**Moves, checked explicitly for `Required` vs. pick-N**: "You get all the basic moves plus three Wronged moves. You get this one:" — **I Know My Prey** (`Required = true`) + "Then pick two of these:" 7 named options (Berserk, NEVER AGAIN, What Does Not Kill Me..., Fervor, Safety First, DIY Surgery, Tools Matter). `MoveGrantCount = 3` (1 required + pick 2 of 7), matching "three Wronged moves" exactly. **I Know My Prey itself cross-references "Who You Lost"** ("the breed of monster that caused your loss" — i.e. "My prey," below) — the same Move-references-bespoke-Section pattern already seen throughout this catalogue (Curse-Eater's Devour Evil/Corruption, Initiate's Gear/Sect), noted for Phase 4, no new schema. **Checked DIY Surgery's own "On a 7-9... plus one of these, your choice" sub-list explicitly against the Move-internal-pick trap and the in-play-menu pattern — it's neither.** This is ordinary PbtA partial-success roll-outcome branching (the same "on a 7-9, pick one of these complications" shape used throughout the base game and dozens of already-catalogued playbooks' own Moves, e.g. Crooked's Fixer), not a distinct structural pattern worth flagging — the first explicit confirmation in this catalogue that not every Move-embedded bulleted list is one of the two named traps; ordinary roll-result branching is a third, unremarkable case that was simply never ambiguous enough to need calling out before.

### Who You Lost

- **Section**: Title "Who You Lost". `Description`: `null` — no framing text applies to the whole ruleset; each of the three sub-blocks below carries its own question as its own `Title`, the same "push framing text down to where it actually belongs, leave the umbrella `Description` null" resolution already used for Forged's Origin and The Visitor's Expatriation. No `EffectText` — nothing follows the last sub-block in the source.
- **Select**: 3 of 3 at the top level — all three sub-blocks mandatory (the same "pick all N of N" idiom as Fate/Origin/Expatriation's Home Culture).
- **Top-level options** (category dividers, `Title` populated from the source's own question text, each with its own nested children and select count):
  1. **Who did you lose?** — `MinSelect=1, MaxSelect=null` ("one or more," no stated cap, same representation as Heat's/Expatriation's Lines). The redundant pick-count phrase ("Pick one or more of:") is dropped, fully captured structurally. Title-only children (Tag Pick shape), each with a semantically-implied blank (no printed marker after any of the 5 trailing colons — checked directly against the item stream, same "blank by intent" convention as Changeling/Covenant/Expatriation, now confirmed a fourth time): Your parent(s) `{{blank}}` / Your sibling(s) `{{blank}}` / Your spouse/partner `{{blank}}` / Your child(ren) `{{blank}}` / Your best friend(s) `{{blank}}`.
  2. **What did it?** — `DescriptionText`: "With the Keeper's agreement, pick the monster breed." (kept, not dropped — a real procedural rule, not a pick-count restatement, same reasoning as Combat Magic's kept "with at least one base" sentence). `MinSelect=1, MaxSelect=1`, exactly one child — `Title="My prey"` (a distinct, real printed label of its own, unlike Agency name's label-less child), `DescriptionText="{{blank}}"` — the same single-mandatory-child attachment shape as Professional's "Agency name:" pattern, reused a second time.
  3. **Why couldn't you save them?** — `Title` includes the connecting grammar the tags need to read correctly ("Why couldn't you save them? You were"), with only the redundant parenthetical pick-count ("(pick one or more)") dropped. `MinSelect=1, MaxSelect=null` (same uncapped-minimum shape as Who did you lose?). Title-only children, no blanks: at fault / selfish / injured / weak / slow / scared / in denial / complicit.

**Content fidelity: verified via the pipeline (ran solo — `dump-page.mjs`/`list-fonts.mjs`/`extract-runs.mjs`, font ids resolved scoped to page 58 alone), finalized 2026-08-29** (`node extract-runs.mjs ... --minX 25 --maxX 270 --minY 80 --maxY 300`). Confirmed programmatically: the only bold run found is "What did it?" itself — a category-divider's own `Title`, excluded from stored markup per the standing "titles never carry markup" convention, same treatment as "Who You Lost"'s own page-level heading. Every other string (the intro sentence, all 5 "Who did you lose?" tags, "My prey," all 8 "Why couldn't you save them?" tags) is entirely plain — no italic anywhere on the page.

**No numeric-leaf option needed here** — checked explicitly, consistent with the standing discipline since Spooktacular. Who You Lost has no bounded-numeric or track mechanic of any kind.
